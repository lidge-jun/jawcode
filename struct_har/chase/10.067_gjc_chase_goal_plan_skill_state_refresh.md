# 10.067 — gjc chase: goal/plan skill-state refresh and role artifacts

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · P1
> Reviewed source: GJC `20c299eb..79b42377` (`upstream/dev`, v0.7.8→v0.7.10) vs JWC worktree `5c375b1`.

## Why This Is A Gap

GJC hardened goal/plan workflow state after v0.7.8: goal identity drift was eliminated, ralplan role artifacts persist through env handoff, update refreshes stale on-disk default workflow skills, deep-interview simple-task behavior was tightened, and Fable 5 built-in profiles were restored/added. These touch workflow correctness and default-skill freshness, so JWC must compare them against its `jwc goal`, `jwc orchestrate`, and bundled default definitions.

## Source Facts

| fact | upstream anchor |
|---|---|
| goal identity source | `4ad9f6d4` fix(ultragoal): eliminate goal-identity drift; single durable source |
| ralplan artifacts | `a3e2808b` fix(ralplan): persist role artifacts via env handoff |
| default skill refresh | `ef6569dc` fix(update): refresh stale on-disk default workflow skills after update |
| Fable 5 profiles | `f5150970`, `fc7dfcd2` |
| deep-interview simple task / clarification | `105a3e8c`, `64c1445f` |
| delegation guidance | `111274a8` docs(ultragoal): force sub-agent delegation on big scope |

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| workflow identity | Compare against JWC goal aggregate objective and ledger behavior before adopting. |
| plan artifacts | Env handoff may map to native `jwc planphase` persistence rules. |
| skill refresh | High-value if source-bundled workflow skills can drift after update. |
| Fable 5 | Model-profile support may need JWC naming and provider catalog checks. |

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | adapt candidates; workflow-state review required |
| Decision B — naming impact | `ralplan`/`ultragoal` source terms translate to `plan`/`goal` public docs |
| Decision C — test impact | goal/plan runtime tests if adopted |
| Decision D — docs impact | chase now; structure/AGENTS if workflow contract changes |
| Decision E — rollout impact | goal/orchestrate state integrity |
| Decision F — residual risk | medium-high; persistent state |
| Decision G — reviewer needed | backend/architecture |
| Decision H — bundle policy | separate workflow-state goal |

## WP3 Implementation Status

WP3 investigated the eight listed source commits against current JWC behavior. Overall status: **PARTIAL / core runtime changes implemented**.

| source | status | JWC disposition |
|---|---|---|
| `4ad9f6d4` goal identity | **IMPLEMENTED** | Complete checkpoints now validate the target record in durable `.jwc/goal/goals.json` and hash a compact identity derived from that record. The external `goal({"op":"get"})` snapshot is no longer required or trusted as a second identity source. The old receipt field and CLI flag remain compatibility shells so existing ledgers/callers can migrate without a schema break. `goal/SKILL.md` still contains old snapshot examples; updating it is DEFERRED because bundled skill files are outside WP3 ownership. |
| `a3e2808b` role artifacts | **ACCEPT** | JWC's native `planphase` writer persists role artifacts directly with run id, stage number, SHA-256, index entry, and pending-approval copy. This is stronger than an env-only handoff and uses JWC's `plan`/`orchestrate` naming. |
| `ef6569dc` default refresh | **ACCEPT** | Current `update-cli.ts` already invokes `jwc setup defaults` after a successful update and prints a manual recovery command if propagation fails. |
| `f5150970` Fable profiles | **IMPLEMENTED** | Added `claude-fable` and `fable-opus-codex`. The combo adapts to JWC's current GPT-5.6 Terra/Sol role selectors while retaining Fable 5 as default and Opus 4.8 for planning/critique. |
| `fc7dfcd2` Fable catalog restore | **ACCEPT** | Current generated model catalog already contains `anthropic/claude-fable-5` and its effort variants. No direct `models.json` edit was needed. |
| `105a3e8c` simple-task behavior | **ACCEPT** | JWC's public routing contract already keeps clear low-risk work on the direct path and invokes `jaw-interview` only for genuine ambiguity. No GJC deep-interview skill wording was copied. |
| `64c1445f` choice clarification | **DEFER** | JWC rejects blank free text, but it has no first-class “ask about these choices” non-answer result. A correct implementation crosses `ask`, selector/controller UI, RPC types, and tests outside WP3 ownership; an agent-wire-only partial shape would be dead protocol. |
| `111274a8` forced delegation | **PARTIAL / ACCEPT** | JWC Goal already requires parallel executors for large independent slices and keeps checkpoint ownership in the leader. GJC's numeric `3+ files` / `~200 lines` thresholds are policy text outside WP3 ownership and are not required for runtime correctness, so no stricter port was made. |

### Verification And Residuals

- Focused source formatting passed.
- Goal runtime retained broad behavior: 100+ goal tests passed, including checkpoint receipts, steering, stale target mutation, quality gates, and reconciliation.
- Six old goal tests now fail because they assert that the removed external snapshot source is mandatory or validated; these are expected contract-refresh gaps, not runtime blockers.
- Two profile tests fail only because they hard-code 17 built-ins and do not know the two new Fable profiles. Additional profile failures in the same run predate WP3 and assert superseded GPT-5.4/5.5 mappings.
- Full package typecheck is blocked by unrelated existing errors in `packages/ai/src/providers/cursor.ts` and `packages/coding-agent/test/file-lock-gc-toctou.test.ts`.

### Changed Files

- `packages/coding-agent/src/jwc-runtime/state-writer.ts`
- `packages/coding-agent/src/jwc-runtime/state-runtime.ts`
- `packages/coding-agent/src/jwc-runtime/goal-engine.ts`
- `packages/coding-agent/src/jwc-runtime/goal-guard.ts`
- `packages/coding-agent/src/config/model-profiles.ts`
- `struct_har/chase/10.070_gjc_chase_workflow_intent_state_artifacts.md`
- `struct_har/chase/10.067_gjc_chase_goal_plan_skill_state_refresh.md`
