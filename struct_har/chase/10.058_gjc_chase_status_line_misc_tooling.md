# 10.058 — gjc chase: status line + misc tooling (memory GC · web-search · coordinator · steer)

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P3**
> Split from [10.053](./10.053_gjc_chase_local_provider_terminal_bell_windows.md) (cluster 5 of 5).
> Reviewed source: GJC `fa995807` (`upstream/dev`, v0.7.7) vs JWC worktree `ef28a98` (2026-06-29).
> Range: 55-commit cluster `a791d72a..fa995807`; this card isolates the status-line + misc-tooling polish sub-cluster.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](./008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

The tail of the 55-commit delta is polish: status-line custom editor, Codex usage fix in the status line, custom model preset snapshot, and a set of small tooling fixes (safety-bounded memory GC, configurable web-search timeout, coordinator-mcp ack/await timeouts, busy-queued steer without Esc, redundant-tool pruning, prompt scroll-to-previous, per-story try-harder nudge guard).

Behavioral surface:
1. Status line: custom editor, Codex usage fix, custom model preset snapshot.
2. Memory GC: safety-bounded GC for browser & computer-use tools.
3. web-search: configurable hard timeout (default 300s).
4. coordinator-mcp: require runtime prompt ack; honor long await timeouts.
5. Busy-queued steer delivered without requiring Esc.
6. Tool pruning: `inspect_image`→`read`, cron trio→`cron`.
7. Misc UX: prompt action to scroll to previous user input; ultragoal per-story try-harder nudge guard; live terminal width for prompt rendering.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `fa995807` |
| status custom editor | `c1794d2b` feat(coding-agent): add status line custom editor (#1256) |
| Codex usage in status | `e2ca8279` Fix Codex usage in status line (#1255) |
| model preset snapshot | `d5ed0705` Fix custom model preset snapshot generation (#1244) |
| memory GC bounds | `8431b14e` feat(tools): add safety-bounded memory GC for browser & computer-use tools (#1227) |
| web-search timeout | `6f155095` feat(web-search): configurable hard timeout, default 300s (#1226) |
| coordinator ack | `12302778` fix(coordinator-mcp): require runtime prompt ack (#1251) |
| coordinator await | `e34b9087` fix(coding-agent): honor long coordinator await timeouts (#1241) |
| busy-queued steer | `1383957e` fix(coding-agent): deliver busy-queued steer without requiring Esc (#1223) |
| tool pruning | `278e8d8f` refactor(tools): prune redundant tools (inspect_image→read, cron trio→cron) (#1225) |
| prompt scroll | `70ed8684` Add prompt action to scroll to previous user input (#1233) |
| try-harder nudge | `86c3fc8f` feat(ultragoal): per-story try-harder nudge guard (#1224) |
| prompt width | `e5535779` fix(tui): use live terminal width for prompt rendering (#1259) |
| notify daemon slim | `b99246c1` fix(coding-agent): slim notify daemon startup (#1243) |
| primary source areas | `packages/coding-agent/src/`, `packages/tui/`, tools surface |
| comparison range | `a791d72a..fa995807` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 8431b14e --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 278e8d8f --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| status line | Adapt: custom editor/model preset useful; verify no `gjc` literals leak into JWC status display. |
| memory GC | Import bounds for browser/computer-use tools. |
| web-search timeout | Import configurable hard timeout (overlaps 10.043 _fin web-search hardening). |
| coordinator-mcp | Adapt ack/await to JWC coordinator naming. |
| tool pruning | Confirm JWC tool catalog already matches (`read` absorbs `inspect_image`; single `cron`). |
| busy-queued steer | Check against JWC steer delivery (overlaps OMP 20.005 _fin). |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [ ] Source facts checked against `fa995807` or a newer recorded GJC head.
- [ ] JWC owner files listed before implementation.
- [ ] Naming translation checked against [008](./008_gjc_jwc_naming_contract.md).
- [ ] Import/adapt/reject decision recorded per item.
- [ ] Focused test or manual evidence for adopted items.
- [ ] `git diff --check` passes.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | open: mixed import/adapt/reject per item |
| Decision B — naming impact | command/help text + status-line display strings |
| Decision C — test impact | focused tests for memory GC bounds + web-search timeout |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | no migration; polish + safety bounds |
| Decision F — residual risk | open until per-item triage |
| Decision G — reviewer needed | backend (coordinator/tools), frontend (status line) |
| Decision H — bundle policy | low-priority polish bundle; align with 10.043 / 20.005 |
