# 20.009 — omp chase: isolated profiles and profile-scoped auth/config

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · **P2**
> Reviewed source: OMP `dc14689fc` (`05fd49955` #1435, v15.13.1–v16.0.2 range) vs JWC `d60b7822` lineage.
> **참조 원칙**: 1:1 이식 아님. JWC `.jwc`/profile/auth boundary에 맞춰 설계만 채굴.

## 왜 gap인가

OMP added isolated profiles via `--profile`, `OMP_PROFILE`, alias bootstrap, profile-scoped config discovery, profile-scoped MCP OAuth handling, and active-profile managed skills. JWC has its own runtime state and public workflow surface; useful part is profile boundary and auth isolation semantics.

## Source facts

| fact | OMP evidence |
|---|---|
| CLI profile/alias | `packages/coding-agent/src/cli/profile-alias.ts`, `profile-bootstrap.ts` |
| profile dirs | `packages/utils/src/dirs.ts` |
| OAuth per profile | `packages/coding-agent/src/mcp/oauth-credentials.ts` |
| tests | `test/profile-*.test.ts`, `test/mcp-profile-auth-binding.test.ts` |

## JWC reconcile

- `OMP_PROFILE` would become a JWC-specific setting/env only after product decision.
- Auth isolation is the key design fact; plugin/skill marketplace details are secondary.
- Must not conflict with `.jwc/state` session-scoped workflow ledgers.

## 완료 기준

- [ ] Decide whether JWC supports named profiles or only project/session state.
- [ ] If adopted, auth/config roots are profile-scoped and test-covered.
- [ ] Public docs avoid OMP naming leakage.
