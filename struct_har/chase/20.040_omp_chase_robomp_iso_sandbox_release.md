# 20.040 — OMP chase: robomp, ISO copy, sandbox, release/update references

Status: active reference card
Lane: OMP
Source range: `0ea6ea630..d0c1890a6` (`origin/main`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

OMP added or hardened several non-core but operationally relevant surfaces:

- `16600e89a`, `dfaa41f6b`, `b7bcc0bbf`, `6d16c19a0`, `22e9224ae`, `b3c7646b4` — `python/robomp` sandbox/worktree/commit-message repair/linting behaviors.
- `2335c717a` and `crates/pi-iso/src/rcopy.rs` — copy/apply stderr draining and ISO copy hardening.
- Dockerfile/dependency installation updates: `4d80a7bb1`, `6d2e9f8f7`.
- Update/usage CLI and release reporting fixes: `eacaf65ea`, `469333d74`, `8c648e4d0`, `5c171b286`, `packages/coding-agent/src/cli/update-cli.ts`, `usage-cli.ts`.
- Misc process/network timeout hardening: `76c480646`, `5bc796d4c`, `7d1ab692a`, `3315ef000`.

## JWC decision

Classification: **reference/split**.

JWC has `python/robojwc` directory-specific rules and release/update behavior that may diverge from OMP. Any Python-side adoption must read `python/robojwc/AGENTS.md` first. Release/update changes must be checked against `structure/60_release_publishing.md` and JWC's OIDC-first policy.

## JWC owner candidates

- `python/robojwc/` or equivalent JWC Python automation surface
- `crates/pi-natives/` / ISO-copy equivalents if present
- `packages/coding-agent/src/cli/update-cli.ts`
- `packages/coding-agent/src/cli/usage-cli.ts`
- Docker/release scripts only after release-policy review

## Verification expectation

Before closing, split by surface. Python sandbox/worktree behavior needs Python tests; release/update behavior needs CLI tests and release-policy consistency; network/process timeouts need focused lifecycle tests. Do not mix these into an unrelated product-code patch.

## Refresh — OMP v16.3.4 to v16.3.12

Source cluster: `d0c1890a6..f25ab54c5` (541 commits). Runtime/sandbox/release anchors: `18154fb78` updates Docker generation to run tool-view generation alongside docs, `165a33eec` adds install-script network timeouts, `0de57eb75` avoids `os.cpus()` during startup, `ff0419e41` tags routine cleanup errors and deadlines, `0006252d6` validates tool-error handling and cleanup propagation, `04258b98d`, `1877c7ed2`, `79e083124`, and `ce7630906` harden `pi-shell` spawn registry behavior on Windows, `1d7e20115` extracts shared ISO command-failed formatting, and `48c53f8c5` records a release/CI dependency-yank response.

JWC decision: still **reference/split**. Release and sandbox facts must be checked against JWC OIDC release policy and `python/robojwc/AGENTS.md` before any runtime adoption.

Verification expectation: install timeout tests, spawn registry lifecycle tests, release-policy consistency checks, and Python sandbox tests only if the relevant JWC surface is touched.
