# 20.125 — OMP chase: task, rebuild, search, and rendering contracts

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P1 — Split task API decisions from autonomous rebuild, debug, search parsing, and renderer hardening.
> Reviewed source: OMP `v17.1.0..59619623` vs JWC worktree.

## Chase Scope

**Classification: split · autonomy bucket: C.** Removing per-call task model selection conflicts with JWC's callable role/model surface and requires a user decision; the remaining correctness slices are A candidates.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi show --stat c78066288 9f8aa87db e7bb0556a c6dcfa413 03e6564c0 759e551e8 db937bd14 484fa319e 5acdefc7b ff49b986d 68e76c624 e0509d5d6 3c80e6f9c
rg -n 'agentModelOverrides|async:|raw-sse|liveToolComponents' packages/coding-agent/src
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `c78066288` | Adds fallback routing for unadvertised tool calls. | `packages/agent/src/agent-loop.ts`; `types.ts`; coding-agent SDK |
| `9f8aa87db` | Removes per-call task model override. | `coding-agent/src/task/types.ts`; task prompts/schema/tests |
| `e7bb0556a` | Adds concurrent-edit guidance to task prompt. | `coding-agent/src/prompts/tools/task.md` |
| `c6dcfa413` | Compacts oversized raw SSE debug payloads. | `coding-agent/src/debug/raw-sse-buffer.ts` |
| `03e6564c0` | Accepts bare bullet-minus rows in hashline. | `packages/hashline/src/parser.ts`; `messages.ts` |
| `759e551e8`, `484fa319e`, `68e76c624`, `3c80e6f9c` | Corrects mid-stream rebuild preservation for resolved, async, and shared live tools; exposes options. | `coding-agent/src/modes/interactive-mode.ts`; `modes/utils/ui-helpers.ts` |
| `db937bd14` | Adds coarse task effort. | `coding-agent/src/task/`; `thinking.ts` |
| `5acdefc7b` | Introduces structured web-search query parsing. | `coding-agent/src/web/search/query.ts`; provider adapters/tests |
| `ff49b986d` | Adds language-specific display formatters. | `coding-agent/src/tools/eval-format/`; browser/eval renderers |
| `e0509d5d6` | Preserves catalog-first bench resolution. | `coding-agent/src/cli/bench-cli.ts` |

## Worktree Verification

| probe | actual JWC tree evidence |
| --- | --- |
| Task model contract | `packages/coding-agent/src/config/model-registry.ts:98,111` and `packages/coding-agent/src/task/index.ts:1114` retain settings-based role overrides; removal is a public workflow decision. |
| Async tasks | `packages/coding-agent/src/task/index.ts:675,990` exposes running async job handles, so rebuild fixes need JWC-specific regression fixtures. |
| Raw SSE | `packages/coding-agent/src/debug/index.ts:18-32,103` exposes the viewer/buffer owner; payload compaction is an adaptable local hardening. |
| Live tools | `packages/coding-agent/src/modes/controllers/event-controller.ts:117-199` owns current live components; upstream preservation rules cannot be inferred equivalent. |

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD C** | Retain JWC role/model selectors or remove per-call selection. |
| 2 | **OPEN A** | Adapt rebuild/debug/hashline fixes with focused regressions. |
| 3 | **OPEN A** | Compare structured query parsing against JWC's bundled hidden `search` workflow before adoption. |

