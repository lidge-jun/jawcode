# 10.007 — gjc chase: team `@gjc-profile` self-heal

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P1**
> Reviewed: GJC `050aa17` (#546) vs JWC `d60b7822`.

## 왜 gap인가

Upstream distinguishes a **gjc-launched** tmux leader that lost `@gjc-profile` from a foreign session. Self-heal only when `GJC_TMUX_LAUNCHED_ENV === "1"`; otherwise `unmanaged_tmux_session`.

## Source facts (upstream)

| fact | upstream |
|------|----------|
| `retagGjcLaunchedTmuxSession` | `team-runtime.ts:1646-1654` |
| env-guarded retag | `1668-1676` |
| foreign → `unmanaged_tmux_session` | `1676-1681` |

## JWC reconcile (2026-06-14)

| item | jwc |
|------|-----|
| `@gjc-profile` option constants | **✅** `tmux-common.ts` |
| Launch marker env | **✅** `GJC_TMUX_LAUNCHED_ENV` in `launch-tmux.ts` |
| Leader requires profile value | **✅** strict check — **no retag** |
| Upstream self-heal retag | **⬜ missing** |

### jwc 코드 스니펫

```typescript
// packages/coding-agent/src/jwc-runtime/tmux-common.ts
export const GJC_TMUX_PROFILE_OPTION = "@gjc-profile";
export const GJC_TMUX_PROFILE_VALUE = "1";

// packages/coding-agent/src/jwc-runtime/launch-tmux.ts
export const GJC_TMUX_LAUNCHED_ENV = "GJC_TMUX_LAUNCHED";
// tmux new-session command sets: exec env GJC_TMUX_LAUNCHED=1 …

// packages/coding-agent/src/jwc-runtime/team-runtime.ts (~1655)
if (readJwcTmuxProfileValue(tmuxCommand, sessionName) !== GJC_TMUX_PROFILE_VALUE)
  throw new Error(buildTeamTmuxLeaderRequirementMessage(`unmanaged_tmux_session:${sessionName}`));
```

**포팅 시**: public surface는 `jwc` / `.jwc` 유지; 내부 env 이름 `GJC_TMUX_LAUNCHED`는 [008 D4](./008_gjc_jwc_naming_contract.md) dual-read 전례.

## 완료 기준

- [ ] Port or explicit non-goal in [002](./002_gap_inventory.md).
- [ ] Tests: tagged leader · launch-marker untagged heal · foreign reject.