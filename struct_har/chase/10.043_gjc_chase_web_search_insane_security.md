# 10.043 — gjc chase: web search and public URL hardening

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P1**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: 638d2f11, 6527ee01 with search/read URL commits 7a9b53a9, 5554ffd1, 70173544, 1380d4ae, d90c7a8f, e49d93f8, 4cc65051, 730e00b3, e8baec13, 42a00dbe, 6a1cb18c, 0471c35d.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](./008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the web search and public URL hardening cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Native model-provider search, configurable fallback, provider-id local baseUrl guard, public/private URL boundary, citation/read hardening.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | 638d2f11, 6527ee01 with search/read URL commits 7a9b53a9, 5554ffd1, 70173544, 1380d4ae, d90c7a8f, e49d93f8, 4cc65051, 730e00b3, e8baec13, 42a00dbe, 6a1cb18c, 0471c35d |
| primary source areas | `packages/coding-agent/src/web/search/**; packages/coding-agent/src/tools/read/**; vendor/insane-search/**; packages/coding-agent/test/**/*search*` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- packages/coding-agent/src/web/search/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- packages/coding-agent/src/web/search/**
```

## Phase 1B Hardening Note

Web-search/read bucket checked at GJC `a791d72a`; existing native search and local-baseUrl guard anchors were preserved while insane-search, auth-required token, citation, provider API, official endpoint, CI, and private-network URL commits were made explicit.

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [ ] Source facts checked against `a791d72a` or a newer explicitly recorded GJC head.
- [ ] JWC owner files listed before implementation.
- [ ] Naming translation checked against [008](./008_gjc_jwc_naming_contract.md).
- [ ] Import/adapt/reject/split decision recorded for each sub-feature.
- [ ] Focused test or explicit manual evidence proves the chosen behavior.
- [ ] `git diff --check` passes for the patch.
- [ ] MOC/follow-index/gap inventory updated if priority or status changes.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## JWC Phase 7 Split Evidence — 2026-06-28

Phase 7 records this card as the search/public URL boundary owner inside the security/provider/network bundle.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/70_phase7_security_provider_network_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/73_phase7_search_url_boundary_split.md`

JWC owner surfaces:

- `packages/coding-agent/src/web/search/**`
- `packages/coding-agent/src/tools/read.ts`
- `docs/tools/web_search.md`
- `docs/tools/read.md`
- `packages/coding-agent/test/tools/web-search-*.test.ts`
- `packages/coding-agent/test/read-tool-group.test.ts`

Candidate follow-up slices:

- `10.043-A`: private/local network deny tests for URL reads and public fetch paths.
- `10.043-B`: search provider auth/baseUrl guard tests, especially direct provider-id mappings.
- `10.043-C`: citation/read hardening without upstream snapshot parity.

Status: still active. Phase 7 records split boundaries only; it does not close web search/public URL done-gates.

## JWC Phase 24 Partial Evidence — 2026-06-28

Slice `10.043-A` landed as a JWC-native syntactic private/local URL guard for public URL reads.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/240_phase24_url_private_network_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/241_phase24_url_private_network_audit.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/242_phase24_url_private_network_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/243_phase24_url_private_network_check.md`

JWC implementation evidence:

- `packages/coding-agent/src/web/public-fetch-url.ts`
- `packages/coding-agent/src/tools/fetch.ts`
- `packages/coding-agent/src/web/scrapers/types.ts`
- `packages/coding-agent/src/web/scrapers/utils.ts`
- `packages/coding-agent/test/read-tool-group.test.ts`
- `packages/coding-agent/test/fetch-private-network.test.ts`

Verified behavior:

- Direct URL reads reject `localhost`, `*.localhost`, `0.0.0.0`, loopback IPv4, link-local IPv4, RFC1918 IPv4, IPv6 loopback, IPv6 link-local, IPv6 unique-local, and URL credentials before public fetch execution.
- `loadPage()` and `fetchBinary()` manually validate redirect hops and do not fetch private/local redirect targets.
- Subprocess refetch fallbacks receive guarded public URLs.

Verification:

- `bun test packages/coding-agent/test/read-tool-group.test.ts packages/coding-agent/test/fetch-private-network.test.ts` -> 36 pass, 0 fail, 65 expect() calls.
- `cd packages/coding-agent && bun run check:types` -> pass.
- scoped `git diff --check` -> pass.

Residual risk:

- DNS rebinding and post-resolution IP validation remain out of scope.
- `.local`, `.internal`, and `.home.arpa` hostnames remain deferred unless they match explicit syntactic deny rules.
- `10.043-B` search provider auth/baseUrl guard tests and `10.043-C` citation/read hardening remain open.

Status: still active. This closes only candidate slice `10.043-A`.

## JWC Phase 25 Partial Evidence — 2026-06-28

Slice `10.043-B` was partially hardened as JWC-native search provider mapping evidence.

Upstream anchor:

- GJC `6527ee01 fix(web-search): apply local-baseUrl guard to direct provider-id mapping`

JWC decision:

- Upstream `6527ee01` operates on an `ActiveSearchModelContext` with `provider`, `baseUrl`, `api`, and `webSearch`.
- Current JWC `resolveProviderChain()` receives only `activeModelProvider?: string`, so the exact upstream `provider: "openai"` + local `baseUrl` guard is not directly portable without a resolver API expansion.
- This phase therefore pins current JWC behavior for local registry provider aliases and records the exact upstream baseUrl-aware guard as deferred.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/250_phase25_search_provider_mapping_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/251_phase25_search_provider_mapping_audit.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/252_phase25_search_provider_mapping_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/253_phase25_search_provider_mapping_check.md`

JWC implementation evidence:

- `packages/coding-agent/test/tools/web-search-duckduckgo.test.ts`

Verified behavior:

- `ollama`, `llama.cpp`, and `lm-studio` active model provider strings fall back to DuckDuckGo in auto web-search mode even when stray OpenAI/Codex OAuth exists.
- Existing hosted OpenAI positive controls remain unchanged: `openai` and `openai-codex` still map to `codex` when OAuth is available.
- Keyed standalone providers remain explicit-selection only and are not auto-selected by credential presence.

Verification:

- `bun test packages/coding-agent/test/tools/web-search-duckduckgo.test.ts` -> 25 pass, 1 skip, 0 fail, 37 expect() calls. The skip is the existing opt-in live DuckDuckGo e2e.
- `cd packages/coding-agent && bun run check:types` -> pass.
- scoped `git diff --check` -> pass.

Residual risk:

- Exact `provider: "openai"` + local OpenAI-compatible `baseUrl` denial remains deferred until JWC passes active model context into search provider resolution.
- `10.043-C` citation/read hardening remains open.

Status: still active. This partially closes candidate slice `10.043-B` but does not close the full card.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | Translate public surface to `jwc`/`.jwc`; preserve upstream names only as source citations. |
| Decision C — test impact | Focused tests required unless this remains docs-only/track-only. |
| Decision D — docs impact | chase now; promote only stable implemented contracts. |
| Decision E — rollout impact | classify during implementation; assume local/runtime risk until proven otherwise. |
| Decision F — residual risk | open until implementation or explicit rejection evidence exists. |
| Decision G — reviewer needed | backend/security/architecture depending on touched surface. |
| Decision H — bundle policy | separate card; can join a PABCD bundle only with adjacent cards named here. |
