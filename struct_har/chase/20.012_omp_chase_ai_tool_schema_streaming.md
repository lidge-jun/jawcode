# 20.012 — omp chase: AI tool schema and streamed argument hardening

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · **P1**
> Reviewed source: OMP `dc14689fc` (`269359cd5`, `489776004`, `781180e58`, `5e1215f0e`, `57bd545b3`) vs JWC `d60b7822` lineage.
> **참조 원칙**: provider/schema hardening is selectively portable; OMP catalog/provider surface is not adopted wholesale.

## 왜 gap인가

OMP v16.x hardened malformed MCP/tool schemas, streamed object argument merging, prefixed Responses deltas, Gemini AUTO raw-planning behavior, nullable enum schemas, and JSON array-string coercion. These are broad tool-use correctness risks for JWC if equivalent protections are absent.

## Source facts

| fact | OMP evidence |
|---|---|
| strict schema quarantine | `packages/ai/src/utils/schema/strict-tool-validation.ts` |
| schema TS/wire utils | `packages/ai/src/utils/schema/*` |
| OpenAI/Codex stream args | `packages/ai/src/providers/openai-*.ts`, Cursor stream handlers |
| tests | `schema-strict-mode.test.ts`, `tool-argument-coercion.test.ts`, `openai-responses-tool-quarantine.test.ts` |

## JWC reconcile

- Cross-link JWC provider/schema drift under `99.02` and GJC [10.031](./10.031_gjc_chase_provider_auth_reliability.md).
- Prioritize corruption/fabrication prevention over catalog breadth.
- Verify any adopted schema coercion does not mask bad tool contracts.

## 완료 기준

- [ ] JWC tool schema validation failure modes compared to OMP.
- [ ] Streaming argument merge behavior covered by regression tests.
- [ ] Bad MCP schemas are quarantined without disabling unrelated tools.
