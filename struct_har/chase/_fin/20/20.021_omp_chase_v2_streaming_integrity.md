# 20.021 — omp chase: v2 streaming integrity + replay

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · closed 260701 (IMPORT, 1 JWC slice: partialJson terminal scrub)
> Split from [20.016](../../20.016_omp_chase_advisor_thinking_dialect_session_title.md) (cluster 5 of 6).
> Reviewed source: OMP `0fc6d136` (v16.1.20) → `ca9f2847e` (v16.2.5) vs JWC worktree `ef28a98` (2026-06-29).
> Source range: `devlog/_omp_chase/oh-my-pi` `0fc6d136..ca9f2847e`. **1:1 port ❌ — design reference.**
> Cross-link: replay integrity overlaps GJC [10.055](../10/10.055_gjc_chase_codex_replay_stability.md).

## Why This Is A Gap

OMP reworked v2 streaming for integrity: symbol-based streaming state to isolate per-stream metadata, partial-json leakage cleanup, a side-channel Map for partialJson, v2 stream integrity + replay logic, and v2 streaming remote compaction for model history. This may inform JWC's streaming/compaction/replay design and overlaps the GJC replay-stability card.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `0fc6d136..ca9f2847e` |
| v2 stream integrity & replay logic | `08e1ea727` |
| symbol-based streaming state (isolated metadata) | `357c29224` |
| partial-json leakage cleanup | `cfa0cd84b` |
| v2 streaming remote compaction for model history | `102d6d54a` |
| side-channel Map for partialJson | `9c795f886` |
| primary path anchors | `packages/ai/{src,test}` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 08e1ea727 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 102d6d54a --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| streaming integrity | Evaluate symbol-based metadata isolation + partial-json cleanup against JWC streaming layer. |
| replay overlap | Cross-check GJC 10.055 (Codex replay stability) for shared concerns. |
| compaction | v2 remote compaction may inform JWC compaction/replay design. |
| tests | Focused JWC tests only if adapted. |

## Done Gate

- [x] Source anchors rechecked against OMP `ca9f2847e` — 5 commits triaged (cfa0cd84b/08e1ea727/357c29224/9c795f886/102d6d54a).
- [x] GJC 10.055 reviewed for replay overlap — distinct surface (Codex encrypted replay vs JWC v2 proxy partialJson); no shared code path.
- [x] Adopt/defer/reject decision recorded — IMPORT cfa0cd84b (scrub), DEFER③ Map/symbol/compaction (see Closure table).
- [x] Adopted behavior has JWC tests — `packages/agent/test/proxy-toolcall-partial-json.test.ts` 5 pass/0 fail.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only; evaluate against JWC streaming layer |
| Decision B — naming impact | none now |
| Decision C — test impact | future focused tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | none in reference phase |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | backend (ai stream) if promoted |
| Decision H — bundle policy | may align with GJC 10.055 |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: IMPORT**

IMPORT — v2 streaming integrity/replay 무결성 채택. 회귀가드(스트리밍 레이어 대비) 명시.

## Closure (260701) — IMPORT, 1 JWC slice

JWC gap: `packages/agent/src/proxy.ts` stored streaming `partialJson` on typed ToolCall blocks via `as any` and only deleted it in the happy-path `toolcall_end`. On abort/error/early-done the field leaked into the finalized AssistantMessage.

JWC slice (commit `e84dc5d`): added `scrubPartialJson(partial)` helper; invoked at the three terminal emit sites — `catch` (before `error` push), SSE `done` handler (before final `message`), SSE `error` handler (before final `error`). Streaming-time blocks keep `partialJson` (consumer contract preserved: `event-controller.ts:475`, `ui-helpers.ts:474`, `session-transcript-replay.ts:413` read `"partialJson" in content` for mid-stream preview UX). Happy-path `toolcall_end` delete retained.

### 5-commit triage

| OMP commit | judgement | rationale |
|---|---|---|
| `cfa0cd84b` partial-json leakage cleanup | **IMPORT** | scrub-only essence, adapted to JWC v2 proxy terminal paths |
| `357c29224` symbol-based streaming state | DEFER ③ | symbol carrier breaks 3 JWC coding-agent consumers reading `"partialJson" in content` — needs consumer migration |
| `9c795f886` side-channel Map for partialJson | DEFER ③ | Map-only access breaks same consumers — needs consumer migration |
| `08e1ea727` v2 stream integrity & replay | DEFER ③ | JWC already has remote compaction (`compaction/openai.ts`, `remote-compaction.test.ts`) |
| `102d6d54a` v2 streaming remote compaction | DEFER ③ | same — JWC compaction layer already present |

### Audit & verification

- A-phase audit (Turing, gpt-5.4): initial Map/symbol design **FAIL** (would break coding-agent partialJson consumers) → corrected to scrub-only.
- Independent reviewer (Socrates, gpt-5.4): **PASS** — terminal leak paths scrubbed, consumer contract preserved, happy-path intact, no new `as any`, naming clean.
- Verification: `bun test packages/agent/test/proxy-toolcall-partial-json.test.ts` 5 pass/0 fail; `tsgo --noEmit` EXIT 0; biome clean; naming violations 0 (added lines); all cited SHAs resolve; `git diff --check` clean.
- Commit: `e84dc5d` fix(agent): scrub streaming partialJson from finalized proxy messages (20.021).
