# 20.049 — OMP chase: OAuth refresh serialization and MCP lifecycle

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · reference-only
> Reviewed source: OMP f25ab54c5..7aa1d581c (origin/main, v16.3.12→v16.4.2) vs JWC worktree 0e8d9f4+dirty.
> Closed: 2026-07-17 · CLOSE_NOW — current JWC auth storage and runtime-MCP owners cover refresh coalescing, race protection, scopes, and discovery reconciliation.

## Why This Card Exists

OMP hardened two connected seams: OAuth credential refresh under concurrency and MCP discovery/auth
lifecycle after startup. JWC has substantial but differently named ownership in
`packages/ai/src/auth-storage.ts` and auth-broker/provider code, plus
`packages/coding-agent/src/runtime-mcp/`, `packages/coding-agent/src/session/agent-session.ts`, and
SDK/model discovery. This G2 card extracts race and reconciliation invariants only; it does not direct
a 1:1 port from OMP's `src/mcp/` layout.

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `b60dc669e` | Fences OAuth refresh writes so stale completions cannot overwrite newer credentials. | `packages/ai/src/auth-storage.ts`; `packages/ai/test/auth-storage-oauth-refresh-race.test.ts`; `packages/coding-agent/src/mcp/manager.ts` |
| `cb3285313` | Renews refresh leases across long-running refresh work. | `packages/ai/src/auth-storage.ts`; `packages/ai/test/auth-storage-oauth-refresh-race.test.ts`; `packages/coding-agent/test/mcp-manager-oauth-refresh.test.ts` |
| `cf021ad39` | Serializes MCP OAuth refreshes through shared auth storage. | `packages/ai/src/auth-storage.ts`; `packages/coding-agent/src/mcp/manager.ts`; `packages/coding-agent/test/mcp-manager-oauth-refresh.test.ts` |
| `497d385ce` | Treats login success independently from immediate model-list refresh success. | `packages/coding-agent/src/modes/controllers/selector-controller.ts`; `packages/coding-agent/test/controllers/selector-controller-login.test.ts` |
| `898643f9a` | Refreshes expired OAuth while discovering built-in models/providers. | `packages/coding-agent/src/config/model-registry.ts`; `packages/catalog/src/model-manager.ts`; `packages/coding-agent/test/model-discovery.test.ts` |
| `bce6aa89a` | Sends requested OAuth scopes during MCP dynamic client registration. | `packages/coding-agent/src/mcp/oauth-flow.ts`; `packages/coding-agent/test/oauth-flow.test.ts` |
| `390a4ae92` | Reconciles MCP servers discovered after SDK/session construction. | `packages/coding-agent/src/sdk.ts`; `packages/coding-agent/test/sdk-mcp-auto-discovery.test.ts` |
| `bf6480647` | Keeps macOS stdio MCP servers attached instead of detaching their process lifecycle. | `packages/coding-agent/src/mcp/transports/stdio.ts`; `packages/coding-agent/src/modes/controllers/input-controller.ts`; stdio transport test |
| `0d606f2f3` | Preserves MCP tools when a caller applies the general tools filter. | `packages/coding-agent/src/sdk.ts`; `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/test/sdk-mcp-instructions.test.ts` |
| `3e9057622` | Updates profile-to-auth binding expectations for MCP tests. | `packages/coding-agent/test/mcp-profile-auth-binding.test.ts` |

## Reference Decisions

| commit group | decision | rationale |
| --- | --- | --- |
| `b60dc669e`, `cb3285313`, `cf021ad39` | **adapt** | JWC needs one credential-generation/lease authority so concurrent provider and MCP refreshes cannot regress stored tokens. |
| `497d385ce`, `898643f9a` | **adapt** | Separate durable login success from optional discovery refresh, while still refreshing expired credentials before authenticated discovery. |
| `bce6aa89a` | **adapt** | Scope propagation is an OAuth protocol invariant; map it into JWC's registration/auth-broker contract. |
| `390a4ae92`, `0d606f2f3` | **adapt** | Late-discovered MCP servers and filtered tool sets must reconcile through JWC's runtime discovery registry. |
| `bf6480647` | **reference/adapt** | Adopt only if JWC reproduces macOS child detachment; process ownership must remain explicit and platform-tested. |
| `3e9057622` | **reference** | Use as a test-matrix reminder for profile credential binding, not as production behavior. |

Reject direct path translation from OMP `src/mcp/manager.ts` to JWC: JWC's visible owner is
`src/runtime-mcp/` and its session-level discovery state is broader than the OMP layout.

## Evidence Commands Run

```bash
for h in b60dc669e cb3285313 cf021ad39 497d385ce 898643f9a bce6aa89a 390a4ae92 bf6480647 0d606f2f3 3e9057622; do
  git -C devlog/_omp_chase/oh-my-pi show --stat --oneline "$h"
done
rg -n "OAuth|oauth|refresh lease" packages/ai/src packages/coding-agent/src --glob '*.ts'
rg -n "MCP|mcp.*discover|tools filter|dynamic client" packages/coding-agent/src --glob '*.ts'
```

## JWC Worktree Verification

| probe | exists / missing | JWC owner signal |
| --- | --- | --- |
| Session MCP discovery and selected-tool reconciliation | **exists** | `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/runtime-mcp/discoverable-tool-metadata.ts` |
| Runtime MCP manager surface | **exists, renamed** | `packages/coding-agent/src/runtime-mcp/manager.ts` rather than OMP's `src/mcp/manager.ts` |
| OAuth/provider discovery seams | **exists** | `packages/ai/src/`; model/provider discovery under `packages/coding-agent/src/` |
| Literal refresh-lease contract | **missing/unclear** | The targeted search did not establish OMP-equivalent lease fencing; requires owner-level test review. |

Close only with race tests for stale refresh completion and lease renewal, discovery tests for expired
OAuth and late MCP arrival, scope registration proof, macOS stdio lifecycle evidence where applicable,
and tool-filter/profile-binding regression coverage.

## WP9 triage — 2026-07-11

**REFERENCE-NOTED.** JWC covers runtime MCP discovery/reconciliation and central auth-storage ownership, but no equivalent refresh-generation lease/fencing contract was established. Keep stale-write prevention, lease renewal, scope propagation, and late-discovery reconciliation as future auth/MCP test requirements; OMP's manager layout and process rules are not directly applicable.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | **CLOSE_NOW / reference-covered**; JWC now has explicit refresh race tests and shared in-flight refresh ownership. |
| Decision B — naming impact | none; keep `AuthStorage` and `runtime-mcp/` as canonical JWC owners. |
| Decision C — test impact | no new test for docs-only closure; retain OAuth mutex/race, broker generation, MCP OAuth scope, and discovery tests. |
| Decision D — docs impact | this chase card only. |
| Decision E — rollout impact | none; no auth or MCP runtime code changed. |
| Decision F — residual risk | OMP's renewable lease/process model is implementation-specific; future cross-process refresh changes need their own fencing review. |
| Decision G — reviewer needed | auth/security and runtime-MCP review only for future behavior changes. |
| Decision H — bundle policy | keep auth refresh, OAuth protocol, stdio lifecycle, and discovery reconciliation as separate owner slices. |

## Closure Evidence — 2026-07-17

- `packages/ai/src/auth-storage.ts:683-684` coalesces refreshes, and `packages/ai/test/auth-storage-oauth-refresh-race.test.ts:244-284` proves same-credential refresh serialization.
- `packages/ai/src/auth-storage.ts:2965` rejects a stale refresh-token basis after re-reading the current credential.
- `packages/coding-agent/src/runtime-mcp/oauth-flow.ts:111-112` and `:173-174` carry discovered/requested scopes into authorization.
- `packages/coding-agent/src/runtime-mcp/manager.ts:282` owns discovery/connect, while `packages/coding-agent/src/sdk.ts:1246-1255` exposes selected/discoverable tool reconciliation through the session.
