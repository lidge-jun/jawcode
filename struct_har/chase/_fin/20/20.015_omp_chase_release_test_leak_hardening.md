# 20.015 — omp chase: release test leak hardening

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ _fin 260701 · reference-only / track-only closure
> Reviewed source: OMP `cc0c67be` (v16.1.13) → `0fc6d136` (v16.1.20) vs JWC worktree (2026-06-28).
> Source range: `devlog/_omp_chase/oh-my-pi` `cc0c67be..0fc6d136`.
> Cross-link: related GJC card [10.048](../10/10.048_gjc_chase_dev_ci_release_packaging.md).

## Why This Is A Gap

OMP's v16.1.13→v16.1.20 range contains a concentrated release test leak hardening reference slice. JWC should not copy it 1:1; this card preserves the source facts so later implementation work can decide whether to adapt, split, or reject each behavior in JWC terms.

Summary: legacy compile entrypoints, changelog/version bumps, release asset docs, llama cpp context, task max concurrency, MCP tool args, goal compaction release merge, terminal/TUI bench references.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `cc0c67be..0fc6d136` |
| source commits | 08977b226, 0c1ef77a7, 7f7a7e59d, 374c51122, 2787b6dff, 2c3e0d79a, e431f5caa, f278ea6a9, 93d730c01, 6591fc29a, aed01caaa, 5a50b047d, 4e56e8bd3, 7690dfe7f, 0fc6d136c |
| primary path anchors | `.github/**; packages/*/test/**; packages/tui/bench/**; packages/terminal-bench/**; docs/**; changelogs` |
| dirstat source | `git -C devlog/_omp_chase/oh-my-pi diff --dirstat=files,0 cc0c67be..0fc6d136` |

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| reference-only | Treat OMP as design/test evidence, not a direct port source. |
| GJC overlap | Check [10.048](../10/10.048_gjc_chase_dev_ci_release_packaging.md) before implementation to avoid duplicate chase work. |
| naming | Public docs stay JWC-first: `jwc`, `.jwc`, `@jawcode-dev/*`. |
| tests | If implemented, use JWC-focused behavior tests rather than OMP snapshot parity. |

## Suggested Split

1. Verify the commit/path anchors against OMP `0fc6d136`.
2. Mark sub-behaviors as adapt / reject / defer.
3. Cross-check the related GJC card before creating implementation work.
4. Promote only stable JWC contracts to `structure/` after implementation exists.

## Done Gate

- [x] Source anchors rechecked against OMP `0fc6d136` / local evidence `0fc6d136c`.
- [x] Related GJC card reviewed for overlap: `10.048` is the JWC release/CI/package implementation owner.
- [x] Adopted behavior is evidence-only: existing JWC release guards are mapped; no standalone product code is adopted by this card.
- [x] Rejected/deferred source behavior is documented with rationale below.

## Verification

- `bun test scripts/release-publish-order.test.ts`
- `bun run check:no-github-workflows`
- `bun scripts/check-workflow-yaml.ts`
- `git diff --check`
- `bun run check:ts`
- Card contains `cc0c67be` and `0fc6d136` source anchors.
- Reference-only wording remains intact; this card must not become a direct implementation instruction.

## JWC Phase 5 Evidence — 2026-06-28

Devlog artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/50_phase5_release_test_leak_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/51_phase5_release_test_leak_overlap.md`

Source recheck:

- OMP local evidence head: `0fc6d136c`.
- Range rechecked: `cc0c67be..0fc6d136`.
- The range spans `.github/`, docs, package tests, coding-agent tests, provider/catalog/eval/runtime areas, and benchmark references. It is not a single directly portable release-script patch.

JWC overlap result:

- `10.048` remains the implementation owner for release/CI/package code.
- This card remains OMP reference/split-audit evidence and must not close independently of `10.048` for release packaging changes.

Existing JWC guards recorded:

| guard | JWC file |
|---|---|
| canonical release validation | `scripts/jwc-release-validation.ts` |
| CI release validation and pack guards | `.github/workflows/ci.yml` |
| publish order/package contract | `scripts/release-publish-order.test.ts` |
| hosted runner guard | `scripts/check-no-github-workflows.ts` |
| public legacy identity leak guard | `scripts/check-public-legacy-zero.ts` (existing guard surface; current standalone run has pre-existing findings, so not counted as green Phase 5 evidence) |
| workflow YAML parser guard | `scripts/check-workflow-yaml.ts` |

Status: closed/reference-only. Phase 5 rechecked source anchors and mapped existing JWC guards; Phase 8 closes the card as track-only evidence after `10.048` closed the JWC implementation owner surface. Future code must name the exact JWC owner file and update `10.048` or a successor release card, not this OMP reference card alone.

## Closure (260701)

Closed via clean-follow goal `5f4d1e8e-672` Phase 8.

Decision: `20.015` is **track-only/reference-only**. It preserves OMP release/test leak hardening source facts and JWC guard mapping, but it does not introduce new release, CI, package, runtime, or test-fixture behavior. JWC release implementation ownership is already closed in [10.048](../10/10.048_gjc_chase_dev_ci_release_packaging.md), which adopted the concrete affected-path Bun cwd false-green guard and deferred broader release topology changes.

| bucket | final result |
|---|---|
| release asset/package leak tests | Evidence-only: existing `jwc-release-validation`, CI pack dry-run/size/count guards, and publish-order tests remain the JWC guard surface. |
| workflow/runner safety | Evidence-only: hosted-runner and workflow YAML guards remain the JWC guard surface. |
| changelog/version release metadata | Deferred to `10.048` lineage or future release-specific work. |
| test fixture leak hardening | Deferred until a concrete JWC owner appears. |
| provider/catalog/auth/runtime leaks | Out of scope; handled by provider/security/session cards. |
| terminal/TUI/bench references | Out of scope; handled by TUI/bench cards. |

Verification bundle:

- `bun test scripts/release-publish-order.test.ts`
- `bun run check:no-github-workflows`
- `bun scripts/check-workflow-yaml.ts`
- structural `rg` over MOC/index/backlinks and `test ! -e` old active path
- `git diff --check`
- `bun run check:ts`

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **track-only** |
| Decision B — JWC naming impact | none now; translate only if implementation follows. |
| Decision C — test impact | future focused tests only if adapted. |
| Decision D — docs impact | chase only. |
| Decision E — rollout impact | no migration in docs phase. |
| Decision F — residual risk | closed as track-only; reopen only with a concrete JWC owner file and release/test behavior gap. |
| Decision G — reviewer needed | docs + architecture if implementation starts. |
| Decision H — bundle policy | OMP latest-delta bundle; implement separately. |
