# 20.044 — OMP chase: plan execution, agent hooks, subagent output, TTSR

Status: ✅ _fin reference-only card
Lane: OMP
Source range: `d0c1890a6..f25ab54c5` (`origin/main`, v16.3.4→v16.3.12)
JWC baseline: worktree HEAD at review time, `main` `0e8d9f4` (ahead 4)

## Why This Card Exists

OMP's plan/agent/subagent commits cross several runtime seams: plan artifacts entering execution,
tool-scoped TTSR abort labeling, nested subagent output resolution, auto-handoff hook vetoes,
handoff retry compatibility, and eval-cell rejection handling. JWC does not run OMP plan mode as a
1:1 surface; JWC's local workflow owner is `jwc orchestrate`/PABCD plus task agents. This card
therefore maps runtime invariants to JWC owner files without directing a port.

The initial range command required zsh glob protection for `packages/coding-agent/src/modes/plan*`;
the successful run used `noglob` so Git received the requested pathspec.

## Evidence Commands Run

```bash
noglob git -C devlog/_omp_chase/oh-my-pi log --oneline --stat d0c1890a6..f25ab54c5 -- packages/agent/src/agent-loop* packages/coding-agent/src/task packages/coding-agent/src/modes/plan*
git -C devlog/_omp_chase/oh-my-pi show --stat e812c368c 1bb29873e ed4b5b5b0 f31366970 389515baa 38486e56d
rg -n "localArtifact|ttsrAbort|autoHandoff|stickyRules|evalCell" packages/coding-agent/src/ packages/agent/src/ --type ts 2>/dev/null | head -20
```

## OMP Commit Anchors

| Commit | OMP file anchors | Behavior signal |
| --- | --- | --- |
| `e812c368c` | `packages/coding-agent/src/modes/interactive-mode.ts`; `packages/coding-agent/src/prompts/system/plan-mode-active.md`; plan review test | Carries local artifacts into plan execution. |
| `1bb29873e` | `packages/agent/src/agent-loop.ts`; `packages/coding-agent/test/agent-session-concurrent.test.ts` | Adapts scoped TTSR abort labels to completed-call retention. |
| `ed4b5b5b0` | `packages/coding-agent/src/internal-urls/registry-helpers.ts`; `agent-protocol-nested.test.ts` | Resolves `agent://` for nested subagent output. |
| `f31366970` | `packages/coding-agent/src/session/agent-session.ts`; `agent-session-handoff.test.ts` | Respects vetoed auto-handoff hooks. |
| `389515baa` | `packages/agent/src/compaction/compaction.ts`; `packages/agent/test/handoff.test.ts` | Retries handoff with `toolChoice: auto` when providers reject `none`. |
| `38486e56d` | `packages/coding-agent/src/eval/js/worker-core.ts`; `packages/utils/src/postmortem.ts` | Handles unawaited promise rejections in eval cells. |

## Source Cluster Notes

- The path-limited log also includes `8b17f0d3a`, the earlier scoped TTSR abort implementation in
  `packages/agent/src/agent-loop.ts`, and `32158b74e`, its merge commit.
- Task executor commits `ae21a7a7e`, `ddd6ae118`, `17163a2b9`, and `4409e6cfb` preserve timeout,
  model selector, auth fallback, and fallback-chain behavior for deferred task execution.
- Task worktree commits `12acf7e64` and `cca325d24` are task-isolation robustness items, adjacent
  but separable from plan execution.
- `45d95e7bf` constrains nested task rendering overflow and belongs with TUI/task display if
  promoted.

## JWC Worktree Verification

The JWC probe returned concrete runtime anchors:

- `packages/coding-agent/src/session/agent-session.ts:1075` — `#ttsrAbortPending` state.
- `packages/coding-agent/src/session/agent-session.ts:1604` — TTSR pending query.
- `packages/coding-agent/src/session/agent-session.ts:2108` through `2133` — TTSR abort pending
  scheduling and reset.
- `packages/coding-agent/src/session/agent-session.ts:2235` through `2238` — continuation gate
  explicitly distinguishes TTSR abort from non-TTSR abort.
- `packages/coding-agent/src/session/agent-session.ts:2792` — pending TTSR injection guard.
- `packages/agent/src/compaction/compaction.ts:40` and `689` — auto-handoff threshold prompt.
- `packages/coding-agent/src/tools/eval.ts:34`, `44`, and `48` — `evalCellSchema` and eval cell
  input typing.

No probe hit appeared for `localArtifact` or `stickyRules`. JWC may carry those ideas under
different workflow names, but the literal OMP plan/sticky-rule surface is not visible by the
requested search.

## JWC Owner Files

- `packages/coding-agent/src/session/agent-session.ts` — TTSR continuation, handoff, and session
  scheduling owner.
- `packages/agent/src/agent-loop.ts` — tool-call abort labeling owner if JWC shares the same agent
  package path.
- `packages/agent/src/compaction/compaction.ts` — auto-handoff retry/prompt compatibility owner.
- `packages/coding-agent/src/task/` — deferred task execution and subagent lifecycle owner.
- `packages/coding-agent/src/internal-urls/registry-helpers.ts` — likely `agent://` artifact
  resolution owner if present.
- `packages/coding-agent/src/tools/eval.ts` and `src/eval/js/worker-core.ts` — eval cell execution
  and rejection containment owner.

## Decisions

| Slot | Decision |
| --- | --- |
| A — classification | **reference/split**; JWC workflow architecture differs from OMP plan mode. |
| B — naming impact | translate plan-mode terms to JWC orchestrate/PABCD/task-agent terms. |
| C — test impact | focused session/task/eval tests per adopted invariant. |
| D — docs impact | chase only unless orchestrate/task contracts change. |
| E — rollout impact | runtime behavior; user-visible only through fewer stranded outputs/continuations. |
| F — residual risk | open; local-artifact and sticky-rule concepts need JWC-specific owner search. |
| G — reviewer needed | backend/runtime review; workflow review for orchestrate artifact carry-through. |
| H — bundle policy | split by invariant: artifacts, TTSR, subagent output, handoff, eval rejections. |

## Interview Decision (2026-07-09)

전부 참조 + 적용 가능한 것 adapt. OMP는 reference-only이며 1:1 port 금지.

## Reference Triage (2026-07-09)

- reference-adopt — Local artifact carry-through: adapt for JWC orchestrate if an equivalent artifact registry/ownership path exists.
- reference-adopt — Subagent output resolution: adapt nested output resolution so task/subagent artifacts remain reachable through JWC runtime references.
- reference-adopt — TTSR abort scoping: adapt scoped abort labeling so completed-call retention is not polluted by unrelated tool aborts.
- reference-defer — Auto-handoff veto: defer until JWC handoff policy needs this provider/runtime guard.
- reference-reject — Advisor refresh: reject because JWC has no advisor surface for this OMP-specific behavior.

## Closure Evidence (2026-07-09)

Closed by documentation triage only. This is a reference-only OMP card; adopted items are adaptation candidates for future owner-specific implementation review, not direct ports.

## Adoption Guidance

Carry forward the runtime invariants, not the OMP plan implementation. Local artifacts must survive
the transition from review/plan text into execution only if JWC has an equivalent artifact registry.
TTSR abort labels should remain scoped to the tool call that actually aborted. Auto-handoff retry
logic should only mask provider tool-choice compatibility errors, not unrelated 400s. Eval cells
must attribute floating rejections to the owning run where possible and avoid process-level crashes.

## Verification Expectation

Close with per-invariant proof: artifact carry-through or rejection reason, TTSR scoped abort test,
nested subagent output resolution, auto-handoff veto/retry behavior, and eval-cell floating
rejection containment. If an invariant is OMP-only, record the missing JWC owner.

## Jawdev chase expansion — 2026-07-09

> Document: `struct_har/chase/20.044_omp_chase_plan_execution_agent_subagent_hooks.md`
> Title: 20.044 — OMP chase: plan execution, agent hooks, subagent output, TTSR
> Lane: OMP
> Status: ✅ _fin reference-only card
> Canonical source: `devlog/_omp_chase/oh-my-pi` (`origin/main`)
> JWC stance: reference-only; adapt runtime invariants through JWC orchestrate/task owners.
