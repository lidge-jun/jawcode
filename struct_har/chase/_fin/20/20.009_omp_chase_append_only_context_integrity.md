# 20.009 — omp chase: append-only context integrity

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · reference-only · closed 260701 (IMPORT, 3 JWC slices)
> Reviewed source: OMP `cc0c67be` (v16.1.13) → `0fc6d136` (v16.1.20) vs JWC worktree (2026-06-28).
> Source range: `devlog/_omp_chase/oh-my-pi` `cc0c67be..0fc6d136`.
> Cross-link: related GJC card [10.051](../10/10.051_gjc_chase_agent_composer_toolcall_integrity.md).

## Why This Is A Gap

OMP's v16.1.13→v16.1.20 range contains a concentrated append-only context integrity reference slice. JWC should not copy it 1:1; this card preserves the source facts so later implementation work can decide whether to adapt, split, or reject each behavior in JWC terms.

Summary: append-only log prefix preservation, tool-result metadata in digests, stable prefix bounds, provider payload digests, malformed tool-call queue matching/clearing.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `cc0c67be..0fc6d136` |
| source commits | 65a974aa9, cce627ee4, a2471339a, 5b9e009a8, 2b68785e8, 7d8f1ed02, 8d0667d40, c35319f99 |
| primary path anchors | `packages/agent/src/**; packages/agent/test/**; packages/coding-agent/src/session/**` |
| dirstat source | `git -C devlog/_omp_chase/oh-my-pi diff --dirstat=files,0 cc0c67be..0fc6d136` |

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| reference-only | Treat OMP as design/test evidence, not a direct port source. |
| GJC overlap | Check [10.051](../10/10.051_gjc_chase_agent_composer_toolcall_integrity.md) before implementation to avoid duplicate chase work. |
| naming | Public docs stay JWC-first: `jwc`, `.jwc`, `@jawcode-dev/*`. |
| tests | If implemented, use JWC-focused behavior tests rather than OMP snapshot parity. |

## Suggested Split

1. Verify the commit/path anchors against OMP `0fc6d136`.
2. Mark sub-behaviors as adapt / reject / defer.
3. Cross-check the related GJC card before creating implementation work.
4. Promote only stable JWC contracts to `structure/` after implementation exists.

## Done Gate

- [x] Source anchors rechecked against OMP `0fc6d136` or a newer recorded head (8 commits resolve; see Closure table).
- [x] Related GJC card reviewed for overlap (10.051 — composer/toolcall integrity, no duplicate; this card owns append-only log digest + malformed-call sanitizer).
- [x] Any adopted behavior has JWC-specific tests or explicit manual evidence (96 pass / 0 fail; see Closure).
- [x] Rejected/deferred source behavior is documented with rationale (all 8 commits IMPORT-adapted; none deferred).

## Closure (260701) — IMPORT, 3 JWC slices implemented

Closed via goal `f8909338-255` autonomous PABCD (WP5). OMP는 reference-only(1:1 port ❌)이고, 독립 audit(explorer Hilbert gpt-5.4 갭분석 PASS)로 8개 source commit을 3개 JWC-native 슬라이스로 묶어 구현했다. JWC는 OMP에 없는 seed-prefix fork 시맨틱을 보유하므로 보존하며 재설계. 채택 3건은 독립 read-only 검증자(Galileo gpt-5.4)가 file:line 근거로 PASS 판정.

| # | OMP source commit | anchor | verdict | evidence (JWC) |
|---|---|---|---|---|
| 1 | preserve byte-stable prefix on in-place rewrite | `65a974aa9` | **IMPORT** (WP5-A) | `syncMessages` 3-case 재작성(compaction clear / in-place rewrite는 longest byte-stable prefix까지 `truncate` 후 diverged tail append / append tail) — provider KV 캐시를 divergence 지점까지 보존. `AppendOnlyLog.truncate(count)` 신설(`packages/agent/src/append-only-context.ts:155`). commit `04421e3`. |
| 2 | tool-result metadata in digest | `cce627ee4` | **IMPORT** (WP5-B) | `#messageDigest` payload에 `tcid`(toolCallId/tool_call_id), `tn`(toolName/name), `err`(isError) 추가(`packages/agent/src/append-only-context.ts:362`) — content 미변경 상태의 tool-result 메타데이터 rewrite도 prefix invalidation 트리거. commit `04421e3`. |
| 3 | subagent inherited-prefix rewrite test | `a2471339a` | **IMPORT (test)** (WP5-A) | seeded-fork child-local rewrite 허용 + 물리 seed 영역 손상 시 `seed prefix changed` throw 회귀 테스트(`packages/agent/test/append-only-context.test.ts`). JWC re-prepend 시맨틱 반영해 reachable defensive-contract 경로로 재작성. commit `04421e3`. |
| 4 | bound stable prefix by log length | `5b9e009a8` | **IMPORT** (WP5-A) | `stableCount = Math.min(#longestStablePrefix(messagesToSync), log.length)` — public `log.clear()`가 sync 커서를 물리 log 앞에 남길 때 drift 방지. 회귀 테스트 "clamps the stable prefix to the physical log length after a direct clear". commit `04421e3`. |
| 5 | provider payloads in digest | `2b68785e8` | **IMPORT** (WP5-B) | `#messageDigest` payload에 `pp`(providerPayload) 추가 — Galileo 검증: `providerPayload`는 replay 입력으로 재주입되고(`packages/ai/src/providers/openai-codex-responses.ts:2603/2628`) `dt` 유무로 append vs full-replace 분기(`:2643`)까지 좌우하므로 해시 포함이 정확(미포함 시 stale prefix 오재사용). 테스트 "detects a providerPayload rewrite". commit `04421e3`. |
| 6 | drop empty-name tool calls | `7d8f1ed02` | **IMPORT** (WP5-C) | `isMalformedToolCallName`(empty/whitespace) + `sanitizeMalformedToolCalls`를 `transformMessages` 첫 줄에 삽입(`packages/ai/src/providers/transform-messages.ts`) — 모든 provider가 funnel하는 canonical sanitize 경계. malformed `toolCall` block + 매칭 `toolResult` 제거 + 빈 assistant 턴 제거. idempotent fast-path. commit `b7a6705`. |
| 7 | positional occurrence matching | `8d0667d40` | **IMPORT** (WP5-C) | per-id FIFO `dropQueues`(set 기반 대신) — composite id(`callId|itemId`) 충돌 시 surviving valid duplicate의 real result 보존. commit `b7a6705`. |
| 8 | clear queues at turn boundaries | `c35319f99` | **IMPORT** (WP5-C) | assistant/user/developer/기타 non-result 경계에서 `dropQueues.clear()` — rejection result 미도착 stale malformed 항목이 재사용 id의 later valid result를 먹지 않도록 윈도 스코핑. 회귀 테스트 "does not let a missing malformed-result consume a later valid call with the same id". commit `b7a6705`. |

Audit: explorer(Hilbert gpt-5.4) 갭분석 PASS — 8 commit 전부 MISSING-GAP(JWC 미구현) 확인, JWC seed-prefix 보존 제약 식별. Build 후 read-only 검증자(Galileo gpt-5.4) PASS: digest 확장 over-invalidation 아님(replay 바이트 보존 정확), sanitizer idempotent + FIFO/boundary-clear가 valid duplicate 보존, call-site 삽입이 toolCallIdMap/latestAssistantIndex 이후 계산이라 안전, seed-fork 회귀 없음. Verification: 96 pass / 0 fail(append-only-context + transform-messages-malformed + duplicate-tool-results + anthropic-prefill), `cd packages/agent && bun run check:types` exit 0, `cd packages/ai && bun run check:types` exit 0, biome clean, added 라인 brand-literal(gjc/gajae/oh-my-pi/roboomp) 0, 인용 OMP SHA 8개 resolve, git diff --check clean. 2 atomic commits: `04421e3`(agent: WP5-A+B), `b7a6705`(ai: WP5-C).

## Verification

- `git diff --check` for this card and affected indexes.
- Card contains `cc0c67be` and `0fc6d136` source anchors.
- Reference-only wording remains intact; this card must not become a direct implementation instruction.

## JWC Phase 6 Evidence — 2026-06-28

Related JWC implementation owner:

- [10.051](../10/10.051_gjc_chase_agent_composer_toolcall_integrity.md)

Devlog artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/60_phase6_runtime_context_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/63_phase6_toolcall_context_split.md`

Source recheck:

- OMP local evidence head: `0fc6d136c`.
- Range retained: `cc0c67be..0fc6d136`.

JWC overlap result:

- `20.009` remains OMP reference/split-audit evidence.
- `10.051` owns any future implementation touching tool-call/composer/context integrity.
- JWC already documents append-only session semantics in `docs/session.md`; future code must prove behavior with JWC-focused tests, not OMP snapshot parity.

Status: still active/reference-only. Phase 6 adds source/overlap evidence only and does not close the card.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **track-only** |
| Decision B — JWC naming impact | none now; translate only if implementation follows. |
| Decision C — test impact | future focused tests only if adapted. |
| Decision D — docs impact | chase only. |
| Decision E — rollout impact | no migration in docs phase. |
| Decision F — residual risk | open reference card. |
| Decision G — reviewer needed | docs + architecture if implementation starts. |
| Decision H — bundle policy | OMP latest-delta bundle; implement separately. |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: IMPORT**

IMPORT — append-only context integrity 무결성 채택.
