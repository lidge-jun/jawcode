# 20.016 — omp chase: review command PR URL / diff-slice support

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ✅ · **P3**
> Reviewed source: OMP `dc14689fc` (`dcdfb581c`, `ff33c59c2`, `02bd6c52b`, `b2a68398f`) vs JWC `d60b7822` lineage.
> **참조 원칙**: JWC already has a GitHub skill route; review UX parity is optional.

## 왜 gap인가

OMP `/review` can accept explicit GitHub PR URLs and detected PR URLs from conversation, including diff-slice URL handling and avoiding local reads for remote PR review. JWC has a `github` skill and local workflow rules; this is useful only if JWC wants equivalent one-command review UX.

## Source facts

| fact | OMP evidence |
|---|---|
| bundled review command | `packages/coding-agent/src/extensibility/custom-commands/bundled/review/index.ts` |
| review prompt | `packages/coding-agent/src/prompts/review-request.md` |
| tests | `packages/coding-agent/test/extensibility/custom-commands/review.test.ts` |
| behavior | PR URL detection, explicit PR URL support, diff-slice URLs, no local reads |

## JWC reconcile

- Prefer existing `/skill:github` conventions unless product wants built-in `/review` parity.
- Remote PR review must cite fetched PR/diff data and avoid pretending local worktree reflects remote state.
- GitHub-only behavior belongs behind explicit GitHub routing.

## 종료 판정 (2026-06-16)

JWC는 GitHub 작업을 `/skill:github`로 라우팅한다. OMP `/review` PR URL parity는 JWC public workflow/tool surface에 새 built-in review command를 추가해야 하는데, 현재 제품 가치 대비 충돌면이 크다. 따라서 **비채택 종료**: remote PR review는 GitHub skill이 맡고, chase에는 “원격 PR URL 리뷰 시 로컬 worktree 상태로 오인하지 말 것” 원칙만 보존한다.

## 완료 기준

- [x] JWC review UX는 skill-based route 유지.
- [x] Remote PR URL review는 GitHub skill에서 fetched PR/diff source를 citation하고 local-state confusion을 피하는 원칙으로 귀속.
- [x] Built-in `/review` parity tests는 비채택이므로 불필요.
