# 20.028 — omp chase: web-search DuckDuckGo scrape + search provider settings

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · reference-only · closed 260701 (IMPORT, 2 JWC slices; gemini api-key search deferred)
> Reviewed source: OMP `ca9f2847e` (v16.2.5) → `b6c9747d4` (v16.2.9) vs JWC worktree `cfe71cd` (2026-07-01).
> Source range: `devlog/_omp_chase/oh-my-pi` `ca9f2847e..b6c9747d4` (175 commits); this card isolates the web-search/provider-settings cluster. **1:1 port ❌ — design reference.**
> Naming: OMP `omp`/`pi` search surface → JWC `jwc` per [008](../../008_gjc_jwc_naming_contract.md).
> Neighbors: relates to JWC web-search hardening [10.043](../10/10.043_gjc_chase_web_search_insane_security.md) (_fin).

## Why This Is A Gap

OMP v16.2.5→v16.2.9 reworked web-search backends and provider selection: DuckDuckGo search now scrapes the HTML frontend instead of the Instant Answer API (with native-browser-aligned requests), the `--provider auto` override is honored, search provider settings are applied from config, gemini api-key search is supported (reusing the gemini oauth helper), and the DuckDuckGo result cap was intentionally kept unchanged. JWC's web-search surface (hardened in 10.043 _fin) can reference the DDG-scrape migration and provider-settings plumbing.

Behavioral surface:
1. DuckDuckGo: scrape HTML frontend instead of Instant Answer API; align requests with native browser behavior.
2. Honor explicit `--provider auto` override; apply search provider settings from config.
3. Support gemini api-key search (reuse gemini oauth helper).
4. DuckDuckGo result cap intentionally unchanged (`keep` commit — documented non-change).

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `ca9f2847e..b6c9747d4` (v16.2.5→v16.2.9) |
| DDG HTML scrape | `755a61de0` fix(web-search): scrape DuckDuckGo HTML frontend instead of Instant Answer API |
| DDG native-browser align | `09061ed80` feat(coding-agent/web): aligned duckduckgo search requests with native browser behavior |
| --provider auto override | `ba6b64bf8` fix(search): honored explicit --provider auto override |
| apply provider settings | `3560d108d` fix(cli): applied search provider settings |
| gemini api key search | `530113eb7` fix(providers): supported gemini api key search |
| gemini oauth helper reuse | `6e166274c` fix(web-search,mcp): reused gemini oauth helper and formatted npx shim |
| DDG cap kept (no-change) | `6f8f76be4` Keep DuckDuckGo result cap unchanged |
| primary path anchors | `packages/coding-agent/src/web/`, `src/cli/` (OMP) |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 755a61de0 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 3560d108d --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| DDG scrape | JWC web-search was hardened in 10.043 (_fin); the DDG HTML-scrape migration is a backend reference — evaluate whether JWC's DDG path needs the same Instant-Answer-API→HTML move. |
| provider settings | `--provider auto` + config-applied provider settings is a CLI/config reference for JWC search routing. |
| gemini search | Gemini api-key + oauth-helper reuse overlaps 20.024 (mcp oauth) + 10.062 (Gemini UA); cross-reference. |
| security | Web-search fetch/scrape is security-sensitive (SSRF/scrape robustness) — review against 10.043 guardrails if adopted. |
| naming | search command/provider surface stays JWC-named. |
| source clone | Treat `devlog/_omp_chase/oh-my-pi` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source anchors rechecked against OMP `b6c9747d4` — 6 web-search/provider commits triaged in WP14 plan.
- [x] Adopt/defer/reject decision recorded per feature — 2 ADOPT, 3 CONFIRM/no-op, 1 DEFER (gemini api-key search).
- [x] DDG-scrape + provider settings cross-checked against 10.043 (_fin) guardrails — same fixed DDG endpoints; no new SSRF surface.
- [x] Naming translated per [008](../../008_gjc_jwc_naming_contract.md) — JWC-native command/settings behavior; source SHAs cited only as references.
- [x] Adopted behavior has JWC-specific tests — DDG request-shape assertion + standalone CLI provider-settings test.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | **IMPORT closed**: DDG scrape already shipped; ADOPT native-browser-aligned DDG request shape + standalone CLI provider settings; DEFER gemini api-key search |
| Decision B — naming impact | search command/provider config surface |
| Decision C — test impact | DDG fetch-shape test + standalone CLI settings test |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | zero-migration; changes only keyless DDG request fingerprint and no-flag CLI provider resolution |
| Decision F — residual risk | gemini api-key search deferred to a credential/security pass; DDG HTML markup can still drift |
| Decision G — reviewer needed | backend reviewer completed; security review only for deferred gemini credential path |
| Decision H — bundle policy | single WP14 PABCD for DDG + CLI settings; gemini api-key search separated |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: IMPORT**

IMPORT — web-search DuckDuckGo scrape + provider settings 채택.

## Closure (260701) — IMPORT, 2 JWC slices implemented

Closed via goal `f8909338-255` autonomous PABCD (WP14). OMP is reference-only (1:1 port ❌); JWC adopted only the concrete gaps that matched existing JWC surfaces. Gemini api-key search remains deferred because it is a credential-path expansion overlapping 10.062/20.024 and needs a dedicated security pass.

### Feature triage

| # | OMP feature | anchor | verdict | JWC outcome |
|---|---|---|---|---|
| 1 | DuckDuckGo HTML scrape | `755a61de0` | CONFIRM | Already shipped in `packages/coding-agent/src/web/search/providers/duckduckgo.ts` (`html.` + `lite.` scrape), no code change. |
| 2 | native-browser-aligned DDG requests | `09061ed80` | **ADOPT** | Added `Sec-Ch-Ua*`, `Sec-Fetch-*`, `Referer`, cache/priority/upgrade headers and blank `b` form param for html endpoint POST. |
| 3 | explicit `--provider auto` override | `ba6b64bf8` | CONFIRM | JWC resolver already distinguishes omitted provider from explicit `"auto"`; no code change. |
| 4 | apply configured provider settings in standalone CLI | `3560d108d` | **ADOPT** | `runSearchCommand` now applies `providers.webSearch` from settings only when no explicit CLI provider is supplied. |
| 5 | DDG cap unchanged | `6f8f76be4` | CONFIRM | JWC cap remains `MAX_NUM_RESULTS=20`. |
| 6 | gemini api-key search | `530113eb7` / `6e166274c` | DEFER ③ | OAuth/api-key dual-path search is a large credential/security surface; revisit with 10.062/20.024. |

### ADOPT slices

- `packages/coding-agent/src/web/search/providers/duckduckgo.ts` — `fetchAndParse` keeps JWC's per-attempt user-agent rotation, adds browser-aligned headers, and posts blank `b` only on the html endpoint. Same fixed DDG endpoints, so no new SSRF surface.
- `packages/coding-agent/src/cli/web-search-cli.ts` — `runSearchCommand` loads `Settings.init({ cwd: getProjectDir() })`, validates `providers.webSearch` with `isSearchProviderPreference`, and calls `setPreferredSearchProvider` only when `cmd.provider` is absent. Explicit `--provider auto` still bypasses configured preference through `params.provider`.
- `packages/coding-agent/test/tools/web-search-duckduckgo.test.ts` — asserts `Sec-Fetch-*` / `Sec-Ch-Ua-Mobile` / `Referer` / `Upgrade-Insecure-Requests` and blank `b` on the html request.
- `packages/coding-agent/test/web/search/cli-provider-settings.test.ts` — asserts no-flag CLI uses configured Tavily preference, and explicit `--provider auto` falls through to successful DuckDuckGo output without leaving a nonzero process exit.

### Audit & verification

- A-phase audit: first pass failed because #2 was only partial; plan promoted native-browser-aligned DDG request shape to ADOPT.
- B-phase read-only verifier (Backend employee): `DONE`, no findings. Reported `bun test packages/coding-agent/test/web/search/cli-provider-settings.test.ts packages/coding-agent/test/tools/web-search-duckduckgo.test.ts` => 28 pass, 1 skip, 0 fail; `bun run check:ts` => EXIT 0.
- Local verification: focused suite `bun test test/tools/web-search-duckduckgo.test.ts test/web/search/cli-provider-settings.test.ts` => 28 pass, 1 skip, 0 fail; `cd packages/coding-agent && bun run check:types` => EXIT 0; `bunx biome check packages/coding-agent/test/web/search/cli-provider-settings.test.ts` clean; `git diff --check` clean.
- Commits: `841312c` plan, `d986be9` audit revision, `c874eab` implementation, `5790c88` test-harness exit-clean fix.

### Re-open conditions

- Re-open DDG request-shape work if DuckDuckGo's html/lite markup or required browser fingerprint changes enough to break the fixture/live e2e path.
- Re-open CLI settings if standalone `jwc q`/`web-search` moves away from `runSearchCommand` or settings initialization changes.
- Track gemini api-key search separately with a security review; do not reopen this card for that credential path alone.
