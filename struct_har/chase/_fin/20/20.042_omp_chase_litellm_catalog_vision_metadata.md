# 20.042 — OMP chase: LiteLLM catalog metadata, vision, cache versioning

Status: ✅ _fin reference-only card
Lane: OMP
Source range: `d0c1890a6..f25ab54c5` (`origin/main`, v16.3.4→v16.3.12)
JWC baseline: worktree HEAD at review time, `main` `0e8d9f4` (ahead 4)

## Why This Card Exists

OMP's LiteLLM work is concentrated in `packages/catalog`, while JWC's local surface is
`packages/ai/src/provider-models`. The packages are not shape-identical. This card keeps the
behavioral lessons: preserving rich metadata, preserving vision metadata, cache versioning,
fallback metadata, llama.cpp modality detection, fallback chain selection, and Azure Foundry
Anthropic strict-tool compatibility.

The source is useful because model discovery bugs often look harmless until a model selector,
vision request, or tool-call route depends on lost metadata. The JWC response should be a targeted
comparison, not a catalog-package import.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi log --oneline --stat d0c1890a6..f25ab54c5 -- packages/catalog packages/ai/src/provider-models
git -C devlog/_omp_chase/oh-my-pi show --stat e2a01aa4f 8aae263ea 6aa8aefb1 961e27aae 90aabbac9 8d484435c 32953c1b3
rg -n "litellm|LiteLLM|visionMetadata|azureFoundry|fallbackChain" packages/ai/src/ packages/coding-agent/src/config/ --type ts 2>/dev/null | head -20
```

## OMP Commit Anchors

| Commit | OMP file anchors | Behavior signal |
| --- | --- | --- |
| `e2a01aa4f` | `packages/catalog/src/provider-models/openai-compat.ts`; `packages/catalog/test/litellm-provider.test.ts` | Preserves LiteLLM rich metadata during discovery. |
| `8aae263ea` | `packages/catalog/CHANGELOG.md`; `packages/catalog/src/provider-models/openai-compat.ts`; `packages/catalog/test/litellm-provider.test.ts` | Continues discovery to preserve LiteLLM vision metadata. |
| `6aa8aefb1` | `packages/catalog/src/provider-models/openai-compat.ts`; `packages/catalog/test/litellm-provider.test.ts` | Updates LiteLLM cache version behavior. |
| `961e27aae` | `packages/catalog/CHANGELOG.md`; `packages/catalog/src/provider-models/openai-compat.ts`; `packages/catalog/test/litellm-provider.test.ts` | Restores bundled metadata fallback. |
| `90aabbac9` | `packages/coding-agent/src/config/model-discovery.ts`; `packages/coding-agent/test/model-discovery.test.ts` | Detects llama.cpp `architecture.input_modalities` vision metadata. |
| `8d484435c` | `packages/coding-agent/src/config/model-registry.ts`; `settings-schema.ts`; selector/session tests | Restores fallback model selection and fallback-chain UI/session plumbing. |
| `32953c1b3` | `packages/ai/src/providers/anthropic.ts`; `packages/catalog/src/compat/anthropic.ts`; `packages/ai/test/issue-4679-repro.test.ts` | Disables strict tools on Azure Foundry Anthropic. |

## Source Cluster Notes

- `bfb170ae6` is the underlying non-merge fix for bundled LiteLLM metadata fallback, surfaced by
  merge `961e27aae`.
- `2dfddfd6a` and `8c01b1633` handle LiteLLM aggregate/sentinel filtering before the rich metadata
  fixes; they matter if JWC sees provider placeholder models.
- `3458b037a` and `2859dc5be` are broader catalog/model bumps; do not mistake regenerated model
  JSON changes for the behavior owner.
- `41a29c83c` is the non-merge Azure Anthropic strict-tools source for merge `32953c1b3`.

## JWC Worktree Verification

The JWC probe returned live LiteLLM and fallback-chain anchors:

- `packages/ai/src/types.ts:136` — `"litellm"` is a provider/model source type.
- `packages/ai/src/provider-models/openai-compat.ts:1567` — JWC has a LiteLLM section.
- `packages/ai/src/provider-models/openai-compat.ts:1570` — `LiteLLMModelManagerConfig`.
- `packages/ai/src/provider-models/openai-compat.ts:1575` — `litellmModelManagerOptions`.
- `packages/ai/src/provider-models/openai-compat.ts:1580` — bundled reference map for `litellm`.
- `packages/coding-agent/src/config/settings-schema.ts:1049` — `retry.fallbackChains`.
- `packages/ai/src/auth-storage.ts:1591` through `1593` — LiteLLM login hook.
- `packages/ai/src/provider-models/descriptors.ts:254` through `257` — LiteLLM descriptor and
  unauthenticated catalog metadata.

No probe hit appeared for `visionMetadata` or `azureFoundry` by name. JWC may still implement the
behavior under different names, so any implementation pass must read the owner files instead of
matching identifiers only.

## JWC Owner Files

- `packages/ai/src/provider-models/openai-compat.ts` — primary LiteLLM discovery owner.
- `packages/ai/src/provider-models/descriptors.ts` — provider descriptor registration.
- `packages/ai/src/types.ts` — provider/source type contract.
- `packages/coding-agent/src/config/model-registry.ts` — fallback model selection owner.
- `packages/coding-agent/src/config/settings-schema.ts` — fallback-chain config persistence.
- `packages/ai/src/providers/anthropic.ts` — strict-tool compatibility surface for Anthropic-like
  providers.

## Decisions

| Slot | Decision |
| --- | --- |
| A — classification | **reference/adapt**; LiteLLM behavior may map, package layout does not. |
| B — naming impact | none now; keep `LiteLLM` provider naming if promoted. |
| C — test impact | provider-models discovery tests plus model-registry fallback-chain tests. |
| D — docs impact | chase only unless JWC changes provider metadata semantics. |
| E — rollout impact | user-visible if model picker/vision/tool strictness changes. |
| F — residual risk | open; identifier probe does not prove JWC preserves vision metadata. |
| G — reviewer needed | backend/provider review; TUI/settings review only for fallback-chain UX. |
| H — bundle policy | split: LiteLLM metadata, llama.cpp modalities, Azure strict tools, fallback chains. |

## Interview Decision (2026-07-09)

전부 참조 + 적용 가능한 것 adapt. OMP는 reference-only이며 1:1 port 금지.

## Reference Triage (2026-07-09)

- reference-defer — LiteLLM metadata: defer because JWC uses its own provider-model generator and should compare invariants before changing generated/catalog flow.
- reference-adopt — Vision metadata detection: adapt the capability-preservation pattern so discovery does not erase usable vision/modality facts.
- reference-adopt — Azure strict tools: adopt conditionally if Azure Anthropic/Foundry routing is active in JWC provider compatibility logic.

## Closure Evidence (2026-07-09)

Closed by documentation triage only. This is a reference-only OMP card; adopted items are adaptation candidates for future owner-specific implementation review, not direct ports.

## Adoption Guidance

Adapt the invariants, not OMP's catalog package. Discovery should merge late capability metadata
without erasing earlier display/rich metadata, should avoid caching stale schema assumptions across
LiteLLM response shapes, and should reject aggregate placeholder models when they are not runnable
targets. Azure Foundry Anthropic handling belongs in provider compatibility logic, not in model
selector UI.

## Verification Expectation

Close only with explicit evidence for LiteLLM rich metadata, vision capability preservation,
fallback metadata, and fallback-chain persistence. If rejected, record whether JWC's current
provider-model manager already covers the behavior or lacks the upstream provider route.

## Jawdev chase expansion — 2026-07-09

> Document: `struct_har/chase/20.042_omp_chase_litellm_catalog_vision_metadata.md`
> Title: 20.042 — OMP chase: LiteLLM catalog metadata, vision, cache versioning
> Lane: OMP
> Status: ✅ _fin reference-only card
> Canonical source: `devlog/_omp_chase/oh-my-pi` (`origin/main`)
> JWC stance: reference-only; adapt provider-discovery invariants, not OMP package layout.
