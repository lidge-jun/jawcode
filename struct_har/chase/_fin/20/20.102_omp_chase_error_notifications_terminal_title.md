# 20.102 — OMP chase: Terminal error notification lifecycle and run-state title

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · P2 — ADAPT partial; terminal title run-state slice landed, error-notification half remains a named residual.
> Reviewed source: OMP `5e362714f..4e5cb4d40` (`v17.0.8..v17.1.0`, 539 non-merge commits) vs JWC worktree.
> Closed: 2026-08-07 — run-state title slice implemented and independently re-verified; opt-in terminal error notifications remain open as a tracked residual; JWC commits `9b12fdf`, `58d22fc`, `27938bd`, `82a6ac5`.

Owner paths: `packages/coding-agent/src/utils/title-generator.ts`, `packages/coding-agent/src/modes/controllers/event-controller.ts`, `packages/coding-agent/src/modes/controllers/command-controller.ts`, `packages/coding-agent/src/modes/controllers/extension-ui-controller.ts`, `packages/coding-agent/src/modes/controllers/selector-controller.ts`, `packages/coding-agent/src/modes/interactive-mode.ts`, `packages/coding-agent/src/modes/index.ts`, `packages/coding-agent/src/config/settings-schema.ts`.

## Chase Scope

Classification: **adapt**. Autonomy bucket: **A**. This card groups 10 source commits after inspecting changed paths and source facts. It is a chase decision record, not authorization to copy OMP code. JWC-facing commands, state, and examples remain `jwc` and `.jwc`.

- [ ] Map each behavior against the cited JWC owner before implementation.
- [ ] Preserve JWC package boundaries, naming, tests, and user-curated TUI contracts.
- [ ] Close only with focused behavior evidence; matching names are insufficient.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi rev-list --no-merges --count v17.0.8..v17.1.0
git -C devlog/_omp_chase/oh-my-pi log --no-merges --name-only v17.0.8..v17.1.0
rg -n 'error|notifications|terminal|title' packages/coding-agent/src packages/ai/src packages/agent/src packages/tui/src packages/stats/src
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `2a328207d` | Make error notifications opt in | packages/coding-agent/src/config/settings-schema.ts; packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts |
| `90527a5ae` | fix(coding-agent): read the settled turn from agent_end for error notifications | packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts |
| `997f2c5e8` | feat(coding-agent): reflect agent run state in terminal title | packages/coding-agent/src/config/settings-schema.ts; packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/src/modes/interactive-mode.ts; packages/coding-agent/src/utils/title-generator.ts (+1 more) |
| `55630876f` | fix(coding-agent): reconcile terminal title run-state with session lifecycle | packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/src/modes/controllers/extension-ui-controller.ts; packages/coding-agent/src/modes/controllers/session-focus-controller.ts; packages/coding-agent/src/utils/title-generator.ts |
| `2a3b6856f` | fix(coding-agent): gate error toasts while auto-retry is pending | packages/coding-agent/CHANGELOG.md; packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts |
| `f9e481baa` | fix(coding-agent): preserve final retry error toasts | packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/src/session/agent-session.ts; packages/coding-agent/test/agent-session-retry-fallback.test.ts; packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts |
| `2c56d23e4` | fix(coding-agent): retain retry notification suppression | packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts |
| `ea3882f8a` | fix(coding-agent): close terminal retry lifecycle | packages/coding-agent/src/session/agent-session.ts; packages/coding-agent/test/agent-session-prewalk.test.ts; packages/coding-agent/test/agent-session-retry-fallback.test.ts |
| `e1e5e0b53` | fix(coding-agent): kept attention title until the last approval prompt resolved | packages/coding-agent/src/modes/controllers/event-controller.ts |
| `8490654df` | refactor(coding-agent): expressed run state via the title separator instead of prefix glyphs | packages/coding-agent/node_modules; packages/coding-agent/src/config/settings-schema.ts; packages/coding-agent/src/modes/controllers/event-controller.ts; packages/coding-agent/src/modes/interactive-mode.ts (+3 more) |

## Worktree Verification

| probe | actual JWC tree | exact JWC evidence / implication |
| --- | --- | --- |
| Primary owner | **exists; semantic comparison required** | packages/coding-agent/src/notifications/session-lifecycle.ts:1 is JWC's notification lifecycle owner. |
| Adjacent contract | **exists / fork-specific** | packages/coding-agent/src/session/agent-session.ts:8976-9057 owns fallback/error lifecycle. |
| Naming boundary | **JWC-specific** | Root `AGENTS.md` requires `jwc`, `.jwc`, and `@jawcode-dev/*`; this card adopts no OMP-facing command/path vocabulary. |

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **RESOLVED** | **adapt** confirmed. The run-state title is a genuine gap: `title-generator.ts` emitted only `🦈: label` with no state input, no dedup and no spinner. |
| 2 | **RESOLVED** | Bucket **A** confirmed for the title slice. The error-notification half is also autonomous in principle but is a separate product surface, so it is deferred rather than folded into the same cycle. |
| 3 | **RESOLVED** | Smallest JWC-native slice = the state→separator contract plus its lifecycle wiring. Upstream's settled form (`8490654df`) was adapted over the earlier prefix-glyph version (`997f2c5e8`). |
| 4 | **RESOLVED** | Owner paths declared above; focused tests are `test/terminal-title-state.test.ts`, `test/modes/controllers/event-controller-title-attention.test.ts`, `test/modes/controllers/session-replacement-title.test.ts`. |
| 5 | **RESOLVED** | JWC brand `🦈` kept (upstream uses `π`); flat `ui: { tab, label, description }` metadata per JWC's `UiBase` — upstream's `group:` key does not exist here. `packages/tui/src/tui.ts` and `modes/components/welcome.ts` untouched across all four commits. |
| 6 | **RESOLVED** | Implementation SHAs and verification output recorded below; the notification residual is named in slot F. |

## Decision Slots

| slot | status | decision |
| --- | --- | --- |
| A | SPLIT | Adapt the terminal-title run-state slice only. The card's other half — opt-in terminal error notifications — is a separate product surface and is deferred, not dropped. |
| B | NONE | No upstream code copied. JWC's brand, settings metadata shape, approval surface and session-replacement paths all differ; the implementation is written against JWC's own owners. |
| C | TESTED | 24 focused cases across three suites. Two controller regressions were verified RED against the pre-fix implementation (superseded `agent_end`, held plan approval), and the session-replacement wiring test was verified RED with its call sites deleted. |
| D | CHASE ONLY | Archives the title run-state slice only. |
| E | NO MIGRATION | No state, config or credential migration. `tui.titleState` defaults to `true`; disabling it restores the previous `🦈: label` layout exactly. |
| F | ADAPT — partial implementation, tracked residual | **Residual: opt-in terminal error notifications.** Upstream `2a328207d` (error notifications opt-in) and `90527a5ae` (read the settled turn from `agent_end`) have no JWC analogue: there is no `error.notify` key and no `sendErrorNotification` path — JWC sends terminal notifications only for completion (`event-controller.ts`) and ask (`tools/ask.ts`). The retry-suppression intent of `2a3b6856f` / `2c56d23e4` / `f9e481baa` is already satisfied for the in-TUI toast (gated on `!event.success`), but a toast is not a notification. This half needs its own cycle. |

## Implementation Evidence

- `9b12fdf` — state→separator contract, spinner, dedup and extension-override slot in `packages/coding-agent/src/utils/title-generator.ts`; `tui.titleState` in `settings-schema.ts`; attention-id set and lifecycle transitions in `event-controller.ts`; teardown ordering in `interactive-mode.ts`.
- `58d22fc` — C-review defects: superseded-`agent_end` guard, plan-approval attention held across its own turn, postmortem disposal before emergency restore, branch resync, live `tui.titleState` toggle.
- `27938bd` — manual and auto handoff resync the authoritative title.
- `82a6ac5` — `/fork` resync plus a controller-level test that guards the call sites themselves.

### Verification

`bun run check:ts` exit 0 · `verify-g002-gates` PASS · `check-visible-definitions` PASS · `rebrand-inventory --strict` PASS · `check:schemas` PASS · `default-jwc-definitions` 21/0 · `ci:test:smoke` ok · title/controller suites 85 pass / 0 fail.

A pre-existing unrelated failure in `event-controller-idle-compaction.test.ts` (fixture missing `getCwd`/`getSessionId`) was repaired in `58d22fc` so the suite is genuinely green rather than green-except-one.

### Review record

Two independent adversarial rounds, both of which returned FAIL and both of which were folded rather than argued down:

- **A-phase** — disproved the plan's central claim that the notification half was "already satisfied"; that error is retracted in `110`/`111` and is why this card closes partial.
- **C-phase** — found five defects in the first implementation, two of them omissions against this plan's own spec (the `isStreaming` guard). Re-review then found `/fork` still stranding an extension override, and that the first regression test proved the clearing contract without guarding the wiring.
