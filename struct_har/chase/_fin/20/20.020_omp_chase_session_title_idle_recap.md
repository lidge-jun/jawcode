# 20.020 — omp chase: session title tracking + LLM idle recap

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · closed 260701 (ADAPT, 1 JWC slice: title casing reconciliation; 5 defer storage/recap arch)
> Split from [20.016](../../20.016_omp_chase_advisor_thinking_dialect_session_title.md) (cluster 4 of 6).
> Reviewed source: OMP `0fc6d136` (v16.1.20) → `ca9f2847e` (v16.2.5) vs JWC worktree `ef28a98` (2026-06-29).
> Source range: `devlog/_omp_chase/oh-my-pi` `0fc6d136..ca9f2847e`. **1:1 port ❌ — design reference.**

## Why This Is A Gap

OMP added persistent session-title tracking (centralized/atomized persistence, casing logic, audit logging for title changes) and replaced the static idle recap with an LLM-generated recap, plus a config schema for idle-recap and title-refresh behavior. JWC session management differs from OMP; this is session UX design input for `.jwc` state.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `0fc6d136..ca9f2847e` |
| persistent session title tracking | `0ce330ab7` |
| audit logging for session title changes | `222b036a1` |
| centralized/atomized title persistence | `9c1b55dbe` |
| LLM-generated idle recap (replaces static) | `96a1aed19` |
| config schema for idle recap/title refresh | `a36f12be8` |
| session title casing logic | `52b8fb156` |
| primary path anchors | `packages/coding-agent/{src,test}` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 0ce330ab7 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 96a1aed19 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| session UX | Evaluate title persistence + LLM idle recap as JWC session UX improvement. |
| state design | Title/recap state must live under `.jwc`; evaluate against existing session state. |
| naming | JWC-first config keys; translate OMP naming. |
| tests | Focused JWC tests only if adapted. |

## Done Gate

- [x] Source anchors rechecked against OMP `ca9f2847e` or newer recorded head — 6-commit delta triaged; `52b8fb156` ADAPT (casing), 5 DEFER③.
- [x] Adopt/defer/reject decision recorded with rationale — ADAPT 1 slice (reconcileTitleCasing); storage/recap arch deferred (no JWC surface).
- [x] Any adopted behavior has JWC-specific tests or explicit manual evidence — 8 pass `title-generator.test.ts` (5 new), tsgo EXIT 0, commit `89f4105`.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only; evaluate session-title UX |
| Decision B — naming impact | `.jwc` state + config keys |
| Decision C — test impact | future focused tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | local state design if adopted |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | architecture (session state) if promoted |
| Decision H — bundle policy | session UX bundle on adoption |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT**

ADAPT — session title tracking + LLM idle recap UX 채택, JWC 세션 표면에 맞춰 조정.

## Closure (260701) — ADAPT, 1 JWC slice (title casing reconciliation)

JWC `generateSessionTitle` (`packages/coding-agent/src/utils/title-generator.ts`) stripped quotes/punctuation from the small-title model output but never reconciled casing against the user's message — small models mangled distinctive tokens (`daemon`→`dAemon`, `TinyVMM`→`tinyvmm`). Added `reconcileTitleCasing(title, sourceText)` export with 4 rules: verbatim user tokens preserved → distinctive proper-noun casing restored from message → camelCase artifacts flattened when absent from message → remaining model casing preserved (GitHub/OAuth). 1:1 port rejected (OMP storage backend absent in JWC).

### Commit triage (OMP `0fc6d136..ca9f2847e`, 6 commits)

| commit | disposition | rationale |
|---|---|---|
| `52b8fb156` | **ADAPT** | session-title casing reconciliation — JWC-native `reconcileTitleCasing` |
| `0ce330ab7` | DEFER③ | persistent title-slot storage backend — no JWC indexed/SQL title store |
| `222b036a1` | DEFER③ | title-slot persistence wiring — depends on storage backend |
| `9c1b55dbe` | DEFER③ | title-slot persistence wiring — depends on storage backend |
| `96a1aed19` | DEFER③ | LLM idle recap — JWC idle path is compaction, not recap |
| `a36f12be8` | DEFER③ | idle-recap/title-refresh config schema — depends on deferred recap/storage |

### Audit

- **A-phase (Plato, gpt-5.4): FAIL→fix** — 2 doc-accuracy corrections: (1) "NO titleCase util" absolute claim → unrelated `titleCase`/`titleCaseSentence` helpers exist (todo-command-controller, repology) but do not own session-title; (2) OMP `52b8fb156` fallback rationale corrected (no forced title-case when sourceText absent — cleaned title preserved). Scope/design/defer sound.
- **B-phase reviewer (Epicurus, gpt-5.4): PASS** across 5 axes.

### Verification

- `bun test packages/coding-agent/test/title-generator.test.ts` → 8 pass / 0 fail (5 new `reconcileTitleCasing` cases).
- `cd packages/coding-agent && bun run check:types` (tsgo --noEmit) → EXIT 0.
- `bunx biome check` → clean (2 files, no fixes).
- Naming guard → 0 new gjc/gajae/oh-my-pi literals.
- `git diff --check` → clean. Source SHA `52b8fb156` resolves in `devlog/_omp_chase/oh-my-pi`.
- JWC commit `89f4105`.

> Re-open only with new evidence: a JWC persistent session-title store or LLM idle-recap surface would reactivate the 5 deferred commits.
