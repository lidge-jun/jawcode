# 20.014 — omp chase: extensions/plugins discovery and marketplace hardening

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ · **P3**
> Reviewed source: OMP `dc14689fc` (`3aea0bfb3`, `15184332b`, `39d819e8b`, `b7a6a5e44`) vs JWC `d60b7822` lineage.
> **참조 원칙**: architecture watch only unless JWC explicitly expands extension marketplace surface.

## 왜 gap인가

OMP hardened plugin/extension discovery: directory manifest entries one level deep, `.omp-plugin/marketplace.json`, legacy plugin config handling, slash-command filtering for plugin skills, and linked-plugin listing. JWC currently distinguishes bundled workflow skills/tool-help skills and role agents; extension marketplace behavior must not blur that surface.

## Source facts

| fact | OMP evidence |
|---|---|
| extension discovery | `packages/coding-agent/src/extensibility/plugins/*`, `discovery/*` |
| marketplace catalog | marketplace fetcher/manager paths |
| slash command filtering | plugin skill discovery and command registry tests |
| tests | `plugin-*.test.ts`, `extensions-discovery.test.ts`, `marketplace/*.test.ts` |

## JWC reconcile

- If adopted, plugin skills must honor JWC public workflow/tool-help split.
- Runtime user/project `.jwc` discovery remains supported, but source-bundled defaults stay in packages.
- Marketplace path names must not expose `.omp-plugin` in JWC docs.

## 종료 판정 (2026-06-17)

**부분 채택 / architecture-watch 종료.** JWC already has extension, plugin, marketplace, and JWC-plugin boundaries in source and tests. This card closes against those existing boundaries rather than introducing a new OMP-style public marketplace surface:

- JWC plugin roots are isolated from legacy Claude plugin providers by no-surface tests.
- Existing plugin/extension discovery and marketplace code remains the implementation surface.
- OMP-specific `.omp-plugin` naming and wholesale marketplace parity are not adopted into JWC docs or public workflow naming.

## 완료 기준

- [x] JWC extension discovery contract is represented by existing `extensibility/extensions`, `extensibility/plugins`, and `extensibility/jwc-plugins` boundaries.
- [x] Plugin/skill command visibility is covered by JWC plugin no-surface and activation tests.
- [x] OMP-specific marketplace naming is not adopted; JWC keeps its existing plugin boundary.
