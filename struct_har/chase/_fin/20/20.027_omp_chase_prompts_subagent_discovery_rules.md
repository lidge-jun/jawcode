# 20.027 — omp chase: system prompt · tester/sonic subagent · discovery rules

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · closed 260701 (ADAPT — 1 ADOPT #5 agent-param, 5 defer/reject) · reference-only
> Reviewed source: OMP `ca9f2847e` (v16.2.5) → `b6c9747d4` (v16.2.9) vs JWC worktree `cfe71cd` (2026-07-01).
> Source range: `devlog/_omp_chase/oh-my-pi` `ca9f2847e..b6c9747d4` (175 commits); this card isolates the prompt/subagent/discovery cluster. **1:1 port ❌ — design reference.**
> Naming: OMP subagent names (`oracle`/`quick_task`/`sonic`/`tester`) and `pi` prompt identity → JWC role-agent + `jwc` identity per [008](../../008_gjc_jwc_naming_contract.md). **Identity-sensitive.**

## Why This Is A Gap

OMP v16.2.5→v16.2.9 reworked prompts and the subagent roster: the system prompt simplified verification + delivery constraints and standardized tool-instruction/communication flow, bash-tool constraints were relaxed, the oracle subagent was replaced with a new tester subagent, `quick_task` was renamed to sonic, a loop-guard redirect notice is injected during thinking-loop retries, the agent parameter was made optional with a default, and Go syntax/API discovery rules were added. JWC keeps its own 4 role-agents and `jwc` prompt identity, so this is a design reference for prompt/subagent UX — never a verbatim port (prompt identity must stay JWC).

Behavioral surface:
1. Simplified system-prompt verification + delivery constraints; standardized tool-instruction/communication flow.
2. Relaxed bash-tool constraints + simplified critical instructions.
3. Replaced oracle subagent with a tester subagent; renamed `quick_task` → sonic.
4. Loop-guard redirect notices during thinking-loop retries.
5. Optional agent parameter with a default value.
6. Go syntax/API discovery rules added to discovery builtin-rules.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `ca9f2847e..b6c9747d4` (v16.2.5→v16.2.9) |
| system prompt verification/delivery | `213fdca79` feat(coding-agent/prompts): simplified system prompt verification and delivery constraints |
| standardized tool instruction flow | `51ce5a87f` feat(prompt): standardized tool instruction and communication flow |
| relaxed bash constraints | `63ff563ec` feat(coding-agent): relaxed bash tool constraints and simplified critical instructions |
| tester subagent replaces oracle | `720fb3f12` feat(coding-agent): replaced the oracle subagent with a new tester subagent |
| sonic rename (refactor) | `6c1152647` refactor(coding-agent): renamed the quick_task subagent to sonic |
| loop-guard redirect notices | `6b7d7e6e7` feat(coding-agent): injected loop-guard redirect notices during thinking loop retries |
| optional agent param default | `9ccd83a13` feat(coding-agent): made the agent parameter optional with a default value |
| Go discovery rules | `dcc7a1ce2` feat(coding-agent/discovery): added Go syntax and API discovery rules |
| primary path anchors | `packages/coding-agent/src/prompts/system/`, `src/prompts/tools/`, `src/discovery/builtin-rules/`, `src/task/` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 213fdca79 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 720fb3f12 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| prompt identity | **Critical**: JWC system prompt carries `jwc`/Jawcode identity. Adopt prompt-structure patterns (verification/delivery constraints, tool-flow standardization) only by re-authoring with JWC identity — never copy `pi`/OMP prompt text. Relates to GJC 10.065 prompt grounding. |
| subagent roster | JWC ships 4 role-agents (Frontend/Backend/Docs/etc. via jaw). OMP's oracle→tester/sonic rename is a roster-design reference; JWC must not blindly rename its roles. |
| bash constraints | Relaxed bash-tool constraints is a behavior-policy reference; weigh against JWC security posture (10.043/10.047 _fin). |
| discovery rules | Go discovery rules are an additive, low-risk reference for JWC discovery builtin-rules. |
| loop-guard | Thinking-loop redirect notices relate to JWC goal/agent loop guards (10.022 _fin). |
| naming | subagent names + prompt identity stay JWC; no `pi`/`omp`/`oracle` leak. |
| source clone | Treat `devlog/_omp_chase/oh-my-pi` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source anchors rechecked against OMP `b6c9747d4` (6 sub-feature stats confirmed via `git -C devlog/_omp_chase/oh-my-pi show`).
- [x] Adopt/defer/reject decision recorded per feature (reference triage — see Closure).
- [x] Prompt/identity items re-authored / not copied (no `pi`/OMP prompt leak; #1 rejected); cross-ref 10.065.
- [x] Bash-constraint relaxation weighed against JWC security (#2 deferred per 10.043/10.047).
- [x] Adopted behavior (#5 agent-param) has JWC-specific test `test/task/agent-default.test.ts`.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only (reference); discovery rules + loop-guard are low-risk candidates |
| Decision B — naming impact | prompt identity (`jwc`), subagent role names; **reject `pi`/`oracle` leak** |
| Decision C — test impact | future prompt-contract / discovery tests if adapted |
| Decision D — docs impact | chase only; prompt content is the implementation surface |
| Decision E — rollout impact | affects all sessions if prompt adopted; no migration |
| Decision F — residual risk | identity leak if copied; bash-constraint security tradeoff |
| Decision G — reviewer needed | docs/identity (prompt), security (bash) if promoted |
| Decision H — bundle policy | split per-feature; cross-ref 10.065 / 10.022 / 10.043 |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT**

ADAPT — discovery-rules/loop-guard는 저위험 채택, subagent 이름은 JWC 네이밍으로 재작성(oracle/sonic/tester 직역 금지). identity content reject.


## Closure (260701)

WP12 of the chase-tier1 implementation loop (goal `f8909338-255`). OMP reference-only card; 6 sub-features triaged against JWC reality.

### Triage outcome

| # | OMP feature | upstream | verdict | rationale |
|---|---|---|---|---|
| 1 | system-prompt verification/delivery simplify | `213fdca79` | **REJECT** | JWC keeps its own JWC-authored verification/delivery policy; OMP introduces a tester-agent dependency JWC's roster lacks. |
| 2 | bash constraint relaxation | `63ff563ec` | **DEFER** | Conflicts with JWC security posture (10.043/10.047 _fin); needs a dedicated security review, not this cycle. |
| 3 | oracle→tester + quick_task→sonic | `720fb3f12`, `6c1152647` | **N/A DEFER** | JWC public task roster = executor/executor_ext/architect/planner/critic (+ hidden task). No oracle/sonic in code; `quick_task` survives only as legacy commit-agentic literals (`commit/agentic/agent.ts:44`), a separate subsystem. Renaming JWC roles to OMP names is forbidden by 008. |
| 4 | loop-guard redirect on thinking-loop retry | `6b7d7e6e7` | **DEFER** | JWC has no `AIError.Flag.ThinkingLoop` nor `model.loopGuard.enabled` infra (0 grep hits); would require porting upstream AIError flag + setting first. Related busy/loop guard tracked in 10.022 _fin. |
| 5 | agent param optional + default `task` | `9ccd83a13` | **ADOPT** | Only real JWC slice — implemented. |
| 6 | Go AST discovery rules | `dcc7a1ce2` | **DEFER** | JWC has a markdown rules subsystem but only metadata/advisory matching, no AST-aware auto-matching (`docs/rulebook-matching-pipeline.md:198`). OMP Go rules rely on AST conditions. Content/advisory layer is ADAPT-able later; infra gap defers it. |

### ADOPT slice (#5) — implementation

- `packages/coding-agent/src/task/types.ts` — task schema `agent` field now `.optional()`; `TaskParams.agent?: string`.
- `packages/coding-agent/src/task/index.ts` — exported `DEFAULT_TASK_AGENT="task"` + exported `resolveTaskAgent(agent)`; single-point normalization at `execute()` and `#executeSync()`; resume-metadata fallback. JWC zod adaptation (NOT a 1:1 arktype port).
- `packages/coding-agent/src/prompts/tools/task.md` — doc note: `agent` defaults to the general-purpose worker when omitted.
- `packages/coding-agent/test/task/agent-default.test.ts` — regression test importing the real `resolveTaskAgent`/`DEFAULT_TASK_AGENT`.
- Public contract decision: defaulting omitted `agent` to the hidden general-purpose `task` worker is accepted (non-breaking; explicit agents unchanged).

### Audit (A)

- Auditor Lagrange (gpt-5.4): FAIL → revised. #5 design widened from 2-point to single-point normalization (agent read at 6+ sites); #3/#6 triage wording corrected (legacy quick_task literals exist; markdown rules subsystem exists, only AST auto-matching absent).

### Reviews (B)

- Pauli (gpt-5.4): FAIL → addressed. Test re-implemented its own `normalizeAgent` (didn't exercise production); lowercase `omp` literal in comments. Fixed: extracted exported `resolveTaskAgent`, test imports it; citation changed to allowed `OMP 9ccd83a13` form.
- Cicero (gpt-5.4): PASS. Test exercises production helper, naming clean, type-sound, no regression.

### Verification

- `bun test packages/coding-agent/test/task/agent-default.test.ts` → 5 pass / 0 fail.
- Focused suite (agent-default + spawn-gate) → 23 pass / 0 fail.
- `cd packages/coding-agent && bun run check:types` → tsgo EXIT 0.
- `git diff --check` clean; naming scan on slice clean (only `OMP 9ccd83a13` citation).
- Task suite 144 pass / 20 pre-existing baseline fail (subprocess/yield/wall-clock; baseline was 22 without the slice — no new failures).

### JWC commits

- `872b604` docs(plan): WP12 triage plan.
- `52c1b34` docs(plan): revise per audit.
- `ef09588` feat(task): make agent param optional, default to general-purpose worker.

### Re-open conditions

- Re-open #4 if JWC gains an `AIError.Flag.ThinkingLoop`/loop-guard setting (then port the redirect notice).
- Re-open #6 if JWC adds AST-aware discovery matching (then adapt Go rule content).
- Re-open #2 under a dedicated bash-security review (10.043/10.047 cross-ref).
