# 20.124 — OMP chase: AI provider and stream integrity

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · P1 — ADAPT partial; provider wire-integrity slice landed, five C-bucket auth/catalog anchors remain excluded.
> Reviewed source: OMP `v17.1.0..59619623` vs JWC worktree.
> Closed: 2026-07-26 — implemented provider wire/history and timeout-sentinel slice closed; named five-anchor auth/catalog residual remains; JWC commits `9b2dddf`, `2d51fc8`.

Owner paths: `packages/ai/src/providers/anthropic.ts`, `packages/ai/src/utils/idle-iterator.ts`, `packages/agent/src/compaction/compaction.ts`, `packages/ai/src/providers/anthropic-messages-server.ts`.

## Chase Scope

**Classification: adapt · autonomy bucket: A (A-slice only).** Provider-specific wire/stream correctness can proceed with focused wire fixtures; account/tier semantics remain descriptor-owned.

> **Bucket slice (C-stage review amendment, 2026-07-25):** the autonomous A-slice is provider wire-integrity only (stream parsing, replay, transport correctness). Authenticated model discovery, Z.AI quota/tier identifier renaming (`443398a9d`), and any auth/catalog policy are **C bucket** — they require a product/catalog decision and stay descriptor-owned.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi show --stat 07fcec1e6 d373a8ab0 1e25f13c4 d86851914 b461b8e76 6aa8c8cfb 0e2d2a226 df0e77c58 8191cf41d d1cedfe18 036636d39 443398a9d ae178422a a421ea8ac 946b7d9bd
rg -n 'modelRoles|allowUnauthenticated|UsageServerSideTools' packages/coding-agent/src/config packages/ai/src packages/agent/src
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `07fcec1e6`, `d373a8ab0`, `1e25f13c4` | Preserves and emits Anthropic server-tool history through direct and gateway paths. | `ai/src/providers/anthropic*.ts`; gateway tests |
| `d86851914` | Removes server-tool blocks from exported shares. | `coding-agent/src/export/share.ts` |
| `b461b8e76` | Counts server-tool blocks in compaction estimates. | `agent/src/compaction/compaction.ts` |
| `6aa8c8cfb`, `0e2d2a226` | Restricts preservation to validated web-search history blocks. | `ai/src/providers/anthropic-wire.ts`; `anthropic-messages-server.ts` |
| `df0e77c58`, `8191cf41d` | Honors `modelRoles.default` and authenticated registry models. | `coding-agent/src/config/model-resolver.ts`; bench CLI |
| `d1cedfe18`, `036636d39` | Corrects OpenCode Zen auth header and context-management beta. | `ai/src/providers/anthropic.ts` |
| `443398a9d` | Renames Z.AI quota/tier identifiers. | `ai/src/usage/zai.ts` |
| `ae178422a` | Recovers unstreamed signed thinking blocks. | `ai/src/utils/leaked-thinking-stream.ts` |
| `a421ea8ac`, `946b7d9bd` | Disables local first-event watchdogs while preserving the disabled sentinel. | `ai/src/utils/idle-iterator.ts`; OpenAI provider tests |

## Worktree Verification

| probe | actual JWC tree evidence |
| --- | --- |
| Model roles | `packages/coding-agent/src/config/model-registry.ts:98,111` explicitly owns `modelRoles` and its default role. |
| Authenticated discovery | `packages/coding-agent/src/config/model-registry.ts:1818` and `packages/ai/src/provider-models/descriptors.ts:107-115` gate catalog discovery; adapt, do not copy catalog assumptions. |
| Server-tool telemetry | `packages/agent/src/telemetry.ts:138` already names server-tool usage, but this does not prove Anthropic history round-trip parity. |

## Semantic Parity Probe

- **Upstream behavior:** At `946b7d9bd`, `packages/ai/src/utils/idle-iterator.ts:66-90` preserves an explicit OpenAI-family first-event-watchdog disable as numeric `0` through timeout resolution rather than collapsing it to `undefined`.
- **JWC probe:** Ran `rg -n 'getOpenAIStreamFirstEventTimeoutMs|PI_OPENAI_STREAM_FIRST_EVENT_TIMEOUT_MS' packages/ai/src packages/ai/test`; it produced no matches (exit 1). Inspection of `packages/ai/src/utils/idle-iterator.ts:53-58` found only the generic resolver, which returns `normalizeIdleTimeoutMs(...)` directly.
- **Gap disposition:** **absent** — JWC lacks the OpenAI-specific resolver that preserves the disabled `0` sentinel across the local first-event and idle-timeout layers.
- **Nearest focused test:** `packages/ai/test/stream-timeout-defaults.test.ts` exists for generic timeout defaults, but is missing the OpenAI-specific `0`-sentinel assertions; `packages/ai/test/openai-first-event-timeout.test.ts` is the nearest wire-level owner.

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **OPEN A** | Add direct/gateway/share/compaction fixtures before adapting server-tool handling. |
| 2 | **OPEN A** | Verify timeout sentinel behavior per provider before removing watchdogs. |
| 3 | **OPEN A** | Keep provider auth/tier changes descriptor-driven and regenerate model data where required. |

## Decision Slots

| slot | status | decision |
| --- | --- | --- |
| A | SPLIT | Adapt provider wire-integrity only: server-tool history, stream projection/retry, and first-event timeout behavior. |
| B | NONE | Keep auth/catalog policy descriptor-owned; implemented owners are `packages/ai/src/providers/anthropic.ts`, `packages/ai/src/providers/anthropic-messages-server.ts`, `packages/ai/src/utils/idle-iterator.ts`, and `packages/agent/src/compaction/compaction.ts`. |
| C | TESTED | Anthropic direct/gateway/replay/compaction fixtures and OpenAI timeout/stream robustness tests cover the adopted slice. |
| D | CHASE ONLY | Archive only the wire-integrity A-slice; C-bucket product decisions remain open elsewhere. |
| E | NO MIGRATION | Wire/history handling only; no credential or catalog migration. |
| F | ADAPT — partial implementation, tracked residual | Five C-bucket anchors remain excluded per the card amendment: authenticated model discovery `df0e77c58` and `8191cf41d`; OpenCode Zen auth/header policy `d1cedfe18` and `036636d39`; Z.AI quota/tier identifier rename `443398a9d`. These require product/catalog decisions and remain descriptor-owned. |

## Implementation Evidence

- JWC implementation commit `9b2dddf` landed server-tool history/replay/compaction, stream projection/retry, and first-event sentinel handling at `packages/ai/src/providers/anthropic.ts`, `packages/ai/src/utils/idle-iterator.ts`, and `packages/agent/src/compaction/compaction.ts`.
- JWC implementation commit `2d51fc8` hardened server-tool sequence pairing at `packages/ai/src/providers/anthropic-messages-server.ts` with gateway tests.
