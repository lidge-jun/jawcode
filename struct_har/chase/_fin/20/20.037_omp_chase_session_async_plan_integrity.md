# 20.037 — OMP chase: session, async jobs, plan-mode integrity

Status: ✅ _fin reference-only card
Closed: 2026-07-17
Lane: OMP
Source range: `0ea6ea630..d0c1890a6` (`origin/main`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

OMP added a large session/agent integrity tranche:

- `234a46fa3` — delayed session termination for pending async background jobs.
- `01ab7e26`, `549b4c13a`, `24c6b3a9`, `326a3406a`, `69db80fbe`, `f614ec153`, `23d9f7c89`, `babb731cf` — session fence/atomic rewrite/flush/close integrity.
- `d82b9bdc5` — dynamic model resolution per LLM call.
- `a06d0c54b` — switched models during goal recovery.
- `285d384ca`, `cff8f22d6`, `b3cda0d92` — task/agent discovery and disabled plugin ordering.
- `285d384ca..0d23cb75c` area plus tests for plan-mode convergence, todo reminders, goal mode integration, session storage, and async jobs.

## JWC decision

Classification: **reference/adapt candidate**.

JWC has its own goal mode, `.jwc` session state, subagent/background rules, and PABCD/orchestrate gates. The session integrity fixes are high value, but direct import can break JWC state semantics. Treat this as a split card: async-job termination, atomic session rewrite, plan-mode convergence, goal recovery, and agent discovery each need separate closure evidence.

## JWC owner candidates

- `packages/coding-agent/src/session/`
- `packages/coding-agent/src/task/`
- `packages/coding-agent/src/modes/controllers/`
- `packages/coding-agent/test/agent-session-*`
- `packages/coding-agent/test/session-*`
- `packages/coding-agent/test/goals/*`

## Verification expectation

Before closing this card, prove every adopted slice with focused tests. At minimum, check async job termination does not drop retained background work, session atomic rewrites preserve newest data, and plan/goal convergence does not bypass JWC approval gates.

## Refresh — OMP v16.3.4 to v16.3.12

Source cluster: `d0c1890a6..f25ab54c5` (541 commits). Session/plan anchors: `e812c368c` carries local artifacts into plan execution, `17a510c57` carries local artifacts into approve execute, `72df91c3c` routes plan-mode guidance via `internalGuidance`, `d16818833` queues approved plans behind turns flushed by compaction, `b914f552f` pins plan-reference compaction window to 200k, `da6f0ebc3` uses wire model id for chat compaction, `241beb9b3` supports chat-completions remote compaction, `389515baa` retries handoff auto-only tool choice errors, and `f31366970` respects vetoed auto-handoff hooks.

JWC decision: plan execution/input improvements get new card 20.044; this card remains the session/async/plan umbrella.

Verification expectation: artifact carry-through, compaction queue ordering, handoff veto, and remote compaction behaviors each need focused tests if adopted.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | **reference/adapt/close**; JWC has native async-job retention, serialized session persistence, approved-plan/PABCD gates, model resolution, goal recovery, and compaction queue coverage, while OMP plan mode remains reference-only. |
| Decision B — naming impact | translate OMP plan/goal terms to JWC approved-plan, orchestrate/PABCD, goal, and task-agent owners; no rename required. |
| Decision C — test impact | no new closure test; existing async-job manager, session persistence, compaction queue, plan review, handoff, and goal runtime suites own the invariants. |
| Decision D — docs impact | this closed umbrella card only; split implementation cards remain the place for any future promoted slice. |
| Decision E — rollout impact | none; no runtime behavior changes. |
| Decision F — residual risk | OMP-specific artifact carry-through and remote-compaction variants are not asserted as identical; require focused tests only if JWC adopts those variants. |
