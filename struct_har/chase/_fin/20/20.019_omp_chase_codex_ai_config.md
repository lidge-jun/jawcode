# 20.019 — omp chase: codex/AI config (base URL · reasoning · verbosity · tiny role)

> MOC: [20_omp_chase_MOC](../../20_omp_chase_MOC.md) · G2 · ✅ **_fin** · reference-only · closed 260701 (ADAPT, 2 JWC slices)
> Split from [20.016](../../20.016_omp_chase_advisor_thinking_dialect_session_title.md) (cluster 3 of 6).
> Reviewed source: OMP `0fc6d136` (v16.1.20) → `ca9f2847e` (v16.2.5) vs JWC worktree `ef28a98` (2026-06-29).
> Source range: `devlog/_omp_chase/oh-my-pi` `0fc6d136..ca9f2847e`. **1:1 port ❌ — design reference.**

## Why This Is A Gap

OMP standardized codex base-URL parsing, set codex reasoning context to all_turns, made OpenAI stream textVerbosity configurable, added default reasoning/verbosity settings, and introduced a tiny model role for background tasks. These are config-surface decisions JWC can evaluate against its own `.jwc` AI settings.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `0fc6d136..ca9f2847e` |
| standardized codex base URL parsing | `f46d03093` |
| codex reasoning context = all_turns | `2708c1edb` |
| configurable textVerbosity for OpenAI stream | `2863a9c4e` |
| default reasoning/verbosity settings | `f018c5ce8` |
| tiny model role for background tasks | `f0f7a5ba8` |
| primary path anchors | `packages/ai/{src,test}` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show f46d03093 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show f0f7a5ba8 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| config surface | Evaluate textVerbosity + reasoning defaults against JWC ai settings schema. |
| tiny model role | Evaluate background-task model role against JWC role catalog (cf. 10.036). |
| naming | Settings keys must be JWC-first; translate any `pi`/`omp` naming. |
| tests | Focused JWC tests only if adapted. |

## Done Gate

- [x] Source anchors rechecked against OMP `ca9f2847e` (5 config-item SHAs resolve).
- [x] Adopt/defer/reject decision recorded per config item (see Closure table: 2 ADOPT, 1 INAPPLICABLE, 2 DEFER).
- [x] Adopted behavior has JWC-specific tests (openai-codex-base-url.test.ts, openai-responses-text-verbosity.test.ts).

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only; evaluate verbosity/reasoning defaults |
| Decision B — naming impact | settings keys (JWC-first) |
| Decision C — test impact | future focused tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | local config only if adopted |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | backend (ai config) if promoted |
| Decision H — bundle policy | may align with GJC 10.036 |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT**

ADAPT — verbosity/reasoning/base-URL/tiny-role 동작 골격 채택, 기본값·네이밍은 JWC 기준 재해석.


## Closure (260701) — ADAPT, 2 JWC slices implemented

Closed via goal `f8909338-255` autonomous PABCD (WP4). OMP는 reference-only(1:1 port ❌)이고, 독립 audit(explorer Bacon 갭분석 + adversarial Hubble re-audit)로 5개 config item을 per-item triage했다. JWC식 재설계로 실제 코드 채택 2건, 나머지 3건은 evidence-backed 보류/비해당. 채택 2건은 독립 read-only 검증자(Rawls gpt-5.4)가 DONE 판정.

| # | OMP config item | anchor | verdict | evidence (JWC) |
|---|---|---|---|---|
| 1 | codex usage base-URL parsing | `f46d03093` | **ADOPT** (WP4-A) | `normalizeCodexBaseUrl`(`packages/ai/src/usage/openai-codex.ts:190`)을 `new URL()` 파싱 + https·exact-host allowlist(chatgpt.com/chat.openai.com)로 재작성. streaming override `https://chatgpt.com/backend-api/codex/responses` 입력 시 기존엔 그대로 반환→`buildCodexUsageUrl`이 깨진 `.../codex/responses/wham/usage` 생성하던 버그 수정. 회귀 테스트 `packages/ai/test/openai-codex-base-url.test.ts`(8 case). commit `bc79608`. JWC-native(파일 분리 없이 inline, OMP의 별도 `openai-codex-base-url.ts` 미모방). |
| 2 | codex reasoning `all_turns` | `2708c1edb` | **INAPPLICABLE** | JWC `ReasoningConfig`/`CodexRequestOptions`에 `context`/`reasoningContext`·`all_turns` surface 0(`packages/ai/src/providers/openai-codex/request-transformer.ts`). 막을 대상 없음. |
| 3 | configurable textVerbosity (OpenAI stream) | `2863a9c4e` | **ADOPT** (WP4-B) | `SimpleStreamOptions.textVerbosity`(`packages/ai/src/types.ts`) + `OpenAIResponsesOptions` 추가, `stream.ts` `mapOptionsForApi`가 openai-responses·openai-codex-responses case에 전달(azure 제외=옵션 타입에 필드 없음), `openai-responses.ts` `buildParams`는 official-endpoint gate(`isOfficialOpenAIResponsesEndpoint`, provider=openai AND baseUrl 미설정/api.openai.com)일 때만 `text.verbosity` 매핑→non-official proxy엔 미전송. 테스트 `packages/ai/test/openai-responses-text-verbosity.test.ts`(3 case, 게이팅 포함). commit `027c3a9`. |
| 4 | default reasoning/verbosity 상향 | `f018c5ce8` | **DEFER** | #2(all_turns) 의존 + verbosity low→high·summary 강제는 동작/비용 기본값 정책 변경. ADAPT "작고 안전" 범위 밖 → 009 ③ 백로그 후보. |
| 5 | tiny model role (background tasks) | `f0f7a5ba8` | **DEFER** | JWC built-in role=`default`만(`packages/ai/src/model-registry.ts`), tiny role 개념·`tiny/` 디렉터리 부재. role registry+resolver+schema+callsites 동반 확장 슬라이스 → 009 ③ 백로그 후보. |

Audit: explorer(Bacon gpt-5.4) 5-verdict 갭분석 + adversarial(Hubble gpt-5.4) re-audit FAIL→플랜 4개 수정(WP4-A https+exact-host gating, WP4-B azure 제외, endpoint gate resolvedBaseUrl, 경로/라인 정정). Build 후 read-only 검증자(Rawls gpt-5.4) DONE: 게이팅 정확, azure 정상 제외, params.text clobber 없음, SSRF surface 미증가, broken caller 없음. Verification: 11 pass / 0 fail, `cd packages/ai && bun run check:types` exit 0, biome clean, added 라인 brand-literal 0, 인용 OMP SHA 5개 resolve, git diff --check clean. #4/#5 DEFER는 009 ③ 백로그.
