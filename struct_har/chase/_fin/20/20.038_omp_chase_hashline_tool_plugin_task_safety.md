# 20.038 — OMP chase: hashline/edit, tools, plugins, task safety

Status: ✅ _fin reference-only card
Closed: 2026-07-17
Lane: OMP
Source range: `0ea6ea630..d0c1890a6` (`origin/main`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

OMP landed multiple correctness and safety fixes across edit/tool/plugin/task surfaces:

- Hashline/edit recovery: `0059aed4f`, `367a002e4`, `f42fa38f7`, `72a30af57`, `b38eba2be`, plus `packages/hashline/*` changes.
- Tool argument/coercion and path handling: `2c8daf057`, `e73a25489`, `packages/coding-agent/src/tools/*`, `tool-argument-coercion.test.ts`.
- Custom tool / extension / plugin exit guards and cache ordering: `a76a33476`, `6ad4db58a`, `e015e79f0`, `4e12e5bf7`, `15c4835e9`, `0eb820e7c`, `621e3f3a8`.
- Task/worktree isolation and extension-agent discovery: `ecbf0760c`, `285d384ca`, `cff8f22d6`, `packages/coding-agent/src/task/*`.
- Debug/DAP/SSH/worker process lifecycle: `23cbeb49f`, `d0fa896c1`, `4cca96c5b`, `b0e52a13a`, `08899a439`.

## JWC decision

Classification: **reference/split**.

This is a high-value integrity cluster but too broad for one import. JWC should split into hashline/edit provenance, custom-tool/plugin loader safety, tool argument compatibility, task/worktree isolation, and subprocess/SSH/DAP lifecycle cards or implementation phases.

## JWC owner candidates

- `packages/coding-agent/src/edit/`
- `packages/coding-agent/src/tools/`
- `packages/coding-agent/src/extensibility/`
- `packages/coding-agent/src/task/`
- `packages/coding-agent/src/subprocess/`
- `packages/coding-agent/src/ssh/`, `src/dap/`
- `packages/hashline/` if/when JWC carries the same package surface

## Verification expectation

Before closing, every adopted split must have regression tests. Do not close this card from broad lint alone; the risk is silent corruption or hidden process leaks, which lint is famously terrible at catching.

## Refresh — OMP v16.3.4 to v16.3.12

Source cluster: `d0c1890a6..f25ab54c5` (541 commits). Tool/plugin/task anchors: `c8df93ca3`, `48a6e4675`, `c40b0ff5d`, `6c8e7625a`, `ff3b0c795`, and `c493d12f9` harden literal path vs selector handling for read/grep; `c92cf0a90`, `c42d2b290`, `f276d80fc`, `d9ef874e2`, `7e4360d31`, and `47c172c70` harden Claude/plugin discovery and legacy plugin validation; `ed4b5b5b0` resolves nested `agent://` output; `cca325d24` preserves isolated commits on transfer failure; `12acf7e64` auto-skips empty commits during task-branch cherry-pick; `ae21a7a7e` aborts underlying MCP calls when proxy tool timeout fires.

JWC decision: skill discovery/autocomplete gets new card 20.043; literal-path/tool safety remains on this umbrella card.

Verification expectation: selector/literal regression tests, plugin manifest matrix tests, nested `agent://` resolution tests, and timeout-abort tests are required for any adoption.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | **reference/adapt/close**; JWC already owns hashline recovery, literal-path/selectors, plugin compatibility, nested task output IDs, worktree isolation, abort propagation, and DAP/SSH disposal through its own architecture. |
| Decision B — naming impact | none; retain JWC tool, plugin, task, `agent://`, DAP, and SSH names. |
| Decision C — test impact | no new closure test; existing hashline/apply-patch, read-selector, task/worktree, plugin, MCP lifecycle, DAP disposal, and SSH suites are the owner seams. |
| Decision D — docs impact | this closed umbrella card only; future promoted invariants should use focused cards. |
| Decision E — rollout impact | none; reference triage changes no tool or process behavior. |
| Decision F — residual risk | this broad card does not claim commit-by-commit OMP parity; reopen only for a concrete corruption, isolation, timeout, or process-leak regression. |
