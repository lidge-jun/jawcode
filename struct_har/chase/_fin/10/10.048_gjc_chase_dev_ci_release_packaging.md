# 10.048 — gjc chase: dev CI release packaging

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · **P2** · closed 260701
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: 793006a3, d7bce535, 32b90f67, 75808e1e, 1a6553c0, 009d11ce with dev/CI/release commits f6fbd957, 0464f0f9, e56bd259, 9c34e751, 816aa07c, 85af2a07, 14137324, beddd221, 641e9294, 9f0f09fb, 45d9f956, 195d7f04, 1e258255, 7d390005, a256bdd7.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the dev CI release packaging cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Release metadata/changelogs, affected-path CI skip, pre-existing dev test repair, dev linked source doctor, native build/platform warnings.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | 793006a3, d7bce535, 32b90f67, 75808e1e, 1a6553c0, 009d11ce with dev/CI/release commits f6fbd957, 0464f0f9, e56bd259, 9c34e751, 816aa07c, 85af2a07, 14137324, beddd221, 641e9294, 9f0f09fb, 45d9f956, 195d7f04, 1e258255, 7d390005, a256bdd7 |
| primary source areas | `.github/workflows/**; scripts/**; package.json; packages/*/package.json; crates/**` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- .github/workflows/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- .github/workflows/**
```

## Phase 1C Hardening Note

Dev/CI/release bucket checked at GJC `a791d72a`; existing release metadata, CI skip, dev-test repair, linked-source, native tokenizer, and rust warning anchors were preserved while tag release, native-free schemas, affected-path validation, hosted runners, Intel Mac, bundle minification, and platform support commits were made explicit.

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [x] Source facts checked against GJC `816aa07c`, `641e9294`, `d7bce535` inside the `a791d72a` release/CI cluster.
- [x] JWC owner files listed before implementation (`scripts/ci-dev-affected.ts`, `scripts/ci-dev-affected.test.ts`, chase indexes).
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md) — public JWC names preserved.
- [x] Import/adapt/reject/split decision recorded: adopted `10.048-A` false-green guard; deferred matrix fanout/runner/native-platform split; rejected release credential changes for this cycle.
- [x] Focused test proves the chosen behavior: `bun test scripts/ci-dev-affected.test.ts` (6 pass / 0 fail).
- [x] `git diff --check` passes for the patch.
- [x] MOC/follow-index/tier/cycle/slice-map updated on closure.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## JWC Phase 5 Cross-Check — 2026-06-28

Related OMP card:

- [20.015](../20/20.015_omp_chase_release_test_leak_hardening.md)

Phase 5 devlog artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/50_phase5_release_test_leak_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/51_phase5_release_test_leak_overlap.md`

Cross-check result:

- `20.015` is treated as OMP reference/split-audit evidence, not as an independent release implementation owner.
- JWC already has release validation surfaces: `scripts/jwc-release-validation.ts`, `.github/workflows/ci.yml`, `scripts/release-publish-order.test.ts`, `scripts/check-no-github-workflows.ts`, `scripts/check-public-legacy-zero.ts`, and `scripts/check-workflow-yaml.ts`. `check-public-legacy-zero.ts` is an existing guard surface with current pre-existing findings, not green Phase 5 evidence.
- Any future release/CI/package code change should land against this `10.048` card and cite the `20.015` overlap result if the OMP range motivated it.

Status: still active. Phase 5 adds overlap evidence only and does not close the GJC release packaging card.

## JWC Phase 8 Split Evidence — 2026-06-28

Phase 8 devlog artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/80_phase8_workflow_team_packaging_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/85_phase8_release_packaging_split.md`

JWC owner files identified before implementation:

- `.github/workflows/ci.yml`
- `.github/workflows/dev-ci.yml`
- `.github/workflows/build-natives.yml`
- `.github/workflows/release.yml`
- `scripts/jwc-release-validation.ts`
- `scripts/ci-dev-affected.ts`
- `scripts/ci-release-publish.ts`
- `scripts/ci-release-build-binaries.ts`
- `scripts/ci-build-native.ts`
- `scripts/release-publish-order.test.ts`
- package manifests under `package.json`, `packages/*/package.json`, and `crates/**/Cargo.toml`

Split result:

- `10.048-A`: affected-path CI and workflow YAML validation hardening.
- `10.048-B`: release publish order, changelog, package manifest, and native build guard tests.
- `10.048-C`: platform support warnings and native binary lane changes only after current runner support is checked.

Historical Phase 8 status before closure: active at that time. The closure below supersedes this status while preserving the conclusion: `10.048` is the release/CI implementation owner and `20.015` is overlap evidence only.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | Translate public surface to `jwc`/`.jwc`; preserve upstream names only as source citations. |
| Decision C — test impact | Focused tests required unless this remains docs-only/track-only. |
| Decision D — docs impact | chase now; promote only stable implemented contracts. |
| Decision E — rollout impact | classify during implementation; assume local/runtime risk until proven otherwise. |
| Decision F — residual risk | open until implementation or explicit rejection evidence exists. |
| Decision G — reviewer needed | backend/security/architecture depending on touched surface. |
| Decision H — bundle policy | separate card; can join a PABCD bundle only with adjacent cards named here. |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT**

ADAPT — dev/CI/release packaging 채택, JWC 빌드/릴리스 표면에 맞춰 조정.

## Closure (260701)

Closed via goal `f8909338-255` autonomous PABCD (WP15). JWC adopted the focused
`10.048-A` affected-path false-green guard, not the broad GJC dev-ci matrix fanout.

| sub-feature | source anchor | decision | evidence |
|---|---|---|---|
| Bun `--cwd` false-green prevention for affected package/web tasks | GJC `816aa07c` (#622) | ADAPT | JWC `8b3ab60`: `scripts/ci-dev-affected.ts` now stores package/web scoping in `Task.cwd`; commands are `["bun","run",<script>]`; `runCommand(command, cwd)` executes in the selected directory. |
| CI selector self-test scheduling | GJC `816aa07c` / selector hardening pattern | ADAPT | JWC `8b3ab60`: `scripts/ci-dev-affected.test.ts` added; `planTasks(["scripts/ci-dev-affected.ts"], ...)` schedules `affected-selftest`. |
| Full affected-path matrix fanout | GJC `641e9294` | DEFER | Larger workflow topology change; current JWC serial dev-ci remains functional and the concrete gap was the false-green guard. |
| Irrelevant PR skip / release credential rewiring / runner lane changes | GJC `d7bce535` plus release cluster | REJECT/DEFER for this cycle | JWC already has OIDC-first release source of truth in `structure/60_release_publishing.md`; no `NPM_TOKEN`, trusted-publishing, or runner matrix changes were needed for this card. |

Verification:

- `bun test scripts/ci-dev-affected.test.ts` — 6 pass / 0 fail.
- `CI_DEV_CHANGED_PATHS=scripts/ci-dev-affected.ts bun scripts/ci-dev-affected.ts --dry-run` — plans `affected-dry-run` + `affected-selftest`.
- `bun run check:ts` — exit 0.
- Backend verifier: DONE.

Residual risk: closed for affected-path false-green prevention. Re-open only with new
evidence that JWC needs GJC-style matrix fanout, hosted runner lane changes, or a separate
release credential/package topology update.
