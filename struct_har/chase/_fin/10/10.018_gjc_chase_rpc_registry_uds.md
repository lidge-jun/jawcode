# 10.018 — gjc chase: RPC session registry + persistent UDS server

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **완료 Phase 1 + UDS Phase 2 (260615)** · P1
> Reviewed: GJC `269387b` (#589) vs JWC `d60b7822`.
> **명명**: [008](../../008_gjc_jwc_naming_contract.md) · **묶음**: [007](../../007_follow_index.md)
> **evidence**: `6fb79682` (Phase 1) · UDS Phase 2 [14_uds_phase2_implementation_log](../../../../devlog/_plan/260614_chase_rpc_harness_bundle/14_uds_phase2_implementation_log.md)

## JWC reconcile — Phase 1 + UDS Phase 2 landed

| fact | jwc |
|------|-----|
| Registry `rpc-sessions/*.json` | **✅** `session-registry.ts` |
| `rpc-mode.ts` register/unregister (stdio) | **✅** |
| `jwc_rpc.list_sessions` | **✅** |
| Tests | **✅** registry bun + pytest |
| UDS `--listen` | **✅ Phase 2** (`rpc-uds-listen`, socket guard, Python `connect_unix`) |

## Rediscovery (operational)

1. **Discover**: `jwc_rpc.list_sessions()` or TS `listRpcSessions(getAgentDir())` under `~/.jwc/agent/rpc-sessions/` (stale PIDs reaped).
2. **Attach stdio owner**: RPC `switch_session` with `sessionFile` from record (see `rpc-stdio-redteam` persistence test).
3. **Attach UDS owner**: records with `transport: "socket"` expose `endpoint`; Python hosts use `RpcClient.connect_unix(owner.endpoint)`.
4. **Harness/orchestrate**: default harness remains stdio; UDS is opt-in via `jwc --mode rpc --listen <path>`.

## 완료 기준

- [x] Registry + Python list + tests.
- [x] Rediscovery § above.
- [x] UDS — Phase 2 landed (`jwc --mode rpc --listen`, `RpcClient.connect_unix`).
- [ ] Multi-client fanout / Windows named pipe — out of scope.