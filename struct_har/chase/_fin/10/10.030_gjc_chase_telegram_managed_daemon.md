# 10.030 — gjc chase: managed Telegram daemon and singleton poller

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P1**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: `eb379ebc`, `7384e76a`, `06c15d72`, `e9e199a3`, `2f903777`, `f2bac1e5`, `1fcbc13d`, `9c50168c`.

## Why This Is A Gap

GJC ships a managed Telegram daemon that scans notification endpoint files, owns the Telegram `getUpdates` long poll, sends per-session messages, and routes inbound updates back to sessions. JWC has no daemon runtime or singleton poller for Telegram.

This card depends on [10.028](./10.028_gjc_chase_notifications_sdk.md) and [10.029](./10.029_gjc_chase_notify_config_cli.md).

## Source Facts

| capability | upstream anchor |
|---|---|
| daemon owner/spawn/roots/locks | `packages/coding-agent/src/notifications/telegram-daemon.ts` |
| daemon internal CLI | `packages/coding-agent/src/notifications/telegram-daemon-cli.ts` |
| daemon control/reload/stop | `packages/coding-agent/src/notifications/telegram-daemon-control.ts` |
| generic daemon CLI | `packages/coding-agent/src/cli/daemon-cli.ts`, `src/commands/daemon.ts` |
| managed session scanner | `packages/coding-agent/src/notifications/managed-daemon.ts` |
| operator runtime/backoff | `packages/coding-agent/src/notifications/operator-runtime.ts` |
| rate limit lanes | `packages/coding-agent/src/notifications/rate-limit-pool.ts` |
| tests | `daemon-control.test.ts`, `notifications-compiled-daemon-smoke.test.ts`, `notifications-managed-engine.test.ts`, `notifications-telegram-daemon.test.ts` |

Key invariants:

| invariant | detail |
|---|---|
| one token/chat owner | Telegram allows one active `getUpdates`; daemon prevents 409 conflict by lock/state ownership. |
| roots registry | sessions register roots in daemon state, daemon scans `.gjc/state/notifications/*.json`. JWC must use `.jwc`. |
| heartbeat | owner has `heartbeatAt`; stale owners can be replaced only when pid/deadness rules pass. |
| no overlap reload | reload writes owner-scoped control request, signals old daemon, waits for pid death before spawning fresh poller. |
| bounded retries | transient network errors retry; persistent poll failure backs off. |
| session scan independent of long poll | scan interval is separate so asks do not wait for a long `getUpdates` poll to return. |

## JWC Reconcile Notes

| issue | decision pressure |
|---|---|
| public daemon CLI | Decide whether JWC exposes `jwc daemon` or keeps Telegram daemon internal behind `jwc notify`. |
| settings agent dir | State files must live under JWC agent dir and `.jwc` roots. |
| compiled binary | Build entrypoints must include daemon internal if bundled binary supports notifications. |
| reload semantics | Avoid double poller overlap; do not spawn fresh owner until old pid is dead. |
| logs | Daemon logs to file; no raw tokens. |

## Suggested Split

1. Port root registration + singleton state files with JWC names.
2. Add hidden `jwc notify daemon-internal` smoke.
3. Add daemon scanner that connects to notification endpoints.
4. Add Telegram poll loop with retry/backoff and owner lock.
5. Add `daemon status/reload/stop` only if product wants public daemon control.

## Done Gate

- [ ] Multiple JWC sessions attach to one fresh daemon owner for same token/chat.
- [ ] A second poller is not started while owner pid is live.
- [ ] Reload/stop are owner-scoped and do not clear newer owner requests.
- [ ] Session endpoint files created after daemon startup are connected within scan interval.
- [ ] Poller survives transient network errors and logs bounded failures.
- [ ] Token/chat fingerprinting avoids secret logs.

## Verification

- Focused unit tests for ownership, stale detection, reload wait-for-pid-death, roots registry, scan cadence.
- Daemon smoke equivalent to `g005-daemon-smoke.ts` / `g011-daemon-path-smoke.ts` if compiled.
- Red-team: simulate two sessions + same token/chat; expect one owner.

## JWC Phase 2 Evidence — 2026-06-28

Implemented partial JWC-native transport shell evidence:

- `packages/coding-agent/src/notifications/transport-state.ts`
  - `<agentDir>/notifications/telegram/` owner and roots path contract.
  - `fingerprintSecret()` with 12-character SHA-256 prefixes for token/chat identity.
  - owner freshness predicates with EPERM-aware default pid liveness.
  - private `0700` directory and `0600` JSON writes where supported.
  - lock-protected `.jwc/state` root registry.
- `packages/coding-agent/src/notifications/transport-shell.ts`
  - registered-root scan of `.jwc/state/notifications/*.json`.
  - per-file safe read with bounded errors.
  - masked inert observations with `inboundMode: "drop"`.
  - fail-closed inbound decision: `authorization_not_implemented`.
- Tests:
  - `packages/coding-agent/test/notifications-transport-state.test.ts`
  - `packages/coding-agent/test/notifications-transport-shell.test.ts`

Verification snapshot:

```sh
bun test packages/coding-agent/test/notifications-transport-state.test.ts packages/coding-agent/test/notifications-transport-shell.test.ts
# 8 pass, 0 fail
```

Status: still active. This evidence covers owner predicates, roots registry, scanner observations, and secret non-leak checks only. It does not close the card because daemon process spawn, Telegram network polling, reload/stop semantics, endpoint connection, retry/backoff, and compiled daemon smoke remain deferred.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | `.jwc` roots, `jwc notify`, optional `jwc daemon`; no public `gjc` strings. |
| Decision C — test impact | Daemon ownership/reload/scanner tests required. |
| Decision D — docs impact | chase now; later operations docs. |
| Decision E — rollout impact | Long-lived process + local state files. |
| Decision F — residual risk | Process lifecycle, singleton ownership, Telegram 409 conflicts. |
| Decision G — reviewer needed | Backend/architecture. |
| Decision H — bundle policy | Separate from rendering and session lifecycle commands. |

## JWC Final Close — 2026-06-28 (phases 2, 37, 38, 39, 40)

Closed: a JWC-native managed Telegram daemon runtime. JWC chose a **TypeScript in-process daemon**
(`runManagedDaemon`) over the upstream Rust/N-API crate; the upstream sources were read-only evidence.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/370-373_phase37_telegram_poll_client_owner_*`
- `devlog/_plan/260628_jwc_native_chase_implementation/380-383_phase38_managed_daemon_engine_*`
- `devlog/_plan/260628_jwc_native_chase_implementation/390-393_phase39_daemon_control_loop_*`
- `devlog/_plan/260628_jwc_native_chase_implementation/400-403_phase40_daemon_runtime_smoke_*`

JWC implementation (`packages/coding-agent/src/notifications/`):

- `transport-state.ts` / `transport-shell.ts` (phase 2) — owner/roots/heartbeat/fingerprint + session scanner.
- `telegram-api.ts` (phase 37) — token-safe `getTelegramUpdates`/`sendTelegramMessage` + error classification + `nextBackoffMs`.
- `daemon-owner.ts` (phase 37) — `decideOwnerClaim` (one live poll owner; defer/keep/claim).
- `daemon-engine.ts` (phase 38) — `runDaemonTick` (claim+heartbeat+scan+poll+offset/backoff).
- `daemon-control.ts` + `daemon-loop.ts` (phase 39) — owner-scoped stop/reload + `runDaemonLoop`.
- `daemon-runtime.ts` (phase 40) — `runManagedDaemon` (real-I/O runnable daemon).

Done-gate (all met, tested end-to-end via `runManagedDaemon` + smoke):

| Gate | Evidence |
|---|---|
| one fresh owner per token/chat | `decideOwnerClaim` + smoke single-owner |
| no second poller while owner pid live | `decideOwnerClaim` defer + two-daemon smoke |
| reload/stop owner-scoped, no newer-owner clobber | `decideDaemonControl` + stop smoke |
| endpoint connect within scan interval | `runDaemonTick` scans each loop |
| poller survives transient errors, bounded backoff | engine `nextBackoffMs` + loop |
| token/chat fingerprinting, no secret logs | `fingerprintSecret` + token sanitize |

Verification: `bun test test/notifications-*.test.ts test/notify-cli.test.ts` → 108 pass / 0 fail;
`bun run check:types` exit 0. Commits: 37 `e6e5f30`, 38 `8fa1ded`, 39 `e4f1e9d`, 40 `2dda816`.

Residual (monitored, NOT blocking): the literal OS-detached process launch / public-vs-internal
`jwc daemon` CLI binary is a deployment concern — open a NEW card only if a runnable binary surface is
wanted. The behavioral daemon runtime is complete and demonstrable in-process.

Status: **`10.030` closed → `_fin/10`.**
