# 10.047 — gjc chase: security privacy guardrails

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P1**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: 16c0bdd0, df086ee3, 679ab5ba, 6527ee01, 27cb59df with security/privacy commits bbec0fb5, ec69fe36, 0a265141, 0471c35d, 088fe7c3, 8230b65c, 79b387e8, d90c7a8f, 1380d4ae.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the security privacy guardrails cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Ignore project dotenv for provider creds, live env override, socket token guard, private/local URL guard, env scrub, secret/log redaction.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | 16c0bdd0, df086ee3, 679ab5ba, 6527ee01, 27cb59df with security/privacy commits bbec0fb5, ec69fe36, 0a265141, 0471c35d, 088fe7c3, 8230b65c, 79b387e8, d90c7a8f, 1380d4ae |
| primary source areas | `packages/ai/src/**; packages/coding-agent/src/auth/**; packages/coding-agent/src/tools/**; packages/coding-agent/src/robogjc/**` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- packages/ai/src/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- packages/ai/src/**
```

## Phase 1C Hardening Note

Security/privacy bucket checked at GJC `a791d72a`; existing auth/env/RPC/search/env-scrub anchors were preserved while proxy-header, replay-token, browser-origin, private-network, user-context exclusion, workflow ownership, bash file IO guard, read auth token, and public-URL fallback commits were made explicit.

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [ ] Source facts checked against `a791d72a` or a newer explicitly recorded GJC head.
- [ ] JWC owner files listed before implementation.
- [ ] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md).
- [ ] Import/adapt/reject/split decision recorded for each sub-feature.
- [ ] Focused test or explicit manual evidence proves the chosen behavior.
- [ ] `git diff --check` passes for the patch.
- [ ] MOC/follow-index/gap inventory updated if priority or status changes.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## JWC Phase 7 Split Evidence — 2026-06-28

Phase 7 records this card as the cross-cutting security/privacy guardrail owner inside the security/provider/network bundle.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/70_phase7_security_provider_network_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/74_phase7_security_privacy_split.md`

JWC owner surfaces:

- `packages/coding-agent/src/secrets/**`
- `packages/coding-agent/src/session/contribution-prep.ts`
- `packages/coding-agent/src/modes/shared/agent-wire/event-envelope.ts`
- `packages/coding-agent/src/exec/non-interactive-env.ts`
- `packages/coding-agent/src/commands/auth-gateway.ts`
- `packages/coding-agent/src/cli/auth-gateway-cli.ts`
- `packages/coding-agent/src/modes/bridge/**`
- `docs/secrets.md`
- `docs/environment-variables.md`

Candidate follow-up slices:

- `10.047-A`: secret/log redaction regression tests for contribution prep, agent-wire envelopes, and logs.
- `10.047-B`: security-policy owner for non-interactive env scrub expansion only for concrete missing variables; coordinate with Phase 6 `10.037-B` runtime cleanup before code.
- `10.047-C`: browser-origin/auth-gateway no-auth guard if a matching JWC owner gap is confirmed; bridge token helper tests exist, but dedicated auth-gateway no-auth negative tests remain TBD before code closes.

Overlap rule: provider credential behavior belongs to `10.036`, RPC socket/control-plane behavior belongs to `10.038`, and public URL/search behavior belongs to `10.043`. This card owns cross-cutting policy and redaction/env guardrails only, preventing duplicate implementation ownership.

Status: still active. Phase 7 records split boundaries only; it does not close security/privacy done-gates.

## JWC Phase 14 Partial Evidence — 2026-06-28

Phase 14 implements part of `10.047-A` with redaction regression coverage and one minimal redaction fix.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/140_phase14_security_auth_redaction_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/142_phase14_security_auth_redaction_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/143_phase14_security_auth_redaction_check.md`

JWC evidence:

- `packages/coding-agent/test/contribution-prep.test.ts` adds OAuth client secret, provider refresh token, and provider cookie fixtures.
- `packages/coding-agent/src/session/contribution-prep.ts` now redacts `OAUTH_*` secret environment names using the existing provider-key redaction path.
- `packages/coding-agent/test/agent-wire/event-observation.redteam.test.ts` extends secret markers with shaped auth header, provider env, and private endpoint payloads.

Verification:

- `bun test packages/coding-agent/test/contribution-prep.test.ts packages/coding-agent/test/agent-wire/event-observation.redteam.test.ts` — 11 pass / 0 fail.
- `cd packages/coding-agent && bun run check:types` — exit 0.

Status: still active. This closes only the tested `10.047-A` redaction subset; env scrub expansion and auth-gateway/browser-origin negatives remain open.

## JWC Phase 16 Partial Evidence — 2026-06-28

Phase 16 implements the browser-origin negative-test portion of `10.047-C` and fixes a small JWC-facing naming drift in auth-gateway command help.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/160_phase16_auth_gateway_browser_guard_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/162_phase16_auth_gateway_browser_guard_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/163_phase16_auth_gateway_browser_guard_check.md`

JWC evidence:

- `packages/ai/test/auth-gateway-browser-origin.test.ts` proves browser preflight receives CORS headers while real browser-origin `GET /v1/models` and `POST /v1/responses` requests still return 401 without bearer auth.
- The same test proves authenticated browser-origin `GET /v1/models` succeeds without mocking provider streaming.
- `packages/coding-agent/src/commands/auth-gateway.ts` and `packages/coding-agent/src/cli/auth-gateway-cli.ts` now use `jwc auth-gateway` and `JWC_AUTH_BROKER_URL` in JWC-facing help/error text.

Status: still active. This closes only the tested `10.047-C` browser-origin negative subset; `10.047-B` env scrub expansion and any broader no-auth policy decisions remain open.

## JWC Phase 17 Partial Evidence — 2026-06-28

Phase 17 implements the `10.047-B` security-policy portion for non-interactive bash env scrub in coordination with `10.037-B`.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/170_phase17_non_interactive_env_scrub_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/172_phase17_non_interactive_env_scrub_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/173_phase17_non_interactive_env_scrub_check.md`

JWC evidence:

- `packages/coding-agent/src/secrets/index.ts` exposes the shared secret env-name classifier used by both obfuscation and env scrub policy.
- `packages/coding-agent/src/exec/non-interactive-env.ts` removes credential-like env names from non-interactive bash execution while preserving operational `SSH_AUTH_SOCK` / `SSH_AGENT_PID`.
- `packages/coding-agent/test/bash-executor.test.ts` proves provider/API/broker/bridge token-style env values are absent from spawned non-interactive bash output.

Status: still active. This closes only the tested `10.047-B` non-interactive bash env scrub subset; broader privacy/security guardrails remain active where not explicitly evidenced above.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | Translate public surface to `jwc`/`.jwc`; preserve upstream names only as source citations. |
| Decision C — test impact | Focused tests required unless this remains docs-only/track-only. |
| Decision D — docs impact | chase now; promote only stable implemented contracts. |
| Decision E — rollout impact | classify during implementation; assume local/runtime risk until proven otherwise. |
| Decision F — residual risk | open until implementation or explicit rejection evidence exists. |
| Decision G — reviewer needed | backend/security/architecture depending on touched surface. |
| Decision H — bundle policy | separate card; can join a PABCD bundle only with adjacent cards named here. |

## JWC Phase 30 Final Close — 2026-06-28

Close decision: **adapt, closed.** `10.047`'s owned lane — cross-cutting redaction + non-interactive env scrub + auth-gateway browser-origin/no-auth policy (overlap rule above) — is fully implemented and tested. Provider-credential, RPC-socket, and public-URL/search behaviors are owned by `10.036` / `10.038` / `10.043` and are not this card's scope.

### Owned sub-slice evidence rollup

| Sub-slice | Owned behavior | Phase | Passing focused test |
|---|---|---|---|
| 10.047-A | secret/log redaction (contribution-prep, agent-wire envelopes, `OAUTH_*`) | 14 | `packages/coding-agent/test/contribution-prep.test.ts`, `packages/coding-agent/test/agent-wire/event-observation.redteam.test.ts` |
| 10.047-B | non-interactive bash env scrub (credential env removal; keep `SSH_AUTH_SOCK`/`SSH_AGENT_PID`) | 17 | `packages/coding-agent/test/bash-executor.test.ts` |
| 10.047-C | auth-gateway browser-origin no-auth negatives + JWC naming | 16 | `packages/ai/test/auth-gateway-browser-origin.test.ts` |

### Out-of-owned-lane / deferred provenance (NOT closed under 036/038/043)

Per the Phase 30 A-phase audit, these source-cluster security/privacy commits fall outside `10.047`'s owned lane and are neither A/B/C nor owned by 036/038/043. They are deferred provenance — future-new-evidence only if a matching JWC owner surface appears:

- `bbec0fb5` strip OpenAI beta header — provider-request-transform lane (JWC `openai-request-transform.ts` has no beta handling).
- `088fe7c3` exclude user-context files from prompt — prompt-context (`system-prompt.ts`) lane.
- `8230b65c` bridge controller workflow-gate ownership — control-plane (`agent-wire/scopes.ts`) lane.
- `79b387e8` composer bash file-IO guard — file-egress lane; **no matching JWC surface** (`composer-bash-policy.ts` absent).
- `ec69fe36` robogjc replay-token-to-browser — **non-applicable**; JWC has no `python/robogjc`.

Env-scrub commit `27cb59df` (macOS malloc-stack-logging) is already neutralized in JWC via `packages/utils/src/procmgr.ts` + `packages/coding-agent/src/cli.ts` delete-in-place — no env-scrub hole.

### Verification (Phase 30 check)

A/B/C focused suites re-run green; `bun run check:types` (packages/ai + packages/coding-agent) exit 0; `git diff --check` clean; inbound-reference link scan points to `_fin/10`. See `devlog/_plan/260628_jwc_native_chase_implementation/303_phase30_security_privacy_final_close_check.md`.

### Decision slot updates

- Decision A — adapt (owned lane closed).
- Decision F — residual risk: **closed** for owned lane; out-of-owned-lane commits above are deferred provenance, not owned debt.
- Decision G — reviewer: independent read-only audit (Phase 30 A) returned PASS.

Artifacts: `300_phase30_security_privacy_final_close_plan.md`, `301_phase30_security_privacy_final_close_audit.md`, `302_phase30_security_privacy_final_close_build.md`, `303_phase30_security_privacy_final_close_check.md`.
