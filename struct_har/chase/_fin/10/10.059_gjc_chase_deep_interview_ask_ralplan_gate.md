# 10.059 — gjc chase: deep-interview wording · ralplan/ultragoal ask gate · render guard

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · closed 260701 (ADAPT+IMPORT, 4 JWC slices; 2 defer no-surface, 1 already-covered)
> Reviewed source: GJC `20c299eb` (`upstream/dev`, v0.7.8) vs JWC worktree `cfe71cd` (2026-07-01).
> Range: v0.7.8 delta `fa995807..20c299eb` (21 commits); this card isolates the deep-interview / ask-gate cluster (6 commits).
> Naming: upstream `gjc`/`gjc-runtime`/`deep-interview`/`ralplan`/`ultragoal` → JWC `jwc`/`jwc-runtime`/`jaw-interview`/`plan`/`goal` per [008](../../008_gjc_jwc_naming_contract.md).
> Neighbors: extends [10.042](../../10.042_gjc_chase_deep_interview_ask_goal_state.md) (deep-interview/ask/goal state).

## Why This Is A Gap

GJC v0.7.8 hardened the interview/ask/goal approval surface across six commits: the deep-interview skill now keeps implementation wording inside the interview phase, accepts long inline `--spec`, and avoids noisy optional settings reads; ralplan routes through the ask approval gate; ultragoal scopes its ask guard to the active session only; and renderer string helpers are guarded against `undefined` (fixing a deep-interview ask `/background` crash). JWC carries the same surface under renamed modules (`jaw-interview`, `plan`/`goal` skills, `jwc-runtime`), so each behavior must be reconciled by name, not copied.

Behavioral surface:
1. Deep-interview keeps implementation-detail wording confined to the interview phase (spec contract).
2. Deep-interview accepts long inline `--spec` arguments without truncation.
3. Deep-interview avoids noisy optional settings reads (reduced log/IO noise).
4. ralplan uses the ask approval gate instead of an ungated path.
5. ultragoal ask guard scoped to the active session (no cross-session leakage).
6. Renderer string helpers guarded against `undefined` (deep-interview ask `/background` crash + class hardening).

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `20c299eb` (v0.7.8) |
| interview wording contract | `20c299eb` fix(deep-interview): keep implementation wording in interview phase — `src/defaults/gjc/skills/deep-interview/SKILL.md`, `test/deep-interview-skill-contract.test.ts` |
| ralplan ask gate | `bb6f0e98` feat(ralplan): use ask approval gate — `src/defaults/gjc/skills/ralplan/SKILL.md`, `src/modes/shared/agent-wire/deep-interview-gate.ts`, `src/tools/ask.ts`, `test/tools/ask.test.ts` |
| long inline `--spec` | `eef6ec3a` fix(deep-interview): accept long inline --spec — `src/gjc-runtime/deep-interview-runtime.ts`, `test/gjc-runtime/deep-interview-runtime.test.ts` |
| noisy optional settings | `f1343220` fix(deep-interview): avoid noisy optional settings reads (#1294) — `src/defaults/gjc/skills/deep-interview/SKILL.md` |
| render undefined guard | `2bc0e2c5` fix(render): guard renderer string helpers against undefined (#1290) — `src/deep-interview/render-middleware.ts`, `src/tools/render-utils.ts`, `packages/tui/src/utils.ts` |
| ultragoal ask-guard scope | `19408acc` fix(ultragoal): scope ask guard to active session (#1287) — `src/gjc-runtime/ultragoal-guard.ts`, `src/tools/ultragoal-ask-guard.ts`, `src/session/agent-session.ts` |
| primary source areas | `packages/coding-agent/src/defaults/gjc/skills/` (→ JWC `defaults/jwc/skills/`), `src/gjc-runtime/` (→ `jwc-runtime/`), `src/tools/ask.ts`, `src/deep-interview/`, `packages/tui/src/utils.ts` |
| comparison range | `fa995807..20c299eb` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 20c299eb --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 19408acc --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 2bc0e2c5 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| skill names | Upstream `deep-interview`/`ralplan`/`ultragoal` skills map to JWC `jaw-interview`/`plan`/`goal` (`packages/coding-agent/src/defaults/jwc/skills/`). Wording/spec contract must be applied to the JWC skill files, not copied verbatim. |
| runtime path | Source `gjc-runtime/deep-interview-runtime.ts`, `gjc-runtime/ultragoal-guard.ts` map to JWC `jwc-runtime/` (`jaw-interview-runtime.ts` present; ultragoal-guard equivalent must be located/confirmed). |
| ask gate | `src/tools/ask.ts` exists in JWC — verify the ask approval gate wiring (`deep-interview-gate.ts`) and session-scoped guard parity. |
| render guard | `render-middleware.ts` not found under JWC `src/deep-interview/`; the undefined-guard for renderer string helpers may live elsewhere (`render-utils.ts`, `tui/src/utils.ts`). Locate JWC equivalent before deciding import. |
| crash safety | The `/background` undefined-guard is a defensive data-safety fix — prioritize import/adapt regardless of fork naming. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source facts checked against `20c299eb` (v0.7.8) — 6-commit cluster triaged.
- [x] JWC owner files identified — `tools/render-utils.ts`, `tools/eval.ts`, `packages/tui/src/utils.ts`, `jwc-runtime/jaw-interview-runtime.ts`, `tools/ask.ts`, `modes/shared/agent-wire/jaw-interview-gate.ts`, `defaults/jwc/skills/jaw-interview/SKILL.md`.
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md) — 0 new gjc/ralplan/ultragoal literals on added lines (deep-interview is a JWC RpcWorkflowStage value).
- [x] Import/adapt/reject recorded per sub-feature (6 items) — see Closure triage.
- [x] Focused tests prove behavior — 110 pass across 7 files (render guard, --spec, workflow-gate override, skill wording).
- [x] `git diff --check` passes; coding-agent + tui tsgo EXIT 0.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | open: likely adapt (skill wording + ask gate) + import (render undefined guard) |
| Decision B — naming impact | skill names (jaw-interview/plan/goal), runtime path (`jwc-runtime`), command text |
| Decision C — test impact | new/updated focused tests for ask gate + session scope + render guard |
| Decision D — docs impact | chase only; skill SKILL.md wording is the implementation surface |
| Decision E — rollout impact | no migration; behavior/UX hardening |
| Decision F — residual risk | open until implementation or explicit rejection |
| Decision G — reviewer needed | backend (ask gate/session scope); architecture for render guard placement |
| Decision H — bundle policy | may bundle with 10.042 (deep-interview/ask/goal state) |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT+IMPORT**

ADAPT+IMPORT — ralplan ask gate는 JWC 인터뷰 흐름에 adapt, render undefined guard는 순수 방어라 import.

## Closure (260701) — ADAPT+IMPORT, 4 JWC slices

GJC v0.7.8 hardened the interview/ask/render surface across 6 commits. Reconciled by name into JWC (1:1 port rejected): 2 IMPORT (pure-defensive), 2 ADAPT (JWC-authored), 1 already-covered, 2 DEFER (no JWC surface).

### 6 sub-feature triage (GJC `fa995807..20c299eb`)

| GJC commit | feature | disposition | JWC slice / rationale |
|---|---|---|---|
| `2bc0e2c5` | guard renderer string helpers vs undefined | **IMPORT** | `getPreviewLines`/`shortenPath` string guards (render-utils.ts), git_log nullish cast (eval.ts), `safeText` coercion (tui/utils.ts). `render-middleware.ts`/`normalizeText` portion DEFER — no JWC surface. |
| `eef6ec3a` | accept long inline `--spec` | **IMPORT** | `jaw-interview-runtime.ts` resolveSpecContent — added `ENAMETOOLONG` to the path-or-inline fallthrough. |
| `bb6f0e98` | ralplan uses ask approval gate | **ADAPT (stage-only)** | JWC `plan` skill is superseded by native orchestrate P. Reusable value imported as `ask` tool `workflowGate.stage` override (ask.ts + jaw-interview-gate.ts); **kind fixed to `"question"`** (audit: approval/execution gates use a different decision contract). |
| `20c299eb` | keep implementation wording in interview phase | **ADAPT** | JWC-authored phase-boundary wording in `jaw-interview/SKILL.md` (jaw-interview / orchestrate P, no upstream literals). |
| `f1343220` | avoid noisy optional settings reads | **ALREADY COVERED** | JWC `jaw-interview/SKILL.md` already forbids direct settings-file reads (native activation owns precedence). No change. |
| `19408acc` | ultragoal ask-guard scoped to active session | **DEFER ③** | JWC has no ultragoal-ask-guard; `goal-guard.ts` is completion-receipt arch, `ask.ts` has no goal/ultragoal ask-block path. No cross-session hijack surface to fix. |

### Audit

- **A-phase (James, gpt-5.4): FAIL→fix** — Slice C `kind` override unsafe (`questionToGate` always emits a question-answer schema; approval/execution use a `decision` contract). Fixed: stage-only override, kind fixed to `"question"`. Other 5 items VERIFIED-CORRECT.
- **B-phase reviewer (Avicenna, gpt-5.4): PASS** 5 axes. Nice-to-have closed: added E2E test forwarding `workflowGate` through `AskTool.execute`.

### Verification

- `bun test` 7 focused files → 110 pass / 0 fail (render-utils, issue-848-repro, jaw-interview-runtime, jaw-interview-workflow-gates, jaw-interview-skill-policy, ask, ask-unattended-gate).
- `cd packages/coding-agent && bun run check:types` → EXIT 0; `cd packages/tui && bun run check:types` → EXIT 0.
- biome clean; naming guard 0 new literals; `git diff --check` clean. Source SHAs resolve in `devlog/_gjc_chase/gajae-code`.
- JWC commits: `348a63a` (render guard), `6537d4b` (--spec), `41be49e` (ask workflowGate), `3f38b1c` (interview wording), test follow-up.

> Re-open only with new evidence: a JWC ultragoal-ask-guard surface (#19408acc) or a JWC deep-interview render-middleware (#2bc0e2c5 normalizeText) would reactivate the 2 deferred items.
