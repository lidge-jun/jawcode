# 10.065 — gjc chase: system prompt self-awareness source grounding

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · closed 260701 (ADAPT, 1 JWC slice — identity-safe)
> Reviewed source: GJC `20c299eb` (`upstream/dev`, v0.7.8) vs JWC worktree `cfe71cd` (2026-07-01).
> Range: v0.7.8 delta `fa995807..20c299eb` (21 commits); this card isolates the system-prompt grounding change (1 commit).
> Naming: upstream system prompt identifies as `gjc`/Gajae; JWC system prompt must identify as `jwc`/Jawcode per [008](../../008_gjc_jwc_naming_contract.md) — **identity-sensitive**.
> Neighbors: prompt/identity surface (relates to AGENTS identity rules, not a prior numbered card).

## Why This Is A Gap

GJC v0.7.8 added self-awareness source grounding to the system prompt (5 added lines in `system-prompt.md`) so the agent more accurately describes what it is and where its knowledge/identity comes from. JWC ships its own `packages/coding-agent/src/prompts/system/system-prompt.md` with JWC/Jawcode identity. The *behavior* (grounding the agent's self-description in an authoritative source) may be worth adopting, but the *content* must use JWC identity — a verbatim copy would leak `gjc`/Gajae self-identification into JWC. This is the most identity-sensitive card in the delta.

Behavioral surface:
1. System prompt gains self-awareness source-grounding lines (what the agent is + authoritative identity source).

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `20c299eb` (v0.7.8) |
| prompt grounding | `aa7322f6` feat(prompt): add self-awareness source grounding — `packages/coding-agent/src/prompts/system/system-prompt.md` (+5 lines) |
| primary source area | `packages/coding-agent/src/prompts/system/system-prompt.md` |
| comparison range | `fa995807..20c299eb` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show aa7322f6
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| identity safety | **Critical**: JWC `system-prompt.md` exists with Jawcode identity. Adopt the grounding *pattern* (anchor self-description to an authoritative source) but never copy `gjc`/Gajae identity strings. Any imported wording must read `jwc`/Jawcode. |
| prompt diff | The upstream change is only 5 lines; diff them, then author the JWC-equivalent grounding lines from scratch using JWC identity facts. |
| behavior value | Self-awareness grounding reduces hallucinated self-claims; low-risk, low-cost adopt-by-adaptation. |
| review gate | Identity/prompt changes affect every session — warrants a docs/identity review before landing. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source facts checked against `20c299eb` (GJC `aa7322f6`, +5 lines).
- [x] JWC owner file: `packages/coding-agent/src/prompts/system/system-prompt.md`.
- [x] Naming checked against [008](../../008_gjc_jwc_naming_contract.md): zero gjc/gajae/Gajae/Yeachan in added lines; agent-identity-leak.test passes.
- [x] ADAPT: grounding pattern adopted, content JWC-authored (Jaw/Jawcode, repo lidge-jun/jawcode).
- [x] Independent identity review (Newton gpt-5.4) PASS; agent-identity-leak.test 4 pass.
- [x] `git diff --check` clean.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | open: likely adapt (grounding pattern, JWC-authored content) |
| Decision B — naming impact | identity strings (`jwc`/Jawcode); **reject any `gjc`/Gajae leak** |
| Decision C — test impact | optional prompt-contract test; manual identity review |
| Decision D — docs impact | chase only; prompt content is the implementation surface |
| Decision E — rollout impact | affects all sessions; no migration |
| Decision F — residual risk | identity leak if copied verbatim — must author JWC content |
| Decision G — reviewer needed | docs/identity reviewer (every-session impact) |
| Decision H — bundle policy | single-card commit; small, isolated prompt change |


### Interview decision (2026-07-01)

> ② chase 결정 인터뷰 (goal `f47e0507-b13` 후속, theme7 라운드) 확정.
> **Tier: ①** · **Decision A 확정: ADAPT**

ADAPT — self-grounding 동작 채택, prompt content는 항상 JWC-authored(jwc/Jawcode 정체성). upstream 문구 1:1 유입 금지. follow 가능하되 content-JWC 불변식 필수.


## Closure (260701)

> ADAPT — closed via WP11 (goal `f8909338-255`). Plan: `devlog/_plan/260701_chase_tier1_impl/80_phase11_10065_prompt_self_awareness.md`.

### Decision
- GJC `aa7322f6` added a `<self-awareness>` block instructing the agent to clone the gajae-code
  repo before answering self/usage questions. **ADAPT**: JWC adopts the grounding *pattern* only;
  the content is JWC-authored — prefer the in-repo jwc source, otherwise clone the canonical JWC
  repo `https://github.com/lidge-jun/jawcode`, cite concrete `jwc`/`.jwc` paths. Identity strings
  are `jwc`/Jawcode; zero `gjc`/Gajae leak (the identity-sensitivity gate).

### Audit + review
- A-audit (Erdos gpt-5.4): PASS — GJC diff +5 lines, JWC repo URL, insertion point, identity match all confirmed; no snapshot test; agent-identity-leak.test will validate.
- B-review (Newton gpt-5.4): PASS 4 axes (identity safety, correctness, placement, test impact).

### Verification
- `bun test agent-identity-leak.test.ts` → 4 pass (forbids `\bGJC\b` prose).
- Added-line naming scan → zero gjc/gajae/Gajae/Yeachan.
- `packages/coding-agent` tsgo --noEmit EXIT 0; `git diff --check` clean.
- 3 pre-existing `system-prompt-templates.test.ts` failures confirmed unrelated (present in parent commit).

### JWC commit
- `982abba` feat(prompt): add jwc self-awareness source grounding

### Re-open conditions
- If JWC identity/repo URL changes, update the block.
- If a system-prompt snapshot test is later added that must capture this block.
