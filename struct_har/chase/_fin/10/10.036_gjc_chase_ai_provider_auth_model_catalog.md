# 10.036 — gjc chase: AI provider auth and model catalog

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P1**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: 0622573c, 998b7f50, 5a70598b, 16c0bdd0, df086ee3, 2c38c996, 774b1372, a12a751f, 137f9f78, 00a28d51, dd70181d, b632a4f8, b37a4256, 32578059, 7382c627, 5a3f3b90, a791d72a.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the AI provider auth and model catalog cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Provider credential resolution, opaque Codex tokens, forced tool_choice fallback, preset-first /model UX, GLM/Fable/Minimax catalog drift, credential import.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | 0622573c, 998b7f50, 5a70598b, 16c0bdd0, df086ee3, 2c38c996, 774b1372, a12a751f, 137f9f78, 00a28d51, dd70181d, b632a4f8, b37a4256, 32578059, 7382c627, 5a3f3b90, a791d72a |
| primary source areas | `packages/ai/src/**; packages/ai/test/**; packages/coding-agent/src/config/model-*; packages/coding-agent/src/setup/**` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- packages/ai/src/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- packages/ai/src/**
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [ ] Source facts checked against `a791d72a` or a newer explicitly recorded GJC head.
- [ ] JWC owner files listed before implementation.
- [ ] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md).
- [ ] Import/adapt/reject/split decision recorded for each sub-feature.
- [ ] Focused test or explicit manual evidence proves the chosen behavior.
- [ ] `git diff --check` passes for the patch.
- [ ] MOC/follow-index/gap inventory updated if priority or status changes.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## JWC Phase 7 Split Evidence — 2026-06-28

Phase 7 records this card as the provider auth/catalog owner inside the security/provider/network bundle.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/70_phase7_security_provider_network_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/71_phase7_provider_auth_catalog_split.md`

JWC owner surfaces:

- `packages/ai/src/auth-storage.ts`
- `packages/ai/src/auth-broker/**`
- `packages/ai/src/model-manager.ts`
- `packages/ai/src/provider-models/**`
- `packages/coding-agent/src/config/model-registry.ts`
- `packages/coding-agent/src/setup/provider-onboarding.ts`

Candidate follow-up slices:

- `10.036-A`: credential resolution negative tests, including project dotenv exclusion, runtime env precedence, and broker sentinel non-leak.
- `10.036-B`: model catalog/profile drift tests for JWC-supported providers only.
- `10.036-C`: credential import/onboarding hardening if a concrete JWC UX gap appears.

Status: still active. Phase 7 records split boundaries only; it does not close provider auth/model catalog done-gates.

## JWC Phase 14 Partial Evidence — 2026-06-28

Phase 14 implements part of `10.036-A` with JWC-native regression coverage only.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/140_phase14_security_auth_redaction_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/142_phase14_security_auth_redaction_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/143_phase14_security_auth_redaction_check.md`

JWC evidence:

- `packages/ai/test/auth-storage-project-dotenv.test.ts` proves project `.env` provider credentials do not become env fallback keys or auth-storage rows.
- `packages/ai/test/auth-storage-config-override.test.ts` proves runtime overrides beat env/config/storage and suppress stale OAuth account attribution.
- `packages/ai/test/auth-storage-broker-no-sentinel.test.ts` proves `AuthStorage.exportSnapshot()` replaces raw OAuth refresh tokens with `REMOTE_REFRESH_SENTINEL`.

Verification:

- `bun test packages/ai/test/auth-storage-config-override.test.ts packages/ai/test/auth-storage-broker-no-sentinel.test.ts packages/ai/test/auth-storage-project-dotenv.test.ts` — 12 pass / 0 fail.
- `cd packages/ai && bun run check:types` — exit 0.

Status: still active. This closes only the tested `10.036-A` regression subset; provider catalog/profile drift and onboarding/import hardening remain open.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | Translate public surface to `jwc`/`.jwc`; preserve upstream names only as source citations. |
| Decision C — test impact | Focused tests required unless this remains docs-only/track-only. |
| Decision D — docs impact | chase now; promote only stable implemented contracts. |
| Decision E — rollout impact | classify during implementation; assume local/runtime risk until proven otherwise. |
| Decision F — residual risk | open until implementation or explicit rejection evidence exists. |
| Decision G — reviewer needed | backend/security/architecture depending on touched surface. |
| Decision H — bundle policy | separate card; can join a PABCD bundle only with adjacent cards named here. |

## JWC Phase 31 Evidence — 10.036-B model catalog drift

Phase 31 closes the `10.036-B` catalog/profile drift gap for JWC-supported providers with one
JWC-native regression test. P-phase read-only investigation found the other two GJC catalog-drift
subjects already handled: MiniMax-M3 context cap is pinned (`packages/ai/test/issue-385-minimax-m3.test.ts`),
and GLM/zai catalog cost/context is generator-owned (`packages/ai/src/models.json`, no JWC overlay)
so pinning it would guard the build artifact, not JWC code. The one untested JWC-curated invariant
was Fable forced-tool_choice.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/310_phase31_fable_tool_choice_catalog_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/312_phase31_fable_tool_choice_catalog_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/313_phase31_fable_tool_choice_catalog_check.md`

JWC evidence:

- `packages/ai/test/fable-tool-choice-catalog.test.ts` pins that `getBundledModel("anthropic","claude-fable-5")` keeps forced tool use disabled (`deriveToolChoiceSupport(model.compat).support === "auto"`) and that `isClaudeForcedToolChoiceIncapableModelId` classifies fable/mythos incapable and normal Claude capable. Translates GJC #482 (`0622573c`) into JWC — preventing the Anthropic 400 "tool_choice forces tool use is not compatible with this model" from silently regressing via model-id rename, `models.json` regen, or predicate-regex drift.
- Owner surfaces guarded: `packages/ai/src/utils/tool-choice-capability.ts` (predicate), `packages/ai/src/models.ts` (`applyBundledCompatDefaults`), `packages/ai/src/model-thinking.ts` (`enrichModelThinking`).

Verification:

- `bun test packages/ai/test/fable-tool-choice-catalog.test.ts` — 2 pass / 0 fail / 5 expect().
- `cd packages/ai && bun run check:types` — exit 0.

Status: `10.036-B` closed. `10.036-A` closed (phase 14). `10.036-C` (credential import/onboarding hardening) remains conditional — card stays active until a `10.036-C` assessment, then final close.

## JWC Phase 32 Final Close — 2026-06-28

Phase 32 resolves the last conditional slice `10.036-C` and closes the whole card to `_fin/10/`.

Close decision: **adapt, closed**. `-A` (credential resolution) closed phase 14 (`2c60aed`), `-B`
(model catalog drift) closed phase 31 (`71240b6`), `-C` (credential import/onboarding) resolved as
**REJECT/DEFER**: JWC already ships an equivalent, tested on-demand local-credential-import path, and
the security-critical redaction slice is owned by the already-closed `10.047` (overlap rule).

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/320_phase32_ai_catalog_final_close_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/321_phase32_ai_catalog_final_close_audit.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/322_phase32_ai_catalog_final_close_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/323_phase32_ai_catalog_final_close_check.md`

### `10.036-C` REJECT/DEFER rationale (independently audited PASS)

| Axis | GJC | JWC | Verdict |
|---|---|---|---|
| Mechanism | batch discovery wizard `setup/credential-import.ts` (`32578059`) | on-demand `/login <provider> local` via `packages/ai/src/utils/oauth/local-token-detect.ts` (`detectGrokCliToken`/`detectClaudeCodeToken`/`detectCodexCliToken`) + `LocalTokenImportMode` (`off`\|`fallback`\|`only`), wired into `loginXai`/`loginOpenAICodex`/Anthropic flows | equivalent safety surface |
| Sources read | `~/.claude/.credentials.json` file, macOS Keychain, `~/.codex/auth.json`, env | macOS Keychain (`security find-generic-password -s 'Claude Code-credentials'`) / Linux `secret-tool`; `~/.codex/auth.json` requiring both `access_token`+`refresh_token`; xai grok-cli token. **JWC does NOT read the `~/.claude/.credentials.json` file, and a bare `OPENAI_API_KEY` (no `auth.json`) is invisible to the import path** (handled separately by the general OpenAI provider, not local-import). | parity-with-gaps |
| No-leak | #654 retrofit `sanitizedFailureReason` | detectors structurally non-leaking (silent try/catch → `null`) | JWC at-or-ahead |
| Redaction | in import core | owned by **10.047** (already `_fin`): cross-cutting policy + redaction/env guardrails, overlap rule `10.047:98`. (`provider-onboarding.ts` is in 10.036 auth/import ownership, not redaction.) | out of 10.036-C scope |
| Tests | GJC credential-import tests | `packages/ai/test/oauth-local-import.test.ts` — **4 tests, xai/grok only, 3 modes (`only`/`fallback`/`off`), no anthropic/codex coverage, no no-leak fixture** | xai path covered; see deferred gaps |

Conclusion: porting GJC's batch wizard is net-new UX gold-plating, not a hardening gap. `-C` is
rejected as "JWC-adequate". If the batch-wizard UX is ever wanted, it is a NEW card, not `-C`.

### Deferred gaps (monitored, not blocking)

- Claude local detection is Keychain (darwin) / `secret-tool` (Linux) only — **no file fallback, no Windows path** (`readFromSecureStorage` returns null).
- Codex local import reads `auth.json` tokens only — pure `OPENAI_API_KEY` without `auth.json` is not imported by the local path.
- `oauth-local-import.test.ts` covers only the xai path; anthropic/codex local-import and a no-leak fixture are untested.
- GJC batch discovery wizard UX is intentionally not ported (out of scope).

Residual: closed / monitored. Promote any of the deferred gaps to a NEW card only if a concrete JWC UX or hardening need appears.
