# 10.041 — gjc chase: TUI input render Windows psmux

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · **P2** · closed 260701
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: c558504e, 11e3e5f4, 19bba222, 38ac6dcd, a8d0d5f4, 932f5e07, 61dbe110, 5316e261.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the TUI input render Windows psmux cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Windows psmux boundary, keystroke echo priority, Other input @ autocomplete, active-monitor panel, deterministic redraw metrics, selector row identity, long-session render loop.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## JWC Phase 9 Split Evidence — 2026-06-28

Phase 9 devlog artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/90_phase9_tui_perf_research_docs_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/91_phase9_tui_input_render_split.md`

JWC owner files identified before implementation:

- `packages/coding-agent/src/modes/controllers/input-controller.ts`
- `packages/coding-agent/src/modes/controllers/selector-controller.ts`
- `packages/coding-agent/src/modes/components/composer-chrome.ts`
- `packages/coding-agent/src/modes/components/composer-footer.ts`
- `packages/coding-agent/src/modes/components/hook-input.ts`
- `packages/coding-agent/src/modes/components/jobs-overlay.ts`
- `packages/coding-agent/src/modes/jobs-observer.ts`
- `packages/coding-agent/src/modes/interactive-mode.ts`
- `packages/coding-agent/src/modes/controllers/event-controller.ts`
- `packages/coding-agent/src/tui/**`
- `packages/coding-agent/test/input-controller-escape.test.ts`
- `packages/coding-agent/test/input-controller-keybindings.test.ts`
- `packages/coding-agent/test/hook-selector-inline-input.test.ts`
- `packages/coding-agent/test/tui-tree-list-collapsed-lines.test.ts`
- `packages/coding-agent/test/jobs-observer.test.ts`
- `packages/coding-agent/test/event-controller-abort-render.test.ts`
- `packages/coding-agent/test/modes/controllers/event-controller-*.test.ts`

Overlap and defer boundaries:

- `struct_har/chase/_fin/20/20.006_omp_chase_tui_input_micro_fixes.md` already closed OMP TUI Esc and AST whitespace micro-fixes.
- The `20.006` `resetDisplay` defer remains out of scope unless a fresh JWC regression proves it belongs in `10.041-C`.
- Windows psmux/team behavior remains under Phase 8 `10.050` unless the proven gap is strictly TUI rendering.

Split result:

- `10.041-A`: input-controller and composer key handling beyond closed OMP micro-fixes.
- `10.041-B`: selector identity, hook-selector inline input, and active-monitor/jobs panel behavior after confirming JWC gaps.
- `10.041-C`: render-loop and terminal lifecycle hardening for long sessions and resize/preedit edge cases, excluding the `20.006` `resetDisplay` defer unless fresh regression evidence exists.

Status: closed 260701. Phase 9 remains historical split evidence; this closure implemented the surviving inline/list-slot `@` autocomplete gap and kept Windows/psmux/team behavior in its dedicated cards.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | c558504e, 11e3e5f4, 19bba222, 38ac6dcd, a8d0d5f4, 932f5e07, 61dbe110, 5316e261 |
| primary source areas | `packages/tui/**; packages/coding-agent/src/modes/**; packages/coding-agent/src/gjc-runtime/psmux*; packages/coding-agent/test/**/*tui*` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- packages/tui/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- packages/tui/**
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [x] Source facts checked against `a791d72a` (`c558504e`, `11e3e5f4`, `19bba222`, `38ac6dcd`, `a8d0d5f4`, `932f5e07`, `61dbe110`, `5316e261`).
- [x] JWC owner files listed before implementation (`packages/tui/src/components/editor.ts`, `packages/coding-agent/src/modes/components/hook-selector.ts`, `packages/coding-agent/src/modes/controllers/extension-ui-controller.ts`, HookSelector tests).
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md) — no new public upstream command/state names.
- [x] Import/adapt/reject/split decision recorded for each sub-feature — see Closure triage.
- [x] Focused tests prove the chosen behavior: inline/list-slot `@` autocomplete first Enter applies completion, second Enter submits; no-provider path submits literal `@`.
- [x] `bun run check:ts` and `git diff --check` pass in final C phase.
- [x] MOC/follow-index/gap inventory/cycle/_fin index updated for `_fin` closure.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **ADAPT** `38ac6dcd`; already-covered/defer/reject the rest per Closure triage. |
| Decision B — JWC naming impact | none; package namespace remains `@jawcode-dev/*`, public command/state naming unchanged. |
| Decision C — test impact | focused HookSelector inline/list-slot autocomplete regression tests plus existing input-render/jobs/model-selector guards. |
| Decision D — docs impact | chase closure only; no structure/AGENTS promotion. |
| Decision E — rollout impact | no migration; local TUI selector behavior only. |
| Decision F — residual risk | closed; Windows/psmux/team surfaces intentionally remain under `10.057`, `10.061`, and `10.050`. |
| Decision G — reviewer needed | Frontend/TUI plan audit and B-phase re-verifier PASS. |
| Decision H — bundle policy | single-card PABCD cycle in clean-follow goal `5f4d1e8e-672`. |

## Closure (260701) — ADAPT inline/list-slot autocomplete

Closed under goal `5f4d1e8e-672`, clean-follow 1-tier loop card `10.041`. JWC implemented the only surviving TUI selector gap from this cluster and recorded the rest as already covered, identity-rejected, or intentionally split to adjacent cards.

| GJC commit | feature | disposition | JWC evidence |
|---|---|---|---|
| `38ac6dcd` | inline Other input `@` autocomplete | **ADAPT** | `packages/tui/src/components/editor.ts` exposes `getAutocompleteProvider()`; `packages/coding-agent/src/modes/controllers/extension-ui-controller.ts` passes the main editor provider to custom-input selectors; `packages/coding-agent/src/modes/components/hook-selector.ts` wires it into inline/docked/list-slot editors and delegates Enter to `Editor` while autocomplete is open. |
| `19bba222` | input render priority | **already implemented** | JWC already has input-priority render scheduling and existing input-render latency/redteam tests. |
| `a8d0d5f4` | passive active monitor/jobs panel | **already adapted** | JWC uses `background-footer-panel*`, `JobsObserver.backgroundRows`, `app.background.expand`, and compact footer rows. |
| `932f5e07` | deterministic redraw metrics | **already covered** | JWC has metrics tests and existing redraw evidence. |
| `5316e261` | long-session render lifecycle | **already split/covered** | 10.060 imported render isolation; broader lifecycle remains out of this narrow selector patch absent a fresh regression. |
| `c558504e` | Windows/psmux tmux provider boundary | **DEFER/SPLIT** | Covered by `10.057` Windows hardening, `10.061` tmux/team Windows psmux titles, and older `10.050` session/tmux/team scope. |
| `61dbe110` | model selector row identity | **REJECT as direct patch** | JWC has a different two-pane/profile model selector design with current tests. |
| `11e3e5f4` | upstream status identity wording | **REJECT** | Identity-sensitive upstream branding does not apply to JWC. |

### Verification

- `bun test packages/coding-agent/test/hook-selector-inline-input.test.ts packages/coding-agent/test/hook-selector-list-slot.test.ts packages/tui/test/input-render-latency.test.ts packages/tui/test/input-render-redteam.test.ts packages/coding-agent/test/background-footer-panel-model.test.ts packages/coding-agent/test/jobs-observer.test.ts packages/coding-agent/test/model-selector-two-pane.test.ts` → 48 pass / 0 fail.
- `bun run check:ts` → exit 0 (Biome info only: deprecated `recommended` config field; all workspace `tsgo` checks exit 0).
- `git diff --check` → exit 0.
- Frontend B-phase re-verifier → PASS; no provider export, HookSelector wiring, Enter delegation, or test-coverage blockers.
- Plan/audit evidence: `devlog/_plan/260701_clean_follow_tier1_impl/20_phase2_10041_tui_input_render_windows_psmux.md`; A-phase Frontend/TUI audit failed twice on missing list-slot/provider import specificity, then passed after the plan was narrowed.

> Re-open only with new evidence that an inline/list-slot autocomplete regression exists, or continue the split Windows/psmux/team surfaces through `10.057`, `10.061`, or `10.050`.
