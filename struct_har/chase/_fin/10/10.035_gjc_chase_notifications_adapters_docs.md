# 10.035 — gjc chase: notification adapters, docs, and release surface

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P3**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: `d474c47d`, `fb07a1f2`, `23192d1c`, `8b94c54a`, `dffe42c6` #1043, `153f4082`, `a44bb69b`, `e75a6b09`.

## Why This Is A Gap

GJC documented notifications as a broader SDK and added chat adapter abstractions beyond Telegram (Discord/Slack). JWC currently has cli-jaw channel concepts, but not a JWC-native notification SDK/adapters doc surface.

This card is mostly documentation/product alignment after implementation slices land.

## Source Facts

| capability | upstream anchor |
|---|---|
| SDK docs | `docs/notifications-sdk.md` |
| Telegram onboarding | `docs/telegram-onboarding.md` |
| bot integration | `docs/bot-integration.md` |
| chat adapters | `packages/coding-agent/src/notifications/chat-adapters.ts` |
| presentation engine | `packages/coding-agent/src/notifications/engine.ts` |
| Discord/Slack adapter settings | `notifications.discord.*`, `notifications.slack.*` in config |
| tests | `notifications-chat-adapters.test.ts`, `bot-integration-docs.test.ts`, `lifecycle-notification-docs.test.ts` |

## Phase 1A Hardening Note

Checked against GJC `a791d72a` docs and notification commit history. The old broad release-doc label is now pinned to concrete onboarding, changelog, and threaded-mode documentation commits so this docs/adapters card is auditable without scanning the full release history.

## JWC Reconcile Notes

| issue | decision pressure |
|---|---|
| cli-jaw overlap | JWC is embedded into cli-jaw, which already has Telegram/Discord channel delivery. Avoid duplicating user-facing docs until product boundary is clear. |
| transport-agnostic SDK | Valuable as internal session endpoint contract even if only Telegram ships first. |
| Discord/Slack | GJC has adapter scaffolding; JWC should probably document as deferred unless a user asks for those transports. |
| release docs | Changelogs and install docs should not promise mobile notifications until implementation + smoke exists. |

## Suggested Split

1. Keep this chase card as the docs/product rollup.
2. After 10.028-10.034 implementation, write JWC docs for the supported subset only.
3. Explicitly reject/defer Discord/Slack if not shipped.
4. Add docs tests to prevent stale command names/secrets.

## Done Gate

- [ ] `docs/notifications-sdk.md` or JWC equivalent matches implemented JWC subset.
- [ ] `docs/telegram-onboarding.md` uses `jwc`, `.jwc`, `JWC_*`, and JWC settings.
- [ ] Bot integration docs distinguish JWC-native notifications from cli-jaw channel send.
- [ ] Changelog does not overclaim unsupported Discord/Slack or lifecycle features.
- [ ] Docs tests assert no raw token examples and no stale `gjc notify` public command in JWC docs.

## Verification

- Docs-focused tests equivalent to GJC docs tests.
- `rg -n "gjc notify|\\.gjc|GJC_NOTIFICATIONS" docs packages/coding-agent/src/defaults` after docs port; every hit must be source citation or intentional compatibility note.

## JWC Phase 3 Evidence — 2026-06-28

Implemented partial JWC-native docs evidence:

- `docs/notifications-sdk.md`
  - documents the currently supported JWC subset: `jwc notify status/setup`, `.jwc/state/notifications/*.json`, transport shell helpers, and pure remote answer safety helpers.
  - explicitly marks live Telegram polling, outbound sending, Discord/Slack adapters, session lifecycle commands, media/file transfer, and compiled daemon entrypoints as deferred.
- `docs/telegram-onboarding.md`
  - explains that Telegram support is a guarded foundation, not a production bot runtime.
  - uses JWC command/state names and placeholder token/chat examples.
- `packages/coding-agent/test/notifications-docs.test.ts`
  - asserts no public `gjc notify`, no `.gjc/state`, no raw-looking bot token examples, and no unsupported Discord/Slack/live Telegram runtime claims.

Verification snapshot:

```sh
bun test packages/coding-agent/test/notifications-docs.test.ts
# included in Phase 3 focused run; 3 docs tests pass
```

Status: still active. This evidence covers supported-subset docs and guard tests only. It does not close the card because broader bot integration docs, changelog/release positioning, Discord/Slack adapter decisions, and full notification runtime docs remain deferred.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **split** |
| Decision B — JWC naming impact | High: docs must be JWC-first. |
| Decision C — test impact | Docs tests + naming inventory. |
| Decision D — docs impact | This card is docs/product rollup. |
| Decision E — rollout impact | Avoid public promise until runtime slices are green. |
| Decision F — residual risk | Product confusion with cli-jaw messaging. |
| Decision G — reviewer needed | Docs + architecture. |
| Decision H — bundle policy | Close after implementation cards define actual supported subset. |

## JWC Final Close — 2026-06-28 (phase 56)

Closed after the Telegram implementation cards (10.028–10.034) all landed in `_fin/10`. This card
reconciled the docs/product surface to the **actually shipped + unit-tested** JWC subset and added
release positioning + bot-integration disambiguation.

Artifacts:
`devlog/_plan/260628_jwc_native_chase_implementation/560-563_phase56_notifications_docs_release_rollup_*`.

Docs (JWC):
- `docs/notifications-sdk.md` — rewritten with a Shipped/Deferred matrix (doubles as release
  positioning). Shipped: managed daemon logic, threaded surface, reply-bridge, lifecycle commands,
  ask keyboard/callback remote answers, connection-gated `telegram_send` egress — each qualified
  "unit-tested with injected fetch; live = operator token + running daemon". Discord/Slack deferred.
- `docs/telegram-onboarding.md` — media section rewritten to the shipped `telegram_send` tool +
  enforced security invariants; private-chat-only pairing + Threaded Mode fallback retained.
- `docs/bot-integration.md` (new) — distinguishes JWC-native notifications (session endpoint) from
  cli-jaw channel send (`POST /api/channel/send`).
- `packages/coding-agent/test/notifications-docs.test.ts` — 7 tests / 54 expect(): no hosted-bot
  overclaim, Discord/Slack still deferred, shipped `telegram_send` framing, no raw token, no stale
  `gjc notify` / `.gjc/state`, bot-integration distinction.

Done-gate (all met):

| Gate | Evidence |
|---|---|
| sdk doc matches implemented JWC subset | `notifications-sdk.md` Shipped/Deferred matrix |
| onboarding uses jwc/.jwc/JWC_* | `telegram-onboarding.md` + docs test |
| bot integration distinguishes JWC-native vs cli-jaw channel send | `bot-integration.md` + docs test |
| changelog/positioning does not overclaim Discord/Slack/lifecycle | Shipped/Deferred matrix + overclaim test |
| docs tests assert no raw token + no stale `gjc notify` | `notifications-docs.test.ts` |

Verification: `bun test test/notifications-docs.test.ts` → 7 pass / 0 fail / 54 expect();
`bun run check:types` exit 0; leak scan shows only intentional `GJC_NOTIFICATIONS*` compat notes.

Residual (product decision, NOT a code gap): Discord/Slack adapters (deferred until requested) and a
live production bot deployment (operator token + running daemon).

Status: **`10.035` closed → `_fin/10`. Telegram stack 10.028–10.035 fully closed.**
