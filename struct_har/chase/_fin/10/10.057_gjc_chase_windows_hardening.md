# 10.057 — gjc chase: Windows hardening (npm shims · team workers · psmux)

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · **P3** · closed 260701
> Split from [10.053](../../10.053_gjc_chase_local_provider_terminal_bell_windows.md) (cluster 4 of 5).
> Reviewed source: GJC `fa995807` (`upstream/dev`, v0.7.7) vs JWC worktree `ef28a98` (2026-06-29).
> Range: 55-commit cluster `a791d72a..fa995807`; this card isolates the Windows/platform sub-cluster.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC made several Windows-platform fixes: npm-managed update shims, safe team-worker launch, tolerance for update backup-cleanup failures, package-manager update-state verification, psmux tmux attach targets, and keeping the psmux pane alive in `--tmux` launch. Worktree launch failures are now preserved instead of swallowed. JWC must translate `gjc` shim/psmux naming and decide which platform fixes apply.

Behavioral surface:
1. Windows npm-managed update shims.
2. Safe team-worker launch on Windows.
3. Tolerate Windows update backup-cleanup failures.
4. Verify package-manager update state.
5. psmux tmux session attach target fix + keep pane alive in `--tmux` launch.
6. Preserve worktree launch failures (do not silently swallow).

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `fa995807` |
| npm update shims | `6eafcde7` Fix Windows npm-managed gjc update shims (#1274) |
| safe team workers | `7b5fd99b` fix(coding-agent): launch team workers safely on Windows (#1269) |
| backup cleanup tolerance | `56ffa73e` fix(coding-agent): tolerate Windows update backup cleanup failures (#1267) |
| update-state verify | `fa995807` fix(coding-agent): verify package-manager update state |
| psmux attach target | `365e1633` Fix psmux tmux session attach targets (#1276) |
| psmux keep-alive | `9618c21f` fix(coding-agent): keep psmux pane alive in gjc --tmux launch (#1234) |
| worktree launch failures | `97672024` fix(coding-agent): preserve worktree launch failures (#1265) |
| primary source areas | `packages/coding-agent/src/gjc-runtime/` (→ JWC `jwc-runtime`) |
| comparison range | `a791d72a..fa995807` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 6eafcde7 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 365e1633 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| npm shims | Import selectively: shim + backup tolerance relevant to JWC update path. |
| psmux/tmux | Naming needs `jwc` translation; check JWC psmux/session naming (overlaps 10.041, 10.050). |
| runtime path | Source `gjc-runtime/` maps to JWC `jwc-runtime/`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source facts checked against `fa995807` plus v0.7.8 follow-up boundaries.
- [x] JWC owner files listed before implementation (`cli/update-cli`, `jwc-runtime`, `commands/launch`, `coordinator-mcp`).
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md).
- [x] Import/adapt/reject decision recorded per fix.
- [x] Focused tests cover Windows/npm/package-manager/team/worktree/coordinator/tmux target behavior where feasible on macOS.
- [x] `bun run check:ts` passes.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | ADAPT/IMPORT selectively: npm shims, package-manager verification, cleanup tolerance, Windows-safe team command, worktree/coordinator error preservation; tiny tmux exact-target helper |
| Decision B — naming impact | public JWC preserved (`jwc`, `.jwc`, `jawcode`); internal `GJC_TEAM_*` and tmux profile names retained as compatibility protocol |
| Decision C — test impact | focused tests added/updated in update-cli, team-runtime, launch-worktree, coordinator MCP, launch-tmux, tmux-sessions |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | no migration; local runtime/platform robustness |
| Decision F — residual risk | v0.7.8 psmux detection/provider, Windows `--tmux`, and titles later closed in [10.061](./10.061_gjc_chase_tmux_team_windows_psmux_titles.md); broader tmux/session lifecycle remains 10.050 |
| Decision G — reviewer needed | Backend/platform audit PASS in A phase; B verifier required before final C |
| Decision H — bundle policy | single-card PABCD cycle; no 10.061 title/psmux provider work bundled |

## Completion Evidence — 260701

Implemented JWC-adapted Windows hardening:

- `packages/coding-agent/src/cli/update-cli.ts`
  - detects Windows npm `.cmd`/`.ps1` shims pointing to npm-managed `jawcode`;
  - routes those shims through `npm install -g jawcode@<version>`;
  - verifies package-manager update state even after nonzero install exits;
  - preserves verified binary replacement when backup cleanup fails, returning a cleanup warning.
- `packages/coding-agent/src/jwc-runtime/team-runtime.ts`
  - adds PowerShell quoting and platform-aware worker command resolution/building;
  - exports `buildWorkerCommand` for regression coverage;
  - keeps internal `GJC_TEAM_*` worker protocol names.
- `packages/coding-agent/src/jwc-runtime/launch-worktree.ts`
  - replaces bare `worktree_target_mismatch:<path>` with expected/existing/remediation diagnostics;
  - keeps `.jawcode-worktrees`.
- `packages/coding-agent/src/commands/launch.ts` and `packages/coding-agent/src/coordinator-mcp/server.ts`
  - persist launch-startup failures to coordinator runtime state when configured;
  - preserve `final_response` from session state;
  - terminalize active turns from errored runtime state even when `current_turn_id` is null.
- `packages/coding-agent/src/jwc-runtime/tmux-common.ts`, `launch-tmux.ts`, and `tmux-sessions.ts`
  - centralize exact-session target construction as a small helper only; no full psmux provider import.

Verification:

```sh
bun test packages/coding-agent/test/update-cli.test.ts packages/coding-agent/test/jwc-runtime/team-runtime.test.ts packages/coding-agent/test/jwc-runtime/launch-worktree.test.ts packages/coding-agent/test/coordinator-mcp-server.test.ts packages/coding-agent/test/jwc-runtime/launch-tmux.test.ts packages/coding-agent/test/jwc-runtime/tmux-sessions.test.ts
# 106 pass, 0 fail, 600 expect() calls

git diff --check
# exit 0

bun run check:ts
# exit 0
```

Deferred:

- full psmux detection/provider and Windows `--tmux` enablement (`9618c21f`) → v0.7.8 scope closed by [10.061](./10.061_gjc_chase_tmux_team_windows_psmux_titles.md); broader lifecycle remains 10.050;
- v0.7.8 psmux corrected follow-up (`1d65050d`) → [10.061](./10.061_gjc_chase_tmux_team_windows_psmux_titles.md) ✅;
- tmux workspace titles (`beec1af0`) → [10.061](./10.061_gjc_chase_tmux_team_windows_psmux_titles.md) ✅.
