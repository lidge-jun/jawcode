# 10.012 — gjc chase: perf(serialization) — clone equality 최적화 (#558)

> 상태: 구현 계획 ✅ / Sonnet 검증 ⬜ / 구현 ⬜
> upstream: gjc dev `f40f0d6` (Optimization Suite v3 Lane 3)
> MLB 가치: **70** (P2 deferred 영역의 upstream 직접 해결)
> 비용: 소 (clean patch, `packages/agent/src/append-only-context.ts` 단일 파일)
> **Sonnet 검증 ✅** — 충돌 없음, cherry-pick 안전. 카드 설명 정정(아래).

## 변경 내용 (Sonnet 검증으로 정정)

~~`cloneJson` 교체~~ → 실제 패치는 **두 가지**:

1. **StablePrefix identity cache**: 3개 private 필드(`#sourceSystemPrompt`·`#sourceTools`·
   `#sourceIntentTracing`)가 마지막 build()의 입력 참조를 추적. 3개 모두 동일 참조면
   `takeSnapshot` fingerprint 계산을 건너뜀 (fast path).
2. **`#computeDigest` 오버홀**: 부분-필드 FNV 해시 → 전체 메시지 `JSON.stringify` source-string
   비교 + `#computeDigestRange` 슬라이스 헬퍼 + `MessageDigest` 타입(`{ hash, source }`).

- `cloneJson`은 **변경 없음** (양쪽 동일 `JSON.parse(JSON.stringify(value))`).
- 벤치: `packages/agent/bench/append-only-context.ts` (279줄, 성능 측정 전용, parity assert 없음).

## 구현 계획

1. `f40f0d6` diff를 jawcode `packages/agent/src/append-only-context.ts`에 적용 — **jawcode에
   포크 수정 없음(byte-identical), 충돌 0**.
2. `packages/agent/bench/` 디렉터리 생성 + `append-only-context.ts` 벤치 가져옴.
3. `packages/agent/test/append-only-context.test.ts` 확장분(120줄 추가, 0 삭제) 가져옴.
4. `bun test packages/agent/test/append-only-context.test.ts` 통과.
5. `bunx tsgo --noEmit -p packages/agent/tsconfig.json` 통과.

## Sonnet 검증 결과

- **충돌**: 없음 — jawcode 파일이 upstream baseline과 byte-identical.
- **벤치**: parity assert 없음 (성능 측정 전용). 파일명 정정(`append-only-clone.ts` → `append-only-context.ts`).
- **`#syncedDigest` 타입 변경** (`number` → `MessageDigest`): private이라 외부 의존 없음, 안전.
- **dist-node bundle**: 소스 패치 후 rebuild 필요(표준 절차).
- **판정: cherry-pick 안전 ✅**.
