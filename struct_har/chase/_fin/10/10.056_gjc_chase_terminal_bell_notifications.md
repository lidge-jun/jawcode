# 10.056 — gjc chase: terminal bell notifications + completion hook — ADAPT

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin 260701 · **P3**
> Split from [10.053](../../10.053_gjc_chase_local_provider_terminal_bell_windows.md) (cluster 3 of 5).
> Reviewed source: GJC `fa995807` (`upstream/dev`, v0.7.7) vs JWC worktree `ef28a98` (2026-06-29).
> Range: 55-commit cluster `a791d72a..fa995807`; this card isolates the terminal-bell sub-cluster.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC added a terminal bell as a new local notification channel, plus a generic completion-notification command hook. This is a UX-tier gap: JWC may want the same "turn finished" signal, but settings must live in `.jwc` and access must be guarded so a missing config never throws.

Behavioral surface:
1. Terminal bell notification channel emitted on turn/task completion.
2. Settings guard so missing/partial bell settings do not crash access.
3. Generic completion-notification command hook (run a command when work finishes).

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `fa995807` |
| terminal bell | `06dca0e2` feat(coding-agent): add terminal bell notifications (#1278) |
| settings guard | `4842c5cb` fix(coding-agent): guard terminal bell settings access |
| completion hook | `2bdb0858` Add completion notification command hook (#1272) |
| primary source areas | `packages/coding-agent/src/` notifications/settings surface |
| comparison range | `a791d72a..fa995807` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 06dca0e2 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 2bdb0858 --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| naming | Bell prefs must live under `.jwc/config`; rename `gjc` settings namespace → `jwc`. |
| completion hook | Adapt: ensure the hook command surface uses JWC command naming. |
| overlap | Check alignment with existing JWC notifications work (10.028-10.035 _fin). |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source facts checked against `fa995807` and GJC anchors `06dca0e2`, `4842c5cb`, `2bdb0858`.
- [x] JWC owner files listed before implementation in `devlog/_plan/260701_clean_follow_tier1_impl/50_phase5_10056_terminal_bell_notifications.md`.
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md): new command env uses `JWC_NOTIFICATION_*`.
- [x] Import/adapt/reject decision recorded.
- [x] Focused test evidence covers bell fires, per-event toggle, missing settings safe, foreground hook, global-only command boundary.
- [x] `git diff --check` planned as final card gate before commit.

## Implementation Closure (2026-07-01)

Decision: **ADAPT**.

Implemented in JWC:

- `packages/coding-agent/src/config/settings-schema.ts`: added `notifications.terminalBell`, `notifications.bellOnComplete`, `notifications.bellOnApproval`, `notifications.bellOnAsk`, and `completion.notifyCommand`.
- `packages/coding-agent/src/config/settings.ts`: added `Settings.getGlobal()` so machine-local command hooks read only user/global config.
- `packages/coding-agent/src/modes/utils/terminal-bell.ts`: added guarded terminal BEL utility and ask/approval selector classification.
- `packages/coding-agent/src/modes/controllers/event-controller.ts`: completion success now can ring terminal bell and run a detached/unref'd global-only notify command with `JWC_NOTIFICATION_*` env; desktop terminal notification remains background-only.
- `packages/coding-agent/src/modes/controllers/extension-ui-controller.ts`: hook selector asks/approvals can ring the terminal bell before rendering.
- `packages/coding-agent/test/terminal-bell.test.ts` and `packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts`: covered opt-in default, per-event toggles, missing settings, completion bell, foreground hook, `JWC_NOTIFICATION_*`, and runtime-override non-activation.
- `schemas/config.schema.json`: regenerated from `SETTINGS_SCHEMA`.

Rejected/deferred:

- Public README/docs additions from GJC were not imported in this card; JWC has no current settings-guide owner for `completion.notify`.
- Upstream `GJC_NOTIFICATION_*` environment variables were not exposed for the new hook surface; JWC uses `JWC_NOTIFICATION_*`.

Verification evidence:

```bash
bun test packages/coding-agent/test/terminal-bell.test.ts
# 5 pass, 0 fail

bun test packages/coding-agent/test/modes/controllers/event-controller-abort-guard.test.ts
# 11 pass, 0 fail

bun scripts/generate-json-schemas.ts --check
# exit 0

git diff --check
# exit 0

bun run check:ts
# exit 0
```

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | ADAPT: local UX notification surface plus global-only command hook |
| Decision B — naming impact | new command env is `JWC_NOTIFICATION_*`; runtime settings remain under `.jwc` config |
| Decision C — test impact | new bell utility test + existing completion notification controller test extended |
| Decision D — docs impact | chase only; public docs deferred until a settings-guide owner exists |
| Decision E — rollout impact | no migration; defaults are opt-in/off for terminal bell and empty command hook |
| Decision F — residual risk | closed; command hook cannot be activated by project/runtime override |
| Decision G — reviewer needed | Backend B verifier before C due command-hook security boundary |
| Decision H — bundle policy | single-card PABCD commit |
