# 10.013 — gjc chase: perf(compaction) — token estimation 캐시 (#557)

> 상태: 구현 계획 ✅ / Sonnet 검증 ⬜ / 구현 ⬜
> upstream: gjc dev `78ed07c` (Optimization Suite v3 Lane 2)
> MLB 가치: **65** (대형 컨텍스트 라운드당 CPU 절감)
> 비용: 소~중 (`packages/agent/src/compaction/compaction.ts` + `openai.ts`)

## 변경 내용

- `estimateEntryTokens`: per-entry **WeakMap 캐시** + fingerprint(entry type·id·role·fragment 길이·prunedAt).
  같은 entry가 반복 추정 요청 시 재계산 없이 캐시 히트 → findCutPoint 등에서 O(entries²) → O(entries).
- `estimateEntriesTokens` export + `countCollectedMessageFragments` 헬퍼 추출.
- 벤치 포함 (`packages/agent/bench/compaction-estimate.ts`, 264줄).

## 구현 계획

1. `78ed07c`의 `packages/agent/src/compaction/compaction.ts` + `openai.ts` diff 적용.
2. 벤치 `packages/agent/bench/compaction-estimate.ts` 가져옴.
3. jawcode `compaction.ts` 포크 수정 확인 — **pruning.ts에 우리 수정 있음**(10_moc G8 후보 논의), compaction.ts 자체는 SOFT-EDIT만 예상.
4. `bun test packages/agent/test` 전체 통과.
5. `bunx tsgo --noEmit` 통과.

## 검증 포인트

- WeakMap 키가 `SessionEntry` 객체인데, jawcode에서 entry 객체를 재생성하는 경로(compaction 후 재구성 등)가 있으면 캐시가 항상 miss → 효과 없음. 해당 경로 확인.
- `estimateEntriesTokens` export가 jawcode에서 import되는 곳 확인 (신규 export → 기존 사용처 없으면 무해).
- fingerprint의 `prunedAt` 필드가 jawcode pruning 경로와 호환되는지.
