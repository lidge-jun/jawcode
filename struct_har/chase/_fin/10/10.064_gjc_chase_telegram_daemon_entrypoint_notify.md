# 10.064 — gjc chase: compiled Telegram daemon entrypoint · Windows Terminal bell workaround

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P2**
> Reviewed source: GJC `20c299eb` (`upstream/dev`, v0.7.8) vs JWC worktree `cfe71cd` (2026-07-01).
> Range: v0.7.8 delta `fa995807..20c299eb` (21 commits); this card isolates the notifications/daemon cluster (2 commits).
> Naming: upstream `gjc` notify CLI/daemon → JWC `jwc`; settings/state under `.jwc/` per [008](../../008_gjc_jwc_naming_contract.md).
> Neighbors: extends [10.030](./10.030_gjc_chase_telegram_managed_daemon.md) (Telegram managed daemon, _fin) and [10.056](./10.056_gjc_chase_terminal_bell_notifications.md) (terminal bell).

## Why This Is A Gap

GJC v0.7.8 fixed a compiled-binary Telegram daemon startup failure and documented a Windows Terminal bell workaround in two commits. The daemon fix adds a `daemon-paths.ts` resolver so the compiled entrypoint starts correctly (relevant to `ci-release-build-binaries.ts`), and the bell fix adds a settings-schema flag + README/docs note + config schema update for the Windows Terminal bell quirk. JWC's notifications stack (managed daemon shipped in 10.030, bell in 10.056) needs both reconciled against its own daemon entrypoint and bell settings.

Behavioral surface:
1. Compiled Telegram daemon entrypoint startup fix — `daemon-paths.ts` resolver, `telegram-daemon-cli.ts` wiring, binary build script.
2. Windows Terminal bell workaround — settings-schema flag, README/docs note, `schemas/config.schema.json` update.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `20c299eb` (v0.7.8) |
| compiled daemon entrypoint | `d00cbe2c` Fix compiled Telegram daemon entrypoint startup (#1299) — `src/notifications/daemon-paths.ts`, `src/notifications/telegram-daemon-cli.ts`, `src/notifications/telegram-daemon.ts`, `src/cli/notify-cli.ts`, `scripts/ci-release-build-binaries.ts`, `test/notifications-compiled-daemon-smoke.test.ts` |
| Windows Terminal bell | `308d4a20` fix(notifications): document Windows Terminal bell workaround — `src/config/settings-schema.ts`, `packages/coding-agent/README.md`, `schemas/config.schema.json`, `test/terminal-bell.test.ts` |
| primary source areas | `packages/coding-agent/src/notifications/`, `src/config/settings-schema.ts`, `schemas/config.schema.json`, `scripts/ci-release-build-binaries.ts` |
| comparison range | `fa995807..20c299eb` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show d00cbe2c --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 308d4a20
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| daemon entrypoint | JWC notifications dir present (`daemon-*.ts`, `telegram-*.ts`); the compiled-entrypoint path resolver is a binary-packaging correctness fix — verify JWC's compiled daemon startup + `daemon-paths` equivalent. Relevant to JWC release-binary build. |
| bell settings | JWC carries bell prefs (10.056); the Windows Terminal workaround adds a settings flag — ensure it lands under `.jwc/config` namespace, not `gjc`, and that `schemas/config.schema.json` stays in sync. |
| docs surface | README/docs note about the Windows Terminal bell quirk — adapt wording to JWC naming. |
| ci-release | `scripts/ci-release-build-binaries.ts` change ties the daemon entrypoint to the binary build; verify JWC's release-build script parity. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [x] Source facts checked against `20c299eb` or a newer recorded GJC head.
- [x] JWC owner files listed before implementation (`src/notifications/daemon-cli.ts`, `cli/notify-cli.ts`, `config/settings-schema.ts`, build scripts, `schemas/config.schema.json`).
- [x] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md) (bell settings under `.jwc`, public examples use `jwc`, hook env remains `JWC_NOTIFICATION_*`).
- [x] Import/adapt/reject decision recorded per sub-feature (2 items).
- [x] Focused test or manual evidence proves chosen behavior (compiled daemon smoke; terminal-bell test).
- [x] `git diff --check` passes.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | ADAPT: hidden JWC daemon entrypoint adapter + explicit compile entrypoint; Windows bell workaround is docs/settings description only |
| Decision B — naming impact | public examples use `jwc`; state stays under `.jwc`; hook env remains `JWC_NOTIFICATION_*`; no new public `gjc notify daemon` surface |
| Decision C — test impact | added `notifications-compiled-daemon-smoke.test.ts`; extended `terminal-bell.test.ts`; kept `notify-cli.test.ts` green |
| Decision D — docs impact | package README documents local completion hook + Windows Terminal PowerShell beep workaround |
| Decision E — rollout impact | no migration; hidden command only; compiled binary build scripts include daemon adapter entrypoint |
| Decision F — residual risk | live production bot deployment remains operator runtime from 10.030; this card only proves the source/compiled entrypoint and docs |
| Decision G — reviewer needed | Backend plan audit PASS after one FAIL-driven plan correction |
| Decision H — bundle policy | closed as one follow-up slice to 10.030 + 10.056 |

## JWC Final Close — 2026-07-01

Closed as a JWC-native adaptation rather than a 1:1 upstream port.

Implementation:

- `packages/coding-agent/src/notifications/daemon-cli.ts`
  - hidden `jwc notify daemon-internal` adapter for `runManagedDaemon`;
  - `--smoke`, `--agent-dir`, `--owner-id`, and `--max-ticks` parsing;
  - owner PID liveness guard before startup;
  - unconditional settings init, token/chat resolution through JWC config, no raw secret output.
- `packages/coding-agent/src/cli/notify-cli.ts` and `packages/coding-agent/src/commands/notify.ts`
  - hidden action branch wired without exposing `daemon-internal` in public action options.
- `packages/coding-agent/scripts/build-binary.ts` and `scripts/ci-release-build-binaries.ts`
  - explicit daemon adapter compile entrypoint for compiled binaries.
- `packages/coding-agent/src/config/settings-schema.ts`, `schemas/config.schema.json`, and `packages/coding-agent/README.md`
  - Windows Terminal BEL limitation documented through existing `notifications.terminalBell` and `completion.notifyCommand`.
- Tests:
  - `packages/coding-agent/test/notifications-compiled-daemon-smoke.test.ts`
  - `packages/coding-agent/test/terminal-bell.test.ts`

Verification snapshot:

```sh
bun test packages/coding-agent/test/notifications-compiled-daemon-smoke.test.ts
# 3 pass, 0 fail
bun test packages/coding-agent/test/terminal-bell.test.ts
# 6 pass, 0 fail
bun test packages/coding-agent/test/notify-cli.test.ts
# 6 pass, 0 fail
bun scripts/generate-json-schemas.ts --check
# exit 0
bun run check:ts
# exit 0
git diff --check
# exit 0
```

Status: **closed → `_fin/10`**.
