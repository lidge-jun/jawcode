# 10.078 — gjc chase: skill discovery + slash commands

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ **_fin** · P2
> Closed: 2026-07-17
> Reviewed source: GJC `db7938e1..b3b5b8a9` (`upstream/dev`, v0.7.11→v0.9.1) vs JWC worktree.
> Naming: upstream `gjc` → `jwc` per [008](./008_gjc_jwc_naming_contract.md).

## Chase Frame

This card tracks the GJC slice that moved skills from a mostly static invocation
surface into runtime discovery plus inline prompt invocation, then expanded the
slash-command palette around reasoning effort, quit/exit, clear, and changelog.
JWC already carries some of this surface; the chase job is not a blind import.
It is a parity audit with selective adaptation.

Verifier shape: spec-work done when every commit below has an owner decision,
JWC worktree evidence, and a follow-up bucket.

## Interview Decision (2026-07-09)

> **Classification**: import all for runtime discovery and inline invocation; adapt slash-command adjacencies where JWC already has local behavior.
> **Execution order**: skill phase in the sequence `model/provider → context/perf → session → skill → TUI → Telegram`.
> **Implementation approach**: Import both runtime skill discovery and inline skill invocation because the combined result is a major JWC skill UX upgrade; keep status open until implementation and focused tests land.
> **OPEN ASSUMPTIONS**: Slash commands adjacent to the skill UX (`/effort`, `/quit`, `/clear`, `/changelog`) still need per-command parity checks before code import.

## Commands Run

```bash
git -C devlog/_gjc_chase/gajae-code log --oneline --stat db7938e1..b3b5b8a9 -- packages/coding-agent/src/slash-commands packages/coding-agent/src/skill* packages/coding-agent/src/modes/components/skill*
git -C devlog/_gjc_chase/gajae-code show --stat d3bc0f89 9912d7fd fd6817fa 4c3f401a c55783d3 003f39f5
rg -n "runtimeSkillDiscovery|inlineSkill|SlashCommand|/effort|/quit|/clear|/changelog" packages/coding-agent/src/slash-commands/ packages/coding-agent/src/skill* --type ts 2>/dev/null | head -30
```

## Upstream Commit Anchors

| commit | upstream fact | file anchors from diff |
|---|---|---|
| `d3bc0f89` | Runtime skill discovery tool and prompt. | `packages/coding-agent/src/extensibility/runtime-skill-discovery.ts`; `packages/coding-agent/src/tools/skill-discovery.ts`; `packages/coding-agent/src/prompts/tools/skill-discovery.md`; `packages/coding-agent/src/system-prompt.ts`; `packages/coding-agent/test/tools/skill-discovery.test.ts` |
| `9912d7fd` | Inline skill invocation from prompt/editor/ACP. | `packages/coding-agent/src/extensibility/skills.ts`; `packages/coding-agent/src/modes/controllers/input-controller.ts`; `packages/coding-agent/src/modes/prompt-action-autocomplete.ts`; `packages/coding-agent/src/modes/acp/acp-agent.ts`; `packages/tui/src/components/editor.ts` |
| `fd6817fa` | Skill tool default-registered instead of discoverable. | `packages/coding-agent/src/tools/skill.ts` |
| `4c3f401a` | `/effort` command and selector. | `packages/coding-agent/src/slash-commands/builtin-registry.ts`; `packages/coding-agent/src/modes/components/thinking-selector.ts`; `packages/coding-agent/src/modes/controllers/selector-controller.ts`; `packages/coding-agent/src/modes/interactive-mode.ts`; `packages/coding-agent/test/acp-builtins.test.ts` |
| `c55783d3` | Deep-interview bounded trace pre-skill option. | `packages/coding-agent/src/commands/deep-interview.ts`; `packages/coding-agent/src/defaults/gjc/skills/deep-interview/SKILL.md`; `packages/coding-agent/src/gjc-runtime/deep-interview-runtime.ts`; `packages/coding-agent/test/gjc-runtime/deep-interview-runtime.test.ts` |
| `003f39f5` | `/clear` slash command preserving session. | `packages/coding-agent/src/modes/controllers/command-controller.ts`; `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/session/session-manager.ts`; `packages/coding-agent/src/slash-commands/builtin-registry.ts`; `packages/coding-agent/test/slash-command-builtin-registry.test.ts` |

## Additional Range Hits

| commit | range stat fact | chase meaning |
|---|---|---|
| `59f4c924` | Adds `/quit` alias for `/exit` in `builtin-registry.ts`. | JWC has `/exit`; ACP filtering references `/quit`, but registry evidence did not show a `quit` entry in the narrow scan. Verify before adopting. |
| `43b42d4f` | Restores `/changelog` in `builtin-registry.ts`. | JWC has changelog rendering in command controller; registry routing still needs exact parity check. |
| `7fb3e9a1`, `2455cc11`, `e035d512` | Model slash command assignment and UI refresh churn. | Adjacent to `/effort`; do not mix with skill-discovery import unless tests force it. |
| `2d95f753`, `a20c15a5`, `0e072d3a` | Deep-interview mutation guard changes. | Treat as separate jaw-interview parity thread unless trace pre-skill touches current JWC workflow loading. |

## JWC Worktree Verification

| local anchor | current JWC state |
|---|---|
| `packages/coding-agent/src/config/skill-settings-defaults.ts:1` | JWC already has `SkillDiscoverySettings` with `enabled`, command toggles, provider toggles, custom directories, ignore/include lists. |
| `packages/coding-agent/src/hooks/native-skill-hook.ts:45` | JWC builds default effective skill config and merges raw `skills.*` config from user/project config files. |
| `packages/coding-agent/src/hooks/native-skill-hook.ts:103` | JWC resolves config paths from `~/.jwc/agent/config.yml` and project `.jwc/config.yml`. |
| `packages/coding-agent/src/slash-commands/builtin-registry.ts:38` | JWC has `/effort` argument parsing with Codex vocabulary aliases `none→off` and `minimal→min`. |
| `packages/coding-agent/src/slash-commands/builtin-registry.ts:950` | JWC registers `/effort` with text/ACP metadata and TUI handling. |
| `packages/coding-agent/src/modes/controllers/selector-controller.ts:256` | JWC has an interactive reasoning-effort selector. |
| `packages/coding-agent/src/slash-commands/acp-builtins.ts:12` | ACP command export filters TUI-only commands and explicitly mentions `/quit`. |
| `packages/coding-agent/src/modes/controllers/command-controller.ts:540` | JWC can render recent/full changelog entries. |
| `packages/coding-agent/src/slash-commands/builtin-registry.ts:1799` | The narrow registry scan hit `clear` as a subcommand/semantic label, not proof of GJC `/clear` parity. |
| `packages/coding-agent/src/slash-commands/builtin-registry.ts:1927` | JWC has `/exit`; `/quit` registry alias still needs direct owner confirmation. |

## Owner Files

| slice | JWC owner files |
|---|---|
| Skill discovery config and config merge | `packages/coding-agent/src/config/skill-settings-defaults.ts`; `packages/coding-agent/src/config/settings-schema.ts`; `packages/coding-agent/src/hooks/native-skill-hook.ts` |
| Runtime skill discovery tool | `packages/coding-agent/src/tools/skill-discovery.ts`; `packages/coding-agent/src/tools/index.ts`; `packages/coding-agent/src/extensibility/runtime-skill-discovery.ts` if imported |
| Skill invocation and autocomplete | `packages/coding-agent/src/extensibility/skills.ts`; `packages/coding-agent/src/modes/controllers/input-controller.ts`; `packages/coding-agent/src/modes/prompt-action-autocomplete.ts`; `packages/tui/src/components/editor.ts` |
| Slash command registry | `packages/coding-agent/src/slash-commands/builtin-registry.ts`; `packages/coding-agent/src/slash-commands/acp-builtins.ts`; `packages/coding-agent/src/slash-commands/types.ts` |
| Reasoning effort UI | `packages/coding-agent/src/modes/controllers/selector-controller.ts`; `packages/coding-agent/src/modes/components/thinking-selector.ts`; `packages/coding-agent/src/modes/interactive-mode.ts`; `packages/coding-agent/src/modes/types.ts` |
| Session-preserving clear | `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/session/session-manager.ts`; `packages/coding-agent/src/modes/controllers/command-controller.ts` |
| Jaw-interview trace analogue | `packages/coding-agent/src/defaults/jwc/skills/jaw-interview/SKILL.md`; `packages/coding-agent/src/jwc-runtime/*interview*`; current GJC files are `deep-interview` specific |

## Concrete Decisions Per Slice

| decision | status | rationale |
|---|---|---|
| Runtime skill discovery | **Import**. | User decision: import runtime discovery as part of a broad JWC skill UX upgrade. Preserve JWC `.jwc` boundaries and existing config precedence while importing. |
| Skill discovery project boundary | Must preserve JWC `.jwc` boundary. | Upstream fix mentions runtime skill project boundary; local config resolution already uses `.jwc` paths. Any import must preserve JWC naming and project/user precedence. |
| Inline skill invocation | **Import**. | User decision: import inline invocation together with runtime discovery. Implementation must cover editor, prompt, ACP, and autocomplete tests. |
| Skill tool default registration | Likely import if absent. | The GJC change is tiny but behaviorally important: the skill tool should be registered by default, not hidden behind discovery metadata. |
| `/effort` | Already substantially present. | Local registry and selector anchors show JWC implemented the command and dropdown; chase action is test parity, not feature import. |
| `/quit` | Small parity candidate. | Local ACP docs mention `/quit`, but registry search only proved `/exit`; add alias only after confirming no hidden registration path. |
| `/clear` preserving session | Needs semantic comparison. | Local search did not prove the same session-preserving `/clear` command; import only if JWC clear currently wipes artifacts/session identity differently. |
| `/changelog` | Controller exists; routing check needed. | Rendering owner exists, but slash registration must be confirmed before marking parity. |
| Deep-interview trace pre-skill | Do not import directly. | JWC public workflow is `jaw-interview`; translate the idea only if jaw-interview needs a bounded trace pre-step. |
| ACP/startup/profile side effects | Defer. | Inline skill and slash command ACP coverage must be isolated from startup profile registry changes outside this card. |

## Tests To Chase

| upstream test anchor | JWC equivalent target |
|---|---|
| `packages/coding-agent/test/tools/skill-discovery.test.ts` | Runtime discovery boundary, ignore/include list, project/user search order. |
| `packages/coding-agent/test/prompt-action-skill-autocomplete.test.ts` | Inline skill autocomplete and mid-prompt replacement. |
| `packages/coding-agent/test/input-controller-skill-queue.test.ts` | TUI input controller queues inline skill activations correctly. |
| `packages/coding-agent/test/acp-builtins.test.ts` | `/effort`, `/quit`, `/clear`, `/changelog` ACP exposure and filtering. |
| `packages/coding-agent/test/slash-command-builtin-registry.test.ts` | Registry-level alias and command behavior. |
| `packages/coding-agent/test/gjc-runtime/deep-interview-runtime.test.ts` | Only translate if jaw-interview trace parity is accepted. |

## Residual Risks

- Skill discovery can accidentally expose hidden tool-help skills; JWC's AGENTS contract says `browse` and `search` are hidden utilities.
- Inline skill invocation can change prompt parsing order and collide with slash-command autocomplete.
- `/clear` can become destructive if it clears session identity or artifacts instead of only display/memory surfaces.
- `/effort` already exists locally; re-importing upstream code could regress JWC's custom thinking vocabulary.
- GJC `deep-interview` names must not leak into JWC public workflow docs.

## Follow-Up Bundle

1. Audit local skill discovery implementation against `d3bc0f89` and mark missing files explicitly.
2. Add or confirm `/quit`, `/clear`, `/changelog` registry behavior in one slash-command parity patch.
3. Run focused tests for slash commands and skill discovery after any import.
4. Keep jaw-interview trace work as a separate card unless it blocks runtime skill parity.

## Jawdev chase expansion

This chase card is an expansion artifact, not an implementation plan. Promote only
stable decisions into `structure/` or `AGENTS.md`; keep transient upstream commit
mapping here. Next owner should update this card after direct diff comparison of
GJC anchors above against current JWC owner files, then split implementation into
skill-discovery, inline-skill, slash-command, and jaw-interview trace slices.

## Closure — 2026-07-17

Verdict: **adapt — partial implementation, tracked residual**.

| slot | final decision |
|---|---|
| Decision A — source behavior classification | **adapt** — the runtime discovery scanner exists, while local inline invocation and selected slash-command behavior remain JWC-owned. |
| Decision B — JWC naming impact | Preserve `.jwc` project/user boundaries and the hidden `browse` and `search` tool-help skills. |
| Decision C — test impact | Add discovery-tool, prompt-registration, hidden-skill, and inline-invocation integration tests together; a scanner-only unit test is insufficient. |
| Decision D — docs impact | Chase card and MOC only. |
| Decision E — rollout impact | User-visible skill/tool exposure changes require a staged opt-in/default decision, not a silent startup change. |
| Decision F — residual risk | `discoverRuntimeSkills()` and `findRuntimeSkillByName()` have no runtime caller. Wire a dedicated discovery tool plus system-prompt registration and policy-aware inline invocation, then prove precedence, hidden-skill exclusion, and project/user boundaries. That cross-surface integration exceeds the sub-50-line patch limit. |
