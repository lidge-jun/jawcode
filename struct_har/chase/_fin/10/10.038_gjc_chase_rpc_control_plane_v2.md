# 10.038 — gjc chase: RPC control plane v2

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · **P1**
> Reviewed source: GJC `a791d72a` (`upstream/dev`) vs JWC worktree `main` (2026-06-28).
> Upstream cluster: 92ddf3fb, 679ab5ba, ff0e7eb4 with RPC hardening commits 5d2a1809, 45f70618, 251ea268.
> Naming: upstream `gjc`/`.gjc` → JWC `jwc`/`.jwc` per [008](../../008_gjc_jwc_naming_contract.md).

## Why This Is A Gap

GJC's `upstream/dev` advanced by hundreds of commits after the earlier chase baseline. This card isolates the RPC control plane v2 cluster so JWC can decide what to import, adapt, reject, or split without treating the whole upstream history as one unreviewable blob.

Current source summary: Session registry, persistent UDS server, unknown-command id preservation, token-cost fail-closed metrics, --listen socket guard, duplicate refusal cleanup.

This is not a cherry-pick instruction. The source behavior must be translated through JWC naming, workflow, security, and TUI constraints before implementation.

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `a791d72a` |
| source commits | 92ddf3fb, 679ab5ba, ff0e7eb4 with RPC hardening commits 5d2a1809, 45f70618, 251ea268 |
| primary source areas | `packages/coding-agent/src/modes/rpc/**; python/gjc-rpc/**; packages/coding-agent/test/**/*rpc*` |
| comparison range | `498d86bb..a791d72a` |

Suggested local evidence commands:

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code log --oneline --reverse 498d86bb..HEAD -- packages/coding-agent/src/modes/rpc/**
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code diff --stat 498d86bb..HEAD -- packages/coding-agent/src/modes/rpc/**
```

## Phase 1B Hardening Note

RPC control-plane bucket checked at GJC `a791d72a`; existing registry/UDS/fail-closed anchors were preserved and the broad issue-series label was replaced with concrete stdio/control-plane and fast-lane/file-lock hardening commits.

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| public names | Keep user-facing docs/commands as `jwc`, `.jwc`, and `@jawcode-dev/*`. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence after ff; do not patch inside it. |
| implementation owner | Patch JWC product files only after checking whether prior _fin cards already closed a sub-slice. |
| tests | Prefer focused tests around externally observable behavior, not placeholder parity assertions. |
| security | Any auth, token, socket, local-server, file-egress, or process-control surface needs security review before close. |
| docs | Chase docs may describe upstream names as citations; product docs must use JWC names. |

## Suggested Split

1. Inventory the exact upstream files for this cluster and mark already-closed JWC equivalents.
2. Choose import/adapt/reject/split per sub-feature, not per commit count.
3. Land the smallest JWC-facing behavior with a focused regression test.
4. Promote stable product contracts to `structure/` or public docs only after behavior exists.
5. Move this card to `_fin/10/` only with source commit, JWC commit, and verification evidence.

## Done Gate

- [ ] Source facts checked against `a791d72a` or a newer explicitly recorded GJC head.
- [ ] JWC owner files listed before implementation.
- [ ] Naming translation checked against [008](../../008_gjc_jwc_naming_contract.md).
- [ ] Import/adapt/reject/split decision recorded for each sub-feature.
- [ ] Focused test or explicit manual evidence proves the chosen behavior.
- [ ] `git diff --check` passes for the patch.
- [ ] MOC/follow-index/gap inventory updated if priority or status changes.

## Verification

- Start with the source commands above and add focused package tests for the chosen implementation surface.
- For docs-only closure, verify link integrity and that no stale `gjc` command appears in JWC-facing instructions.
- For runtime closure, run the nearest package tests plus `git diff --check`.

## JWC Phase 7 Split Evidence — 2026-06-28

Phase 7 records this card as the RPC control-plane owner inside the security/provider/network bundle.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/70_phase7_security_provider_network_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/72_phase7_rpc_control_plane_split.md`

JWC owner surfaces:

- `packages/coding-agent/src/modes/rpc/rpc-mode.ts`
- `packages/coding-agent/src/modes/rpc/rpc-client.ts`
- `packages/coding-agent/src/modes/rpc/rpc-types.ts`
- `python/jwc-rpc/src/jwc_rpc/**`
- `packages/coding-agent/test/rpc*.test.ts`
- `packages/coding-agent/test/harness-control-plane/**`

Candidate follow-up slices:

- `10.038-A`: unknown-command id preservation and fail-closed token-cost metrics tests.
- `10.038-B`: UDS/listen work only beyond the closed `struct_har/chase/_fin/10/10.018_gjc_chase_rpc_registry_uds.md` baseline; likely duplicate-refusal cleanup or explicit no-op evidence.
- `10.038-C`: Python client registry/listen parity tests if JS/Python drift is found.
- `10.038-D`: RPC fast-lane scheduler/read-snapshot evidence for safe read commands bypassing blocked ordered work without reordering mutating commands.

Status: still active. Phase 7 records split boundaries only; it does not close RPC control-plane done-gates.

## JWC Phase 20 Partial Evidence — 2026-06-28

Phase 20 implements the unknown-command id preservation subset of `10.038-A`.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/200_phase20_rpc_unknown_command_id_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/201_phase20_rpc_unknown_command_id_audit.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/202_phase20_rpc_unknown_command_id_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/203_phase20_rpc_unknown_command_id_check.md`

JWC evidence:

- `packages/coding-agent/test/rpc-get-state-payload.test.ts` proves `dispatchRpcCommand()` preserves the request id when rejecting an unknown command.
- `packages/coding-agent/src/modes/shared/agent-wire/command-dispatch.ts` now returns `rpcError(id, ...)` in the default unknown-command branch.

Verification:

- Direct dispatcher regression: `bun test packages/coding-agent/test/rpc-get-state-payload.test.ts` — 4 pass / 0 fail.
- Package typecheck and scoped biome/diff checks passed.
- Broader subprocess RPC stdio tests were attempted but blocked by local `Bun runtime must be >= 1.3.14 (found v1.3.11)` before exercising this patch.

Status: still active. This closes only the unknown-command id preservation subset of `10.038-A`; fail-closed token-cost metrics, UDS/listen beyond closed baseline, and Python client parity remain open.

## JWC Phase 27 Partial Evidence — 2026-06-28

Phase 27 implements and verifies the new `10.038-D` RPC fast-lane scheduler subset.

Upstream anchor:

- GJC `251ea268 fix(rpc,file-lock): safe fast-lane allowlist + GC owner-token TOCTOU guard`

JWC decision:

- JWC already had the scheduler and fast-lane allowlists in `rpc-mode.ts`; this phase adds focused contract tests and fixes the one drift found during plan audit.
- `10.038-B` remains reserved for UDS/listen. Fast-lane evidence is tracked separately as `10.038-D`.
- File-lock GC TOCTOU from the same upstream commit is not part of this RPC card.

Artifacts:

- `devlog/_plan/260628_jwc_native_chase_implementation/270_phase27_rpc_fastlane_evidence_plan.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/271_phase27_rpc_fastlane_evidence_audit.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/272_phase27_rpc_fastlane_evidence_build.md`
- `devlog/_plan/260628_jwc_native_chase_implementation/273_phase27_rpc_fastlane_evidence_check.md`

JWC evidence:

- `packages/coding-agent/src/modes/rpc/rpc-mode.ts` exposes `RPC_CANCELLATION_COMMANDS`, `RPC_SAFE_READ_CONTROL_COMMANDS`, `isFastLaneRpcCommand()`, and `createRpcCommandScheduler()`.
- `packages/coding-agent/src/modes/shared/agent-wire/command-dispatch.ts` now returns a defensive array snapshot for `get_messages`, matching its fast-lane read-only contract.
- `packages/coding-agent/test/rpc-fastlane.test.ts` proves cancellation/read commands are fast-laned, mutating/async commands stay ordered, fast-lane reads bypass a pending ordered command, ordered commands preserve arrival order, and `get_messages` does not leak later `session.messages` mutations into the response.

Verification:

- `bun test packages/coding-agent/test/rpc-fastlane.test.ts packages/coding-agent/test/rpc-get-state-payload.test.ts` -> 9 pass, 0 fail, 63 expect() calls.
- `bun test packages/coding-agent/test/rpc-fastlane.test.ts packages/coding-agent/test/rpc-listen-socket-guard.test.ts packages/coding-agent/test/rpc-get-state-payload.test.ts` -> new fast-lane tests, get-state tests, and the in-process socket guard pass; the existing subprocess socket guard is blocked locally by `Bun runtime must be >= 1.3.14 (found v1.3.11)` before exercising RPC behavior.
- `cd packages/coding-agent && bun run check:types` -> pass.
- scoped `git diff --check` -> pass.

Status: still active. This closes `10.038-D`; fail-closed token-cost metrics from `10.038-A`, UDS/listen beyond closed baseline from `10.038-B`, and Python client parity from `10.038-C` remain open.

## Decision Slots

| slot | value |
|---|---|
| Decision A — source behavior classification | **adapt** |
| Decision B — JWC naming impact | Translate public surface to `jwc`/`.jwc`; preserve upstream names only as source citations. |
| Decision C — test impact | Focused tests required unless this remains docs-only/track-only. |
| Decision D — docs impact | chase now; promote only stable implemented contracts. |
| Decision E — rollout impact | classify during implementation; assume local/runtime risk until proven otherwise. |
| Decision F — residual risk | open until implementation or explicit rejection evidence exists. |
| Decision G — reviewer needed | backend/security/architecture depending on touched surface. |
| Decision H — bundle policy | separate card; can join a PABCD bundle only with adjacent cards named here. |

## JWC Final Close — 2026-06-28 (phases 7, 20, 27, 58)

Closed: JWC-native RPC control plane v2, reconciled against GJC `a791d72a` as read-only evidence.
The session registry, persistent UDS server, unknown-command id preservation, token-cost fail-closed
budget metrics, `--listen` socket guard, and safe-read fast-lane are implemented in JWC product files
and regression-guarded.

Sub-slice resolution:

| sub-slice | resolution | evidence |
|---|---|---|
| 10.038-A unknown-command id | closed (phase 20) | `command-dispatch.ts` `rpcError(id,...)`, `test/rpc-get-state-payload.test.ts` |
| 10.038-A token-cost fail-closed metrics | closed | `unattended-run-controller.ts:189-191,307-315`, `test/unattended-budget-redteam.test.ts` (unsupported_budget_metric refusal + cost breach) |
| 10.038-B UDS/listen duplicate-refusal + stale cleanup | closed | `rpc-mode.ts:699-701` (refuse live, rm stale), `test/rpc-listen-startup-guard.test.ts` (in-process), `test/rpc-listen-socket-guard.test.ts` + `test/rpc-uds-listen.test.ts` (subprocess integration) |
| 10.038-C Python client registry/listen parity | reject (no drift) | `python/jwc-rpc/tests/{test_registry,test_client_uds,test_client,test_protocol}.py` |
| 10.038-D fast-lane scheduler | closed (phase 27) | `rpc-mode.ts` scheduler/allowlists, `test/rpc-fastlane.test.ts` |

Devlog: `70,72_phase7_*`, `200-203_phase20_*`, `270-273_phase27_*`, `580-583_phase58_rpc_control_plane_closure_*`.

Verification: `test/rpc-listen-startup-guard.test.ts` → 3 pass; adjacent rpc batch 15 pass; check:types
exit 0; `git diff --check` clean. JWC commit `c6eecc6`.

Residual (monitored, NOT blocking): the subprocess clobber-refusal + uds-listen integration tests are
env-blocked on this machine by `Bun >= 1.3.14 (found v1.3.11)` and will run after a Bun upgrade;
behavior + source + an in-process guard are already present.

Status: **`10.038` closed → `_fin/10`.**
