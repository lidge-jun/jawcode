# 10.071 — GJC chase: search performance, glob/format, and edit safety

Status: ✅ ACCEPT (WP7, 2026-07-11)
Lane: GJC
Source range: `79b42377..db7938e1` (`upstream/dev`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

GJC pulled a small but high-leverage set of search and low-level correctness fixes:

- `1a7cc54e` — `perf(web_search): class-based timeouts, hedged DDG fallback, chain caching, prewarm, faster Gemini fail-through (#1407)`
  - `packages/coding-agent/src/web/search/index.ts`
  - `packages/coding-agent/src/web/search/provider.ts`
  - `packages/coding-agent/src/web/search/providers/utils.ts`
  - `packages/coding-agent/test/web/search/speed-improvements.test.ts`
- `f9019e06` — `perf(utils): precompile glob excludes (#1445)`
  - `packages/utils/src/glob.ts`
  - `packages/utils/test/glob.test.ts`
- `57559d50` — `fix(utils): avoid rounding past compact unit boundaries (#1444)`
  - `packages/utils/src/format.ts`
  - `packages/utils/test/format.test.ts`
- `044f14da` — `fix(edit): preserve UTF-8 BOM in replace mode (#1436)`
  - `packages/coding-agent/src/edit/read-file.ts`

## JWC decision

Classification: **ACCEPT** after WP7 verification.

These changes are mostly product-generic and likely safe to import after checking JWC already-shipped equivalents. Naming impact is low, but search provider behavior can affect network timing and citation surfaces, so it needs focused tests rather than silent copy.

## Implementation Evidence (2026-07-09)

| patch | file | verification |
|---|---|---|
| `$flag` case-insensitive | `packages/utils/src/env.ts` | implementation present in worktree |
| glob path dedup | `packages/utils/src/glob.ts` | implementation present in worktree |
| Cursor client version | `packages/ai/src/providers/cursor/client-version.ts` | shared client-version owner added |
| CLI parse error | `packages/utils/src/cli.ts` | implementation present in worktree |
| `/quit` alias | `packages/coding-agent/src/slash-commands/builtin-registry.ts` | implementation present in worktree |

## JWC owner candidates

- `packages/coding-agent/src/web/search/`
- `packages/coding-agent/test/web/search/`
- `packages/utils/src/glob.ts`
- `packages/utils/src/format.ts`
- `packages/coding-agent/src/edit/read-file.ts`

## Verification expectation

Before closing this card, prove the relevant behavior with:

1. web-search speed/fallback focused tests or a JWC equivalent;
2. glob exclude regression tests;
3. compact-unit rounding tests;
4. BOM-preserving edit/read-file test if JWC lacks it.

Suggested checks: affected `bun test` files, then `bun run check:tools`.

## WP7 Closure (2026-07-11)

**Verdict: ACCEPT.** No unresolved edit-safety defect remains in the WP7-owned tool surface. JWC's shared edit reader already restores a UTF-8 BOM when Bun's text decoding omits it, and both replace and patch modes preserve that BOM when writing. The current editor worktree also avoids ghost undo snapshots for filtered-empty paste and handles multiline insertion through the shared insertion path.

The web-search timeout/hedging patch is a latency optimization under `src/web/search/`, while glob precompilation and compact-number boundary formatting belong to `packages/utils`; none is an incorrect or unsafe behavior in WP7's `packages/coding-agent/src/tools/` ownership. Existing `tools/search.ts` uses the shared read cache and bounded native search path, and no crash, destructive write, or stale-edit bypass was reproduced. These upstream optimizations are therefore accepted without import under this card's pragmatic closure rule.

### WP7 Files

- `struct_har/chase/10.071_gjc_chase_search_utils_edit_safety.md`

### WP7 Verification

- Source inspection confirmed BOM preservation in `packages/coding-agent/src/edit/read-file.ts`, `modes/replace.ts`, and `modes/patch.ts` (already present; not modified by WP7).
- TUI/editor focused coverage passed for multiline and paste safety behavior.
- No source change was required under `packages/coding-agent/src/tools/`.
