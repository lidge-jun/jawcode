# 10.107 — gjc chase: context compaction and CI smoke integrity

> MOC: [10_gjc_chase_MOC](../../10_gjc_chase_MOC.md) · G1 · ✅ _fin · P2 — align compaction, stale pruning, hindsight, fork budgets, registry fixtures, and SDK native smoke prerequisites as one bounded-context verification cluster.
> Reviewed source: GJC `3ddf26079..904eab21c` (`upstream/dev`, post-v0.11.1) vs JWC worktree.
> Naming: upstream `gjc` → `jwc` per [008](../../008_gjc_jwc_naming_contract.md).
> Closed: 2026-07-17 — volatile singleton pruning, child-model fork ceilings, and affected-CI SDK smoke prerequisites are enforced.

## Chase Scope

This card tracks the four commits left after routing and config classification. The main change hardens context compaction, stale tool-output pruning, hindsight state, volatile context handling, and subagent fork budgets across runtime and tests. Two CI fixes ensure SDK package smoke receives a native addon artifact. The final registry test initializes settings so availability-cache coverage does not depend on process-global test order.

JWC already has compaction budgets, stale read/search pruning, hindsight state, fork-context advisory limits, native build tasks, packed-SDK smoke scripts, and explicit model-registry settings fixtures. It does not expose upstream's same-path volatile-context-pruning module or cache red-team tests, and its affected-CI selector does not visibly connect the packed SDK smoke to a provisioned native artifact in the same way. The chase must preserve JWC's existing architecture while proving equivalent boundaries.

## Evidence Commands Run

```bash
git -C devlog/_gjc_chase/gajae-code log --oneline 3ddf26079..HEAD --no-merges
git -C devlog/_gjc_chase/gajae-code show --stat --oneline 8425404d 0c541c82 d8880a88 20837148
rg --files packages/agent packages/coding-agent scripts | rg 'compaction|pruning|hindsight|fork|volatile-context|ci-dev-affected|sdk.*smoke|model-registry'
rg -n "keepRecent|prun|stale|hindsight|fork.*budget|budget|cache epoch|native|addon|SDK|sdk.*smoke|settings" packages/agent/src/compaction packages/coding-agent/src/hindsight packages/coding-agent/src/session/agent-session.ts packages/coding-agent/src/session/volatile-context-pruning.ts packages/coding-agent/src/task packages/coding-agent/test/model-registry.test.ts scripts/ci-dev-affected.ts scripts/ci-dev-affected.test.ts 2>/dev/null
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
|---|---|---|
| `8425404d` | harden compaction, stale pruning, hindsight state, volatile context, and fork budgets with broad red-team coverage | `packages/agent/src/compaction/compaction.ts`; `packages/agent/src/compaction/pruning.ts`; `packages/coding-agent/src/hindsight/state.ts`; `packages/coding-agent/src/session/volatile-context-pruning.ts`; `packages/coding-agent/src/task/executor.ts` |
| `0c541c82` | provision a native addon for SDK package smoke | `scripts/ci-dev-affected.ts`; `scripts/ci-dev-affected.test.ts` |
| `d8880a88` | provide the native artifact to SDK package smoke | `scripts/ci-dev-affected.ts`; `scripts/ci-dev-affected.test.ts` |
| `20837148` | initialize settings in the availability-cache registry test | `packages/coding-agent/test/model-registry.test.ts`; `packages/coding-agent/src/internal-urls/docs-index.generated.ts` |

## Worktree Verification

| probe | JWC result | chase interpretation |
|---|---|---|
| compaction budget | `compaction.ts` reserves output tokens, computes keep-recent cuts, and bounds summary input | Compare correction retention, cache fingerprints, and transport accounting against upstream tests. |
| stale pruning | `pruning.ts` identifies stale reads/searches and permits stale protected reads to be pruned | Verify mutation epochs, repeated-result identity, already-pruned entries, and minimum-savings behavior. |
| volatile context | no same-path `session/volatile-context-pruning.ts` or context-cache red-team tests surfaced | Decide whether JWC's session owner needs a dedicated volatile-context boundary or equivalent in-place logic. |
| hindsight | JWC has backend/state/config/client implementations and explicit recall/retain budgets | Compare queue flushing, state disposal, stale session cleanup, render limits, and compaction handoff ordering. |
| fork budgets | JWC has `task/fork-context-advisory.ts` plus settings-backed message/token caps in task execution | Reconcile advisory estimates with actual child-context packing and prevent duplicated budget authority. |
| SDK native smoke | affected CI plans native build for coding-agent runtime and JWC has packed-SDK/native-probe scripts | Prove the exact package-smoke task receives the built artifact rather than relying on ambient workspace output. |
| registry fixture | `model-registry.test.ts` imports settings helpers and contains explicit initialized and deferred-initialization cases | Isolate availability-cache setup so test order and singleton settings state cannot change the result. |

## Decision Log (1-8 TBD)

1. **TBD — compaction budget:** define prompt, reserved output, summary, keep-recent, and correction-preservation budgets from one source.
2. **TBD — pruning freshness:** define file mutation/search epochs and which protected tool results become stale enough to prune.
3. **TBD — cache identity:** define fingerprints and invalidation for pruned, compacted, replayed, and provider-specific context.
4. **TBD — volatile context:** choose a dedicated owner or prove equivalent JWC session behavior with focused tests.
5. **TBD — hindsight lifecycle:** align recall/retain queues, cleanup, compaction hooks, fork inheritance, and render limits.
6. **TBD — fork accounting:** make advisory and enforced child-context budgets agree for messages, tokens, summaries, and attachments.
7. **TBD — SDK smoke artifact:** define the CI dependency edge from native build output to packed SDK installation and native probes.
8. **TBD — fixture isolation:** initialize and reset settings, registries, caches, and native paths explicitly in every affected test.

## Closure Evidence (2026-07-17)

| contract | result |
|---|---|
| volatile context boundary | only superseded `volatile-project-context` and known singleton reminders are pruned, and only from the existing compaction-maintenance path |
| compaction/cache invariants | cache-prefix identity and stale-pruning suites remain green; ordinary custom/user context is preserved |
| fork accounting | frozen seeds are re-clamped to `contextWindow - reserved output - fixed prompt/tool overhead`, retaining newest messages without mutating the parent seed |
| SDK smoke CI | affected `packages/jwc/**` plans now order native build, Node SDK bundle, then packed SDK smoke |

Verification: `bun test packages/coding-agent/test/session/volatile-context-pruning.test.ts packages/coding-agent/test/task/fork-context-budget.test.ts packages/coding-agent/test/task/fork-context-advisory.test.ts packages/coding-agent/test/task-fork-context.test.ts packages/agent/test/compaction-cache-prefix.test.ts packages/agent/test/pruning-staleness.test.ts packages/agent/test/pruning-staleness-redteam.test.ts scripts/ci-dev-affected.test.ts` → 97 pass, 0 fail.
