# 20.022 — omp chase: ssh:// + tooling/UX expansions

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · reference-only
> Split from [20.016](./20.016_omp_chase_advisor_thinking_dialect_session_title.md) (cluster 6 of 6).
> Reviewed source: OMP `0fc6d136` (v16.1.20) → `ca9f2847e` (v16.2.5) vs JWC worktree `ef28a98` (2026-06-29).
> Source range: `devlog/_omp_chase/oh-my-pi` `0fc6d136..ca9f2847e`. **1:1 port ❌ — design reference.**

## Why This Is A Gap

OMP shipped a broad tooling/UX expansion: ssh:// URL support for read/search/write with host autocomplete + directory listing, an `fd` filesystem-search builtin in pi-shell, file-path completion in autocomplete, a redesigned todo HUD with borders/progress, hashline edit-path extraction, configurable worktree base dir + git worktree detection/rendering, markdown HTML component rendering, protocol-based + d-bus desktop notifications, and subagent incremental yields. JWC can evaluate ssh URL support and `fd` builtin as tool/shell expansions.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `0fc6d136..ca9f2847e` |
| ssh:// URL read/search/write | `c63171b90` |
| ssh host autocomplete + dir listing | `3028bc4ef` |
| fd filesystem search builtin (pi-shell) | `70a439ace` |
| file-path completion in autocomplete | `670d60fa6` |
| todo HUD redesign (borders/progress) | `8a2c90a48` |
| hashline edit path extraction | `33e425594` |
| configurable worktree base dir | `97845b3d6` |
| git worktree detection/rendering | `4db3d68bd` |
| markdown html component rendering | `a427449d6` |
| protocol-based desktop notifications | `ccf2b1adf` |
| d-bus desktop toasts | `16fef78c9` |
| subagent incremental yields | `289dd770c` |
| primary path anchors | `packages/coding-agent/{src,test}`, `packages/tui/`, `crates/` (pi-shell → jwc-shell) |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show c63171b90 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 70a439ace --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| ssh tooling | Evaluate ssh:// read/search/write as JWC tool expansion; security review for remote access. |
| fd builtin | Evaluate `fd` builtin; OMP `pi-shell` → `jwc-shell` if adopted. |
| desktop notify | Cross-check against JWC notifications work (10.028-10.035 _fin). |
| subagent yields | Evaluate incremental yields against JWC subagent UX. |
| naming | Public surface JWC-first; translate `pi-shell`/`omp` naming. |
| tests | Focused JWC tests only if adapted. |

## Done Gate

- [ ] Source anchors rechecked against OMP `ca9f2847e` or newer recorded head.
- [ ] Adopt/defer/reject decision recorded per feature.
- [ ] ssh:// remote access security-reviewed if adopted.
- [ ] Any adopted behavior has JWC-specific tests or explicit manual evidence.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only; ssh:// + fd are adoption candidates |
| Decision B — naming impact | `pi-shell`→`jwc-shell`, tool/command naming |
| Decision C — test impact | future focused tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | tool surface expansion if adopted |
| Decision F — residual risk | open reference card; ssh security pending |
| Decision G — reviewer needed | security (ssh), backend (shell builtin) if promoted |
| Decision H — bundle policy | split per-feature on adoption |
