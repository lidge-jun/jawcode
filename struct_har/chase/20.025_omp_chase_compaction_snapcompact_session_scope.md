# 20.025 — omp chase: compaction / snapcompact caps + session-branch scoping

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · reference-only
> Reviewed source: OMP `ca9f2847e` (v16.2.5) → `b6c9747d4` (v16.2.9) vs JWC worktree `cfe71cd` (2026-07-01).
> Source range: `devlog/_omp_chase/oh-my-pi` `ca9f2847e..b6c9747d4` (175 commits); this card isolates the compaction/snapcompact/session-scope cluster. **1:1 port ❌ — design reference.**
> Naming: OMP `packages/agent`/`packages/snapcompact`/`.omp` → JWC `packages/coding-agent`/`.jwc` per [008](./008_gjc_jwc_naming_contract.md).
> Neighbors: relates to [20.014](./_fin/20/20.014_omp_chase_goal_compaction_provider_concurrency.md) (_fin) and JWC compaction chase 10.040 (_fin).

## Why This Is A Gap

OMP v16.2.5→v16.2.9 tightened compaction memory safety and scoped compaction to the active session branch: snapcompact frames are capped (payloads, unknown-window, newest-kept), snapshot caps share an aggregate budget across per-file results / edit-tool oldText-newText / hashline multi-section, pruned-snapshot markers are preserved through single-path aggregation, auto-compaction dead-ends recover via shake elisions, compaction elision is scoped to the active branch (avoiding ctrl-c rewrites for compacted sessions), and mid-run compaction matches persisted messages by key identity. JWC's compaction/resident-memory work (10.040 _fin) can use these as bounds/scoping references.

Behavioral surface:
1. snapcompact frame caps: payloads, unknown-window cap, keep-newest.
2. Shared aggregate snapshot budget across perFileResults / edit oldText-newText / hashline sections.
3. Preserve pruned-snapshot marker through single-path aggregation.
4. Recover from auto-compaction dead-ends via shake elisions.
5. Scope compaction elision to the active branch; avoid ctrl-c rewrites for compacted sessions.
6. Match persisted messages by key identity during mid-run compaction.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `ca9f2847e..b6c9747d4` (v16.2.5→v16.2.9) |
| snapcompact payload cap | `88308f105` fix(compaction): capped snapcompact frame payloads |
| unknown-window cap | `156dfd846` fix(compaction): capped unknown-window snapcompact frames |
| keep newest frames | `39688620f` fix(compaction): kept newest snapcompact frames |
| hashline aggregate cap | `c21cb6325` wrap hashline multi-section aggregate in shared snapshot cap |
| perFileResults cap | `398796949` cap perFileResults snapshots with shared aggregate budget |
| pruned-marker preserve | `3ced3a923` preserve pruned-snapshot marker through single-path aggregation |
| edit snapshot bound | `9e4e0f669` bounded edit-tool oldText/newText snapshots in tool-result details |
| shake elision recovery | `5b026d304` recovered from auto-compaction dead-ends using shake elisions |
| branch-scoped elision | `cc2cf5c7d` fix(session): scope compaction elision to active branch |
| no ctrl-c rewrite (compacted) | `e8b2c5d05` fix(session): avoid ctrl-c rewrites for compacted sessions |
| mid-run key identity | `3aa47d2ab` matched persisted messages by key identity during mid-run compaction |
| primary path anchors | `packages/agent/src/compaction/`, `packages/snapcompact/`, `packages/coding-agent/src/session/` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 5b026d304 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show cc2cf5c7d --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| snapcompact caps | JWC compaction/resident-memory (10.040 _fin) already addressed bounds; cross-check whether snapcompact-frame caps add new protection or duplicate existing guards. |
| aggregate budget | Shared snapshot budget is a memory-safety reference; evaluate vs JWC tool-result detail caps. |
| branch scoping | Active-branch elision scoping is an operator-correctness reference for JWC session branching. |
| mid-run identity | Key-identity message matching is a data-integrity reference (relates to 10.055 replay stability). |
| naming | `packages/agent`/`packages/snapcompact` → JWC coding-agent; `.omp`→`.jwc`. |
| source clone | Treat `devlog/_omp_chase/oh-my-pi` as read-only evidence; do not patch inside it. |

## Done Gate

- [ ] Source anchors rechecked against OMP `b6c9747d4` or a newer recorded head.
- [ ] Adopt/defer/reject decision recorded per feature (reference triage).
- [ ] Cross-checked against 10.040 (_fin) to avoid duplicate/contradictory guards.
- [ ] Naming translated per [008](./008_gjc_jwc_naming_contract.md).
- [ ] Any adopted behavior has JWC-specific tests or explicit manual evidence.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only (reference); branch-scoping + key-identity are integrity candidates |
| Decision B — naming impact | `packages/agent`/`snapcompact`→coding-agent; `.omp`→`.jwc` |
| Decision C — test impact | future focused compaction tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | memory-safety / session-correctness if adopted |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | backend (session/compaction), architecture for budget model |
| Decision H — bundle policy | split per-feature; cross-ref 10.040 / 10.055 |
