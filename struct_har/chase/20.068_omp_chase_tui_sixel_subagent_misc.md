# 20.068 — OMP chase: TUI SIXEL, subagent output, images, and operator miscellany

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P3 — Track small but user-visible rendering, image, liveness, native-loader, plugin, and operator correctness fixes.
> Reviewed source: OMP `7aa1d581c..b0d04e517` (`origin/main`, v16.4.2→v17.0.1) vs JWC worktree.

## Chase Scope

This is a residual correctness cluster rather than one implementation unit. It covers SIXEL geometry, tmux notification probing, subagent output sanitation, mixed assistant ordering, image replay/generation, CJK word navigation, persistent launch services, CLI duration parsing, Cursor max-mode state, magic-keyword boundaries, plugin replacement, startup changelog bounds, IRC liveness, malformed stats records, native loader sentinels, and bounded Mermaid routing.

The card must stay split-minded. Rendering and transcript invariants can be checked against existing JWC owners. OMP's daemon-backed launch stack, Cursor catalog layout, and Mermaid ASCII vendor path do not map directly and must not be imported as a batch. The current worktree also contains parallel edits in several matching files, so this card records evidence only.

- [ ] Verify SIXEL geometry and tmux OSC behavior with focused terminal tests.
- [ ] Verify control-character sanitation and mixed text/tool ordering in all transcript paths.
- [ ] Verify image placeholders, replay, provider fallback, gating, and native response images as one contract.
- [ ] Separate launch, Cursor max-mode, plugin, changelog, IRC, stats, native-loader, and Mermaid decisions by owner.
- [ ] Preserve the user-curated TUI visual/scroll contract while evaluating any upstream rendering fix.

## Evidence Commands Run

```bash
for h in $(awk -F'|' '/^\| [0-9]+ \|/{print $3}' devlog/_plan/260717_chase_pull_delta_tracking/020_omp_D18_tui_sixel_subagent_misc.md | tr -d ' `'); do
  git -C devlog/_omp_chase/oh-my-pi show --stat --oneline "$h"
  git -C devlog/_omp_chase/oh-my-pi show --name-only --format= "$h"
done
rg --files packages/tui/src packages/coding-agent/src packages/agent/src packages/ai/src packages/natives packages/stats/src packages/utils/src | rg 'terminal|image|render|transcript|magic|launch|changelog|plugin|irc|mermaid|loader|cursor'
rg -n 'SIXEL|sixel|OSC 99|tmux|generate_image|magic keyword|max.?time|max.?mode|liveness|malformed|sentinel' packages/tui/src packages/coding-agent/src packages/agent/src packages/ai/src packages/natives packages/stats/src packages/utils/src
```

## Source Commit Anchors

| commit | source fact | source file anchors |
| --- | --- | --- |
| `71d269823` | Rounds SIXEL height down to a multiple of six and rescales width. | `tui/src/terminal-capabilities.ts`; image render test |
| `c46089681` | Suppresses OSC 99 capability probing under tmux. | `tui/src/terminal.ts`; notifications test |
| `e6e7119f3` | Sanitizes subagent progress, retry, and yield output. | `coding-agent/src/task/render.ts`; render test |
| `949ccaa24` | Preserves original order for mixed assistant text/tool segments. | transcript builder; event controller; transcript helpers |
| `31c9f4850` | Omits empty image placeholders from tool outputs. | `ai/src/providers/openai-shared.ts`; focused test |
| `1626799f3` | Treats underscore as a word character for CJK-aware navigation. | `tui/src/utils.ts`; editor test |
| `4cfec9345` | Introduces daemon-backed persistent project services. | `coding-agent/src/launch/*`; launch tool/docs/tests |
| `f83e40921` | Adds a setting and registry toggle for the launch tool. | settings schema; bash/tool registry; docs |
| `4c1c5f40d` | Renders daemon terminal byte streams as launch logs. | launch broker/protocol; bash/launch tools |
| `3876f60d6` | Routes self-update through npm. | `cli/update-cli.ts`; update test |
| `d670dd5d9` | Reports invalid `--max-time` values as concise usage errors. | CLI args/flag table/usage error; launch/acp commands |
| `6fab752eb` | Parses duration suffixes for `--max-time`. | flag table; launch command; CLI test |
| `f98ef2e1e` | Invalidates stale Cursor max-mode discovery cache. | catalog provider special cases; discovery test |
| `358811115` | Preserves the Cursor max-mode flag through provider/catalog discovery. | `ai/src/providers/cursor.ts`; catalog discovery; tests |
| `ba7bcf4cd` | Ignores magic keywords embedded in code symbols. | `modes/magic-keyword-boundary.ts`; keyword test |
| `e7c678dbe` | Allows punctuation adjacent to magic keywords. | magic-keyword boundary; orchestrate/workflow modes |
| `63adfeece` | Replaces pinned git plugin sources safely. | `extensibility/plugins/manager.ts`; install test |
| `449310eb1` | Keeps startup changelog current-version detection accurate. | `utils/changelog.ts`; changelog test |
| `f53411295` | Bounds startup changelog rendering. | `main.ts`; command controller; changelog utility |
| `0828c53ca` | Adds liveness monitoring to IRC wait operations. | `irc/bus.ts`; `tools/irc.ts`; IRC test |
| `12c693a85` | Prefers the active image provider with fallback. | settings schema; `tools/image-gen.ts`; test |
| `1b0b18c7a` | Skips malformed session entries instead of crashing stats sync. | `stats/src/parser.ts`; malformed-entry test |
| `3a75368b9` | Preserves image replay and transcript invariants. | `transform-messages.ts`; assistant-message/render helpers; tests |
| `56fb4c014` | Falls back after an active OpenAI image HTTP failure. | `tools/image-gen.ts`; test |
| `5878ed8f4` | Gates `generate_image` by setting and tool whitelist. | settings schema; SDK; gating test |
| `748b2dff1` | Accepts opaque Codex image keys. | `tools/image-gen.ts`; test |
| `a29627142` | Checks disk state before diagnosing a stale native process. | `natives/native/loader-state.js`; repro test |
| `b6b947bdb` | Renders native images returned by OpenAI Responses. | agent loop/proxy; owned stream; `openai-shared.ts`; tests |
| `cc2041ab7` | Bounds the Mermaid ASCII pathfinder. | vendored `mermaid-ascii/.../pathfinder.ts`; test |
| `e4a10450e` | Distinguishes process-stale from disk-stale native sentinels. | native loader state/declarations; repro test |
| `f9cc18c45` | Skips incompatible image providers during fallback. | `tools/image-gen.ts`; test |

## Worktree Verification

| probe | result | JWC evidence / implication |
| --- | --- | --- |
| SIXEL and terminal capability owners | **exists** | `packages/tui/src/terminal-capabilities.ts`, `terminal.ts`, and `components/image.ts` exist. Existing tmux policy is JWC-specific and must be regression-tested, not overwritten. |
| Subagent output renderer | **exists** | `packages/coding-agent/src/task/render.ts` owns subagent output; sanitation needs focused control-character fixtures. |
| Mixed transcript owner | **different** | OMP's `chat-transcript-builder.ts` is absent. JWC transcript rendering is spread across mode components/helpers, so path matching is insufficient. |
| Empty image placeholder guard | **present in dirty worktree** | `packages/agent/src/image-placeholder-guard.ts` exists as parallel/uncommitted work. Treat it as user/agent-owned and only record the overlap. |
| Image generation stack | **exists** | `packages/coding-agent/src/tools/image-gen.ts` exposes `generate_image`; provider fallback/gating behavior requires current tests. |
| Persistent launch stack | **missing/different** | OMP-style `src/launch/` and `tools/launch.ts` are absent. JWC has tmux/runtime launch helpers, not the same daemon tool contract. |
| Magic-keyword boundary module | **missing/different** | OMP's dedicated `magic-keyword-boundary.ts` is absent; JWC workflow recognition must be located before adaptation. |
| Cursor max mode | **partial/different** | Cursor provider and discovery files exist, but OMP's catalog package and max-mode cache owner differ. |
| Startup changelog | **exists** | `packages/coding-agent/src/utils/changelog.ts` exists; current-version and render-budget tests are viable focused imports. |
| IRC liveness | **exists** | `packages/coding-agent/src/tools/irc.ts` exists; liveness monitoring should align with JWC's current async/session model. |
| Stats malformed entries | **exists** | `packages/stats/src/parser.ts` is the direct owner for a skip-and-continue fixture. |
| Native sentinel diagnostics | **exists** | `packages/natives/native/loader-state.js` already has release sentinel logic; distinguish disk/process staleness with JWC-specific tests. |
| Mermaid ASCII pathfinder | **missing/different** | JWC exports `packages/utils/src/mermaid-ascii.ts` but not OMP's vendored pathfinder path. Reject direct copying. |

## Decision Log (1-8 TBD)

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD** | Adapt SIXEL multiple-of-six geometry and tmux OSC probing only after terminal fixtures. |
| 2 | **TBD** | Adapt subagent sanitation and mixed-segment order through JWC renderer owners. |
| 3 | **TBD** | Consolidate image placeholder, replay, fallback, gating, opaque-key, and native-image tests. |
| 4 | **TBD** | Reject direct daemon-launch import; split a launch card if product direction requires it. |
| 5 | **TBD** | Decide whether JWC needs a dedicated magic-keyword boundary helper. |
| 6 | **TBD** | Evidence-fill Cursor max mode, plugin replacement, and startup changelog behavior. |
| 7 | **TBD** | Adapt IRC liveness, malformed stats handling, and native sentinel diagnostics independently. |
| 8 | **TBD** | Reject OMP Mermaid vendor-path copying; only port a bounded-search invariant if JWC's implementation needs it. |
