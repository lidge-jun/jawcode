# 20.080 — OMP chase: CI, style, changelog, revert, and snapshot maintenance

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P3 — Retain the unclassified maintenance tail as provenance without replaying merge cleanup, visual reverts, or generated snapshots.
> Reviewed source: OMP `b0d04e517..5394081390` (`origin/main`, 175 non-merge commits) vs JWC worktree.

## Chase Scope

This is the catch-all batch note for every commit in the reviewed range that does not belong to cards 20.071–20.079. It records a no-diff CI retry, restored prompt whitespace, changelog normalization, Biome cleanup after conflict resolution, owner-approved merge formatting and type repair, two feature reverts, and restoration of the generated model-catalog snapshot.

These commits describe OMP's merge state, not a coherent JWC feature. JWC has its own `[Unreleased]` changelog and CI contracts, generated model-catalog workflow, and user-curated TUI/scroll behavior. The two upstream reverts are especially non-portable: they must not be used to simplify JWC's inline-image, viewport, editor, or status-line behavior. This card is reference-only unless a concrete JWC failure reproduces independently.

- [ ] Keep the empty CI retry and pure formatting commits as provenance only.
- [ ] Preserve JWC package-level `[Unreleased]` changelogs instead of replaying OMP merge normalization.
- [ ] Do not import the inline-image or status-line reverts into user-curated JWC TUI surfaces.
- [ ] Regenerate model data through JWC's generator/resolver flow; never copy OMP `models.json` snapshots.
- [ ] Apply merge-era test/type repairs only when the same JWC error reproduces.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi log --oneline b0d04e517..HEAD --no-merges
for h in dea5fc877 e207a49c1 f4bbf2dec 2594ae352 f36394ef5 8876eef88 adb2a3c7e 32fecd0c8 2a6d55106 539408139; do
  git -C devlog/_omp_chase/oh-my-pi show --stat --oneline "$h"
  git -C devlog/_omp_chase/oh-my-pi show --name-only --format= "$h"
done
rg -n '\[Unreleased\]|Biome|models\.json|status-line|inline images|viewport|scroll' AGENTS.md package.json packages structure
git status --short
```

## Source Commit Anchors

| commit | source fact | source file anchors |
| --- | --- | --- |
| `dea5fc877` | Records a CI-only retry for flaky workspace tests with no tree diff. | empty commit / CI rerun marker |
| `e207a49c1` | Restores unrelated whitespace in the TAN context-switch prompt. | `prompts/system/tan-context-switch.md` |
| `f4bbf2dec` | Normalizes package changelogs after the merge sweep. | AI/catalog/coding-agent/natives/TUI changelogs |
| `2594ae352` | Applies Biome cleanup to conflict-resolved advisor, mode, SDK, and terminal files. | advisor runtime/test; interactive/RPC modes; SDK; TUI terminal |
| `f36394ef5` | Repairs formatting in merged Cursor, session, slash-command, task-label, and Kimi-search files. | AI/coding-agent tests; `agent-session.ts` |
| `8876eef88` | Reverts the merged inline-image scrollback implementation and its broad TUI fixtures. | TUI box/image/scroll-view/core; terminal capabilities; image/scroll tests |
| `adb2a3c7e` | Formats files from owner-approved selector, bash, image, handoff, telemetry, and logger merges. | coding-agent modes/tools/tests; utils logger |
| `32fecd0c8` | Repairs merge-introduced test type errors in credential redaction and handoff fixtures. | AI redaction test; coding-agent handoff test |
| `2a6d55106` | Reverts the multi-row status line to a single-row priority-drop layout. | status-line component/tests; selector; TUI editor/tests; changelogs |
| `539408139` | Restores bundled `models.json` to the tested generated snapshot. | `packages/catalog/src/models.json` |

## Worktree Verification

| probe | result | JWC evidence / implication |
| --- | --- | --- |
| CI rerun marker | **reference-only** | `dea5fc877` has no tree diff. It provides no portable code change or test invariant. |
| Changelog contract | **exists/different** | JWC package changelogs use `[Unreleased]`; OMP's post-merge normalization must not rewrite current JWC release notes. |
| Formatting cleanup | **tool-compatible/reference-only** | JWC can run its own formatter on owned changes, but OMP's conflict-resolution formatting carries no standalone behavior. |
| Prompt whitespace | **different context** | The OMP TAN prompt cleanup is unrelated merge repair. JWC prompt content should change only for a documented prompt contract. |
| Inline-image revert | **reject** | JWC's TUI image, terminal-capability, and viewport files exist and are dirty from parallel work. Root `AGENTS.md` explicitly protects the user-curated visual and scroll model from simplification. |
| Status-line revert | **reject as batch** | JWC owns its status-line component and tests, but OMP's single-row restoration is a product choice, not a generally portable fix. |
| Merge test/type repair | **conditional** | The touched JWC test families exist, but the OMP fixes should be adapted only if current typecheck/tests reproduce the same errors. |
| Catalog snapshot | **generated/different** | Root rules forbid direct edits to JWC's generated model catalog. Descriptor/resolver changes must be followed by the repository generator. |
| Dirty worktree | **preserve** | Multiple matching TUI, AI, coding-agent, and chase files belong to parallel agents. This card does not alter or revert them. |

## Decision Log (1-8 TBD)

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD** | Keep the empty CI retry as historical evidence only. |
| 2 | **TBD** | Keep pure whitespace and formatting commits reference-only unless they expose a JWC formatter failure. |
| 3 | **TBD** | Preserve JWC `[Unreleased]` changelog ownership and reject OMP merge-sweep rewrites. |
| 4 | **TBD** | Reject the inline-image scrollback revert as a direct port. |
| 5 | **TBD** | Reject the single-row status-line revert as a direct port. |
| 6 | **TBD** | Reproduce merge-era test/type failures before adapting either repair. |
| 7 | **TBD** | Never copy or hand-edit OMP's bundled model snapshot; use JWC generation inputs and checks. |
| 8 | **TBD** | Keep this catch-all card evidence-only and split any independently reproduced behavior into its owning chase card. |
