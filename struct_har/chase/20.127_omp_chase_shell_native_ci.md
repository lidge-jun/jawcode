# 20.127 — OMP chase: embedded shell, natives, and CI

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P2 — Evidence-fill embedded shell utilities and conditional native-build caching against JWC's actual CI.
> Reviewed source: OMP `v17.1.0..59619623` vs JWC worktree.

## Chase Scope

**Classification: evidence-fill · autonomy bucket: B.** These additions are useful only when JWC's embedded shell/build lanes reproduce the capability or performance need.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi show --stat 269c26739 5d4f1cf6b 27e019981 a569385b8 59619623e
rg -n 'cmp|moreutils|find.*exec|target.*cache' crates/pi-shell packages/natives/scripts .github scripts
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `269c26739` | Adds embedded `cmp`. | `crates/pi-shell/src/cmp.rs`; `coreutils.rs`; `shell.rs` |
| `5d4f1cf6b` | Adds six moreutils-inspired builtins. | `crates/pi-shell/src/moreutils/`; `shell.rs` |
| `27e019981` | Advertises shell builtins in bash tool help. | `coding-agent/src/prompts/tools/bash.md`; `tools/bash.ts` |
| `a569385b8` | Captures `find -exec` child stdio into scoped streams. | `crates/pi-shell/src/shell.rs`; vendored `uu-find`/`uu-xargs` |
| `59619623e` | Adds cargo target caching and conditional workflow validation. | `.github/actions/build-native/action.yml`; `.github/workflows/ci.yml`; `scripts/ci-target-cache.ts` |

## Worktree Verification

| probe | actual JWC tree evidence |
| --- | --- |
| Shell ownership | `crates/pi-shell/src/minimizer/filters/listing.rs:208` proves the embedded shell crate exists, but current grep produced no JWC `moreutils` module anchor. |
| CI policy | `.github` and native build scripts are present; importing cache logic requires measuring current jobs and preserving JWC release gates. |
| Gap claim | Only the missing `moreutils` source anchor is claimed; `cmp` token matches elsewhere are not treated as utility parity. |

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TRACK B** | Add builtins only for demonstrated agent workloads and update the static bash prompt together. |
| 2 | **TRACK B** | Reproduce `find -exec` stream loss before vendored-shell changes. |
| 3 | **TRACK B** | Benchmark cache hit/time and validate all affected-path branches before CI adaptation. |

