# 10.085 — gjc chase: provider stream and OAuth integrity

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · P1 — malformed tool streams and credential drift can corrupt requests or fail authentication.
> Reviewed source: GJC `b3b5b8a9..4a80bac9` (`upstream/dev`, v0.9.1→v0.9.6) vs JWC worktree `0e8d9f4+dirty`.
> Naming: upstream `gjc` → `jwc` per [008](./008_gjc_jwc_naming_contract.md).

## Chase Scope

This card protects provider boundaries: Anthropic OAuth must not receive unsupported strict tool-use requests, streamed tool names/arguments and start-block arguments must survive translation, Bedrock must follow one credential-chain contract, orphaned image placeholders must fail closed, and provider login failures must give a concrete action.

JWC owners are `packages/ai/src/providers/`, `packages/ai/src/stream.ts`, `packages/agent/src/agent.ts`, image-placeholder guards, and the coding-agent provider command/session glue. All six commits are G1 absorb candidates; Bedrock is an adapt/import split because local credential precedence may differ.

## Evidence Commands Run

```bash
git -C devlog/_gjc_chase/gajae-code show --stat 8456f7a6 798308e1 4dec9523 0c8a9539 6a96d6f0 1312b799
rg -n "strict.*tool|tool.*name|start.?block" packages/ai packages/agent --glob '*.ts' --glob '*.md'
rg -n "Bedrock|credential.?chain|image.?placeholder|login hint|/login" packages/ai packages/agent packages/coding-agent --glob '*.ts' --glob '*.md'
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
|---|---|---|
| `8456f7a6` | never request strict tool use on Anthropic OAuth requests | `packages/ai/src/providers/anthropic.ts`; `packages/ai/test/anthropic-alignment.test.ts` |
| `798308e1` | preserve Anthropic OAuth tool-call names and streamed arguments | `packages/ai/src/providers/anthropic.ts`; `packages/ai/test/anthropic-stream-envelope.test.ts` |
| `4dec9523` | preserve tool arguments already present on Anthropic start blocks | `packages/ai/src/providers/anthropic.ts`; `packages/ai/test/anthropic-stream-envelope.test.ts` |
| `0c8a9539` | align Bedrock authentication with a shared credential-chain configuration | `packages/ai/src/providers/amazon-bedrock.ts`; `packages/ai/src/providers/aws-credential-config.ts`; `packages/ai/src/providers/aws-credentials.ts`; `packages/ai/test/issue-1934-bedrock-auth.test.ts` |
| `6a96d6f0` | fail closed when image placeholders no longer have backing images | `packages/agent/src/image-placeholder-guard.ts`; `packages/agent/src/agent.ts`; `packages/coding-agent/src/utils/pasted-image-path.ts`; `packages/agent/test/agent.test.ts` |
| `1312b799` | show a concrete provider login hint | `packages/coding-agent/src/slash-commands/builtin-registry.ts`; `packages/coding-agent/test/provider-slash-command.test.ts` |

## JWC Worktree Verification

| sweep | exists / missing |
|---|---|
| tool stream | **Exists:** agent loop resolves internal and custom wire tool names. **Missing/unclear:** Anthropic OAuth-specific strict-tool suppression and start-block merge fixtures. |
| Bedrock | **Exists:** AWS credential implementation and Bedrock model entries. **Missing/unclear:** one explicit credential configuration/precedence owner matching upstream tests. |
| image/login | **Exists:** pasted-image utility and provider slash-command surface. **Missing/unclear:** a dedicated orphan-placeholder fail-closed guard and concrete tested login hint. |

## Owner Files In JWC

| slice | likely JWC owner files |
|---|---|
| Anthropic request and stream translation | `packages/ai/src/providers/anthropic.ts`; provider alignment and stream-envelope tests |
| Bedrock credentials | `packages/ai/src/providers/amazon-bedrock.ts`; `packages/ai/src/providers/aws-credentials.ts`; `packages/ai/src/stream.ts` |
| image integrity | `packages/agent/src/agent.ts`; placeholder guard owner; `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/utils/pasted-image-path.ts` |
| login guidance | `packages/coding-agent/src/slash-commands/builtin-registry.ts`; provider command tests |

## Import / Adapt / Reject Decisions

| feature slice | decision | reason |
|---|---|---|
| suppress strict tools for Anthropic OAuth | **Import** | This is a provider capability constraint, not a UI preference. |
| preserve names and streamed/start-block args | **Import** | Tool-call identity and complete JSON arguments are wire-integrity invariants. |
| Bedrock credential chain | **Import/adapt** | Centralize precedence and visibility while preserving JWC-supported environment/profile sources. |
| orphaned image placeholders | **Import** | Sending unmatched placeholders is silent prompt corruption; fail before provider dispatch. |
| concrete provider login hint | **Adapt** | Point to JWC's actual login/provider command and package naming. |

## Risk Notes

Do not normalize a provider's wire tool name back to an internal name after streaming begins. Start-block arguments and subsequent deltas must merge exactly once. Credential-chain tests must isolate environment variables and child processes to prevent developer credentials from masking failures. Placeholder validation should reject only truly orphaned references, not valid resumed-session blobs.

## Promotion Checklist

- [ ] Add Anthropic OAuth fixtures for strict tool choice, renamed tools, start-block args, and streamed deltas.
- [ ] Document and test Bedrock credential precedence in an isolated process.
- [ ] Validate image placeholders immediately before provider dispatch and on resumed transcripts.
- [ ] Make the login hint name JWC's concrete command and test the rendered text.

