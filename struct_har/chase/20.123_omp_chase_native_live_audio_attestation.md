# 20.123 — OMP chase: native live audio and attestation

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P2 — Track OMP's native realtime stack as a security- and platform-gated product lane.
> Reviewed source: OMP `v17.1.0..59619623` vs JWC worktree.

## Chase Scope

**Classification: split · autonomy bucket: C.** Native WebRTC/audio, DeviceCheck attestation, transcript delegation, and visualizer layout combine platform, identity, privacy, and UI choices.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi show --stat b76c07ece c9c088272 c1d4e38aa 17735c778 6ac587985 90e0a8af2 02114a6bf 16d33dcf7 e9c8a35f5
rg -n 'live|attestation|audio|visualizer' packages/coding-agent/src packages/ai/src
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `b76c07ece` | Adds native WebRTC peer and DeviceCheck bindings. | `crates/pi-natives/src/live.rs`; `devicecheck.rs`; `coding-agent/src/live/attestation.ts` |
| `c9c088272` | Replaces Chromium audio with native capture/playback. | `crates/pi-natives/src/audio.rs`; `coding-agent/src/live/`; `stt/`; `tts/` |
| `c1d4e38aa` | Adds live delegation and turn-based transcripts. | `coding-agent/src/live/controller.ts`; message components |
| `17735c778` | Adds `x-oai-attestation` to ChatGPT OAuth Codex requests. | `ai/src/providers/openai-codex-responses.ts`; `catalog/src/wire/codex.ts` |
| `6ac587985` | Expands live visualizer across container width. | `coding-agent/src/live/visualizer.ts` |
| `90e0a8af2` | Repairs CI dependencies for native audio. | `.github/actions/build-native/action.yml`; `.github/workflows/ci.yml` |
| `02114a6bf`, `16d33dcf7`, `e9c8a35f5` | Hardens non-macOS DeviceCheck and MSVC native builds. | `crates/pi-natives/src/devicecheck.rs`; `packages/natives/scripts/build-native.ts` |

## Worktree Verification

| probe | actual JWC tree evidence |
| --- | --- |
| Codex transport | `packages/ai/src/providers/openai-codex-responses.ts:132` has wire-liveness telemetry, but the inspected JWC anchors do not establish OMP attestation parity. |
| UI live zone | `packages/coding-agent/src/modes/controllers/event-controller.ts:117-199` is tool-render state, not evidence of OMP realtime voice delegation. |
| Gap claim | Native voice/attestation remains **unverified**; no absence is inferred from subjects. A dedicated security/platform comparison is required. |

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD C** | Decide whether realtime voice belongs in JWC. |
| 2 | **TBD C** | Approve attestation credential/privacy boundaries and supported platforms. |
| 3 | **TBD C** | Preserve JWC visual identity if a live visualizer is adopted. |

