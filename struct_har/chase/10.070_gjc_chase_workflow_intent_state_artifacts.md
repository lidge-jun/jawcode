# 10.070 — GJC chase: workflow intent/state/artifact contracts

Status: active chase card
Lane: GJC
Source range: `79b42377..db7938e1` (`upstream/dev`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

GJC added and tightened workflow-state surfaces in the 2026-07-03 pull:

- `cc5e4937` — `feat(coding-agent): track workflow intent routing (#1433)`
  - `packages/coding-agent/src/workflow/workflow-intent-diff.ts`
  - `packages/coding-agent/test/agent-session-workflow-intent-diff.test.ts`
  - `packages/coding-agent/src/modes/shared/agent-wire/workflow-gate-schema.ts`
- `77bbca23` — `feat(ralplan): standardize decision artifacts (#1420)`
  - `packages/coding-agent/src/defaults/gjc/skills/ralplan/SKILL.md`
  - `packages/coding-agent/test/ralplan-decision-artifacts.test.ts`
- `28887239` — `fix(deep-interview): reject blank gate free text (#1435)`
- `171e8dc7` — `fix(state): return lock-owned stamped envelopes (#1428)`
  - `packages/coding-agent/src/gjc-runtime/state-runtime.ts`
  - `packages/coding-agent/src/gjc-runtime/state-writer.ts`

## JWC decision

Classification: **adapt**.

This is semantically relevant to JWC, but direct copy is wrong because public surfaces are `jwc`, `.jwc`, native `orchestrate`, `jaw-interview`, and `planphase` rather than GJC `ralplan`/`deep-interview` paths. The likely JWC patch should be staged against native workflow-state and planphase/orchestrate contracts, not against legacy skill loops.

## JWC owner candidates

- `packages/coding-agent/src/jwc-runtime/orchestrate-state.ts`
- `packages/coding-agent/src/jwc-runtime/orchestrate-runtime.ts`
- `packages/coding-agent/src/commands/planphase.ts`
- `packages/coding-agent/src/modes/shared/agent-wire/workflow-gate-schema.ts`
- `packages/coding-agent/test/*workflow*`, `*planphase*`, `*orchestrate*`

## Verification expectation

Before closing this card, prove:

1. workflow intent diffs or equivalent are represented in JWC terms;
2. blank/invalid workflow-gate free text is rejected where JWC gates accept user text;
3. state writes still return stamped/lock-owned envelopes or an explicitly documented JWC alternative;
4. plan/orchestrate decision artifacts remain durable and receipt-first.

Suggested checks: focused workflow-state/planphase/orchestrate tests plus `bun run check:tools`.

## WP3 Implementation Status

WP3 investigated all four source commits against the current JWC runtime. Overall status: **PARTIAL / actionable runtime fixes implemented**.

| source | status | JWC disposition |
|---|---|---|
| `cc5e4937` workflow intent routing | **DEFER** | JWC already records activated workflow state, command provenance, and receipts in JWC terms, but it does not persist a pre-execution prompt-classifier entry. Porting GJC's regex heuristic would require a non-goal `AgentSession` integration outside WP3 ownership and would reintroduce GJC workflow names. Keep this as a separate product decision rather than adding an unconsumed classifier module. |
| `77bbca23` decision artifacts | **ACCEPT** | `jwc planphase` already persists planner/architect/critic/final Markdown artifacts under `.jwc/plans/planphase/<run-id>/`, appends `index.jsonl` with SHA-256 metadata, returns a write receipt, and copies the final artifact to `pending-approval.md`. This is the JWC-native receipt-first equivalent; no env-only handoff is needed. |
| `28887239` blank gate free text | **ACCEPT** | `jaw-interview-gate.ts` already requires `custom` when `other` is selected and rejects whitespace-only text with `answer.custom.trim() === ""`. The advertised schema also has `minLength: 1`; semantic trim validation closes the whitespace gap. |
| `171e8dc7` stamped writer result | **IMPLEMENTED** | Added `writeWorkflowEnvelopeAtomicWithResult()`, which returns the exact checksum/receipt-stamped envelope passed to the atomic write. `jwc state` now publishes that writer-owned value directly instead of re-reading the path after the write. The existing path-returning wrapper remains for compatibility with callers that only need the path. JWC uses atomic rename rather than GJC's lock/revision writer, so the exact-envelope return is the relevant local contract. |

### Verification

- PASS: Biome on all five changed source files.
- PASS: focused stamped-envelope checks (`orchestrate-state`, `state-writer-drift`) and an inline assertion that the new result contains the persisted receipt.
- PASS: 141 of 157 tests in the broader focused batch.
- Known baseline/out-of-scope failures: four `ralplan`-named plan-state expectations and four pre-existing Codex profile expectations conflict with current JWC state.
- Intentional stale-test failures: external goal snapshot requirements and the hard-coded 17-profile matrix need follow-up in test files outside WP3 ownership.
- Package typecheck reached unrelated existing errors in `packages/ai/src/providers/cursor.ts` and `file-lock-gc-toctou.test.ts`; no WP3 source type error was reported.

### Changed Files

- `packages/coding-agent/src/jwc-runtime/state-writer.ts`
- `packages/coding-agent/src/jwc-runtime/state-runtime.ts`
- `packages/coding-agent/src/jwc-runtime/goal-engine.ts`
- `packages/coding-agent/src/jwc-runtime/goal-guard.ts`
- `packages/coding-agent/src/config/model-profiles.ts`
- `struct_har/chase/10.070_gjc_chase_workflow_intent_state_artifacts.md`
- `struct_har/chase/10.067_gjc_chase_goal_plan_skill_state_refresh.md`
