# 20.007 — omp chase: session layer modularization (reference)

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜
> Reviewed source: OMP `7e8122000ad9` (`24c8bb24c` session APIs rebuild, 15.12.x–15.13.0) vs JWC `d60b7822`.
> **구현가치: 50 (MLB)** — **참조 설계**; jwc `session-manager.ts` 대이식 금지.
> **따라갈 순서**: [02 priority #26](../../devlog/_fin/260614_chase_upstream_pull_priority_report/02_debt_priority_stack.md)

## 왜 gap인가

OMP split monolithic session manager into `session-context.ts`, `session-listing.ts`, `session-loader.ts`, `session-paths.ts`, `session-persistence.ts`, `session-entries.ts`, `session-migrations.ts`. Improves listing, fork/resume, and persistence clarity documented in G2 session-ops references. jwc should **mine boundaries** for future refactors, not 1:1 file copy.

## Source facts

| module | role |
|--------|------|
| `session-context.ts` | Context assembly/export |
| `session-listing.ts` | List/rank sessions |
| `session-loader.ts` | Load paths + migrations hook |
| `session-persistence.ts` | Immediate/mid-close persist races addressed in tests |

## JWC reconcile notes

- jwc HARD-EDIT `session-manager.ts` + display/model context split already partial — map OMP modules to existing symbols before moving files.
- Pair with [20.005](./20.005_omp_chase_steering_delivery.md) agent-session changes in same refactor plan only if PABCD-sized.

## 완료 기준

- [ ] devlog refactor sketch: OMP module → jwc symbol map.
- [ ] No behavior change required for card close — reference-only acceptable.