# 10.027 — gjc chase: RLM research mode

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P2**
> Reviewed source: GJC `5ed80862` (`4928f185` #778, `5ed80862` CLI surface test) vs JWC `d60b7822` lineage.
> **구현가치: 45 (MLB)** — 연구 전용 모드. JWC workflow surface와 충돌 가능성이 있어 설계 결정 선행.

## 왜 gap인가

GJC는 opt-in `rlm` research lane을 추가했다. 별도 research system prompt, `python`/`read`/`web_search` allowlist, optional `DATA.md`, `.gjc/rlm/<session>/notebook.ipynb`, 종료 report를 묶어 interactive research 모드로 제공한다. JWC는 public workflow surface를 `jaw-interview`/`plan`/`goal`/`team`으로 제한하므로 1:1 포트가 아니라 `jwc rlm`이 제품 표면에 들어갈지 먼저 결정해야 한다.

## Source facts

| fact | upstream evidence |
|---|---|
| opt-in RLM command | `packages/coding-agent/src/commands/rlm.ts` |
| research runtime/artifacts | `packages/coding-agent/src/rlm/*` |
| dedicated prompt | `packages/coding-agent/src/prompts/system/rlm-research.md` |
| tests | `packages/coding-agent/test/rlm.test.ts`, `rlm-e2e.test.ts`; CLI surface covered at `5ed80862` |

## JWC reconcile

| decision | note |
|---|---|
| public surface | `jwc rlm` 추가는 workflow-surface product decision 필요 |
| state path | upstream `.gjc/rlm` → JWC라면 `.jwc/rlm` |
| allowed tools | research lane이 normal agent tool surface와 섞이지 않도록 hard gate 필요 |
| overlap | `jaw-interview` requirements workflow와 목적이 다름: RLM은 자료조사/노트북/보고서 lane |

## 완료 기준

- [ ] JWC에서 RLM을 public command로 둘지, hidden/dev-only command로 둘지 결정.
- [ ] `.jwc/rlm` artifact contract와 tool allowlist 문서화.
- [ ] CLI surface/test가 JWC bin·namespace로 검증됨.
