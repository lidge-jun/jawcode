# 20.018 — omp chase: thinking/reasoning normalization (pi dialect removed)

Status: active reference card

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · reference-only
> Split from [20.016](./20.016_omp_chase_advisor_thinking_dialect_session_title.md) (cluster 2 of 6).
> Reviewed source: OMP `0fc6d136` (v16.1.20) → `ca9f2847e` (v16.2.5) vs JWC worktree `ef28a98` (2026-06-29).
> Source range: `devlog/_omp_chase/oh-my-pi` `0fc6d136..ca9f2847e`. **1:1 port ❌ — design reference.**
> Cross-link: AI provider overlap with GJC [10.036](./_fin/10/10.036_gjc_chase_ai_provider_auth_model_catalog.md).

## Why This Is A Gap

OMP normalized thinking/reasoning streams to a canonical, provider-agnostic dialect and removed the "pi" dialect + infrastructure entirely. It added universal thinking-tag healing for text streams, cross-provider thinking demotion to the canonical dialect, signature dedup during persistence, expanded delimiters, signed-thinking-block preservation during demotion, and Gemini reasoning-runaway detection. JWC never had a "pi" dialect, so removal is informational; the normalization behaviors should be evaluated against JWC's AI provider surface.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `0fc6d136..ca9f2847e` |
| universal thinking tag healing | `3e06557c2` |
| cross-provider thinking demotion to canonical | `b08d95111` |
| thinking signature dedup during persistence | `42981154f` |
| expanded thinking delimiters | `eb6b69543` |
| REMOVED pi dialect + infrastructure | `053da98dd` |
| Gemini reasoning runaway detection | `71e044cfe` |
| signed thinking block preservation during demotion | `da07a3ac5` |
| primary path anchors | `packages/ai/{src,test}` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 3e06557c2 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show 053da98dd --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| pi dialect removal | JWC never had pi dialect — no action; confirms canonical thinking format is provider-agnostic. |
| thinking normalization | Evaluate tag-healing + cross-provider demotion against JWC ai provider surface; partial overlap with GJC 10.036. |
| Gemini runaway | Evaluate runaway detection as a JWC robustness guard. |
| tests | Focused JWC tests only if adapted. |

## Done Gate

- [ ] Source anchors rechecked against OMP `ca9f2847e` or newer recorded head.
- [ ] GJC 10.036 reviewed for overlap.
- [ ] Adopt/defer/reject decision recorded with rationale.
- [ ] Any adopted behavior has JWC-specific tests or explicit manual evidence.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only; evaluate tag-healing for adoption |
| Decision B — naming impact | none now |
| Decision C — test impact | future focused tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | none in reference phase |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | backend (ai stream) if promoted |
| Decision H — bundle policy | may align with GJC 10.036 / 10.055 replay work |

## Refresh — OMP v16.3.4 to v16.3.12

Source cluster: `d0c1890a6..f25ab54c5` (541 commits). Thinking/reasoning anchors: `fdd7bdef7` improves reasoning block separation and validation, `a4c26df36` demotes prior reasoning to bare prose for Anthropic-dialect Claude models, `9901d868e` drops redacted siblings with discarded Anthropic thinking, `d23e3296c` keeps assistant `tool_use` blocks trailing on Anthropic replay, `47f49f3fe` keeps thinking-only Google responses retryable, `1c56a9f31` suppresses CCA thinking summaries by default, `8886a528d` sizes title/commit/speech/classifier budgets for backends that ignore `disableReasoning`, and `ea4e91330` records Codex compact usage diagnostics.

JWC decision: still **reference/adapt candidate**. The new facts sharpen the provider-normalization contract: demotion must preserve tool ordering, retry logic must distinguish empty vs thinking-only responses, and budget logic cannot assume every backend honors reasoning disable flags.

JWC owner candidates: `packages/ai/src/**`, provider replay tests, reasoning/compaction budget tests, and `packages/coding-agent/src/session/` only if transcript persistence changes.

Verification expectation: any adoption needs provider-specific stream/replay fixtures plus persistence checks for signed/thinking blocks. Broad typecheck is not enough.

## WP9 triage — 2026-07-11

**REFERENCE-NOTED.** JWC has provider-specific thinking/reasoning normalization and replay owners, but this sweep did not establish full equivalence for OMP's tag healing, discarded signed-block siblings, and thinking-only retry rules. Keep these as provider-test inputs; no 1:1 port or session-layer change is justified by this card.
