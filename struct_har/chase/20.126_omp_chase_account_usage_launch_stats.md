# 20.126 — OMP chase: account, usage, launch, and provider stats

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P2 — Separate account pinning and remote usage contracts from narrow launch ordering fixes.
> Reviewed source: OMP `v17.1.0..59619623` vs JWC worktree.

## Chase Scope

**Classification: split · autonomy bucket: C.** Session account pinning, broker usage reporting, and provider analytics change identity/data UX; daemon ordering/time recovery are isolated A candidates.

## Evidence Commands Run

```bash
git -C devlog/_omp_chase/oh-my-pi show --stat af9e8546a 30994dafb 3676b5d97 f57f388c3 b05226e75 677c35852
rg -n 'usage|StoredAuthCredential|modelRoles' packages/ai/src/auth-storage.ts packages/coding-agent/src/config
```

## Upstream Commit Anchors

| commit | source fact | upstream file anchors |
| --- | --- | --- |
| `af9e8546a` | Adds per-session account selection and pinning slash command. | `ai/src/auth-storage.ts`; `session-account-selector.ts`; `slash-commands/helpers/session-pin.ts` |
| `30994dafb` | Adds provider analytics/utilization dashboard. | `packages/stats/src/client/routes/ProvidersRoute.tsx`; `aggregator.ts`; `db.ts` |
| `3676b5d97` | Adds auth-broker client usage reporting. | `ai/src/auth-broker/`; `auth-storage.ts`; stats aggregation |
| `f57f388c3`, `b05226e75` | Orders active daemons first and preserves true recovered exit time. | `coding-agent/src/launch/broker.ts` |
| `677c35852` | Exercises launch ordering through RPC. | `coding-agent/src/launch/broker-list-order.test.ts` |

## Worktree Verification

| probe | actual JWC tree evidence |
| --- | --- |
| Credential surface | `packages/ai/src/auth-storage.ts:187-193` exposes provider credential mutation; `:233-247` already supports aggregate/per-credential usage lookup. |
| Model/session identity | `packages/coding-agent/src/config/settings.ts:471-555` owns persistent model roles, but does not by itself prove an OMP-style session account-pin UI. |
| Classification | Because credentials and usage are existing JWC contracts, the gap is **semantic/unverified** and requires tests, not a claim of absent files. |

## Decision Log

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD C** | Decide account pin lifetime, `.jwc` persistence, and UI disclosure. |
| 2 | **TBD C** | Decide whether broker/provider analytics are collected and displayed. |
| 3 | **OPEN A** | Adapt daemon ordering and recovered exit-time tests independently. |

