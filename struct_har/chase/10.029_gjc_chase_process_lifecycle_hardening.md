# 10.029 — gjc chase: process/resource lifecycle hardening

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P0**
> Reviewed source: GJC `5ed80862` (`ac519ac4` and follow-up F1/U1–U10 hardening series) vs JWC `d60b7822` lineage.
> **구현가치: 80 (MLB)** — long-running session safety; feature보다 우선 검토.

## 왜 gap인가

GJC 0.5.2/0.5.3 구간은 long-running session freeze/leak, process teardown, PTY/shell resource ownership, JS/Python eval lifecycle, DAP/LSP/MCP cleanup, job manager bounds를 광범위하게 보강했다. JWC는 team/orchestrate/subagent를 오래 유지하므로 orphan process·stuck PTY·unclean runtime cleanup은 직접적인 안정성 리스크다.

## Source facts

| fact | upstream evidence |
|---|---|
| owned process lifecycle | `packages/coding-agent/src/runtime/process-lifecycle.ts` |
| shell/PTY hardening | `crates/pi-natives/src/pty.rs`, `crates/pi-shell/src/shell.rs`, `packages/coding-agent/src/exec/bash-executor.ts` |
| eval lifecycle | `packages/coding-agent/src/eval/js/*`, `src/eval/py/*` |
| DAP/LSP/MCP cleanup | `packages/coding-agent/src/dap/*`, `src/lsp/*`, `src/runtime-mcp/*` |
| async/job bounds | `packages/coding-agent/src/async/job-manager.ts`, `test/async/job-manager-*.test.ts` |

## JWC reconcile

| decision | note |
|---|---|
| bundle boundary | [10.019](./10.019_gjc_chase_gc_file_lock.md) covers stale lock/GC; this card covers live process/resource lifecycle |
| risk | high: process teardown touches shell, eval, DAP/LSP, MCP, subagent jobs |
| order | audit first, then slice by runtime owner; no blanket cherry-pick |

## 완료 기준

- [ ] Runtime owners listed: bash, eval JS, eval Python, DAP, LSP, MCP, async job manager, tmux/team.
- [ ] Each owner has adopt/defer/reject diff notes against JWC.
- [ ] Regression tests cover signal teardown and stale resource cleanup for adopted slices.
