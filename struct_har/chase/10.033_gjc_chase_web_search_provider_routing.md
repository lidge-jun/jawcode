# 10.033 — gjc chase: native model-provider `web_search` routing

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P2**
> Reviewed source: GJC `5ed80862` (`638d2f11` #591) vs JWC `d60b7822` lineage.
> **구현가치: 45 (MLB)** — search provider reliability; must align with JWC `/skill:search` routing.

## 왜 gap인가

GJC added native model-provider search as primary with configurable fallback. JWC already has a search skill/tool routing discipline; adopting this upstream feature should improve provider routing only if it preserves JWC’s citation, escalation, and tool-help skill boundaries.

## Source facts

| fact | upstream evidence |
|---|---|
| provider resolution | `packages/coding-agent/src/web/search/provider.ts` |
| OpenAI-compatible search provider | `packages/coding-agent/src/web/search/providers/openai-compatible.ts` |
| config/types | `packages/coding-agent/src/web/search/types.ts`, settings/config schemas |
| tests | `packages/coding-agent/test/web/search/provider-resolution.test.ts`, `openai-compatible-provider.test.ts`, red-team tests |

## JWC reconcile

| decision | note |
|---|---|
| policy | `/skill:search` remains the search workflow owner for web/current facts |
| provider config | upstream `GJC_*`/provider docs require JWC naming pass |
| risk | bad fallback ordering can silently degrade citation/source quality |

## 완료 기준

- [ ] JWC current web_search provider chain compared to upstream provider-resolution behavior.
- [ ] Fallback behavior preserves mandatory external citation semantics.
- [ ] Config/schema docs use JWC names and do not expose stale GJC labels.
