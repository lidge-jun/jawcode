# 10.062 — gjc chase: DeepInfra provider + service-tier · Gemini CLI user-agent alignment

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P2**
> Reviewed source: GJC `20c299eb` (`upstream/dev`, v0.7.8) vs JWC worktree `cfe71cd` (2026-07-01).
> Range: v0.7.8 delta `fa995807..20c299eb` (21 commits); this card isolates the AI-provider cluster (2 commits).
> Naming: `packages/ai/` is a shared package path; user-facing provider/docs naming stays `jwc`/`@jawcode-dev/*` per [008](./008_gjc_jwc_naming_contract.md).
> Neighbors: extends [10.036](./_fin/10/10.036_gjc_chase_ai_provider_auth_model_catalog.md) (AI provider/auth/model catalog, _fin) and [10.054](./10.054_gjc_chase_local_provider_discovery.md) (local provider discovery).

## Why This Is A Gap

GJC v0.7.8 added a new AI provider and corrected an outbound CLI identity in two commits: DeepInfra provider with service-tier support (OpenAI-compatible, OAuth descriptor, large `models.json` catalog expansion), and an alignment of the Gemini CLI user-agent version (with a spoofed-version guard update). JWC's `packages/ai/` lacks the DeepInfra OAuth/provider wiring and may carry a stale Gemini UA version. These are provider-catalog and outbound-identity reconciles.

Behavioral surface:
1. DeepInfra provider — OpenAI-compatible descriptor, OAuth storage, service-tier support, models catalog entries.
2. Gemini CLI user-agent version alignment + spoofed-version guard (`scripts/check-spoofed-versions.ts`).

## Source Facts

| fact | upstream anchor |
|---|---|
| reviewed head | `devlog/_gjc_chase/gajae-code` @ `20c299eb` (v0.7.8) |
| DeepInfra provider | `68179e2d` Add DeepInfra provider and service-tier support (#1314) — `packages/ai/src/utils/oauth/deepinfra.ts`, `src/provider-models/{descriptors,openai-compat}.ts`, `src/auth-storage.ts`, `src/types.ts`, `src/models.json` (+596), `docs/models.md` |
| Gemini UA alignment | `b200fc0e` fix(ai): align Gemini CLI user agent version (#1284) — `packages/ai/src/providers/google-gemini-headers.ts`, `test/google-gemini-cli-user-agent.test.ts`, `scripts/check-spoofed-versions.ts` |
| primary source areas | `packages/ai/src/utils/oauth/`, `src/provider-models/`, `src/providers/google-gemini-headers.ts`, `src/models.json`, `scripts/check-spoofed-versions.ts` |
| comparison range | `fa995807..20c299eb` |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show 68179e2d --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_gjc_chase/gajae-code show b200fc0e
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| DeepInfra provider | JWC `packages/ai/src/utils/oauth/` has no `deepinfra.ts`; adding it is a clean additive provider import if DeepInfra is a desired JWC provider. Evaluate against existing `.jwc` model config + provider catalog. |
| models catalog | `models.json` is a large shared catalog; importing DeepInfra entries must avoid clobbering JWC-specific catalog edits (diff carefully, additive merge). |
| Gemini UA | `google-gemini-headers.ts` exists in JWC; verify whether JWC carries a stale UA version and whether the spoofed-version guard (`scripts/check-spoofed-versions.ts`) needs the aligned value. Outbound-identity correctness. |
| public names | Keep user-facing provider docs/commands as `jwc`/`@jawcode-dev/*`; provider id `deepinfra` is an upstream-neutral external service name. |
| source clone | Treat `devlog/_gjc_chase/gajae-code` as read-only evidence; do not patch inside it. |

## Done Gate

- [ ] Source facts checked against `20c299eb` or a newer recorded GJC head.
- [ ] JWC owner files listed before implementation (`packages/ai/src/utils/oauth/`, `provider-models/`, `providers/google-gemini-headers.ts`, `models.json`).
- [ ] Naming translation checked against [008](./008_gjc_jwc_naming_contract.md) (public provider/docs surface).
- [ ] Import/adapt/reject decision recorded per sub-feature (2 items).
- [ ] Focused test or manual evidence proves chosen behavior (provider auth flow; Gemini UA test).
- [ ] `git diff --check` passes.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | open: DeepInfra likely import-if-wanted (additive); Gemini UA likely import (identity correctness) |
| Decision B — naming impact | public provider docs/names (`jwc`/`@jawcode-dev`); provider id neutral |
| Decision C — test impact | new provider auth test; Gemini UA user-agent test |
| Decision D — docs impact | chase now; promote provider contract to `docs/`/`structure/` if imported |
| Decision E — rollout impact | additive provider; UA alignment is outbound-identity, no migration |
| Decision F — residual risk | open until implementation or explicit rejection |
| Decision G — reviewer needed | backend (provider/auth); security for outbound UA + OAuth storage |
| Decision H — bundle policy | may bundle with 10.036 (provider/auth/catalog) / 10.054 (local provider) |
