# 10.073 — GJC chase: RPC, session teardown, notifications lifecycle

Status: active chase card
Lane: GJC
Source range: `79b42377..db7938e1` (`upstream/dev`, pulled 2026-07-03)
JWC baseline: `89371141` worktree HEAD at pull time

## Source cluster

GJC updated session lifecycle, notification formatting, and RPC/Python protocol surfaces:

- `c960eec8` — send `session_closed` on process teardown via postmortem (#1432)
  - `packages/coding-agent/src/session/agent-session.ts`
  - `packages/coding-agent/test/notifications-postmortem-close.test.ts`
- `b55284c2` — fix Telegram session recent formatting (#1413)
- `ebbf03c5` — resync typed Python client with the RPC server protocol (#1430)
  - `python/gjc-rpc/src/gjc_rpc/client.py`
  - `python/gjc-rpc/src/gjc_rpc/protocol.py`
  - `python/gjc-rpc/tests/test_client.py`
  - `python/gjc-rpc/tests/test_protocol.py`
- `0c37289b` — keep post-release RPC fix unreleased
- `f1f2d9ce` — print resume hint command on its own line

## JWC decision

Classification: **adapt**.

JWC already has a `python/jwc-rpc` naming contract and must translate `gjc_rpc` semantics through `struct_har/chase/008_gjc_jwc_naming_contract.md`. The session teardown/notification changes are likely relevant, but Telegram and remote lifecycle behavior must stay fail-closed and JWC-named.

## JWC owner candidates

- `packages/coding-agent/src/session/agent-session.ts`
- `packages/coding-agent/src/notifications/`
- `packages/coding-agent/test/notifications-*`
- `python/jwc-rpc/src/jwc_rpc/`
- `python/jwc-rpc/tests/`
- `docs/rpc.md`, `docs/notifications-sdk.md` if public behavior changes

## WP2 Implementation Status

| Decision | Upstream slice | JWC disposition |
|---|---|---|
| **DONE** | `c960eec8` session teardown | Added the typed `session_closed` notification frame, broadcast it before loopback sockets close, and registered a JWC postmortem cleanup that uses the same idempotent server teardown path. |
| **ACCEPT** | `f1f2d9ce` resume hint | Adopted the copy-friendly two-line hint while preserving the public `jwc --resume <id>` command required by the naming contract. |
| **COVERED** | `ebbf03c5` Python RPC drift | Synchronized `python/jwc-rpc` for `inherit`/`max`, passive `open_url` UI requests, and headless UI responses through `extension_ui_response` helpers. Broader upstream RPC changes remain outside this card's requested slice. |

The upstream `b55284c2` Telegram recent-formatting change is already covered by JWC's separate notification formatting/runtime work and requires no WP2 code change here.

## Verification expectation

Before closing this card, prove:

1. session teardown emits the expected close/postmortem event or an explicit JWC equivalent;
2. notification/recent formatting remains correct and JWC-branded;
3. Python RPC protocol/client fixtures are in sync with the JWC server protocol;
4. resume hints remain parseable and user-facing command names are `jwc`.

Suggested checks: focused notification/session tests and Python RPC tests under the JWC package path.
