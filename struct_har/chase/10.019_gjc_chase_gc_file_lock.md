# 10.019 — gjc chase: `gjc gc` + file-lock GC + harness adapter

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P2**
> Reviewed source: GJC `269387b` (`0cce55f6` #588, `251ea268` #618 TOCTOU) vs JWC `d60b7822`.
> **구현가치: 50 (MLB)** — 운영·관측; orchestrate receipt/harness 레인 **10.011** 이후.
> **따라갈 순서**: [02 priority #18](../../devlog/_plan/260614_chase_upstream_pull_priority_report/02_debt_priority_stack.md)

## 왜 gap인가

Upstream added a global garbage-collection command for stale file locks, tmux/session artifacts, and harness-adjacent stores, plus TOCTOU-hardened owner-token GC. Long-running jwc workflows (team, harness, multi-session) accumulate lock/orphan debris without an equivalent first-class `jwc gc` surface.

## Source facts

| fact | upstream evidence |
|------|-------------------|
| `gjc gc` command + `gc-runtime.ts`, `gc-render.ts` | `packages/coding-agent/src/commands/gc.ts`, `gjc-runtime/gc-runtime.ts` |
| `file-lock-gc.ts` + `withFileLock` integration | `config/file-lock-gc.ts`; tests `gc-*.test.ts`, `file-lock-gc-toctou.test.ts` |
| Harness `gc-adapter.ts` | `harness-control-plane/gc-adapter.ts` |
| Team/tmux GC helpers | `gjc-runtime/team-gc.ts`, `tmux-gc.ts` |

## JWC reconcile (2026-06-14)

| fact | jwc @ `d60b7822` |
|------|------------------|
| `withFileLock` | **✅** `packages/coding-agent/src/config/file-lock.ts` — settings, receipt-spool, star-reminder |
| `jwc gc` / `gc-runtime` | **⬜** — no `commands/gc.ts` (upstream `gjc gc` absent) |
| `file-lock-gc.ts` TOCTOU | **⬜** — no dedicated stale-lock sweeper |
| harness `gc-adapter` | **⬜** — compare after [10.011](./10.011_gjc_chase_receipt_spool.md) |

```typescript
// packages/coding-agent/src/config/file-lock.ts (excerpt)
export async function withFileLock<T>(
  filePath: string,
  fn: () => Promise<T>,
  options: FileLockOptions = {},
): Promise<T> { /* acquireLock → fn → release */ }
```

```typescript
// packages/coding-agent/src/harness-control-plane/receipt-spool.ts
import { withFileLock } from "../config/file-lock";
// spool append runs under withFileLock(spoolFile, …)
```

- Public surface: **`jwc gc`** when ported (not `gjc gc`); env/docs per [008](./008_gjc_jwc_naming_contract.md).
- Scope: port **lock GC + harness adapter** first; team/tmux GC only if same tmux contract as [10.007](./10.007_gjc_chase_team_profile_self_heal.md).

## 완료 기준

- [ ] `jwc gc` (or documented subset) runs without mutating user sessions unexpectedly.
- [ ] file-lock GC TOCTOU regression covered.
- [ ] Harness gc-adapter wired if receipt spool ([10.011](./10.011_gjc_chase_receipt_spool.md)) landed.