# 20.026 — omp chase: TUI loader re-arm · double-Esc · MCP enable sources

Status: active reference card

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · reference-only
> Reviewed source: OMP `ca9f2847e` (v16.2.5) → `b6c9747d4` (v16.2.9) vs JWC worktree `cfe71cd` (2026-07-01).
> Source range: `devlog/_omp_chase/oh-my-pi` `ca9f2847e..b6c9747d4` (175 commits); this card isolates the TUI input/loader/MCP-enable cluster. **1:1 port ❌ — design reference.**
> Naming: OMP `packages/tui`/`mcp.json` → JWC `packages/tui`/JWC mcp config per [008](./008_gjc_jwc_naming_contract.md).
> Neighbors: relates to [20.006](./_fin/20/20.006_omp_chase_tui_input_micro_fixes.md) (_fin) and [20.011](./_fin/20/20.011_omp_chase_tui_image_drafts_terminal_edges.md) (_fin).

## Why This Is A Gap

OMP v16.2.5→v16.2.9 fixed TUI input/loader edge cases and MCP enable-source alignment: buffered double-Esc is delivered as two events and the loader re-arms after task completion, the working loader is restored after a subagent ends inside an overlay window, MCP servers are force-enabled from tool-owned sources via `enabledServers`, alternate MCP config sources are re-enabled, a dashboard re-enable no longer flips `enabled:false` in `mcp.json`, `/extensions` MCP status is aligned with `/mcp list`, TUI tool-argument parsing/updates are throttled, and programmatic form submission via editor APIs is enabled. JWC's TUI surface can reference these for loader/Esc robustness and MCP status consistency.

Behavioral surface:
1. Buffered double-Esc delivered as two events; loader re-arms after task completion.
2. Restore working loader after subagent ends inside an overlay window.
3. Force-enable MCP from tool-owned sources via `enabledServers`; re-enable alternate config sources.
4. Dashboard re-enable must not flip `enabled:false` in `mcp.json`; align `/extensions` with `/mcp list`.
5. Throttle TUI tool-argument parsing/updates.
6. Programmatic form submission via editor APIs.

## Source Facts

| fact | upstream anchor |
|---|---|
| comparison range | `ca9f2847e..b6c9747d4` (v16.2.5→v16.2.9) |
| double-Esc two events + loader re-arm | `c2d6c8cf1`, `64734021a` fix(tui): deliver buffered double-Esc as two events and re-arm loader |
| loader after subagent overlay | `7871591b4` restored working loader after subagent ends inside an overlay window |
| force-enable MCP enabledServers | `e34f2a81e` fix(tui): force-enabled MCP from tool-owned sources via enabledServers |
| re-enable alternate MCP sources | `16ef3c54f` fix(tui): re-enabled MCP alternate config sources |
| dashboard re-enable mcp.json | `812b246e7` fix(tui): dashboard re-enable flips enabled:false in mcp.json |
| /extensions ~ /mcp list | `6256f97ea` fix(tui): aligned /extensions MCP status with /mcp list |
| tui arg parse throttle | `68285aa9d` fix(coding-agent): throttled tui tool argument parsing and updates |
| programmatic editor submit | `5cc84db4c` feat(tui): enabled programmatic form submission via editor APIs |
| primary path anchors | `packages/tui/src/`, `packages/coding-agent/src/modes/` (OMP) |

```bash
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show c2d6c8cf1 --stat
git -C /Users/jun/Developer/new/700_projects/jawcode/devlog/_omp_chase/oh-my-pi show e34f2a81e --stat
```

## JWC Reconcile Notes

| pressure | JWC posture |
|---|---|
| loader/Esc | Loader re-arm + double-Esc handling overlaps GJC TUI render resilience (10.060) and OMP 20.006 (_fin); cross-reference before adopting. |
| MCP enable | `enabledServers` + dashboard `mcp.json` consistency is a JWC MCP-config reference; evaluate against JWC's MCP enable/status surface (relates 20.024). |
| editor submit | Programmatic form submission via editor APIs overlaps GJC Ctrl+Enter submit (10.060); cross-reference. |
| throttle | TUI arg-parse throttling is a perf/UX reference. |
| naming | `mcp.json`/`/mcp`/`/extensions` command surface stays JWC-named. |
| source clone | Treat `devlog/_omp_chase/oh-my-pi` as read-only evidence; do not patch inside it. |

## Done Gate

- [ ] Source anchors rechecked against OMP `b6c9747d4` or a newer recorded head.
- [ ] Adopt/defer/reject decision recorded per feature (reference triage).
- [ ] Cross-checked against 10.060 (GJC TUI) + 20.006 (_fin) to avoid duplicate work.
- [ ] Naming translated per [008](./008_gjc_jwc_naming_contract.md).
- [ ] Any adopted behavior has JWC-specific tests or explicit manual evidence.

## Decision Slots

| slot | value |
|---|---|
| Decision A — classification | track-only (reference); MCP enable-source consistency is an adoption candidate |
| Decision B — naming impact | `/mcp`/`/extensions`/`mcp.json` command surface |
| Decision C — test impact | future focused TUI/MCP tests if adapted |
| Decision D — docs impact | chase only |
| Decision E — rollout impact | TUI loader + MCP status robustness if adopted |
| Decision F — residual risk | open reference card |
| Decision G — reviewer needed | frontend/TUI, backend (mcp) if promoted |
| Decision H — bundle policy | split per-feature; cross-ref 10.060 / 20.006 / 20.024 |

## Refresh — OMP v16.3.4 to v16.3.12

Source cluster: `d0c1890a6..f25ab54c5` (541 commits). TUI/MCP/input anchors: `5cdc525d0` accepts mid-prompt slash skill completions, `1fcdc4f88` preserves autocomplete for no-arg slash prompts, `ed63b6288` scopes autocomplete to slash command arguments, `6af3c8080` preserves description-matched skill completion, `10745dfc9` tightens slash autocomplete tests, `568226abb` disposes stale session UI renderers, `4f172fb27` relocates terminal restore registration to TUI init, `90b1b172f` guards selector cursor symbols, and `4a65d2443` corrects skill card header spacing.

JWC decision: still **reference/adapt candidate**. The new commits turn this card from loader/MCP-enable only into slash-skill completion correctness plus renderer lifecycle hygiene. Keep JWC TUI visual-protection rules intact.

JWC owner candidates: `packages/tui/src/components/editor.ts`, slash command autocomplete, skill picker rendering, session renderer lifecycle, and MCP status renderers.

Verification expectation: focused autocomplete tests for no-arg, mid-prompt, description-matched, and stale-token cases; renderer-disposal tests for session switching; visual smoke if UI layout changes.
