# 10.034 — gjc chase: state-writer and workflow invariants

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · **P1**
> Reviewed source: GJC `5ed80862` (`c34faca8`, `4d5099a6`, `35eaeb7d`, `0280edfc`, `e20f5ad9`, `f31f9434`) vs JWC `d60b7822` lineage.
> **구현가치: 60 (MLB)** — workflow-state correctness; semantics-only port because JWC owns native orchestrate.

## 왜 gap인가

GJC hardened state writer CAS/append/idempotency, workflow stop hooks, and ralplan/deep-interview finalization paths. JWC has native `jwc orchestrate`/`jaw-interview`/`.jwc` state, so direct file-level porting is wrong; the chase value is invariant mining: atomic JSONL append, stale-state rejection, idempotent append, and handoff/finalization guards.

## Source facts

| fact | upstream evidence |
|---|---|
| state writer | `packages/coding-agent/src/gjc-runtime/state-writer.ts` |
| state runtime | `packages/coding-agent/src/gjc-runtime/state-runtime.ts` |
| plan/interview runtimes | `gjc-runtime/ralplan-runtime.ts`, `deep-interview-runtime.ts` |
| hooks | `packages/coding-agent/src/hooks/skill-state.ts`, `skill-state/*` |
| tests | `test/gjc-runtime/state-writer-*`, `gjc-skill-state-hooks.test.ts` |

## JWC reconcile

| decision | note |
|---|---|
| naming | upstream `gjc-runtime` maps to JWC workflow/state runtime only by semantics |
| owner | native `jwc orchestrate` state file rules win over upstream ralplan shape |
| risk | state mutation semantics are C4-ish for workflows; audit before patch |

## 완료 기준

- [ ] Extract invariants as tests/specs against JWC `.jwc/state` runtime.
- [ ] No upstream `gjc-runtime` paths leak into public JWC docs.
- [ ] Workflow handoff/finalization remains atomic under concurrent commands.
