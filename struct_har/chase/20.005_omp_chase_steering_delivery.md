# 20.005 — omp chase: steering/queued follow-up 전달 보장

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜
> Reviewed source: OMP `db421bb2ef68` (`42ffc83 fix: re-polled steering after yield and drained queued follow-ups after turns`) vs JWC `ff0003db6ac0`.
> **구현가치: 60 (MLB)** — 참조 후 구현 후보. JWC는 **부분 보유** (followUp idle-path auto-continue는 자체 구현 존재, yield-boundary steering re-poll과 settle-시 drain은 부재).

## 왜 gap인가

steer/followUp 메시지가 **턴 종료 경계의 레이스**로 큐에 발이 묶여(stranded), 다음 수동 prompt까지 전달되지 않는 버그 클래스. OMP는 (1) agent loop의 yield 지점에서 steering을 재폴링하고, (2) 세션이 settle될 때 잔류 큐를 drain하며, (3) steer 경로의 이미지 정규화 await 사이에 턴이 끝난 경우를 미러링했다. JWC는 orchestrate/team이 steer·followUp 큐 전달에 의존하므로 동일 버그 클래스의 영향면이 upstream보다 넓다.

## Source facts

| fact | upstream evidence |
|---|---|
| yield 직전 `lateSteering` 재폴링 → `pendingMessages` 선두에 합류 | `devlog/_omp_chase/oh-my-pi/packages/agent/src/agent-loop.ts:818-826` (post-`42ffc83`) |
| `#drainStrandedQueuedMessages()` — `#promptInFlightCount === 0` settle 시 큐 잔류분 auto-continue 스케줄 | `devlog/_omp_chase/oh-my-pi/packages/coding-agent/src/session/agent-session.ts:1142,1147-1158` |
| steer 경로: 이미지 정규화 await 후 stale `isStreaming` 보정 — idle-path delivery 미러 | `agent-session.ts:5273-5280` |
| 회귀 테스트 159 라인 신설 | `packages/coding-agent/test/agent-session-queued-steer-delivery.test.ts` (`42ffc83` stat) |

## JWC 현황 (부분 보유)

- `packages/agent/src/agent-loop.ts:604` — `onBeforeYield` 후 aside/followUp drain은 있으나 **steering 재폴링 없음** (`lateSteering` grep 0건).
- `packages/coding-agent/src/session/agent-session.ts:5117-5121` — followUp 큐의 idle-path auto-continue(`#canAutoContinueForFollowUp` + `#scheduleAgentContinue`)는 **자체 구현 선행**. settle-시 stranded drain(`#drainStrandedQueuedMessages` 동등물)은 없음.
- GJC는 아직 이 omp 커밋을 안 받았다 (`lateSteering` gjc grep 0건) — gjc 경유로 들어올 가능성이 높으니 **gjc pull 시 충돌 주의** (jwc 자체 idle-path와 중복 스케줄 가능).

## 완료 기준

- [ ] JWC agent-loop yield 경계에 steering 재폴링 동등물 적용 여부 결정 (jwc 큐 모델 기준 재검토).
- [ ] settle-시 drain을 jwc `#canAutoContinueForFollowUp` 게이트와 통합 (이중 continue 방지).
- [ ] upstream 회귀 테스트를 jwc 큐 모델로 번안.
