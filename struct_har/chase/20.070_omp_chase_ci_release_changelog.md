# 20.070 — OMP chase: CI, release, changelog, and post-merge maintenance

> MOC: [20_omp_chase_MOC](./20_omp_chase_MOC.md) · G2 · ⬜ · P3 — Extract reproducible CI and release invariants without importing OMP version history or changelog policy.
> Reviewed source: OMP `7aa1d581c..b0d04e517` (`origin/main`, v16.4.2→v17.0.1) vs JWC worktree.

## Chase Scope

This card records the maintenance wave surrounding OMP 16.4.3 through 17.0.1: version bumps, changelog normalization after farm/union merges, stale fixture repair, test-run isolation, dependency patch refreshes, build-script fixes, and release metadata updates.

OMP's package versions and `VOUCHED` policy are historical evidence only. JWC publishes its own 1.1.2 package line, keeps changes under package-level `[Unreleased]` sections, and uses an OIDC-first release workflow with provenance. The relevant chase work is to compare CI failure modes and build/patch invariants, not to replay OMP's bumps or rewrite JWC changelogs.

- [ ] Keep OMP version, VOUCHED, formatting-only, and changelog-only commits reference-only.
- [ ] Evaluate slow-disk storage tests, settings reset, and UI/TUI test chunk sizing against JWC CI.
- [ ] Verify dependency patch mappings and binary/docs-index build tests after dependency upgrades.
- [ ] Preserve JWC `[Unreleased]` and OIDC trusted-publishing contracts.
- [ ] Do not touch current changelog/release files while this card remains evidence-only.

## Evidence Commands Run

```bash
for h in $(awk -F'|' '/^\| [0-9]+ \|/{print $3}' devlog/_plan/260717_chase_pull_delta_tracking/020_omp_D20_ci_release_changelog.md | tr -d ' `'); do
  git -C devlog/_omp_chase/oh-my-pi show --stat --oneline "$h"
  git -C devlog/_omp_chase/oh-my-pi show --name-only --format= "$h"
done
rg --files .github scripts packages/coding-agent/scripts structure packages | rg 'release|ci-test|CHANGELOG|package\.json|patch'
rg -n '\[Unreleased\]|OIDC|Trusted Publishing|--provenance|settings.*reset|chunk|slow.*disk|Bun\.build|patch' .github scripts packages structure/60_release_publishing.md
```

## Source Commit Anchors

| commit | source fact | source file anchors |
| --- | --- | --- |
| `9fd19696e` | Bumps the OMP workspace to 17.0.1. | root/package manifests; locks; native sentinel; changelogs |
| `2c355102c` | Applies Biome formatting to merged sources. | agent/AI/coding-agent/native/TUI tests and sources |
| `5e9d03f15` | Moves union-merge changelog entries back to the correct packages. | package changelogs |
| `80adf274f` | Records a CI-only rerun for a flaky clipboard check. | empty tree diff; CI rerun marker |
| `d5cd24f39` | Refreshes stale executor mock sessions and spawn-hint assertions. | task executor/guard/spawn tests |
| `d25d9300e` | Repairs tests after prompt and session API changes. | spawn policy; eval description; task tests |
| `1c9291b6e` | Bumps the OMP workspace to 17.0.0. | manifests; locks; changelogs; native sentinel |
| `c2a659df3` | Refreshes stale settings, repaint, and tool tests. | settings schema; preview/repaint/ast-grep tests |
| `34bfa9ad5` | Rewrites package changelogs for the release boundary. | agent/AI/coding-agent/collab/hashline/native/TUI/utils changelogs |
| `55f5ebec4` | Reformats Python, eval, metaharness, and research scripts. | Python scripts; metaharness; snapcompact research; RPC protocol |
| `7d02778c6` | Bumps the OMP workspace to 16.5.2. | manifests; locks; changelogs; native sentinel |
| `ff117fd10` | Cleans package changelogs. | agent/AI/catalog/coding-agent/native/TUI/utils changelogs |
| `f2b70eeed` | Normalizes changelogs after farm fixes. | agent/catalog/coding-agent/TUI/utils changelogs |
| `24a441e6b` | Normalizes another farm-fix changelog wave. | agent/AI/coding-agent/TUI changelogs |
| `14b5da76a` | Bumps the OMP workspace to 16.5.1. | manifests; locks; changelogs; native sentinel |
| `eccf030a7` | Condenses release changelog entries. | package changelogs |
| `7ad94e99a` | Normalizes AI, coding-agent, and utils changelog entries. | package changelogs |
| `a3960bb4e` | Bumps the OMP workspace to 16.5.0. | manifests; locks; changelogs; native sentinel |
| `c69c04836` | Resets settings and shrinks UI/TUI test chunks for CI stability. | `scripts/ci-test-ts.ts`; double-render repro test |
| `883e68f2d` | Updates dependencies and refreshes patch mappings. | `bun.lock`; root manifest; ACP/Ark patches |
| `6fcb1b300` | Synchronizes expectations with output and cleanup formatting. | hashline/status/launch tests; context-switch prompt |
| `01d3fc9b6` | Bumps the OMP workspace to 16.4.8. | manifests; locks; coding-agent changelog |
| `f933f02fc` | Bumps the OMP workspace to 16.4.7. | manifests; locks; coding-agent/stats/TUI changelogs |
| `b1c882e89` | Updates the OMP VOUCHED list. | `.github/VOUCHED.td` |
| `20c0a2e41` | Adapts storage tests to schema v6 and slow CI disks. | `session/agent-storage.ts`; storage perf/SQLite tests |
| `12466ecf4` | Bumps the OMP workspace to 16.4.6. | manifests; locks; changelogs; native sentinel |
| `3d1f9a4a3` | Bumps the OMP workspace to 16.4.5. | manifests; locks; changelogs; native sentinel |
| `a643e9446` | Removes redundant native, SDK, and web-search tests. | native grep; SDK binding tests; web-search header test |
| `d618577da` | Updates package changelogs. | agent/AI/coding-agent/native/TUI changelogs |
| `d39a3ed45` | Repairs stale task/controller tests. | TAN command controller; async task fallback tests |
| `74be4d5f6` | Applies Biome formatting to advisor sources. | advisor runtime/test |
| `0835cde93` | Adds the advisor staleness changelog entry. | coding-agent changelog |
| `5c56144f6` | Updates the OMP VOUCHED list. | `.github/VOUCHED.td` |
| `cf1b3fc3f` | Bumps the OMP workspace to 16.4.4. | manifests; locks; changelogs; native sentinel |
| `6bc4302f6` | Updates coding-agent and natives changelogs. | package changelogs |
| `c893e7ab7` | Backs out a prematurely placed AI changelog change. | AI changelog; auth-storage persistence test |
| `82645c5a6` | Records the E2BIG bundle fix in the 16.4.3 changelog. | coding-agent changelog |
| `f7930048d` | Bumps the OMP workspace to 16.4.3. | manifests; locks; changelogs; native sentinel |
| `d469064d1` | Repairs stale compile, plugin, docs-index, search, and RPC tests. | compile binary; legacy plugin compat; docs index; search tests; robomp test |
| `529effac1` | Adds a contributor to the OMP VOUCHED list. | `.github/VOUCHED.td` |
| `056fc5f69` | Updates package changelogs. | agent/AI/catalog/coding-agent/native changelogs |

## Worktree Verification

| probe | result | JWC evidence / implication |
| --- | --- | --- |
| Package version line | **different** | JWC public packages inspected are `1.1.2`; OMP 16.x/17.x version commits are not portable. |
| Changelog contract | **exists/different** | Agent, AI, coding-agent, natives, TUI, utils, stats, and bridge-client changelogs begin with `[Unreleased]`; `scripts/release.ts` promotes that section. |
| Release workflow | **exists and canonical** | `.github/workflows/release.yml`, `scripts/ci-release-publish.ts`, and `structure/60_release_publishing.md` define OIDC trusted publishing and provenance. |
| OMP CI test runner | **missing/different** | JWC has no `scripts/ci-test-ts.ts`; settings reset/chunk-size changes require mapping to current CI jobs before adaptation. |
| OMP dist bundler | **missing/different** | JWC has no `packages/coding-agent/scripts/bundle-dist.ts`; binary builds use `build-binary.ts` and shared release scripts. |
| Slow-disk storage owner | **exists** | JWC has session storage and tests, but OMP schema-v6 timing constants should only be adopted after reproducing CI latency. |
| Dependency patches | **exists** | JWC has root dependency/patch state and release validation. Refresh patch mappings with the dependency that consumes them, never as isolated churn. |
| Native release sentinel | **exists/different** | JWC documents a native ABI sentinel in `structure/60_release_publishing.md` and `packages/natives`; package bump and ABI bump are intentionally not identical. |
| VOUCHED policy | **not applicable** | OMP's `.github/VOUCHED.td` changes carry no JWC runtime/release behavior. |
| Current dirty release/changelog files | **preserve** | This task is card-only; no package changelog, manifest, workflow, or release script is modified. |

## Decision Log (1-8 TBD)

| # | status | decision to resolve |
| --- | --- | --- |
| 1 | **TBD** | Reject all OMP version-bump and VOUCHED commits as direct ports. |
| 2 | **TBD** | Keep changelog-only and formatting-only commits as historical release evidence. |
| 3 | **TBD** | Reproduce slow-disk storage failures before adapting timeout/perf fixtures. |
| 4 | **TBD** | Map settings reset and test chunk sizing to JWC's actual CI runner. |
| 5 | **TBD** | Refresh dependency patches only with their owning package upgrade and focused tests. |
| 6 | **TBD** | Port stale compile/plugin/docs-index test fixes only where JWC reproduces the API drift. |
| 7 | **TBD** | Preserve JWC `[Unreleased]`, native ABI sentinel, and OIDC provenance contracts. |
| 8 | **TBD** | Evidence-fill a JWC release checklist rather than replaying OMP changelog rewrites. |
