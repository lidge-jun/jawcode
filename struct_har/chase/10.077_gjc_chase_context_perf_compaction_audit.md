# 10.077 — gjc chase: context/perf audit, compaction, prompt lifecycle

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · P1
> Reviewed source: GJC `db7938e1..b3b5b8a9` (`upstream/dev`, v0.7.11→v0.9.1) vs JWC worktree.
> Naming: upstream `gjc` → `jwc` per [008](./008_gjc_jwc_naming_contract.md).

## Why This Card Exists

GJC landed three large context/performance tracks in the chase range: initial-context
tool-discovery shrink, a prompt/hook lifecycle audit, and a token/context-window audit.
It also patched compaction overflow recovery and hardened cache correctness after review.
JWC already has some of these concepts, so the work is a reconciliation audit rather than
a blind import.

## Interview Decision (2026-07-09)

> **Classification**: import/adapt all 13 findings.
> **Execution order**: immediately after model/provider and before session/skill/TUI/Telegram in the sequence: model/provider -> context/perf -> session -> skill -> TUI -> Telegram.
> **Sub-card split needed**: yes — split tool discovery shrink, overflow guard/compaction, cache hardening, prompt lifecycle, file mentions/output spill, and session/RPC perf where needed.
> **Implementation approach**: map every finding to JWC owners and implement/adapt tool discovery shrink, overflow guard, cache hardening, and prompt lifecycle work with focused tests.
> **OPEN ASSUMPTIONS**: status stays open (`⬜`) because implementation is still required; upstream path names are evidence anchors, not direct JWC owner names.

## Implementation Evidence (2026-07-09)

| patch | file | verification |
|---|---|---|
| compaction overflow guard | `packages/agent/src/compaction/compaction.ts` | implementation present in worktree |
| print-mode overflow helpers | `packages/coding-agent/src/modes/print-mode.ts` | implementation present in worktree |

Tool discovery shrink and cache hardening are still pending, so this card stays active (`⬜`) and must not move to `_fin`.

## Source Commands Run

- `git -C devlog/_gjc_chase/gajae-code log --oneline --stat db7938e1..b3b5b8a9 -- packages/agent/src/compaction packages/coding-agent/src/session packages/coding-agent/src/context packages/coding-agent/src/prompts`
- `git -C devlog/_gjc_chase/gajae-code show --stat da4a59ea c4550937 a20c15a5 c259cd34 64d1d2da 98cb3352`
- `rg -n "checkEstimatedContext|toolDiscovery|summarizationInput|dashboardRender|inoKeyedDrift" packages/agent/src/ packages/coding-agent/src/ --type ts 2>/dev/null | head -30`
- JWC file-presence and broader term checks for compaction, tool discovery, file mentions, cache hardening, and prompt lifecycle owners.

## Per-Commit Anchors

| commit | upstream slice | concrete file anchors |
|---|---|---|
| `da4a59ea` | 13-finding token/context-window efficiency audit | `packages/agent/src/compaction/compaction.ts`; `packages/agent/src/compaction/pruning.ts`; `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/session/file-mention-pruning.ts`; `packages/coding-agent/src/prompts/system/volatile-project-context.md`; `packages/coding-agent/src/system-prompt.ts`; `packages/coding-agent/src/tools/output-meta.ts`; `packages/coding-agent/src/task/token-log.ts`; `packages/coding-agent/src/config/settings-schema.ts`; `schemas/config.schema.json` |
| `c4550937` | runtime performance audit, including session pipeline and compact RPC message updates | `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/modes/shared/agent-wire/event-envelope.ts`; `packages/coding-agent/src/modes/rpc/rpc-mode.ts`; `packages/coding-agent/src/modes/rpc/rpc-types.ts`; `packages/coding-agent/src/gjc-runtime/session-state-sidecar.ts`; `packages/coding-agent/src/modes/components/status-line.ts`; TUI/natives anchors covered in 10.076 |
| `a20c15a5` | prompt/hook lifecycle audit findings 1-13 | `packages/agent/src/agent-loop.ts`; `packages/coding-agent/src/extensibility/extensions/runner.ts`; `packages/coding-agent/src/extensibility/gjc-plugins/runtime-adapters.ts`; `packages/coding-agent/src/gjc-runtime/session-state-sidecar.ts`; `packages/coding-agent/src/gjc-runtime/team-runtime.ts`; `packages/coding-agent/src/hooks/native-skill-hook.ts`; `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/session/session-manager.ts`; `packages/coding-agent/src/skill-state/active-state.ts` |
| `c259cd34` | default tool discovery to shrink initial context under 20K | `packages/coding-agent/src/config/settings-schema.ts`; `packages/coding-agent/src/prompts/system/system-prompt.md`; `packages/coding-agent/src/sdk.ts`; `packages/coding-agent/src/system-prompt.ts`; `packages/coding-agent/src/tools/index.ts`; `docs/tools/search_tool_bm25.md`; `schemas/config.schema.json` |
| `64d1d2da` | bound summarization request input so overflow recovery can compact-and-continue | `packages/agent/src/compaction/compaction.ts`; `packages/agent/test/compaction-summary-input-bound.test.ts` |
| `98cb3352` | harden #1722 caches with inode-keyed drift check, bounded hash memo, suffix-equivalence test | `packages/agent/src/agent-loop.ts`; `packages/agent/test/agent-loop-context-cache.test.ts`; `packages/coding-agent/src/extensibility/gjc-plugins/runtime-adapters.ts` |

## Additional Log Anchors From The Same Range

- `9f6b1524`: avoid repeated oversized auto-maintenance attempts in `packages/coding-agent/src/session/agent-session.ts`.
- `dd020259`: reuse overflow detector for maintenance guard in `packages/coding-agent/src/session/agent-session.ts`.
- `40a3636a`: stabilize sharded coding-agent dev tests by touching `packages/coding-agent/src/prompts/tools/search-tool-bm25.md` and `packages/coding-agent/src/session/agent-session.ts`.
- `d3bc0f89`: runtime skill discovery prompt file `packages/coding-agent/src/prompts/tools/skill-discovery.md`.
- `7647a731`, `c3ed7551`: prompt cleanups across agents and tool docs under `packages/coding-agent/src/prompts/...`.
- `074f923b`: long-form media ingestion fallback guard in `packages/coding-agent/src/prompts/system/system-prompt.md`; keep linked to 10.076 media notes.

## JWC Worktree Verification

| checked item | JWC result | implication |
|---|---|---|
| `packages/coding-agent/src/session/agent-session.ts` | exists; requested rg found `#checkEstimatedContextBeforePrompt` at lines around 5116 and 6982 | overflow/maintenance guard concepts already exist; compare behavior, not just file presence |
| `packages/coding-agent/src/tools/index.ts` | exists; has `DEFAULT_ESSENTIAL_TOOL_NAMES` and discovery-mode handling | `c259cd34` may already be partly ported |
| `packages/coding-agent/src/tools/search-tool-bm25.ts` | exists; mentions `tools.discoveryMode` and discovery activation | discovery shrink likely has JWC-local implementation |
| `packages/coding-agent/src/config/settings-schema.ts` | exists; includes `tools.discoveryMode`, `mcp.discoveryMode`, and compaction settings | schema diffs require careful generated-schema treatment if changed later |
| `packages/agent/src/compaction/compaction.ts` | exists; includes `keepRecentTokens`, local/remote summarization, and summary call sites | bounded summarization input still needs semantic diff |
| `packages/agent/src/compaction/pruning.ts` | exists | maintenance pruning concepts have an owner |
| `packages/coding-agent/src/session/file-mention-pruning.ts` | missing | GJC file-mention stale-pruning helper is not present as a separate owner |
| `packages/coding-agent/src/prompts/system/volatile-project-context.md` | missing | volatile-context relocation is not present as that prompt fragment |
| `packages/coding-agent/src/system-prompt.ts` | exists | prompt composition changes belong here and in static markdown prompts |
| `packages/coding-agent/src/extensibility/gjc-plugins/runtime-adapters.ts` | missing | cache-hardening commit targets an upstream path that JWC no longer has under that name |
| `packages/coding-agent/src/extensibility/extensions/runner.ts` | exists | extension runner perf work has a JWC owner |
| `packages/agent/src/agent-loop.ts` | exists; append-only context comments are present | append-only context cache may already be partially reconciled |

The requested rg found only `checkEstimatedContext` in JWC, not `toolDiscovery`,
`summarizationInput`, `dashboardRender`, or `inoKeyedDrift` under the exact search terms.
A broader search shows discovery-mode, append-only context, file-mention, and summarization
owners, which means the exact upstream symbol names are not reliable import checks.

## Owner Files

- Context/LLM loop conversion: `packages/agent/src/agent-loop.ts`; `packages/agent/src/append-only-context.ts`.
- Compaction and pruning: `packages/agent/src/compaction/compaction.ts`; `packages/agent/src/compaction/pruning.ts`; `packages/agent/src/compaction/utils.ts`.
- Prompt composition: `packages/coding-agent/src/system-prompt.ts`; `packages/coding-agent/src/prompts/system/system-prompt.md`; `packages/coding-agent/src/prompts/system/subagent-system-prompt.md`; any new fragment under `packages/coding-agent/src/prompts/system/`.
- Session context pressure and overflow guard: `packages/coding-agent/src/session/agent-session.ts`; `packages/coding-agent/src/session/messages.ts`.
- Tool discovery shrink: `packages/coding-agent/src/tools/index.ts`; `packages/coding-agent/src/tools/search-tool-bm25.ts`; `packages/coding-agent/src/tool-discovery/tool-index.ts`; `packages/coding-agent/src/sdk.ts`; `packages/coding-agent/src/config/settings-schema.ts`.
- File mentions and output spill: `packages/coding-agent/src/utils/file-mentions.ts`; `packages/coding-agent/src/tools/output-meta.ts`; possible new `packages/coding-agent/src/session/file-mention-pruning.ts`.
- Prompt/hook lifecycle caches: `packages/coding-agent/src/extensibility/extensions/runner.ts`; `packages/coding-agent/src/hooks/native-skill-hook.ts`; `packages/coding-agent/src/skill-state/active-state.ts`; `packages/coding-agent/src/gjc-runtime/session-state-sidecar.ts`.

## Import / Adapt / Reject Decisions

| slice | decision | rationale |
|---|---|---|
| Tool discovery default shrink (`c259cd34`) | import/adapt | JWC already has `tools.discoveryMode`, search-tool BM25, and essential tool handling; import missing prompt/schema/test deltas after semantic comparison |
| Bounded summary request input (`64d1d2da`) | import/adapt | reliability fix for overflow recovery; low product-identity risk, but must compare existing summary call sites |
| Keep-recent correction and maintenance pruning (`da4a59ea`) | adapt | touches token accounting, pruning gates, and settings; preserve JWC defaults unless evidence justifies a flip |
| Volatile project context relocation (`da4a59ea`) | import/adapt | JWC lacks `volatile-project-context.md`; adding or mapping the fragment changes prompt composition and cache prefix behavior |
| File mention inline cap/pruning (`da4a59ea`) | adapt | JWC has file mentions but lacks separate pruning owner; avoid silent truncation unless user-visible behavior is tested |
| Read artifact spill/output-meta (`da4a59ea`) | adapt | valuable for token budget, but changes tool result surfacing and artifact policy |
| Task token log/live-runner fixture (`da4a59ea`) | import/adapt | useful instrumentation; map to JWC runtime/test owners with a focused value check |
| Prompt/hook lifecycle caches (`a20c15a5`) | split/adapt | large multi-subsystem perf patch; cache correctness must be proven per owner |
| Inode-keyed plugin drift hardening (`98cb3352`) | adapt invariant to JWC owner | upstream path `gjc-plugins/runtime-adapters.ts` is missing in JWC; port the cache-hardening invariant to the equivalent JWC extension/plugin registry code |
| Agent-loop suffix conversion cache (`a20c15a5` + `98cb3352`) | import/adapt | JWC append-only context exists; import only after suffix-equivalence coverage proves the mapped behavior |
| Runtime perf session pipeline from `c4550937` | split/adapt | session message pipeline and compact RPC are context/perf work; TUI/natives parts stay in 10.076 |
| Prompt cleanup commits (`7647a731`, `c3ed7551`) | adapt with manual review | prompt text affects JWC identity; never bulk-replace upstream wording |

## Concrete Gap Questions For Promotion

- Does JWC already default `tools.discoveryMode` to the desired mode, and do generated schemas match?
- Does `generateSummary` or equivalent local summary construction bound serialized conversation input before backend calls?
- Are file mentions pruned or capped after stale references, duplicate mentions, and oversized content?
- Does append-only context conversion have a suffix-equivalence test across tool-call/tool-result group boundaries?
- Are extension/skill/native hook caches invalidated by mtime/size/inode or the JWC-equivalent file identity?
- Does the estimated-context guard avoid repeated oversized maintenance attempts after a failed compaction?

## Verification Plan If Promoted

- Run focused compaction tests, especially summary input bounds and keep-recent correction.
- Run system prompt template tests after any discovery/volatile-context prompt change.
- Run tool-discovery tests for default mode, essential tools, and BM25 activation.
- Run agent-loop context-cache tests or add suffix-equivalence coverage before importing cache changes.
- Run session tests around `#checkEstimatedContextBeforePrompt` and oversized maintenance signatures.
- Run generated schema checks if `settings-schema.ts` or config defaults change.

## Residual Risk

The major risk is changing model-visible prompt and tool context in a way that improves
token count but regresses JWC's product behavior. Prefer evidence gates: token delta,
prompt snapshot diff, and behavioral tests. Treat upstream defaults as candidates, not
automatic JWC defaults.

## Jawdev chase expansion

- Expanded from a short context/perf gap stub into a per-commit, file-anchored audit card.
- Evidence base: required upstream log/show commands plus JWC rg/file-presence verification.
- Next action: promote bounded compaction and discovery reconciliation first; leave broad cache/prompt lifecycle work split by owner.
