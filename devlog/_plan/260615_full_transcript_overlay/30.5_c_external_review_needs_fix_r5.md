# 30.5 C — external review NEEDS_FIX r5

## Reviewer

`agbrowse web-ai query --vendor chatgpt --model pro --effort standard --new-tab`

- Conversation: https://chatgpt.com/c/6a2fef2f-f558-83ee-8aa1-095c73d1e634
- Session: `01KV5KW9ETDSHF26B5S5DTJR6T`
- Package: `artifacts/ctrl-t-full-transcript-review-c81b5466.zip`
- Pushed branch HEAD reviewed: `c81b54664e9b20e9717ece7274c13fe0bfb7e63c`

## Verdict

`NEEDS_FIX`

## Finding

The stale hidden-thinking fix was accepted, but a separate bash/eval footer leak remained:

- Historical `bashExecution` and `pythonExecution` replay creates `BashExecutionComponent` and `EvalExecutionComponent` instances.
- `renderFullTranscript()` expands those components so all stored output lines render.
- `#updateDisplay()` still computed `hiddenLineCount` from preview-line truncation and passed a positive count to `buildStatusFooter()` while expanded.
- Ctrl+T could therefore show all historical bash/python lines while still displaying a false `… N more lines (ctrl+o to expand)` footer.

## Decision

Route C → B as an implementation bug. Patch bash/eval footer arguments so expanded/full-transcript rendering reports zero hidden lines. Add regression assertions to the long completed bash/python replay test to reject `ctrl+o to expand` and `… N more lines` in full transcript output.