# 20.6 A — Synthesis r1 after delta FAIL

## Runtime state

A-stage audit status is `fail (dual, round 3, a-r1)`. The runtime reports the round cap reached and requires escalation or explicit user-approved override.

## Remaining findings accepted

- PLANNER-A4 / ARCH-A10 — accepted. Remove the split-file option entirely; the persisted test will be added to `packages/coding-agent/test/input-controller-keybindings.test.ts`, so the focused command is stable.
- PLANNER-A7 — accepted. Use one same-test ordered render for live duplicate proof; do not allow a sibling test variant.
- PLANNER-A8 — accepted. Add required manual checklist items for Ctrl+O scope, Ctrl+T historical visibility, `g`/`G`, and editor focus after close.
- PLANNER-A3 / ARCH-A1 — accepted. Add exact harness ordering: create context, install SESSION_DEPS stubs, create controller, setup key handlers, trigger full transcript, extract overlay.
- PLANNER-A11 — accepted. Add explicit C exit checklist.
- Step 1 placeholder tool results / ARCH-A8 — accepted. Replace pseudo-code tool result placeholders with concrete `role: "toolResult"` blocks.
- ARCH-A5 — accepted. The assistant `toolCall` fixture for `bash`/`eval` replays through `ToolExecutionComponent`, not `BashExecutionComponent`/`EvalExecutionComponent`; retarget failures to `ToolExecutionComponent`/tool renderer path. Dedicated `bashExecution`/`pythonExecution` role fixtures are out of scope unless the assistant-tool fixture passes but a role-specific regression is reproduced.
- ARCH-A9 — accepted. No split file; no private helper extraction needed.

## Revised plan changes

- `10_p_reopen_historical_expansion_plan.md` now uses only `packages/coding-agent/test/input-controller-keybindings.test.ts`.
- It contains concrete toolResult blocks for generic/read/bash/eval markers.
- It pins harness order and SESSION_DEPS stubs.
- It retargets bash/eval assistant toolCall failures to `ToolExecutionComponent`/tool renderer path.
- It adds manual checklist and C/D gates.

## Residual risk / escalation

The plan is now implementation-ready from the main session perspective, but the runtime A-stage remains fail due round cap. Proceeding to B requires explicit user-approved override per `jwc orchestrate status` guidance, or else the goal must pause at this PABCD gate.
