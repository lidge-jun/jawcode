# 30.4 C — external review NEEDS_FIX r4

## Reviewer

`agbrowse web-ai query --vendor chatgpt --model pro --effort standard --new-tab`

- Conversation: https://chatgpt.com/c/6a2feb5c-e2e4-83ee-95a5-97645540319a
- Session: `01KV5JYK8QBS3HEQGB2SD5FHMY`
- Package: `artifacts/ctrl-t-full-transcript-review-70bf8c35.zip`
- Pushed branch HEAD reviewed: `70bf8c352018662b0ef9bb37901e2bdb04f9c4fe`
- Ctrl+T fix commit in branch: `1038fdd8`

## Verdict

`NEEDS_FIX`

## Finding

The hidden-thinking patch still had a stale placeholder edge:

1. Ctrl+O or another path can set `AssistantMessageComponent.#thinkingExpanded=true` while `hideThinkingBlock=true`.
2. Normal render still shows `Thinking...` because hidden-thinking wins.
3. Ctrl+T `renderFullTranscript()` changed `hideThinkingBlock=false` then called `setThinkingExpanded(true)`.
4. Because `#thinkingExpanded` was already true, `setThinkingExpanded(true)` returned early and did not rebuild content.
5. Ctrl+T could therefore render the previously built `Thinking...` placeholder instead of the actual thinking body.

## Decision

Route C → B as an implementation bug. Patch `renderFullTranscript()` to set both state fields directly and call `updateContent()` after forcing full-transcript state, then restore both fields and rebuild content in `finally`. Add a regression where hidden-thinking is already expanded before Ctrl+T.