DONE
- Read-only verifier inspected `packages/coding-agent/test/input-controller-keybindings.test.ts`, `10_p_reopen_historical_expansion_plan.md`, and `20.7_b_persisted_regression_pass.md`.
- Verified persisted reopened-session Ctrl+T regression exists and uses `SessionManager.open` with `MemorySessionStorage`.
- Verified historical thinking plus generic/read/bash/eval assistant tool-call result markers are asserted in full transcript output.
- Verified collapsed indicators are absent, live chat is deduped, live tool remains included, and `g`/`G` scroll checks exist.
- Verified `20.7_b_persisted_regression_pass.md` records source-green result, focused test evidence, branch SHA, and behavior coverage.
- Minor note: plan example names `OLD_EVAL_LINE_19` as `G` tail marker, while implementation uses actual bottom marker `OLD_READ_LINE_19`; all full-render markers remain asserted in large viewport output.
