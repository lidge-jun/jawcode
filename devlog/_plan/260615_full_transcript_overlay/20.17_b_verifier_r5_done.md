# 20.17 B — verifier r5 DONE

Read-only verifier: `14-StaleThinkingVerifier`

## Verdict

DONE

## Evidence

Verifier confirmed:

- `AssistantMessageComponent.renderFullTranscript()` sets `hideThinkingBlock=false` and `#thinkingExpanded=true` directly, then calls `updateContent(this.#lastMessage)` before rendering.
- This forces a content rebuild even when `#thinkingExpanded` was already true, preventing stale `Thinking...` placeholder reuse.
- The `finally` block restores both hidden and expanded state and rebuilds content again.
- Normal `updateContent()` still shows `Thinking...` when `hideThinkingBlock=true`, so the override is scoped to Ctrl+T/full transcript rendering.
- `full-transcript-overlay.test.ts` includes the edge regression `shows hidden live thinking in full transcript even when already expanded`, covering pre-overlay hidden placeholder, Ctrl+T actual thinking output, and post-overlay hidden-state restoration.
- Adjacent tests still cover collapsed live hidden thinking and historical hidden thinking restoration.
- No obvious syntax/import mismatch found by read-only inspection.