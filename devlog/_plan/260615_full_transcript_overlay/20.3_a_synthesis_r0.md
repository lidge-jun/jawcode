# 20.3 A — Synthesis r0

## Audit state

- Planner lens: FAIL (`20.1_a_planner_audit_r0*.md`)
- Architect lens: FAIL (`20.2_a_architect_audit_r0*.md`)
- Runtime status after corrected parser receipts: `Audit: fail (dual, round 1, a-r0)`

## Shared root cause

The plan correctly moved toward a persisted-session proof, but the proof was still too narrow and not scroll-aware. It could incorrectly route a real viewport/source bug into a delivery-only branch. The plan also left optional read/bash/eval patches without red fixtures and did not define B/C/D evidence artifacts tightly enough.

## Decisions by finding

### Planner findings

- PLANNER-A1 — accepted. Add scroll-controlled assertions: a large viewport expansion check plus a small viewport `g`/`G` visibility check.
- PLANNER-A2 — accepted. Extend the persisted fixture to include read, bash, and eval historical tool results in addition to thinking + generic fallback. Patches for those components are gated on their exact marker failing.
- PLANNER-A3 — accepted. Pin the harness to inject `reopened.buildVisibleTranscriptContext()` as the visible context; explicitly mark obfuscated-session parity out of scope unless a second fixture is added.
- PLANNER-A4 — accepted. Target file is `packages/coding-agent/test/input-controller-keybindings.test.ts`; split threshold is >80 added lines or duplicated setup, in which case create `packages/coding-agent/test/input-controller-persisted-full-transcript.test.ts`.
- PLANNER-A5 — accepted. Add explicit devlog B/C/D artifact paths and required evidence.
- PLANNER-A6 — accepted. Add A transition rules: A PASS → B; A FAIL → synthesize/revise/delta audit; round cap 3.
- PLANNER-A7 — accepted. Add a second live duplicate fixture block or remove conditional assertion. Revised plan includes an explicit live duplicate block.
- PLANNER-A8 — accepted. Mark manual steps as blocking only when automated proof is impossible; otherwise tie to named tests/artifacts.
- PLANNER-A9 — accepted. Bind overlay UX change to `20.6_b_manual_overlay_check.md` evidence before changing bottom-pin behavior.
- PLANNER-A10 — accepted. Add two zip manifests: delivery-only and patch path.
- PLANNER-A11 — accepted. Add C gate checklist and D summary artifact paths.

### Architect findings

- ARCH-A1 — accepted. Reuse the SESSION_DEPS harness block from `input-controller-keybindings.test.ts` (settingsGet, getToolByName, extensionRunner/getMessageRenderer, getUserMessageText) in the persisted test.
- ARCH-A2 — accepted. Use `overlay.setOverlayViewportRows()` and `overlay.handleInput('g')` / `overlay.handleInput('G')` before visibility assertions.
- ARCH-A3 — accepted with scope statement. The test injects `reopened.buildVisibleTranscriptContext()` as visible context; obfuscation/deobfuscation parity is out of scope for this bug unless a separate red obfuscation fixture is discovered.
- ARCH-A4 — accepted. Add explicit `sessionFile` and inline zeroed usage literal in plan snippet.
- ARCH-A5 — accepted. Bash/eval red path will specify fullTranscript line-cap bypass mirroring ToolExecutionComponent, not merely `setExpanded(true)`.
- ARCH-A6 — accepted. Read patches are gated on missing `OLD_READ_LINE_*` rendered content after full transcript rendering.
- ARCH-A7 — accepted. List required imports.

## Plan sections changed

- Rewrote Step 1 fixture to include thinking, generic, read, bash, eval, and live duplicate markers.
- Rewrote harness section with exact file/import/dependency instructions.
- Rewrote assertions into expansion proof, scroll proof, and live duplicate proof.
- Rewrote red-path patch matrix.
- Added B-stage devlog evidence artifacts, C gate checklist, D summary path, and two zip manifests.

## Residual risk

The external `agbrowse gpt pro` surface may not be authenticated or scriptable. The revised plan requires producing a zip path and exact upload prompt when automated submission is blocked.
