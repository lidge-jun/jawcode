DONE
- Read-only verifier inspected external review fixes and reported DONE.
- Verified read groups are inserted at read tool-call time and later updated from the result.
- Verified image-only read placeholders are deleted and emptied read groups removed from replay items.
- Verified `bashExecution` and `pythonExecution` replay use `setComplete(..., { output, truncation })` directly.
- Verified full transcript overlay no longer returns width-only cached lines.
- Verified generic JSON full transcript mode uses large finite limits for depth, lines, and scalar length.
- Verified regressions cover read ordering, image-only read group removal, completed bash/python output, same-width overlay rerender, generic JSON depth/scalar output, and Ctrl+T persisted order/tail assertions.
