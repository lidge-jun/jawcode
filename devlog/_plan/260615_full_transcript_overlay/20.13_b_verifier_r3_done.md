DONE
- Read-only verifier confirmed `session-transcript-replay.ts` resets `readGroup = null` after visible assistant text/thinking segments.
- Confirmed regression `starts a new read group across visible assistant text boundaries` asserts `src/a.ts < BETWEEN_READS < src/b.ts` and `READ_A_BODY < BETWEEN_READS < READ_B_BODY`.
- Confirmed `30.2_c_external_review_needs_fix_r2.md` and `20.12_b_read_boundary_patch.md` record the external finding, implementation, and verification evidence.
- No obvious syntax/import mismatch found.
