# 10.1 P — Critic synthesis round 1

## Critic verdict

ITERATE.

## Findings accepted

1. **Baseline confusion — accepted.**
   - The draft listed already-landed changes (`sessionLiveItems` dedupe, `ToolExecutionComponent#fullTranscript`, `readToolResultPreview: true`, overlay height protocol) as if they were pending work.
   - Root cause: plan was written from the full investigation history instead of separating current tree state from remaining gap.

2. **Input-controller pseudocode too vague — accepted.**
   - `collectOnlyOutOfBandLiveItems` was not an existing helper and was not defined as a new helper.
   - The plan must either specify the exact inline array expression currently present or list a concrete new helper signature.

3. **Persisted-session proof too vague — accepted.**
   - This is the key remaining proof gap. Existing tests verify synthetic `SessionContext` and direct components, not a reopened JSONL session flow.
   - `packages/coding-agent/test/session-open-single-load.test.ts` provides an existing `MemorySessionStorage`/`SessionManager.open` pattern that can be reused for a concrete regression.

4. **Existing tests not distinguished — accepted.**
   - The plan now must mark existing coverage and only add deltas.

5. **Overlay navigation and conditional audits underspecified — accepted.**
   - `g` top navigation already has tests. Remaining gap is not generic top navigation but proving persisted historical markers are present and reachable in a Ctrl+T overlay output.
   - Bash/eval/session replay audits must be red-driven: add/adjust only if the persisted regression shows a cap/collapse.

## Plan changes

- Rewrote `10_p_reopen_historical_expansion_plan.md` around:
  - Current baseline in tree.
  - Remaining deltas only.
  - Concrete persisted-session regression test file and fixture shape.
  - Red-driven scope for optional bash/eval/session replay changes.
  - Push/external review steps after P/A execution approval, not during P.

## Decision

Accept all critic findings. Patch plan and rerun Critic against revised plan.
