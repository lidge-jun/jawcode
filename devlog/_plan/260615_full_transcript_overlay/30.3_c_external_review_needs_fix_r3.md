# 30.3 C — external review NEEDS_FIX r3

## Reviewer

`agbrowse web-ai query --vendor chatgpt --model pro --effort standard --new-tab`

- Conversation: https://chatgpt.com/c/6a2fe645-7f84-83e8-a95f-85e16fc2396d
- Session: `01KV5HP6TKF8HT98SZJYKTVJGQ`
- Package: `artifacts/ctrl-t-full-transcript-review-483e1a11.zip`
- Commit reviewed: `483e1a1134ab2a3daf8b697ad1c26ccaa887662f`

## Verdict

`NEEDS_FIX`

## Finding

Ctrl+T full transcript could still hide real thinking content when normal UI `hideThinkingBlock` was enabled.

Concrete path:

- `input-controller.ts` passed `this.ctx.hideThinkingBlock` into historical replay.
- `session-transcript-replay.ts` created historical `AssistantMessageComponent` instances with that flag.
- `AssistantMessageComponent.renderFullTranscript()` forced only `#thinkingExpanded=true`; it did not temporarily disable `hideThinkingBlock`.
- Therefore Ctrl+T could render only the static `Thinking...` placeholder instead of the historical thinking body.

## Decision

Route C → B as an implementation bug. Patch the component-level full transcript protocol so Ctrl+T/full transcript rendering temporarily disables hidden-thinking and restores both hidden and expanded state afterward. Add live and historical/resumed regression coverage.