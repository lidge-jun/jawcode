# 20.7 B — Persisted Ctrl+T regression pass

## Result

Source-green path. The reopened persisted session regression passes on current tree, so no additional source patch is required for the marker paths covered by the regression.

## Evidence

Command:

```sh
bun test packages/coding-agent/test/input-controller-keybindings.test.ts
```

Observed output:

```text
21 pass
0 fail
93 expect() calls
Ran 21 tests across 1 file. [427.00ms]
```

Branch SHA:

```text
5b8f2580f9d57d371cff1b17942e8c2131218720
```

## Covered behavior

The new regression in `packages/coding-agent/test/input-controller-keybindings.test.ts` constructs a persisted session with historical thinking plus generic/read/bash/eval assistant tool-call results, closes and reopens it with `SessionManager.open()`, injects the reopened visible transcript context into Ctrl+T, and asserts:

- old thinking is expanded (`OLD_THINK_2`);
- generic fallback output is expanded beyond Ctrl+O cap (`OLD_TOOL_LINE_19`);
- read output is expanded (`OLD_READ_LINE_19`);
- assistant tool-call `bash` output is expanded (`OLD_BASH_LINE_19`);
- assistant tool-call `eval` output is expanded (`OLD_EVAL_LINE_19`);
- collapsed thinking and `ctrl+o to expand` hints are absent;
- session-backed live chat is not duplicated;
- out-of-band live tool remains included;
- `g` shows old expanded history;
- `G` reaches a real bottom marker (`OLD_READ_LINE_19`).
