# 20.15 B — verifier r4 DONE

Read-only verifier: `13-HiddenThinkingVerifier`

## Verdict

DONE

## Evidence

Verifier confirmed:

- `assistant-message.ts` `renderFullTranscript()` saves both `#thinkingExpanded` and `hideThinkingBlock`, forces `hideThinkingBlock=false` and expanded thinking during full transcript render, then restores both states in `finally`.
- Normal rendering still honors `hideThinkingBlock` by showing `Thinking...`, so the override is scoped to Ctrl+T/full transcript rendering.
- `full-transcript-overlay.test.ts` covers live hidden-thinking full transcript and post-render restoration.
- `full-transcript-overlay.test.ts` covers historical hidden-thinking full transcript and post-render restoration.
- `input-controller-keybindings.test.ts` covers reopened/resumed-session Ctrl+T with `ctx.hideThinkingBlock=true` and persisted thinking/tool/read/bash/eval markers.
- No obvious syntax or import mismatch found by read-only inspection.