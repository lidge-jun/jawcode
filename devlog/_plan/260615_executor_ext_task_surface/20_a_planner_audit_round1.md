FAIL
[high] 00_moc §6.3 / §7 vs 10_plan §4.4–§4.5 — MOC requires a behavior test that `agent: "executor_ext"` bypasses workflow actor routing; plan §4.4 only enumerates model-precedence and `cacheAffinity` metadata and §4.5 explicitly avoids duplicating runtime setup, with no step to extend `task-workflow-actor-routing.test.ts` or assert `kind: "none"` / no actor resume for `executor_ext` — Add an explicit test case (in §4.4 or a named addition to `task-workflow-actor-routing.test.ts`) asserting PABCD actor routing is not applied when `agent` is `executor_ext`.
[medium] 00_moc §7 acceptance vs 10_plan §6 — MOC acceptance includes “Tests protect the distinction between workflow skills, default general subagent lanes, and specialist lifecycle/review roles”; plan §6 acceptance list omits this as an independently checkable criterion (only partially implied by workflow-count and routing bullets) — Add a bullet to §6 and map it to concrete tests (e.g. `default-jwc-definitions` four skills + five roles, `agent-visibility` hidden support agents, routing strings in `task/agent-visibility`).
[medium] 00_moc §5 agent-facing expression rule vs 10_plan §3.3 / §6 — MOC requires tool guidance to show a combined JSON example `{ agent: "executor_ext", tasks: [{ ..., model: "provider/modelId:effort" }] }` and persistent `EXECUTOR_EXT` / `task.agentModelOverrides.executor_ext`; plan updates prose/schema but does not enumerate adding that example or naming `EXECUTOR_EXT` in TaskTool description — Add a planned snippet in §3.3 (or §3.2) with the JSON example and persistent-setting sentence; extend §4.2 assertions to include `EXECUTOR_EXT` or `agentModelOverrides.executor_ext` if exposed in rendered description.
[medium] 10_plan §3.3 — Planned `task.md` edits change `agent` / `.model` / `<agent-routing>` but do not update the existing `inheritContext` bullet that lists only `executor` and `architect`; `executor_ext` is generated with `forkContext: "allowed"` in §3.5 — Patch that bullet to include `executor_ext` (or state it inherits executor fork-context policy) so implementers do not leave stale callable-role docs.
[medium] 00_moc §4.2 behavior vs 10_plan §3.2–§3.3 — MOC states research-only `executor_ext` use requires the parent assignment to explicitly say read-only/no product mutation; plan mentions implementation/research for both lanes but never adds this guard to routing notes or acceptance — Add a routing/acceptance line: external-lane research assignments must state read-only boundaries in `assignment`.
[low] 00_moc §4.1 vs 10_plan §3.2 — MOC calls out PABCD B-stage implementation slices using normal `executor` actor/self-fork behavior; plan routing covers generic parallelism and specialists but not B-stage → `executor` — Add one routing bullet under system-prompt or `task.md` agent-routing for PABCD build slices defaulting to `executor`.
[medium] 10_plan §3.10 (all three scripts) — Gate changes allow satisfying callable-role protection with either `name: "executor_ext"` OR `fileName: "executor_ext.md"`, while §3.5 forbids on-disk `executor_ext.md`; an implementer could add only a string literal check without wiring `buildExecutorExtContent()` — Require a single assertion: generated bundled agent map / `loadBundledAgents()` includes visible `executor_ext` and `prompts/agents/executor_ext.md` must remain absent.
[medium] 10_plan §4.6 — “Add content assertions … Or add equivalent static file assertions if current render harness is not convenient” gives two completion paths without naming which file is authoritative or minimum assertions — Pick one path (rendered system prompt vs raw `system-prompt.md`) and list required strings in one subsection only.
[low] 10_plan §5 biome command — Verification lists many touched files but omits `packages/coding-agent/test/task/executor-ext-model-routing.test.ts` and `packages/coding-agent/test/task-bundled-agent-surface.test.ts` though §4 adds/modifies them — Include all §4 test paths in the biome/type verification command block.

The single statement an implementer would most likely misread: that an explicit per-task `.model: "provider/modelId[:effort]"` on the default `agent: "executor"` lane is enough to honor a user’s external/ext/model-diverse executor request, so they can skip `agent: "executor_ext"`.
