FAIL
[high] 10_plan §4.5 — Round-1 fix adds a workflow-actor bypass test but the snippet uses `executeTask`, `capturedActorRouting.kind`, and `result.details.results[0]`; `task-workflow-actor-routing.test.ts` only exposes `createTool()` / `taskTool.execute()` and records `createAgentSession` options via a spy, with no `capturedActorRouting` — Replace §4.5 with harness-accurate steps: mock `discoverAgents` to include `executor` (or bundled executor_ext), run `taskTool.execute` under an active PABCD registry, and assert routing via the existing spy (e.g. fresh child session / no actor `SessionManager.open` resume path) plus receipt `agent` and `cacheAffinity` on the tool result.
[medium] 20_a_planner_audit_round1 (MOC §5 / §4.2 test assertions) vs 10_plan §4.2 — JSON + `EXECUTOR_EXT` / `task.agentModelOverrides.executor_ext` are added in §3.3, but §4.2 description test still omits assertions that the rendered TaskTool text mentions persistent external setup (`EXECUTOR_EXT` or `agentModelOverrides.executor_ext`) — Extend §4.2 `it("advertises executor_ext...")` expected strings to match §3.3 persistent-setting line.
[low] 10_plan §3.3 — `inheritContext` change is only a one-line instruction (“list as executor, executor_ext, architect”) without a planned-after excerpt for the long conditional bullet — Add the full revised `.inheritContext` sentence in §3.3 so implementers do not partially edit the Handlebars block.
[low] 21_a_architect_audit_round1 vs 10_plan §3.10 — Callable-role gate checks allow either parsing `EMBEDDED_AGENT_DEFS` or calling `loadBundledAgents()` across three scripts without picking one canonical mechanism — Pick one (prefer `loadBundledAgents()` in tests + a single shared helper imported by gates) to avoid three divergent parsers.

The single statement an implementer would most likely misread: that putting `.model: "provider/modelId[:effort]"` on `agent: "executor"` satisfies an explicit external/ext/model-diverse executor request, so they can skip `agent: "executor_ext"`.
