FAIL
[HIGH] ARCH-A1 packages/tui/src/tui.ts:1504-1508 — appendGrowthThenViewportRepaint still positions append from stale hardwareCursorRow - prevViewportTop — anchor append from absolute screen position or refresh state before repaint.
[HIGH] ARCH-A2 packages/tui/src/tui.ts:1686-1693 — growth+offscreen path becomes two terminal writes without state reconciliation — merge into one synchronized sequence or require explicit post-append state updates.
[MEDIUM] ARCH-A3 10_renderer_second_patch_plan.md:11-12,143-175,177-191 — mandatory regression omits scrolled-up user repro — add required scrollViewportUp/getViewportY test proving no full replay while viewportY > 0.
[MEDIUM] ARCH-A4 packages/tui/src/tui.ts:1473-1477 — planned append re-truncates already-prepared newLines and may drift from fullRender byte contract — emit prepared newLines bytes directly or use one shared emit function.
[MEDIUM] ARCH-A5 structure/31_scroll.md:73-74 — plan supersedes documented growth exception but owns no doc work item — modify structure/31_scroll.md or add explicit waived/superseded note.
[LOW] ARCH-A6 10_renderer_second_patch_plan.md:211-213 — Biome gate includes first-patch files not touched by this cycle — restrict gate to actual touched files.
[LOW] ARCH-A7 packages/tui/src/tui.ts:1500-1544,1573-1575,1640-1642 — home-anchor affects every viewportRepaint caller — require regression proof for shrink/extraLines and compactViewportFill unchanged.
Most likely break first: appended tail rows land on wrong physical rows because append still uses stale relative cursor math before viewportRepaint can reconcile.
