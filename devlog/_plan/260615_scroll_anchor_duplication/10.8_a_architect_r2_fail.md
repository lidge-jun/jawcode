FAIL
[HIGH] ARCH-A8 10_renderer_second_patch_plan.md:96-101,218-231 — append phase anchors to visible-screen bottom while scrolled-up regression requires viewportY stay off-bottom; production #doRender has no scroll-offset input — specify a non-auto-scrolling/off-bottom branch before implementation.
[MEDIUM] ARCH-A9 10_renderer_second_patch_plan.md:91-133 vs packages/tui/src/tui.ts:1501-1502 — appendGrowthAndRepaintViewport omits fullRedrawCount, renderMetrics.recordFullRedraw, and debug logging — add parity.
[LOW] ARCH-A10 10_renderer_second_patch_plan.md:91-133 — helper duplicates viewportRepaint cursor/repaint loop — constrain implementation to share internals or keep one-buffer helper in sync.
Most likely break first: mandatory scrolled-up regression auto-scrolls to bottom unless renderer branches on an explicit off-bottom signal.
