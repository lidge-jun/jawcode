DONE

# B verifier receipt — unknown viewport scroll append

Commit: `5a7ccff8 fix: avoid unknown-viewport scroll append`
Source: `agent://97-ScrollBVerifier`

Verifier status: DONE.

Evidence summary:

- `packages/tui/src/tui.ts` now appends growth only when `viewportAtBottom === true`; `false` and `undefined` route to `viewportRepaint(...)`.
- `packages/tui/test/above-viewport-repaint.test.ts` now distinguishes:
  - known-bottom: positive `\x1b[20;1H` append anchor;
  - known-off-bottom: negative `\x1b[20;1H` while preserving `answer-10`/`answer-11`;
  - unknown viewport: explicit forwarding `Terminal` wrapper omits `isViewportAtBottom`, no append anchor, tail rows present, no early replay, scrollback preserved, floor row `> input`.
- `structure/31_scroll.md` documents the conservative unknown/undefined viewport policy and supersedes the prior unknown-as-bottom append policy.

Main-session verification already run:

- `TERM=xterm-256color bun test packages/tui/test/above-viewport-repaint.test.ts packages/tui/test/viewport-fill.test.ts packages/tui/test/commit-lane.test.ts packages/tui/test/prepared-line-cache.test.ts` — PASS, 31 tests / 162 assertions.
- `TERM=xterm-256color bun biome check packages/tui/src/tui.ts packages/tui/test/above-viewport-repaint.test.ts structure/31_scroll.md && bun --cwd=packages/tui run check` — PASS.

Note: the same focused test command fails under the session default `TERM=dumb` because `commit-lane.test.ts` intentionally disables `commitLines()` when `detectHistoryLaneMode()` sees `TERM=dumb`; rerun used `TERM=xterm-256color` to exercise the intended standard history lane.
