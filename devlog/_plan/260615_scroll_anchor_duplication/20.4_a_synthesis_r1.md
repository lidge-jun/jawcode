# 20.4 — A synthesis R1: unknown viewport plan revision

## Context

A-stage dual audit reviewed `20_renderer_unknown_viewport_plan.md` revision `unknown-viewport-r1`.

- Planner lens: FAIL (`20.2_a_planner_r1_fail.md`, source `agent://93-ScrollPlannerAudit`).
- Architect lens: PASS with implementation notes (`20.3_a_architect_r1_pass.md`, source `agent://94-ScrollArchitectAudit`).

The main session accepts the Planner FAIL. No findings are waived.

## Finding resolution map

### PLANNER-A1 — known-bottom append path not positively asserted

Decision: accepted.

Root cause: the original plan protected the new `viewportAtBottom === true` predicate but did not prove the positive known-bottom branch still uses `appendGrowthAndRepaintViewport()`.

Plan change: `20_renderer_unknown_viewport_plan.md` now requires a positive `\x1b[20;1H` assertion in the existing known-bottom growth test.

### PLANNER-A2 — unknown viewport scrollback/history assertion missing

Decision: accepted with explicit tradeoff.

Root cause: the plan intentionally moves production unknown terminals away from speculative CRLF append, but the unknown test did not assert history survival or document the loss of append-preservation for real bottom-but-unknown terminals.

Plan change: unknown viewport regression must assert `term.getScrollBuffer()` still contains `thinking-0`; PABCD notes now explicitly state that `ProcessTerminal` must choose duplicate-free viewport repaint over speculative append until it can prove bottom position.

Residual risk: a real bottom-pinned `ProcessTerminal` no longer gets append-growth optimization. This is accepted because duplicate-free live viewport correctness is the current user-facing bug, and known-bottom terminals retain append coverage.

### PLANNER-A3 — known-off-bottom tightening could drop growth markers

Decision: accepted.

Plan change: known-off-bottom test tightening must keep `answer-10` / `answer-11` presence assertions while adding negative `\x1b[20;1H` assertion.

### PLANNER-A4 / ARCH-A1 — subclass override type mismatch

Decision: accepted.

Plan change: unknown viewport harness now uses a wrapper/delegating `Terminal` object that omits `isViewportAtBottom`, not an incompatible `VirtualTerminal` subclass override.

### PLANNER-A5 — plan-10 supersession unclear

Decision: accepted.

Plan change: objective and `structure/31_scroll.md` update requirements now explicitly state this cycle supersedes the prior unknown-as-bottom append policy.

### ARCH-A2 — floor assertion should be exact

Decision: accepted.

Plan change: unknown viewport acceptance now requires exact `term.getViewport()[term.getViewport().length - 1] === "> input"`.

### ARCH-A3 — stale line anchor

Decision: accepted.

Plan change: current evidence now cites `packages/tui/src/tui.ts:1500` for `viewportRepaint()` and `packages/tui/src/tui.ts:1733-1743` for the branch gate.

### ARCH-A4/A5/A6 — integration notes

Decision: accepted as implementation guidance.

- Production change remains one-line predicate replacement at `packages/tui/src/tui.ts:1739`.
- `\x1b[20;1H` remains the branch discriminator for 20-row tests.
- Focused tests remain covered by package test suite.

## Changed plan sections

- Objective/current evidence: explicit plan-10 supersession and live line anchors.
- Test plan: wrapper unknown terminal; exact floor/history assertions; positive known-bottom append assertion; known-off-bottom negative append assertion while preserving tail markers.
- Documentation plan: explicit supersession note in `structure/31_scroll.md`.
- PABCD notes: accepted production unknown tradeoff.

## Delta audit request

Delta auditors should verify only:

1. Every R1 Planner finding is accepted and reflected in the revised plan.
2. Architect wrapper/type issue is resolved by avoiding subclass override.
3. No new ambiguity was introduced in the updated test assertions or documentation scope.
