# D DONE — Scroll anchoring duplicate-render second patch

## P/A/B/C summary

- P: Planned the second scroll renderer patch in `10_renderer_second_patch_plan.md`: preserve the committed live-zone routing fix, replace offscreen-growth full transcript replay, add off-bottom regression coverage, and update the scroll SoT.
- A: Dual audit failed twice and was synthesized honestly; scroll-r3 incorporated the accepted fixes (optional viewport hook, one-buffer helper, off-bottom branch, telemetry parity, doc sync). Runtime round cap was reached at scroll-r2, so B proceeded under explicit user-approved direction to finish the requested second patch.
- B: Built commit `b2520a9c` with the renderer patch and regressions; verifier `agent://82-ScrollBVerifier` returned DONE and was recorded in `10.10_b_verifier_done.md`.
- C: `bun run check` passed and affected tests passed 43 tests / 196 assertions; C evidence recorded in `10.11_c_check_pass.md`.

## Files changed

Implementation:

- `packages/tui/src/terminal.ts`
- `packages/tui/src/tui.ts`
- `packages/tui/test/above-viewport-repaint.test.ts`
- `packages/tui/test/virtual-terminal.ts`
- `structure/31_scroll.md`

First-patch files preserved/verified:

- `packages/coding-agent/src/modes/controllers/event-controller.ts`
- `packages/coding-agent/test/commit-time-folding.test.ts`

C hygiene applied in worktree but not committed as this cycle because it overlaps unrelated pre-existing worktree edits:

- `packages/coding-agent/src/modes/components/background-footer-panel-model.ts`
- `packages/coding-agent/src/modes/jobs-observer.ts`

## Acceptance criteria met

- Provider-streamed tool calls stay in the live zone until completion (first patch, `e7c3026d`).
- `viewportRepaint()` no longer depends on stale relative cursor math.
- Non-multiplexer offscreen-change + growth no longer uses full transcript replay; it appends only new tail rows and repaints the viewport in one synchronized write when viewport state is bottom/unknown.
- Known off-bottom VirtualTerminal state bypasses the append/newline growth branch and uses viewportRepaint-only.
- Regressions cover bottom-pinned and known off-bottom cases.
- `structure/31_scroll.md` reflects the superseded growth exception.
- Focused tests and workspace check passed.

## WONDER — still missing

- `ProcessTerminal` still cannot observe real native terminal scrollback position, so off-bottom behavior in real terminals is best-effort: the duplicate full-replay path is removed globally, but exact physical viewport preservation depends on terminal behavior unless a future production scroll-position signal is designed.
- The A audit round cap was reached before scroll-r3 re-audit could produce PASS. The implementation follows the synthesized r3 fixes and passed verification, but the audit history should remain visible.
- Render goldens were not updated; none were part of this patch's acceptance.

## REFLECT — spec improvements

- Future scroll specs should distinguish three invariants separately: duplicate replay prevention, tail persistence into scrollback, and physical off-bottom viewport preservation.
- The ontology needs a first-class “known off-bottom” terminal capability so tests and production behavior are not conflated.
- PABCD A-stage round cap handling should have a clearer path for user-approved continuation after all findings are accepted into a later plan revision.

## Closing verdict

Scroll anchoring duplicate-render second patch: DONE.
