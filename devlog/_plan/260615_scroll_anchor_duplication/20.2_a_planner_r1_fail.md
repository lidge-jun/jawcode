FAIL

# A Planner audit R1 — unknown viewport plan

Revision: `unknown-viewport-r1`
Plan: `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md`
Source: `agent://93-ScrollPlannerAudit`

[HIGH] PLANNER-A1 `20_renderer_unknown_viewport_plan.md:70-72,127-133` vs `packages/tui/test/above-viewport-repaint.test.ts:108-139` — plan keeps append-growth only for proven bottom (`viewportAtBottom === true`) but acceptance never positively asserts the append anchor on the known-bottom path — extend existing bottom-pinned growth test or add sibling to require `\x1b[20;1H` when `isViewportAtBottom()` is true; current assertions also pass viewportRepaint-only.

[MEDIUM] PLANNER-A2 `20_renderer_unknown_viewport_plan.md:98-104,160-163` — plan trades unknown-viewport append for duplicate safety but acceptance omits scrollback/history assertion — add `getScrollBuffer()` preservation checks for unknown-viewport growth, or explicitly waive production bottom-pinned scrollback append until a real terminal hook exists.

[MEDIUM] PLANNER-A3 `20_renderer_unknown_viewport_plan.md:106-108` vs `packages/tui/test/above-viewport-repaint.test.ts:168-169` — tightening known-off-bottom with only a negative append-anchor check could be misread as permission to drop growth content — keep `answer-10`/`answer-11` presence assertions while adding `\x1b[20;1H` negation.

[LOW] PLANNER-A4 `20_renderer_unknown_viewport_plan.md:82-87` vs `packages/tui/test/virtual-terminal.ts:130-133` — subclass override signature may not compile because `VirtualTerminal.isViewportAtBottom()` returns `boolean` — pin wrapper approach or widen base signature.

[LOW] PLANNER-A5 `20_renderer_unknown_viewport_plan.md:5-6,110-116` vs `10_renderer_second_patch_plan.md:69` — follow-up reverses plan-10 unknown-as-bottom policy but does not call out supersession clearly — add explicit supersession note.

Most likely misread: implementer may think the predicate change only affects explicit off-bottom `false`, when production `ProcessTerminal` yields `undefined`, so every real-terminal offscreen-change+growth render flips from append-growth to viewportRepaint-only.
