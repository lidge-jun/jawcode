PASS

# A Architect audit R1 — unknown viewport plan

Plan: `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md`
Source: `agent://94-ScrollArchitectAudit`

[MEDIUM] ARCH-A1 `packages/tui/test/virtual-terminal.ts:130` — `UnknownViewportTerminal` override returning `boolean | undefined` is incompatible with base `VirtualTerminal.isViewportAtBottom(): boolean` — use a Terminal wrapper delegating to `VirtualTerminal` while omitting `isViewportAtBottom`, or widen VirtualTerminal before subclassing.

[LOW] ARCH-A2 `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md:101-102` — floor assertion should be concrete — pin exact `term.getViewport()[term.getViewport().length - 1] === "> input"`.

[LOW] ARCH-A3 `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md:22` — `viewportRepaint` line anchor is stale — update to helper at `packages/tui/src/tui.ts:1500` and branch gate at `packages/tui/src/tui.ts:1733-1743`.

[INFO] ARCH-A4 `packages/tui/src/tui.ts:1739` — current predicate `viewportAtBottom !== false` matches the plan before-state — one-line production change to `viewportAtBottom === true` is the only production integration surface.

[INFO] ARCH-A5 `packages/tui/src/tui.ts:1550` — append-growth discriminator `\x1b[${height};1H` is append-only; viewportRepaint starts with `\x1b[H` — `\x1b[20;1H` is a valid negative assertion for a 20-row terminal.

[INFO] ARCH-A6 `packages/tui/package.json:35` — focused gates are pre-PR smoke; new/updated cases are covered by default `bun test test/*.test.ts`.

Most likely break first: a subclass-style `UnknownViewportTerminal` fails TypeScript compile; use wrapper or widen the base signature before verifying the predicate guard.
