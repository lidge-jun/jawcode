OKAY

# P Critic receipt — unknown viewport duplicate-render follow-up

Plan reviewed: `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md`

Blocking findings: none.

Non-blocking notes from critic:

- `PLAN-TEST-HARNESS-TS`: `UnknownViewportTerminal` may need a small TypeScript workaround because overriding `isViewportAtBottom(): boolean` with `undefined` can require compatible method typing.
- `PLAN-TEST-FLOOR`: visible viewport floor assertion should mirror sibling tests exactly: `viewport[viewport.length - 1] === "> input"`.
- `PLAN-KNOWN-BOTTOM-COVERAGE`: optional positive `\x1b[20;1H` assertion on the existing known-bottom growth test can lock in the append optimization; not required for this fix.

Summary: plan is actionable for B/C. The production guard change is single-point (`viewportAtBottom === true`), the root cause matches the cited `tui.ts` branch, the write-log assertions distinguish append-growth from viewportRepaint, and acceptance commands cover focused renderer tests plus touched-file/package checks.

Source: `agent://92-ScrollPlanCritic`.
