# 20.8 — C CHECK PASS: unknown viewport scroll append

## Scope audited

Implementation commit: `5a7ccff8 fix: avoid unknown-viewport scroll append`
Verifier receipt commit: `012c9ccf docs: verify unknown viewport scroll fix`

Touched implementation/spec files:

- `packages/tui/src/tui.ts`
- `packages/tui/test/above-viewport-repaint.test.ts`
- `structure/31_scroll.md`
- `devlog/_plan/260615_scroll_anchor_duplication/20_renderer_unknown_viewport_plan.md`
- A/B evidence receipts under `devlog/_plan/260615_scroll_anchor_duplication/20.*.md`

## Mechanical gates

PASS — root workspace check:

```sh
TERM=xterm-256color bun run check
```

Evidence: command completed successfully; root check ran Biome, Node 20 baseline, schema check, UI/rebrand guards, package checks, TypeScript checks, and Rust scope/build checks.

PASS — affected scroll tests:

```sh
TERM=xterm-256color bun test packages/tui/test/above-viewport-repaint.test.ts packages/tui/test/viewport-fill.test.ts packages/tui/test/commit-lane.test.ts packages/tui/test/prepared-line-cache.test.ts
```

Result: `31 pass`, `0 fail`, `162 expect() calls`.

Note: the same affected command was first attempted under the session default `TERM=dumb`; `commit-lane.test.ts` failed because `detectHistoryLaneMode()` intentionally disables `commitLines()` when `TERM === "dumb"`. The rerun with `TERM=xterm-256color` exercises the intended standard history lane.

## Adversarial review

Acceptance criteria checked against the diff:

- Production predicate: `packages/tui/src/tui.ts` changes only `viewportAtBottom !== false` to `viewportAtBottom === true`, so append-growth requires proven bottom and unknown/undefined routes to `viewportRepaint`.
- Known-bottom regression: `above-viewport-repaint.test.ts` now positively asserts `\x1b[20;1H`, tail rows, and no early replay.
- Known-off-bottom regression: same test now asserts no `\x1b[20;1H`, tail rows still present, no early replay, and viewport remains off-bottom.
- Unknown viewport regression: wrapper `Terminal` omits `isViewportAtBottom` without subclass override or object-rest; test asserts no append anchor, tail rows through repaint, no early replay, scrollback preservation, and exact `> input` floor row.
- Scroll SoT: `structure/31_scroll.md` now documents append only for `isViewportAtBottom() === true`, treats `undefined` as viewportRepaint-only, and states this supersedes unknown-as-bottom append policy.

Residual risk:

- Real `ProcessTerminal` bottom-pinned sessions cannot currently prove bottom state, so they use viewportRepaint-only for offscreen-change+growth. This is accepted by A-stage synthesis as the safer tradeoff until a reliable real-terminal viewport hook exists.

Verdict: PASS. All planned gates are green; proceed to D.
