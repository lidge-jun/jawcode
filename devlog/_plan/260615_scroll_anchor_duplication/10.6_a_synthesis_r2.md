# A Synthesis R2 — Scroll second patch

Revision: `scroll-r2`

## Verdicts

- Planner lens: FAIL (`agent://80-ScrollPlannerDeltaR2`)
- Architect lens: FAIL (`agent://81-ScrollArchitectDeltaR2`)

## Shared root cause

R2 resolved the stale append cursor and two-write concerns, but it still expected an off-bottom VirtualTerminal viewport to remain off-bottom while using a raw append/newline sequence. The plan needed an explicit viewport-state signal and branch policy rather than relying on terminal emulator behavior.

## Finding decisions

### Planner findings

- PLANNER-A7: accept. Reword performance interaction: require bottom-pinned and off-bottom regressions, not an undefined repeated-tick loop.
- PLANNER-A8: accept. Add optional `Terminal.isViewportAtBottom?()` and VirtualTerminal implementation; off-bottom regression now forces viewportRepaint-only instead of append/newline.
- PLANNER-A9: accept. Add `#fullRedrawCount`, `renderMetrics.recordFullRedraw`, and debug logging parity to the helper plan.
- PLANNER-A10: accept. Plan owns `structure/31_scroll.md` and provides replacement policy in acceptance.
- PLANNER-A11: accept. Acceptance remains whole `above-viewport-repaint.test.ts`, covering shrink, extraLines, compact; implementation summary will name those cases.

### Architect findings

- ARCH-A8: accept. Add optional terminal hook; known off-bottom state bypasses append-growth helper.
- ARCH-A9: accept. Add telemetry/debug parity.
- ARCH-A10: accept. Prefer shared viewport emit helper or carefully shared logic; keep one-buffer behavior mandatory.

## Plan changes for revision scroll-r3

- Add `packages/tui/src/terminal.ts` optional `isViewportAtBottom?(): boolean | undefined` hook.
- Add `VirtualTerminal.isViewportAtBottom()` using `viewportY + rows >= buffer.length`.
- Branch `grew && !isMultiplexerSession()` through append+repaint only when `viewportAtBottom !== false`; known off-bottom uses viewportRepaint-only.
- Include telemetry/debug side effects in the append+repaint helper.
- Update acceptance and commit strategy to include `terminal.ts`.

Decision: revise plan and run delta A re-audit for `scroll-r3`.
