# P Critic Round 1 — Scroll second patch

Verdict: ITERATE

Source: `agent://76-ScrollPlanCritic`

## Findings

- PLAN-TEST-ASSERT (blocking): primary regression assertion used subjective "full transcript replay burst" wording.
- PLAN-TEST-SETUP (blocking): fixture/mutation setup was not exact enough for B/C implementation.
- PLAN-SOT-DRIFT (non-blocking): plan changes `structure/31_scroll.md` §5 growth exception without noting supersession/follow-up.
- PLAN-VP-ANCHOR (non-blocking): home-anchored `viewportRepaint()` is broader than growth branch and needs explicit acceptance coverage.

## Main-session synthesis

Accepted all findings.

Plan updates applied in `10_renderer_second_patch_plan.md`:

- Added explicit note that the patch intentionally supersedes the `structure/31_scroll.md` §5 growth exception.
- Replaced vague replay-burst assertion with concrete write-log checks:
  - no `2J`/`3J`;
  - appended named rows `answer-10`/`answer-11` present;
  - early marker rows `thinking-0`/`thinking-1` absent from the exercised write;
  - scrollback still contains early marker history;
  - viewport still ends at composer floor.
- Pinned fixture shape to existing `ViewportFill` + `MutableContent` + `ComposerStub` pattern and required `term.clearWriteLog()` before the exercised render.
- Added acceptance coverage note that existing above-viewport shrink cases must still pass under home-anchored `viewportRepaint()`.

Decision: revise and rerun Critic.
