# 10.9 — A-stage synthesis r1

> Revision: a-r1 → a-r2  
> Inputs: `10.7_a_planner_delta_r1.md`, `10.8_a_architect_delta_r1.md`  
> Result: Planner PASS. Architect FAIL on two ordering/reference issues. Revise plan and re-run delta audit.

## Accepted findings

| Finding | Disposition | Plan revision |
|---|---|---|
| ARCH-A1 | Accepted | Add `packages/coding-agent/src/jwc-runtime/workflow-command-ref.ts` as a required modified file. Replace `stateWrite("jaw-interview")`, handoff state-write example, and `--deliberate` bridge with strict `jwc interview --write ... --handoff-status ... --handoff-outcome ...` examples. |
| ARCH-A2 | Accepted | Pin orchestrate guard ordering: for `target === "p"`, resolve `--shared`/session, read same-scope jaw-interview state, validate strict tuple, and return status 1 before `canTransitionPabcd`, envelope construction, `persist()`, goal checkpoint, or `retireJawInterviewStateForWorkflowExit`. |
| PLANNER low notes | Accepted | Add exact spec metadata location for `handoff_outcome`, explicit matrix subcase naming, and guard-before-write ordering wording. |

## No waivers

No new failure is waived. ARCH-A1 and ARCH-A2 are folded into the a-r2 plan.

## Exact plan sections changed

- Current evidence: add `workflow-command-ref.ts:72-123` hazard.
- Planned changes: add new section for `workflow-command-ref.ts`.
- Runtime section: pin guard ordering before `canTransitionPabcd`/`persist`.
- Runtime/test section: specify spec metadata location and matrix subcase test naming.
- Verification/acceptance: include workflow command reference tests/policy assertions.

## Residual risk

The most likely implementation mistake after a-r2 would be updating the SKILL.md prose but leaving generated command-reference blocks stale. The B-stage must update source prompt, command-ref generator/source, and tests together.
