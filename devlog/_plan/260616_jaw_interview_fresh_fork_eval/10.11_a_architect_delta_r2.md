FAIL

[HIGH] ARCH-A1 `packages/coding-agent/src/jwc-runtime/workflow-command-ref.ts:83-101` — §6 adds workflow-command-ref.ts as an owned patch target and removes `stateWrite("jaw-interview")` plus replaces examples/aliases, but does not explicitly remove/replace the jaw-interview `commands[]` entry that still renders `jwc jaw-interview --write --stage final ... --deliberate --json`; `renderCommandRefBlock` publishes every public `commands[]` item under Commands, so a B-stage patch that only updates examples/aliases leaves the legacy bypass live in the primary command list — constrain §6 to delete or replace that `commands[]` deliberate bridge with the strict two-step pattern from §4, and extend §10 cmdref assertions to cover the rendered Commands section, not only examples.

Accepted prior: ARCH-A2 resolved; ARCH-A3 through ARCH-A10 accepted or waived.

Most likely first break: implementer updates SKILL.md Phase 5b and cmdref examples/aliases but leaves the jaw-interview `commands[]` `--deliberate` entry at workflow-command-ref.ts:83-101, so generated cmdref still teaches the ungated bridge under “Commands”.
