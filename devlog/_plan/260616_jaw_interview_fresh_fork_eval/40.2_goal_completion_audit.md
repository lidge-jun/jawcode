# 40.2 — Goal completion audit

## Objective
Implement the approved jaw-interview fresh-fork evaluator, strict pre-P summary confirmation gate, command-reference hardening, and TUI PABCD status refresh plan in jawcode via PABCD, with audit/build/check evidence and focused verification.

## Concrete deliverables and evidence map
1. Fresh-fork evaluator prompt and bundle
   - Evidence: `packages/coding-agent/src/defaults/jwc/skills/jaw-interview/interview-turn-evaluator.md`; `packages/coding-agent/src/defaults/jwc-defaults.ts`; `default-jwc-definitions.test.ts`.
2. Jaw-interview prompt contract
   - Evidence: `SKILL.md` contains fresh evaluator loop, closure guard, summary pending/confirmed handoff, Phase 5 option selection before P, early-exit handling, command bridge rules; `jaw-interview-skill-policy.test.ts`.
3. Strict runtime write and metadata persistence
   - Evidence: `jaw-interview-runtime.ts`; `jaw-interview-runtime.test.ts`; runtime tests cover pending/confirmed/early-exit and invalid tuples.
4. Strict P guard and compatibility
   - Evidence: `orchestrate-runtime.ts`; `orchestrate-state.test.ts`; tests cover pending reject, confirmed pass, early-exit paths, spec requirement, closure fail, direct P compatibility, reset bypass.
5. Command-reference hardening
   - Evidence: `workflow-command-ref.ts`; `skill-command-ref.test.ts`.
6. TUI PABCD refresh
   - Evidence: `event-controller.ts`, `interactive-mode.ts`; `interactive-mode-status.test.ts`, `status-line-pabcd-segment.test.ts`.
7. G002/default workflow gates green
   - Evidence: `scripts/verify-g002-gates.ts`, `README.md`; fresh `bun scripts/verify-g002-gates.ts` pass.
8. PABCD lifecycle evidence
   - Evidence: plan/audit/build/check/done artifacts in this devlog folder and native PABCD close.
9. Verification gates
   - Evidence: focused tests 123 pass/0 fail; `bun scripts/verify-g002-gates.ts` pass; `bun run check` pass; `bun run check:ts` pass.

## Conclusion
All deliverables have direct file, test, gate, verifier, and PABCD evidence. No pending loop phase remains in `00_moc.md`.
