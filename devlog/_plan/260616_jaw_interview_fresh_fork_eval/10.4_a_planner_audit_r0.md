FAIL

[HIGH] PLANNER-A1 `10.1` §2 vs §3 — `§2` adds `packages/coding-agent/src/defaults/jwc/skills/jaw-interview/interview-turn-evaluator.md` but `§3` bundles under `skill-fragments/jaw-interview/` — pick one canonical on-disk source path and one bundled relative path; state which file is ADD vs COPY/bundle target.

[HIGH] PLANNER-A2 `10.1` §2:89-92 vs §4:180 — round consumption requires persisting `established_facts`, `trigger_events`, `dimension_scores`, etc., while MVP runtime explicitly defers those GJC fields to prompt-only — name the authoritative persistence owner (runtime schema vs prompt/session payload) and delete the contradictory sentence.

[HIGH] PLANNER-A3 `00_moc.md` Target #1/#5 + R1 vs `10.1` §4:106-107, §6:223-224 — MOC requires native gate from the first patch and closure enforcement before crystallization, but the plan activates strict gating only when `handoff_status` is present and keeps legacy `--write --stage final` without it fully compatible — add an explicit mandate that the updated jaw-interview prompt MUST always emit strict handoff metadata on final interview writes (or document an approved legacy bypass and who may use it).

[MEDIUM] PLANNER-A4 `10.1` §1 + §2 — no work item defines how the main agent invokes the fresh fork (fragment include, fork spawn API, tool name, or per-round trigger) — add a concrete invocation hook in `SKILL.md` and a matching policy test assertion.

[MEDIUM] PLANNER-A5 `10.1` §115-116, §134-146 vs §9 — `hard_cap` is in the normative type/matrix surface but has no CLI example, prompt transition, or named test — either add full `hard_cap` workflow coverage or remove it from the strict contract for MVP.

[MEDIUM] PLANNER-A6 `00_moc.md` Interview R3 vs `10.1` §4/§9 — R3 requires early-exit spec metadata such as `BELOW_THRESHOLD_EARLY_EXIT` (or equivalent) before P; the plan only says “override metadata in the spec” without schema, write flag, or test — specify the metadata key/value, where it lives (spec frontmatter/body/runtime), and an enforceable test.

[MEDIUM] PLANNER-A7 `10.1` §9:287-291 vs §303-313 vs Test-to-acceptance map:301 — TUI work requires event-controller/interactive-mode tests, but verification only lists `status-line-*.test.ts` and no concrete test file owns the hook/callback/render assertions — name the exact test file(s) and required case names for each TUI acceptance bullet.

[MEDIUM] PLANNER-A8 `10.1` §134-146 vs §9:274-286 — invalid-combination matrix rows lack 1:1 named runtime tests — add explicit test case names per matrix row or trim the matrix to tested rows only.

[MEDIUM] PLANNER-A9 `10.1` §6:215-224 vs §9 — orchestrate pre-P checks summary/closure/spec_ref but does not re-validate other strict fields at P time — state whether P trusts prior `--write` validation only or must re-check the full strict tuple; add the chosen rule to gate rules and tests.

[LOW] PLANNER-A10 `00_moc.md` GJC mechanisms list vs `10.1` — MOC still lists `auto_answer_streak`, `refined_rounds`, `lateral_reviews`, `ambiguity_milestone`, and free-text refinement as mechanisms to reconcile, but the plan owns none of them — record explicit defer/non-goals for each unreconciled mechanism so scope is closed.

[LOW] PLANNER-A11 `10.1` §339-350 — TUI acceptance depends on manual smoke step 5 in §331-337, so the criterion is not independently automatable — split into an automated assertion AC and an optional manual smoke note, or name the automated test that proves rendered `INTERV` → `PLAN`.

[LOW] PLANNER-A12 `10.1` §8:257-258 — timer/file-watcher fallback is allowed with no entry/exit condition — state the decision rule for adopting fallback (what failure of the primary hook proves) or mark it out of MVP scope.

Most likely misread: **“Legacy `--write --stage final` without `handoff_status` remains allowed” means the new pre-P summary gate is optional for jaw-interview completions**, when MOC/R1–R5 require native enforcement from the first patch.
