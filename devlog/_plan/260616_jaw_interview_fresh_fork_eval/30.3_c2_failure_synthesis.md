# 30.3 — C2 failure synthesis

## Verdict
NEEDS_FIX — route to B.

## Mechanical gates before fix
- Focused tests: pass.
- `bun scripts/verify-g002-gates.ts`: pass.
- `bun run check`: pass.
- `bun run check:ts`: pass.

## Adversarial review
Source: `agent://11-C2AdversarialReview`.

Findings fixed in B follow-up:
1. HIGH C2-1 — Phase 4 step 5 could run `jwc orchestrate p` immediately after summary acknowledgement, bypassing Phase 5 execution-option selection.
2. HIGH C2-2 — Round 1+ early exit still said “only then enter P,” bypassing Phase 5.
3. MEDIUM C2-3 — Phase 5 ordering allowed options-before-summary or duplicate-summary interpretations.

## Routing
Chosen route: code/prompt issue → `jwc orchestrate b`.
Rejected routes:
- P route: plan remains valid; the issue is implementation wording.
- I route: requirements are not ambiguous.
- Environment/tooling route: gates are green; issue is prompt contract ambiguity.

## Fix applied
Prompt order unified as: spec generation → mandatory short summary → pending strict write → acknowledgement → confirmed strict write → Phase 5 execution options → `jwc orchestrate p` only on explicit option 1 selection.
