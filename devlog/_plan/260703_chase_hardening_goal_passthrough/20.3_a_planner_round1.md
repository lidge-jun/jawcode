FAIL
[high] PLANNER-A1 `00_moc.md` Phase 20 scope split + `20_plan.md` Cluster C/D — the loop MOC records plugin loader cache-ordering/exit-guard work and task extension-agent discovery safety as Phase 20 scope, but the plan only covers extension runner timeout/shutdown and task worktree containment, with no explicit deferral or tests for plugin cache ordering or extension-agent discovery — either add concrete work items/tests for those two MOC slices or mark them out-of-scope/deferred in Non-goals and acceptance so Phase 20 cannot fake-close them.
[medium] PLANNER-A2 `20_plan.md` Cluster E / Acceptance criteria — subprocess/SSH/DAP lifecycle is declared deferred “unless B finds a local no-risk bug,” but that optional implementation path has no decision boundary, target files, acceptance criteria, or verification command — make this phase unconditional deferral, or define the exact allowed local bug class plus required tests before B-stage.
Most likely misread: “all selected hardening slices” means only the clusters already detailed in `20_plan.md`, not every Phase 20 slice recorded in the loop MOC and chase card.
