# Phase 30 B verifier — C BLOCK fix

Status: DONE
Date: 2026-07-03

## C BLOCK addressed

C-stage adversarial review in `30.5_c_check_block.md` found that explicit replacement paths cancelled running owner jobs but did not suppress already-terminal queued/retrying owner deliveries. That could leak old-session async output into a replacement transcript.

## Implementation evidence

Changed files:

- `packages/coding-agent/src/async/job-manager.ts`
  - Added `acknowledgeDeliveriesForOwner({ ownerId })` to suppress/remove queued and in-flight deliveries for one owner.
  - Added `cancelOwnerForReplacement({ ownerId })` for destructive replacement teardown: owner cleanup, owner delivery suppression, owner running-job cancellation.
- `packages/coding-agent/src/session/agent-session.ts`
  - `#cancelOwnAsyncJobs()` now calls `cancelOwnerForReplacement({ ownerId })`, so `/new`, handoff/new-session, and branch replacement paths use the destructive owner teardown.
- `packages/coding-agent/test/async-job-manager.test.ts`
  - Covers owner terminal retry suppression, owner running-job cancellation, unrelated running-job preservation, and unrelated delivery preservation/drain.
- `packages/coding-agent/test/agent-session-concurrent.test.ts`
  - Covers `AgentSession.newSession()` replacement behavior with stale old-owner terminal retry suppression, old-owner running cancellation, unrelated owner job preservation, and unrelated delivery drain.

## Verification evidence

Fresh local gates:

```text
bun test packages/coding-agent/test/async-job-manager.test.ts packages/coding-agent/test/agent-session-concurrent.test.ts
=> 40 pass, 0 fail, 156 expect() calls

bun test packages/coding-agent/test/async-job-manager.test.ts packages/coding-agent/test/agent-session-concurrent.test.ts packages/coding-agent/test/session-manager-close-race.test.ts packages/coding-agent/test/interactive-mode-plan-review.test.ts packages/coding-agent/test/goals/goal-mode-integration.test.ts
=> 77 pass, 0 fail, 308 expect() calls

git diff --check
=> pass

bun run check:tools
=> pass; Checked 2459 files

bun run check:ts
=> pass; all workspace checks exited 0
```

Read-only verifier:

- `agent://38-VerifyPhase30CBlockFix` returned DONE.

## Verdict

DONE — the C-stage BLOCK has an implementation fix and regression coverage. Route back to C.
