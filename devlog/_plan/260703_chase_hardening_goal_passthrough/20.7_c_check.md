# 20.7 — C-stage check: `20.038` hashline/tool/plugin/task safety

Verdict: PASS

## Mechanical gates

- `bun test packages/coding-agent/test/edit-path-validation.test.ts packages/coding-agent/test/tool-argument-coercion.test.ts packages/coding-agent/test/extensions-runner.test.ts packages/coding-agent/test/task/worktree.test.ts` → 48 pass, 0 fail (rerun after Phase 20 commit and unrelated cleanup commits).
- `bun run check` → passed after fixing unrelated Rust formatting/clippy issues in the ioctl/native slice (`dfccf97`, `8747fcf`). Final run completed `check:ts` and `check:rs` successfully.

Earlier B-stage support gates also passed before commit:

- `git diff --check` → no output.
- `bun run check:tools` → passed.
- `bun run check:ts` → passed.

## Adversarial review against acceptance criteria

- Apply-patch/hashline path headers: `validateEditHeaderPath()` rejects empty, NUL, POSIX absolute, Windows drive/UNC, and `.`/`..` segments while preserving valid relative paths with spaces. The final apply-patch parser wraps failures in `ParseError` with line numbers; streaming preview catches malformed header paths and drops those preview entries rather than throwing.
- Tool argument hardening: `requireRecordToolArgs`, `rejectUnknownToolArgs`, and `allowedToolArgKeysFromWireSchema` are tested independently; `CustomToolAdapter.execute()` rejects non-record args before third-party code and rejects unknown fields only for strict tools with closed canonical wire schemas. Zod, zod-backed TypeBox, raw JSON Schema, and non-strict pass-through are covered.
- Extension timeout/shutdown behavior: source behavior stayed unchanged; tests now lock no unhandled late rejection after timeout and the forwards-every-explicit-shutdown-call contract.
- Task worktree containment: public `ensureIsolation()` rejects traversal-like ids before backend start and checks derived paths remain under `getWorktreesDir()`; `cleanupIsolation()` logs and skips outside-root handles instead of deleting them. Existing task public API is unchanged.
- Deferrals retained: plugin marketplace cache ordering / plugin loader cache invalidation, extension-agent discovery safety, and SSH/DAP/subprocess lifecycle hardening remain explicit non-claims for later chase slices.

## Residual risks / route decision

No red mechanical gate or adversarial finding remains for the selected Phase 20 scope. Deferred source-card clusters are intentionally out of scope, so no B/P/I reroute is needed.
