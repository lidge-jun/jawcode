# 80.3 A-stage synthesis round 1

Status: Planner FAIL addressed; Architect PASS retained
Planner report: `devlog/_plan/260703_chase_hardening_goal_passthrough/80.2_a_planner_round1.md`
Architect report: `.jwc/plans/planphase/2026-07-02-1325-8f6b/stage-80-architect.md`
Revision: `a-r0`

## Lens results

- Planner: FAIL
- Architect: PASS

## Findings

### PLANNER-A1 — `/goal-plan <hint>` alias not independently checkable

Decision: accept.

The plan listed `/goal-plan <hint>` in the trigger surface but paused-state work items only named `/goalplan` and `/goal plan`. That made alias behavior depend on implicit knowledge of the command registry instead of the plan's acceptance/test contract.

Plan changes:

- `80_plan.md` ACP test plan now explicitly includes `/goal-plan paused planning hint`.
- The same bullet states `/goalplan` and `/goal-plan` share the same command handler, but both spellings must appear in paused-state coverage so the alias contract is independently checkable.

### PLANNER-A2 — TUI `/goal set <objective>` submission semantics ambiguous

Decision: accept.

The plan required `handleGoalModeCommand("set <objective>")` to write the durable brief and use the state helper, but did not say whether it must also preserve the pending objective submission. Since the verifier claims submitted prompt text coverage, implementation could otherwise silently update state while dropping the user-visible route effect.

Plan changes:

- `80_plan.md` adjacent TUI coverage for `handleGoalModeCommand("set Replace the objective")` now requires active state, new objective, no paused warning, durable plan brief, and submitted objective text.
- `80_plan.md` acceptance now requires TUI/Interactive `set <objective>` to submit the objective as pending input.

## Residual risk

Architect PASS remains applicable because these are acceptance/test clarity changes, not a new source-file or signature strategy. Delta re-audit should focus on PLANNER-A1 and PLANNER-A2 only.
