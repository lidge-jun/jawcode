DONE
Read-only verification completed against the Phase 80 plan. No files were modified and no gates/formatters/project-wide commands were run, per assignment.

Evidence inspected:
- `builtin-registry.ts:483-493` adds `createOrReplaceGoalState()` with active replace and paused drop/create semantics; `:502-524` routes text `/goalplan`, `/goal plan`, `/goal set`, and direct `/goal <objective>` through durable brief writes plus fresh state creation/replacement; `:540-567` preserves show/status/pause/resume/drop admin behavior; `:656-775` preserves `/goal`, `/goalplan`, and `/goal-plan` dispatch surfaces.
- `interactive-mode.ts:1761-1764` preserves the plan-mode refusal; `:1782-1793` makes paused direct objective write the durable brief, create fresh active goal state, and submit the objective; `:1907-1935` implements durable brief write plus active/paused/none state helper behavior; `:1950-1956` makes planning submit only the generated planning prompt; `:1967-1977` makes `/goal set` work through the same fresh-state path.
- `acp-builtins.test.ts:458-524` covers paused ACP/text `/goal`, `/goal set`, `/goalplan`, `/goal-plan`, and `/goal plan`, including active state, objective/planning brief assertions, durable `readGoalPlan()` assertions, returned prompt shape, and absence of paused diagnostics.
- `goal-mode-integration.test.ts:205-273` covers Interactive/TUI paused direct `/goal`, `/goal set`, and `/goal plan`, including submitted pending text/prompt and durable brief writes; `:276-287` preserves bare paused `/goal` menu resume behavior; `:185-192` preserves plan-mode refusal.
- `goal-planning-start.ts:9-20` confirms planning brief/prompt shape used by both implementation paths and tests (`GOAL_PLAN_PENDING_BRIEF`, `hint: ...`, and `AI-driven goal planning is active.`).
