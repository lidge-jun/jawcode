# 10.8_d_done_summary — Phase 10 D-stage closeout

Status: DONE
Cycle: PABCD phase 10 (`10.071` search/utils/edit safety)

## P/A/B/C summary

- P: Planned the `10.071` hardening slice across utils glob/format, edit-mode BOM reads, and web-search timeout/cache/DuckDuckGo hedge behavior in `10_plan.md`.
- A: Planner and Architect audit loops passed after tightening timeout copy, no-arg timeout preservation, Kimi explicit timeout, cache materialization, DDG hedge tests, and affected test scope.
- B: Built and committed `67129ab fix(search): harden phase 10 safety paths`; read-only B verifier passed after delta fixes for cache-test breadth and abort-aware DDG hedge delay.
- C: Affected tests passed (`46 pass` including the unrelated TUI slice requested for commit hygiene), `bun run check` passed after a local Rust import-format fix, and adversarial review found Phase 10 acceptance met.

## Files changed for Phase 10

Primary implementation/test/doc files:

- `packages/utils/src/glob.ts`
- `packages/utils/src/format.ts`
- `packages/utils/test/glob.test.ts`
- `packages/utils/test/format.test.ts`
- `packages/coding-agent/src/edit/read-file.ts`
- `packages/coding-agent/test/edit-read-file.test.ts`
- `packages/coding-agent/src/web/search/providers/utils.ts`
- `packages/coding-agent/src/web/search/provider.ts`
- `packages/coding-agent/src/web/search/index.ts`
- `packages/coding-agent/src/web/search/providers/base.ts`
- `packages/coding-agent/src/config/settings-schema.ts`
- `packages/coding-agent/src/web/kagi.ts`
- provider callsites in `packages/coding-agent/src/web/search/providers/`
- `packages/coding-agent/test/web/search/speed-improvements.test.ts`
- `packages/coding-agent/test/tools/web-search-hard-timeout.test.ts`
- `schemas/config.schema.json`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/10.6_b_verifier.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/10.7_c_check.md`

Concurrent unrelated commits requested by the user:

- `ed74f18 feat(tui): ambiguous-width mode threaded through both width tables`
- `e0da891 docs: plan compaction latency work`
- `036d3eb chore: persist friction log`

## Acceptance criteria met

- `globPaths()` precompiles effective exclude globs once per call and preserves custom/default excludes.
- `formatNumber()` and `formatBytes()` no longer round below-threshold values into the next suffix/unit while preserving exact thresholds and unbounded B/GB behavior.
- `readEditFileText()` preserves UTF-8 BOM for normal files without changing notebook behavior.
- Web-search direct API providers use API timeout class; LLM providers use LLM timeout class or explicit timeout; Kimi keeps explicit 60s transport ceiling; no-arg non-provider `withHardTimeout()` users keep 60s legacy default.
- `web_search.timeout` applies only when explicitly present in settings and otherwise leaves provider-class defaults active.
- `resolveProviderChain()` caches provider-id chains per AuthStorage/preferred/active-model/generation and materializes returned provider arrays.
- DuckDuckGo hedge starts once for non-zero-index fallback, is reused after pre-DDG failures, is ignored/aborted on primary success, and now propagates caller abort during the delayed hedge wait.

## WONDER — what's still missing?

- Live-network web-search behavior is not covered; the plan intentionally required deterministic unit tests only.
- Deep-search async job delivery was exercised through type/check gates, not a dedicated behavior test in this slice.
- The TUI/native ambiguous-width work was committed for cleanliness but belongs to a separate TUI plan; future cycles should avoid mixing unrelated build fixes into a chase phase unless the user again orders it.

## REFLECT — how to improve the spec?

- The B verifier receipt format should avoid historical negative verdict keywords or the PABCD parser should read the final verdict field instead of keyword-scanning the whole file.
- The C acceptance should explicitly say whether concurrently requested unrelated commits are inside or outside the phase's C-stage gate; here they were included in affected tests and `bun run check` because they were in HEAD.
- Timeout docs should state that `settings.has()` is the source-of-truth for explicitness; schema defaults alone are not runtime overrides.

## PESSIMIST — is the direction still right?

- What did not improve: live provider latency in real network conditions is only inferred from deterministic hedge/timeout behavior, not measured.
- Dead hypothesis: treating a B verifier report as prose with historical negative tokens is safe for `jwc orchestrate verdict`; it is not.
- Evidence that would show the direction is wrong: if real provider traces show the DDG hedge increases tail latency or abort noise under common failures, the next cycle should revisit hedge policy instead of expanding it.

## Loop continuation

Phase 10 is done. The loop MOC now marks Phase 20 (`20.038` hashline/tool/plugin/task safety) active for the next PABCD cycle.
