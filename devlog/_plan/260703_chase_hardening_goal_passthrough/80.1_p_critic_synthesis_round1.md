# 80.1 P Critic synthesis round 1

Status: addressed
Critic receipt: `.jwc/plans/planphase/2026-07-02-1325-8f6b/stage-80-critic.md`
Verdict: ITERATE

## Findings

### C80-01 — TUI durable-plan write contradiction

Decision: accept.

The draft incorrectly said direct paused objective replacement should match `#startGoalFromObjective()` while also requiring durable goal plan brief assertions. Current `#startGoalFromObjective()` enters goal mode and submits text but does not call `#writeGoalPlanFromBrief()`. The plan now states the state helper is state-only and every direct/set/planning caller must explicitly call `#writeGoalPlanFromBrief(...)` before state create/replace where durable brief acceptance is required.

Changed sections:

- `80_plan.md` InteractiveMode implementation steps 1-7.
- `80_plan.md` acceptance criteria for TUI `set` and `plan` routes.

### C80-02 — Missing Text/ACP paused `/goal set <objective>` verification

Decision: accept.

The draft listed `/goal set <objective>` as a trigger and acceptance route but did not explicitly require paused ACP coverage for that route. The plan now requires `/goal set replacement after pause` coverage alongside direct `/goal`, `/goalplan`, and `/goal plan`.

Changed sections:

- `80_plan.md` ACP test plan.

## Result

The plan now has consistent durable-write ownership and route-specific paused-state test coverage. Re-run Critic against the patched `80_plan.md` plus this synthesis.
