# Phase 30 C check — BLOCK

Status: BLOCK
Date: 2026-07-03

## Mechanical gates

Fresh C-stage gates were green:

```text
bun test packages/coding-agent/test/async-job-manager.test.ts packages/coding-agent/test/agent-session-concurrent.test.ts packages/coding-agent/test/session-manager-close-race.test.ts packages/coding-agent/test/interactive-mode-plan-review.test.ts packages/coding-agent/test/goals/goal-mode-integration.test.ts
=> 75 pass, 0 fail, 292 expect() calls

bun run check
=> pass; check:ts and check:rs completed successfully

bun run check:ts
=> pass; all workspace checks exited 0
```

## Adversarial review

Read-only reviewer `agent://37-CReviewPhase30Exec` returned BLOCK.

Blocking issue:

- Normal `AgentSession.dispose()` release semantics are correct, but explicit replacement paths (`/new`, handoff, branch switch) still have a queued-delivery leak.
- `#cancelOwnAsyncJobs()` runs owner cleanup and `cancelAll({ ownerId })`, but `cancelAll()` only affects running jobs.
- Already-terminal owned jobs with queued/retrying deliveries are not suppressed/removed, so stale old-session async output can still be delivered into the replacement transcript.

Evidence called out by reviewer:

- `packages/coding-agent/src/session/agent-session.ts` replacement paths call `#cancelOwnAsyncJobs()`.
- `packages/coding-agent/src/async/job-manager.ts` `cancelAll(filter)` iterates `getRunningJobs(filter)` only.
- Delivery suppression/removal exists via `acknowledgeDeliveries(...)`, but replacement teardown does not use an owner-scoped delivery cleanup.

## Required B-stage fix

Route back to B and implement owner-scoped destructive async teardown for explicit replacement paths:

1. Run owner cleanups.
2. Suppress/remove pending owner deliveries for terminal jobs.
3. Cancel running owner jobs.
4. Preserve unrelated owner jobs/deliveries.
5. Add regression coverage for completed owner job with queued/retrying delivery during new-session/handoff/branch replacement, plus running job cancellation and unrelated owner preservation.

## Verdict

C-stage failed. Route: code issue → B-stage.
