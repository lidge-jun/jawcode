# 20.2_p_critic_synthesis_round2 — Phase 20 P critic iteration

Status: addressed
Critic receipt: `.jwc/plans/planphase/2026-07-02-1325-8f6b/stage-20-critic.md`
Verdict: ITERATE

## Findings addressed

### C20-R2-01 — focused verification command mismatch

Critic claim: Cluster A creates `packages/coding-agent/test/edit-path-validation.test.ts`, but the focused command still listed `edit-apply-patch-parser.test.ts`.

Decision: accept.

Plan change:

- Updated the focused command to run `packages/coding-agent/test/edit-path-validation.test.ts`.

### C20-R2-02 — extension shutdown contract still conditional

Critic claim: Cluster C left shutdown verification conditional while final acceptance requires timeout/shutdown behavior to be proven or hardened.

Decision: accept.

Plan changes:

- Chose the concrete Phase 20 shutdown contract: preserve current forwards-every-request semantics.
- Plan now explicitly says each `ctx.shutdown()` / `runner.shutdown()` call forwards one request to the underlying shutdown handler.
- Plan now explicitly says **do not** make shutdown idempotent in this phase because that would be a behavior change for extension authors.
- Test plan now requires a shutdown contract test proving two explicit shutdown calls forward two requests.
- Cluster C acceptance now requires shutdown forwarding behavior to be regression-protected.

## Result

The plan now has exact focused verification commands and an unconditional extension shutdown regression target. Re-run Critic against `20_plan.md` plus this synthesis.
