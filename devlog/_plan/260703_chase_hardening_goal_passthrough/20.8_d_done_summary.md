# 20.8 — D summary: `20.038` hashline/tool/plugin/task safety

## Cycle summary

- P: Planned a split/adapt hardening slice for edit header path validation, strict custom-tool argument boundaries, extension timeout/shutdown regression coverage, and task worktree containment, while explicitly deferring plugin cache ordering, extension-agent discovery, and SSH/DAP/subprocess lifecycle hardening.
- A: Planner and architect audits initially failed on unresolved deferrals and raw-schema assumptions; revised plan passed after switching strict tool key derivation to `toolWireSchema()` and recording Cluster E deferrals.
- B: Built the selected safety paths and tests; read-only verifier agents returned DONE for edit paths, tool args, and runtime/worktree safety.
- C: Mechanical gates passed (`affected tests 48 pass`, `bun run check` pass) after unrelated native ioctl formatting/clippy cleanup; adversarial review found no remaining blocker for selected scope.

## Files changed for Phase 20

- `packages/coding-agent/src/edit/path-validation.ts`
- `packages/coding-agent/src/edit/apply-patch/parser.ts`
- `packages/coding-agent/src/edit/streaming.ts`
- `packages/coding-agent/src/tools/jtd-utils.ts`
- `packages/coding-agent/src/extensibility/custom-tools/wrapper.ts`
- `packages/coding-agent/src/task/worktree.ts`
- `packages/coding-agent/test/edit-path-validation.test.ts`
- `packages/coding-agent/test/tool-argument-coercion.test.ts`
- `packages/coding-agent/test/extensions-runner.test.ts`
- `packages/coding-agent/test/task/worktree.test.ts`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/20.6_b_verifier.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/20.7_c_check.md`

## Acceptance criteria met

- Parser-level apply-patch headers reject malformed/unsafe paths before execution and include parser line numbers.
- Streaming edit previews drop malformed hashline/apply_patch header paths without crashing, while valid relative paths still preview.
- Custom tool params must be records before third-party execution; strict closed-object tools reject unknown fields deterministically across Zod, zod-backed TypeBox, and raw JSON Schema.
- Extension timeout late-rejection and forwards-every-shutdown-call behavior are regression-tested without changing runtime semantics.
- Task isolation ids and cleanup paths are contained under the managed worktree root.
- Deferred clusters are recorded as non-claims.

## WONDER — what's still missing?

- The broad OMP source card still has unclosed plugin marketplace cache ordering / plugin loader cache invalidation, extension-agent discovery safety, and SSH/DAP/subprocess lifecycle leak hardening.
- Streaming preview malformed-path behavior is verified at strategy level, not through a live TUI tool-render path.
- Task cleanup safety is covered for outside-root handles, but not for symlink/race manipulation inside the managed root.

## REFLECT — improve the spec

- The plan should name exact existing-vs-new test filenames after A audit; the initial `task-worktree-safety.test.ts` name was later adapted to the existing `test/task/worktree.test.ts` file.
- Strict custom-tool key derivation should always specify canonical schema owner (`toolWireSchema`) up front; raw `parameters.properties` language was too ambiguous.
- Future chase slices should separate “runtime behavior change” from “receipt-only regression test” explicitly in each cluster.

## PESSIMIST — is the direction still right?

- What did not improve: this cycle did not reduce plugin/process lifecycle risk; it only closed the locally provable edit/tool/task subset.
- Hypothesis that died: “20.038 can be closed as one broad hardening pass” was false; the card must remain split into separately auditable slices.
- Evidence the current direction is wrong: if Phase 30 or later cycles repeatedly need cross-card edits to the same lifecycle boundary, the chase ordering should be replanned around subsystem ownership rather than source-card numbering.

Terminal state: Phase 20 done; loop continues with Phase 30 active.
