# 10.7_c_check — Phase 10 C-stage check

Status: PASS
Cycle: PABCD phase 10 (`10.071` search/utils/edit safety)

## Scope checked

Primary Phase 10 implementation commit:

- `67129ab fix(search): harden phase 10 safety paths`

Concurrent unrelated commit explicitly requested by user to keep the tree clean:

- `ed74f18 feat(tui): ambiguous-width mode threaded through both width tables`

## Stage 1 — mechanical verification

Affected tests:

```text
bun test packages/utils/test/glob.test.ts packages/utils/test/format.test.ts packages/coding-agent/test/edit-read-file.test.ts packages/coding-agent/test/web/search/speed-improvements.test.ts packages/coding-agent/test/tools/web-search-hard-timeout.test.ts packages/coding-agent/test/web/search/abort-and-timeout.test.ts packages/coding-agent/test/tools/web-search-parallel.test.ts packages/tui/test/ambiguous-width.test.ts packages/tui/test/scroll-out-barrier.test.ts
→ 46 pass, 0 fail, 141 expect() calls
```

Repo gate:

```text
bun run check
→ initial run failed in check:rs because crates/pi-natives/src/text.rs import grouping needed rustfmt-style grouping.
```

Local C fix:

- Re-grouped `std::{ cell::RefCell, sync::atomic::{AtomicBool, Ordering} }` import in `crates/pi-natives/src/text.rs`.
- `bun run check:rs` passed.
- Amended unrelated TUI/native commit to `ed74f18`.

Final repo gate:

```text
bun run check
→ PASS: check:ts and check:rs completed successfully.
```

## Stage 2 — adversarial review

Acceptance review against `10_plan.md`:

- Glob exclude precompile: `packages/utils/src/glob.ts` builds `excludeGlobs` after base/custom/gitignore effective excludes and reuses them in the scan loop. Tests cover custom excludes plus default `node_modules` exclusion with custom excludes.
- Compact formatting: `packages/utils/src/format.ts` clamps bounded K/M and KB/MB bands while leaving exact thresholds and unbounded B/GB behavior intact. Tests cover below-threshold, exact-threshold, and unbounded cases.
- Edit BOM preservation: `packages/coding-agent/src/edit/read-file.ts` keeps notebook path first, reads normal files through one `Bun.BunFile`, detects raw UTF-8 BOM bytes, and prepends `\uFEFF` only when Bun text decoding omitted it. Tests cover BOM, serialization, and non-BOM.
- Search timeout classes: `packages/coding-agent/src/web/search/providers/utils.ts` keeps omitted 60s behavior, adds API/LLM classes, keeps explicit numeric precedence, and gates configured override on `settings.has("web_search.timeout")`. Provider callsites use API/LLM class or documented numeric timeout for Kimi; `packages/coding-agent/src/web/parallel.ts` remains no-arg 60s by static test.
- Provider chain cache: `packages/coding-agent/src/web/search/provider.ts` caches provider-id chains per AuthStorage/preferred/active-model/generation and materializes provider arrays on return. Tests cover repeated hit, fresh returned arrays, preferred-provider keying, setPreferred invalidation, generation invalidation, and explicit clear.
- DuckDuckGo hedge: `packages/coding-agent/src/web/search/index.ts` schedules one non-zero-index DuckDuckGo hedge, reuses it at the DuckDuckGo slot, aborts/ignores it on primary success, and now races the delay against caller abort. Tests cover two-provider hedge, longer pre-DDG chain, primary success, and abort while awaiting delayed hedge.
- User-facing docs/copy: schema, provider base comments, and web search schema text describe provider-class defaults plus optional explicit override.

Residual risks:

- DuckDuckGo hedge timing remains unit-tested with fake providers, not live network calls. This is intentional: plan non-goal forbids networked/live web tests.
- `web_search.timeout` schema still has a default value for configuration UI/schema compatibility, but runtime override is source-aware through `settings.has()`. This is covered by tests and matches the plan.
- The unrelated TUI/native commit was verified with focused TUI tests and the full repo gate, but it belongs to the separate TUI resize plan, not Phase 10 search/utils/edit acceptance.

## Stage 3 — verdict

C-stage verdict: PASS.

Next action: run `jwc orchestrate d`.
