# 80.7 D-stage done summary — Phase 80 goal slash replacement

Status: done

## P/A/B/C summary

- P: Planned `/goal`, `/goal set`, `/goalplan`, `/goal-plan`, and `/goal plan` so explicit user input creates/replaces into a fresh goal state regardless of active/paused/none state, while no-arg/admin commands remain unchanged.
- A: Dual audit passed after one Planner iteration; Planner alias/submission findings were addressed in `80.3_a_synthesis_round1.md`, Planner delta passed in `80.4_a_planner_delta_round1.md`, Architect passed.
- B: Built the command/runtime orchestration in `builtin-registry.ts` and `interactive-mode.ts`, with ACP/text and Interactive/TUI regression tests.
- C: Focused tests and full `bun run check` passed; an initial `check:rs` false positive from gitignored chase clone Rust files was fixed in `scripts/check-rust-scope.ts`.

## Files changed

Product/test/tooling:

- `packages/coding-agent/src/slash-commands/builtin-registry.ts`
- `packages/coding-agent/src/modes/interactive-mode.ts`
- `packages/coding-agent/test/acp-builtins.test.ts`
- `packages/coding-agent/test/goals/goal-mode-integration.test.ts`
- `scripts/check-rust-scope.ts`

Devlog/state artifacts:

- `devlog/_plan/260703_chase_hardening_goal_passthrough/00_moc.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80_plan.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.1_p_critic_synthesis_round1.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.2_a_planner_round1.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.3_a_synthesis_round1.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.4_a_planner_delta_round1.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.5_b_verifier.md`
- `devlog/_plan/260703_chase_hardening_goal_passthrough/80.6_c_check.md`
- `.jwc/goal/goals.json`, `.jwc/goal/ledger.jsonl`

## Acceptance criteria met

- Text/ACP `/goal <objective>` replaces a paused current goal with active fresh state and returns the objective prompt.
- Text/ACP `/goal set <objective>` replaces a paused current goal with active fresh state.
- Text/ACP `/goalplan <hint>`, `/goal-plan <hint>`, and `/goal plan <hint>` replace a paused current goal with AI-driven goal-planning state and return the planning prompt.
- TUI/Interactive direct objective, `set`, and `plan` paths replace paused state, write the durable brief, and submit the correct pending objective or planning prompt.
- No-arg paused `/goal` still opens paused management menu.
- `/goal show|status|pause|resume|drop` text behavior is unchanged.
- Plan-mode refusal remains `Exit plan mode first.`

## Verification evidence

- `bun test packages/coding-agent/test/acp-builtins.test.ts packages/coding-agent/test/goals/goal-mode-integration.test.ts` — PASS, 75 tests, 366 assertions.
- `git diff --check` — PASS before code/check commits.
- `bun run check:tools` — PASS before B commit.
- `bun run check:ts` — PASS before B commit.
- `bun run check` — PASS after `check-rust-scope.ts` chase clone ignore fix.
- B verifier: `80.5_b_verifier.md` — DONE.

## WONDER — what's still missing?

- No browser/TUI manual smoke was run for slash command entry; coverage is unit/integration-level through `handleGoalModeCommand()` and ACP slash command execution.
- The tests assert paused-state replacement, but do not assert old paused goal ledger tombstone details beyond active state/objective/brief outcomes.
- The initial C red gate exposed that chase clone directories can still affect repo-wide tooling if future scripts ignore `.gitignore` policy.

Assumption corrected during build: existing completion-mode integration tests were coupled to durable goal guard behavior; they now explicitly disable that guard at the test harness boundary because the tests are about interactive goal-tool lifecycle, not completion receipt validation.

## REFLECT — spec improvements

- Phase 80 acceptance should explicitly separate state mutation, durable brief write, and prompt submission for every route from the start.
- Tooling/environment gates should mention chase clone directories as ignored mirrors; repo-wide scanners must not treat them as product source.
- Future goal-mode route plans should include both text/ACP and TUI/Interactive route matrices with alias spellings listed explicitly.

## PESSIMIST — direction check

- What did not improve: the remaining seven hardening chase phases are still pending; this cycle only completed the user-requested goal slash state replacement.
- Hypothesis that died: “full repo check failures will be product-code regressions” died; this cycle's C red was a scanner/source-boundary false positive from ignored chase mirrors.
- Evidence that would show the current direction is wrong: if the next hardening cycles repeatedly require broad workflow/runtime rewrites instead of bounded safety patches, the seven-item hardening list should be reclassified from C2/C3 slices into a new architecture/spec pass.

Next P must quote this: chase mirror directories are runtime/reference mirrors and repo-wide scanners must ignore them unless the phase explicitly targets chase clone content.
