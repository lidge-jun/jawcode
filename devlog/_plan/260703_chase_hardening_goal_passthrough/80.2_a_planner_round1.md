FAIL
[high] PLANNER-A1 `80_plan.md` / Loop-spec header + ACP test plan + Acceptance criteria — `/goal-plan <hint>` is listed as an in-scope trigger, but paused-state work items and acceptance only name `/goalplan` and `/goal plan`, so alias behavior is not independently checkable — add explicit paused `/goal-plan <hint>` acceptance/test coverage, or state that the alias is intentionally covered by the shared `/goalplan` handler test.
[high] PLANNER-A2 `80_plan.md` / InteractiveMode implementation step 4 + Acceptance criteria — TUI `/goal set <objective>` is told to write the brief and use the state helper, but the plan never says whether it must preserve current objective submission via `onInputCallback`; this conflicts with the verifier’s “submitted prompt text” claim and can silently drop a user-visible route effect — require the step, acceptance, and TUI paused-set test to assert either submitted objective text is preserved or explicitly changed.
Most likely misread: “Use the same state helper” for TUI `set` means “only update state,” when the route may also need to preserve prompt submission semantics.
