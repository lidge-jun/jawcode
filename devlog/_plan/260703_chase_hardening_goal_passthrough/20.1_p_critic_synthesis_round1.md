# 20.1_p_critic_synthesis_round1 — Phase 20 P critic iteration

Status: addressed
Critic receipt: `.jwc/plans/planphase/2026-07-02-1325-8f6b/stage-20-critic.md`
Verdict: ITERATE

## Findings addressed

### C20-01 — hashline/edit scope unresolved

Critic claim: the draft named `hashline/edit` and cited `packages/coding-agent/src/edit/streaming.ts`, but implementation/acceptance only covered apply-patch parser headers.

Decision: accept.

Plan changes:

- Cluster A now creates `packages/coding-agent/src/edit/path-validation.ts` with `validateEditHeaderPath()`.
- `packages/coding-agent/src/edit/apply-patch/parser.ts` uses the helper for add/delete/update/move headers.
- `packages/coding-agent/src/edit/streaming.ts` uses the helper for hashline header parsing and treats malformed streaming preview paths as no-preview rather than throwing during best-effort rendering.
- New `packages/coding-agent/test/edit-path-validation.test.ts` covers helper rejections, apply-patch parser rejection, and hashline streaming preview behavior.
- Final acceptance now explicitly includes both apply-patch and hashline streaming preview behavior.

### C20-02 — tool argument production integration under-specified

Critic claim: the draft left the executor to find a callsite during B, while acceptance required a production integration point.

Decision: accept.

Plan changes:

- Cluster B now names the exact integration point: `packages/coding-agent/src/extensibility/custom-tools/wrapper.ts`, `CustomToolAdapter.execute()`.
- The plan specifies the raw-input boundary behavior: reject non-record/array params before third-party/custom tool execution, reject unknown keys for strict object-like custom tools, do not mutate/coerce values, preserve forwarding semantics, and allow unknown keys for non-strict custom tools.
- New `packages/coding-agent/test/tool-argument-coercion.test.ts` covers helper behavior and wrapper integration.
- Final acceptance now names `CustomToolAdapter.execute()` strict/non-strict tests.

## Result

The plan no longer requires B-stage guessing for hashline scope or tool-argument integration. Re-run Critic against `20_plan.md` plus this synthesis.
