# 80.6 C-stage check — Phase 80 goal slash replacement

Status: PASS

## Mechanical gates

Commands run:

```bash
bun test packages/coding-agent/test/acp-builtins.test.ts packages/coding-agent/test/goals/goal-mode-integration.test.ts
bun run check
```

Results:

- Focused tests: PASS — 75 pass, 0 fail, 366 expect calls.
- First `bun run check`: FAIL in `check:rs` because `scripts/check-rust-scope.ts` scanned gitignored upstream chase clones under `devlog/_gjc_chase/` and `devlog/_omp_chase/`.
- Fix: updated `scripts/check-rust-scope.ts` `isIgnoredPath()` to ignore `devlog/_gjc_chase/` and `devlog/_omp_chase/`, matching the repository `.gitignore` chase clone policy.
- Rerun `bun run check`: PASS — `check:ts` and `check:rs` completed successfully.

## Adversarial review

Acceptance criteria reviewed against current source/tests:

- Text/ACP `/goal <objective>` replaces paused current goal and returns objective prompt:
  - Implementation: `packages/coding-agent/src/slash-commands/builtin-registry.ts:512-524` via `createOrReplaceGoalState()`.
  - Test: `packages/coding-agent/test/acp-builtins.test.ts:458-524`.
- Text/ACP `/goal set <objective>` replaces paused current goal:
  - Implementation: `builtin-registry.ts:531-570` routes `set` through `startTextGoal()`; helper handles paused drop/create.
  - Test: `acp-builtins.test.ts:458-524`.
- Text/ACP `/goalplan`, `/goal-plan`, and `/goal plan` replace paused current goal with AI-driven planning state:
  - Implementation: `builtin-registry.ts:502-510`, `:764-777`.
  - Test: `acp-builtins.test.ts:458-524` covers all three spellings.
- TUI/Interactive direct objective, `set`, and `plan` paused-state replacement:
  - Implementation: `packages/coding-agent/src/modes/interactive-mode.ts:1782-1793`, `:1907-1935`, `:1950-1977`.
  - Test: `packages/coding-agent/test/goals/goal-mode-integration.test.ts:205-273`.
- No-arg paused `/goal` management menu remains:
  - Implementation: `interactive-mode.ts:1782-1793` opens paused menu when no `subRest`.
  - Test: `goal-mode-integration.test.ts:276-287`.
- Plan-mode refusal remains:
  - Implementation: `interactive-mode.ts:1761-1764`.
  - Test: `goal-mode-integration.test.ts:185-192`.
- Administrative text commands preserved:
  - Implementation: `builtin-registry.ts:540-567` keeps `show/status/pause/resume/drop` behavior outside fresh-objective replacement.

## Residual risks

- The TUI `/goalplan` handler still delegates as `plan <hint>` to `handleGoalModeCommand()`. This is intentional and covered by the same Interactive plan-path tests; ACP/text alias coverage separately verifies `/goal-plan` spelling.
- The `check-rust-scope.ts` fix is a tooling/environment correction caused by the checked-out chase clone directories. It does not expand Rust allowlist policy; it prevents ignored upstream mirror directories from becoming false positives.

Verdict: PASS. Proceed to D-stage.
