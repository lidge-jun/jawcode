FAIL

[MEDIUM] PLANNER-A10 72_p2_2_p3_1_adjacent_plan.md:217-218,279 vs 230-247 — Migrated-session cold-rewrite acceptance is in implementation notes and done criteria but absent from session-open-single-load required cases and verification gates — add required case 4 naming file, pre-current-version fixture, assertion that adoption sets needsFullRewriteOnNextPersist, and first persist-triggering write performs cold full rewrite.
[LOW] PLANNER-A11 72_p2_2_p3_1_adjacent_plan.md:237 — Resident reopen assertion still references unnamed truncation notice from other tests — inline TRUNCATION_NOTICE = "[Session persistence truncated large content]" or require exact substring match like TEXT_TAIL/IMAGE_TAIL.
[LOW] PLANNER-A12 72_p2_2_p3_1_adjacent_plan.md:267-279 — Done criteria omit defensive inline truncation-branch removal/sharing required at plan:61,112 — add done item: differential/viewport emit paths use shared truncation helper or no second truncate policy after prepareLinesForTerminal.

The implementer will most likely replace only the #applyLineResets/#truncateLinesToWidth call sites in #doRender and commitLines and leave the existing defensive truncateToWidth branches in viewport repaint/differential emit paths, because the plan headlines those two call sites while the delete-or-share requirement for downstream branches is easy to overlook.
