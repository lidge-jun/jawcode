FAIL

[HIGH] ARCH-A1 packages/coding-agent/src/session/session-manager.ts:2269 — #adoptLoadedSessionFile() pseudocode lists migrateToCurrentVersion() but omits assigning its boolean return to #needsFullRewriteOnNextPersist, which setSessionFile() currently does before blob/resident/index work — add this.#needsFullRewriteOnNextPersist = migrateToCurrentVersion(this.#fileEntries) inside #adoptLoadedSessionFile() for the non-empty branch; add an open/adopt test that migrated sessions still take the rewrite cold path on first persist.
[HIGH] ARCH-A2 devlog/_plan/260614_performance/72_p2_2_p3_1_adjacent_plan.md:176-201 — Plan says open() resets resident stores as part of adoption, but #adoptLoadedSessionFile() body never calls #resetResidentStores(); setSessionFile() always resets at line 2257 before loading — either call #resetResidentStores() in #adoptLoadedSessionFile() before assigning #fileEntries, or have open() invoke the same pre-adopt setup block as setSessionFile() (#closePersistWriter, #resetResidentStores, persist-error reset) so both entry points share identical pre-adoption state.
[MEDIUM] ARCH-A3 packages/coding-agent/src/session/session-manager.ts:875 — Single-load test contract specifies readTextCountFor, but loadEntriesFromFile() reads via async storage.readText(filePath); a wrapper that only counts readTextSync will not detect the double-load regression — Counting storage must increment on readText() (and any prefix reads if added later); assert readTextCountFor(path.resolve(sessionFile)) === 1 using the same resolved path setSessionFile() uses.
[MEDIUM] ARCH-A4 packages/tui/src/tui.ts:1433-1435,1657-1677 — Plan unifies preparation only in #doRender() and commitLines(), but viewportRepaint and the differential emit loop still inline truncateToWidth + terminator re-append logic copied from #truncateLinesToWidth() — after #prepareLinesForTerminal(), delete or guard the inline truncation branches so emit paths trust prepared newLines; if kept as defense-in-depth, extract shared terminator selection into one helper used by both cache and emit paths.
[MEDIUM] ARCH-A5 devlog/_plan/260614_performance/72_p2_2_p3_1_adjacent_plan.md:108-111 — Cache invalidation is specified in the widthChanged branch after line preparation at tui.ts:1344-1345, so normalization cache can still hit on the first post-resize render while truncation keys change; test case 4 says to assert misses after resize without distinguishing counter kinds — clear #clearPreparedLineCaches() at the start of #doRender() when widthChanged is true (before preparation), or narrow the width-change test to require truncate.miss > 0 while allowing normalize.hit.
[LOW] ARCH-A6 packages/coding-agent/src/session/session-manager.ts:3832 — SessionManager.open() first load uses raw filePath while setSessionFile() resolves with path.resolve() before loadEntriesFromFile(); adopt refactor must not reintroduce divergent path keys for storage wrappers or breadcrumbs — resolve const resolved = path.resolve(filePath) once in open() and pass resolved to both the single load and #adoptLoadedSessionFile().
[LOW] ARCH-A7 devlog/_plan/260614_performance/72_p2_2_p3_1_adjacent_plan.md:239-243 — C-stage gate runs monorepo bun run check plus long focused lists; new files land in default packages/tui/test/*.test.ts and packages/coding-agent bun test anyway, but there is no dedicated CI job for the focused gate bundle — treat listed commands as pre-PR smoke; expect the new tests to run under workspace default test/check scripts without assuming a separate perf gate job.

The single point most likely to break first if implemented as written: #adoptLoadedSessionFile() landing without this.#needsFullRewriteOnNextPersist = migrateToCurrentVersion(...) — migrated sessions opened via SessionManager.open() would stop scheduling the rewrite cold path and can persist incorrectly on first write.
