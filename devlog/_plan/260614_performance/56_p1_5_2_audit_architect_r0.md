FAIL

[CRITICAL] ARCH-A1 `53_p1_5_2_resident_cache_plan.md:15-16,88-95` — Plan correctly states resident image sentinels materialize via persistence-oriented `resolveImageDataUrlSync` / `resolveImageDataSync` (fail-open on missing blobs), but `materializeResidentValueSync()` currently routes all sentinel kinds through those resolvers (`packages/coding-agent/src/session/session-manager.ts` ~1296+) with no `resolveResidentImage*` exports yet in `blob-store.ts`. Step 1 (fail-closed resolvers) must land before switching sentinel materialization; mixing stores without resolver split will either break tests or leak raw `blob:sha256:` refs.

[HIGH] ARCH-A2 `53_p1_5_2_resident_cache_plan.md:99-131` — Resident helper signatures must change from single `BlobStore` to `{ text, image }` stores plus `{ sessionId, sessionFile }` context across `externalizeResidentValueSync`, `prepareEntryForResidentSync`, `materializeResidentValueSync`, `materializeResidentEntrySync`, and `materializeResidentEntriesSync`. Repo has 15+ direct `#residentBlobStore` callsites (`setSessionFile`, `_persist`, `#appendEntry`, reader APIs, `createBranchedSession`, `forkFrom`, etc. at `session-manager.ts:2195,2688,2702,3007,3017,3317,3337,3348,3398,3450,3616,3644,3690`). Plan checklist is accurate but incomplete if any callsite is missed — compile/type errors or wrong-store writes are likely.

[HIGH] ARCH-A3 `53_p1_5_2_resident_cache_plan.md:2110-2119` — Current ownership is `readonly #residentBlobStore = new MemoryBlobStore()` with persistent `#blobStore = new BlobStore(getBlobsDir())` (`session-manager.ts:2110-2119`). Plan’s split (persistent text → `EphemeralBlobStore`, persistent image residents → durable `#blobStore`, in-memory → memory stores) matches `EphemeralBlobStore` already in `blob-store.ts:98+` but requires constructor/factory changes and `close()` disposal of temp dirs; implementing store split before callsite migration will break `prepareEntryForResidentSync(entry, singleStore)` at every path.

[MEDIUM] ARCH-A4 `53_p1_5_2_resident_cache_plan.md:2131-2148,120-131` — `captureState()` / `restoreState()` today snapshot `#fileEntries` by reference (`session-manager.ts:2131+`); plan requires materialized JSON-semantic clones and resident-store reset on restore. No `cloneJsonSemantic` exists locally; omission during partial port leaves sentinel object aliasing across sessions (ownership tests will fail).

[MEDIUM] ARCH-A5 `53_p1_5_2_resident_cache_plan.md:135-170` — Planned NEW tests (`session-resident-cache.test.ts`, `session-resident-lifecycle.test.ts`, `session-resident-ownership.test.ts`, `resident-materialization.test.ts`) are absent; parent matrix already names them (`04_verification_matrix.md:27`, `23_p1_5_upstream_v3_merge_plan.md:60-63`). Existing `packages/coding-agent/test/session-manager/resident-retention.test.ts` overlaps fail-closed/materialization themes — plan’s “do not duplicate” rule is sound; port upstream `devlog/_upstream_gjc/.../resident-materialization.test.ts` concepts without copying long fixtures twice.

[LOW] ARCH-A6 `53_p1_5_2_resident_cache_plan.md:122-124,3115` — `appendModelChange(model, role?)` exists (`session-manager.ts:3115`); optional third metadata arg is backward compatible if added as overload/optional parameter. `ModelChangeEntry` currently lacks `previousModel`/`reason`/`thinkingLevel` (`session-manager.ts:103+`). Low integration risk if fields are optional only.

[LOW] ARCH-A7 `53_p1_5_2_resident_cache_plan.md:181-216` — Focused `bun test` + biome on listed paths + `bun --cwd=packages/coding-agent run check` and C-stage `bun run check` align with parent P1.5.2 row; no schema-generator impact. High conflict surface is manual `session-manager.ts` edits, not CI gates.

The single point most likely to break first if implemented as written: switching `materializeResidentValueSync()` to call not-yet-added `resolveResidentImageDataUrlSync` / `resolveResidentImageDataSync` while image sentinels still use persistence resolvers, or updating materialization signatures without migrating all `#residentBlobStore` callsites — first compile failure or first `session-resident-ownership` / `resident-retention` test exposing raw `blob:sha256:` on missing resident image data.
