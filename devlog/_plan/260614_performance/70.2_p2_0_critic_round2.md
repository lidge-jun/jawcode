# 70.2 — P2.0 critic round 2

Verdict: ITERATE

Source: `agent://44-P20PlanCriticR2`

## Blocking required edits

1. Add a named TUI/replay JSONL integration test that calls `flushPerfEvents`, reads JSONL, and asserts `tui.frame.*` plus `tui.preparedLine.*` placeholder keys with `schema: "jwc.perf-events/1"`.
2. Reconcile wrap/visible-width counters with parent plan 69: add placeholder registry rows or explicit deferral.
3. Require domain integration tests to assert flushed JSONL contents, not only in-memory snapshots; keep `sdk.resume.*` hooks under integration coverage rather than helper-only coverage.

## Main-session synthesis

The critic is correct. The revised plan closed most round-1 blockers but still under-specified TUI JSONL fixture coverage and left wrap/visible-width counters inconsistent with parent P2.0. The plan will be patched to add:

- `packages/tui/test/perf-events-p2_0.test.ts` as the TUI JSONL integration fixture.
- `tui.text.wrap.calls` and `tui.text.visibleWidth.calls` placeholder registry rows with `placeholder: "true"` until exact hook points are selected in P2.2/P2.1.
- JSONL-content assertions for both TUI and coding-agent integration tests.
- No helper-only escape hatch for `sdk.resume.*`; if the full SDK fixture is too broad, the plan must narrow source scope before implementation rather than silently weakening the acceptance criterion.
