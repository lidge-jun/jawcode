FAIL

[MEDIUM] PLANNER-A1 `71_p2_0a_metrics_core_tui_plan.md:76-82` — #doRender hook anchor is unspecified relative to early returns and the multi-step newLines pipeline — pin one guarded block immediately after final line composition (`#truncateLinesToWidth`) and require frame/placeholder hooks on every #doRender entry that reaches that anchor.

[MEDIUM] PLANNER-A2 `71_p2_0a_metrics_core_tui_plan.md:79-83` — placeholder recordEvent cadence is unspecified — state all three placeholder calls fire on every anchored #doRender when metrics are enabled.

[MEDIUM] PLANNER-A3 `71_p2_0a_metrics_core_tui_plan.md:57-64` — post-flush buffer lifecycle is unspecified — successful flush appends all buffered rows then clears the buffer; pin failure retention/drop behavior.

[MEDIUM] PLANNER-A4 `71_p2_0a_metrics_core_tui_plan.md:44-53` — reset() extension is omitted though replay/glitch fixtures call renderMetrics.reset() — require reset() to clear buffered events, snapshot perf counters, eventsBuffered, and eventsDropped while preserving enabled state.

[MEDIUM] PLANNER-A5 `71_p2_0a_metrics_core_tui_plan.md:44-48 vs 121-129` — recordCounter JSONL granularity is unspecified — specify each recordCounter/recordEvent appends exactly one JSONL row.

[MEDIUM] PLANNER-A6 `71_p2_0a_metrics_core_tui_plan.md:56-57 / 99` — resolvePerfLogPath default cites existing runtime conventions without naming resolver — pin default to path.join(getLogsDir(), "perf.jsonl") from @gajae-code/utils, keeping JWC_PERF_LOG override.

[MEDIUM] PLANNER-A7 `71_p2_0a_metrics_core_tui_plan.md:161` — perf.write.error surface unnamed — require snapshot().counters["perf.write.error"] === 1.

[LOW] PLANNER-A8 `71_p2_0a_metrics_core_tui_plan.md:120` — fixture choice minimal TUI or replay harness is ambiguous — pick one named path or define minimal fixture API.

[LOW] PLANNER-A9 `71_p2_0a_metrics_core_tui_plan.md:11 vs 133-142` — broad plan 70 link lacks anti-scope-creep guard — state plan 70 coding-agent/session/sdk blocks are deferred to P2.0b and must not be implemented in P2.0a.

[LOW] PLANNER-A10 `71_p2_0a_metrics_core_tui_plan.md:50-53` — source-qualified snapshot key rule is example-only — make it normative.

Most likely misread: implementer records frame.lines from this.render(width).length instead of post-#truncateLinesToWidth newLines.length.
