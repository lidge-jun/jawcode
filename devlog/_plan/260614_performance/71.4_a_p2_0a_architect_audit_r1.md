FAIL

[P0] ARCH-A1 `packages/tui/src/tui.ts:1342-1531` — plan requires per-#doRender tui.frame counters after final line composition, but #doRender has many early returns before the tail — insert one guarded block immediately after `#truncateLinesToWidth`, before diff branching.

[HIGH] ARCH-A2 `packages/tui/test/replay-harness.ts:160-161,230-241` — `TUI.stop()` sync flush when `renderMetrics.enabled` conflicts with replay pattern; runReplay enables metrics then stops TUI without `JWC_PERF_LOG` isolation — either gate auto-flush on `JWC_PERF` env or require replay harness temp path isolation.

[HIGH] ARCH-A3 `packages/tui/src/metrics.ts:177-197` — reset() currently does not clear new event buffer/counters; stale buffered events would leak across singleton harness runs — extend reset() and test it.

[MEDIUM] ARCH-A4 `packages/utils/src/dirs.ts:260-263` — default log path should use `getLogsDir()`, not hand-rolled home path — implement `resolvePerfLogPath` with `path.join(getLogsDir(), "perf.jsonl")` unless `JWC_PERF_LOG` set.

[MEDIUM] ARCH-A5 `packages/tui/src/tui.ts:645-680` — flush contract omits post-flush buffer drain; repeated stop/flush can duplicate rows — sync flush appends then clears queue and second stop is idempotent.

[MEDIUM] ARCH-A6 `packages/tui/src/utils.ts:141-143` — JSONL text placeholders disagree with helperStats.text.visibleWidth; tests must assert JSONL shape only and not compare placeholders to helperStats.

[MEDIUM] ARCH-A7 `packages/tui/test/metrics-redteam.test.ts:16-50` — disabled no-op snapshot expectations must include new counters/events fields — extend disabled no-op loop and expected snapshot.

[LOW] ARCH-A8 `packages/tui/package.json:35-36,94` — gates are pre-PR smoke; new test will also run in default package test/check — document this.

[LOW] ARCH-A9 `packages/tui/src/metrics.ts:160-161` — singleton constructed at import time; env enablement only works if env is set before import — perf-events test should use renderMetrics.enable()/reset() unless using pre-import setup.

Most likely break first: #doRender instrumentation placed at tail instead of after line composition, causing first-frame/early-return paths to miss frame JSONL rows.
