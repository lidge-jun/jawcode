FAIL

[MEDIUM] PLANNER-A2 `70_p2_0_measurement_plan.md:174-224` — synthesis closed A2 on registry API column only; original constraint also required per-counter API in each MODIFY After block — repeat `recordCounter`/`recordEvent` per hook in assistant/overlay/session/sdk MODIFY After bullets.

[MEDIUM] PLANNER-A9 `70_p2_0_measurement_plan.md:249` vs `70.7_a_p2_0_audit_synthesis_r1.md:20` — synthesis accepted isolated-temp zero-append criterion; test still says "creates no file or writes no rows" — replace with explicit disabled-mode assertion: `JWC_PERF` unset on pre-created `JWC_PERF_LOG` temp file asserts zero new bytes/lines appended.

[MEDIUM] PLANNER-A4 `70_p2_0_measurement_plan.md:299-307` — compromise adds `transcript.overlay.navigation.input` + test `renderPhase: navigation` requirement but done checklist omits it — add done item requiring flushed navigation row with `labels.renderPhase === "navigation"`.

[MEDIUM] PLANNER-A11 `70_p2_0_measurement_plan.md:67-69` vs `140-144` — persistence names `flushPerfEventsSync()`; metrics MODIFY After lists only `flushPerfEvents()` — either add sync helper to metrics After + tests or remove from persistence contract.

[MEDIUM] PLANNER-A12 `70_p2_0_measurement_plan.md:27,69,162` vs `238-249` — `TUI.stop()` named runtime flush owner but no test/done item verifies stop-triggered flush (tests only call `flushPerfEvents()`) — add fixture asserting JSONL written after `tui.stop()` without manual flush.

[MEDIUM] PLANNER-A13 `70.7_a_p2_0_audit_synthesis_r1.md:30` vs `70_p2_0_measurement_plan.md:238-260` — synthesis accepted ARCH-A8 singleton enable constraint; revised test sections never require env-before-import or `renderMetrics.enable()/reset()` — add named test/setup constraint to TUI and coding-agent perf-events fixtures.

[LOW] PLANNER-A14 `70_p2_0_measurement_plan.md:19` vs `102-105` — critic revision summary still references `sdk.resume.*` while registry/SDK tests use `sdk.session.*` — align summary wording to `sdk.session.*` keys.

[LOW] PLANNER-A15 `70_p2_0_measurement_plan.md:104` vs `222-223` — `workspaceTree.start` hook site lacks named span/function unlike other `sdk.session.*` rows — pin to the same `logger.time`/call site used for `buildWorkspaceTree` kickoff.

The implementer will most likely satisfy flush acceptance by calling `flushPerfEvents()` in tests only and leave `TUI.stop()` without a wired flush, since no done item or test asserts stop-triggered persistence.
