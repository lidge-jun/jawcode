# 70.8 — P2.0 A-stage audit synthesis round 2

Planner delta verdict: FAIL (`agent://49-P20PlannerDeltaR2`)
Architect delta verdict: FAIL (`agent://50-P20ArchitectDeltaR2`)

## Root cause

Round 1 synthesis fixed the main code ownership problems, but the revised plan still left two implementation-critical lifecycle details underspecified: synchronous `TUI.stop()` flushing and explicit process-wide metrics singleton enable/reset setup. Planner also found several acceptance wording gaps that could let B-stage pass with helper-only flushes or ambiguous disabled-mode behavior.

## Finding decisions

- PLANNER-A2 accepted: add per-hook API naming in MODIFY sections.
- PLANNER-A9 accepted: disabled-mode tests use pre-created isolated temp files and assert zero appended bytes/lines.
- PLANNER-A4 accepted: done checklist names `transcript.overlay.navigation.input` with `labels.renderPhase === "navigation"`.
- PLANNER-A11 accepted: add `flushPerfEventsSync()` to metrics API and require `TUI.stop()` to call it.
- PLANNER-A12 accepted: add a stop-triggered flush fixture/done item.
- PLANNER-A13 accepted: test sections must explicitly use env-before-import or `renderMetrics.enable()/reset()`; prefer the existing replay-harness enable/reset pattern.
- PLANNER-A14 accepted: replace stale `sdk.resume.*` summary wording with `sdk.session.*`.
- PLANNER-A15 accepted: pin `workspaceTree.start` to the existing buildWorkspaceTree kickoff span.

- ARCH-A13 accepted: add `flushPerfEventsSync` to the metrics API block and mandate sync flush from `TUI.stop()`.
- ARCH-A14 accepted: add explicit singleton enable/reset setup in TUI and coding-agent JSONL test sections.

## Plan sections changed

- Metrics API: adds `flushPerfEventsSync(metrics?: RenderMetrics, path?: string): void`.
- TUI section: explicitly says `TUI.stop()` uses the sync helper.
- Test sections: require process-wide `renderMetrics.enable()` / `reset()` or env-before-import; TUI test must assert stop-triggered JSONL persistence without manual flush.
- Done checklist: adds navigation row, sync stop flush, disabled zero-append, and stale summary cleanup.

## Residual risk

The B-stage implementation must choose a sync file append implementation acceptable for `packages/tui` without introducing interactive logging or long blocking behavior. This is bounded to opt-in perf logging and `TUI.stop()` only.
