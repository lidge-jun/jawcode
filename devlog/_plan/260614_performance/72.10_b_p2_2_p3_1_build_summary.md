# 72.10 — P2.2/P3.1 B build summary

Date: 2026-06-15

## Implementation

### P2.2 prepared-line cache

Committed:

- `79fe1ed0 feat: cache prepared tui lines`
- `edf99f6d test: cover prepared line cache commit lane`

Files:

- `packages/tui/src/tui.ts`
- `packages/tui/test/perf-events-p2_0.test.ts`
- `packages/tui/test/prepared-line-cache.test.ts`

Behavior:

- Added bounded content-keyed prepared-line caches (`PREPARED_LINE_CACHE_LIMIT = 4096`, oldest-25% eviction).
- Added printable-ASCII fast path while preserving reset/terminator byte parity.
- Replaced `#applyLineResets()` / `#truncateLinesToWidth()` call sites with `#prepareLinesForTerminal()` in `#doRender()` and `commitLines()`.
- Kept defensive viewport/differential truncation through shared `#truncatePreparedLineToWidth()`; no copied truncation+terminator policy remains.
- Added cache invalidation on `stop()`, `requestRender(true)`, and width change before preparation.
- Replaced P2.0a prepared-line placeholder metrics with real aggregated `tui.preparedLine` render stats and optional commit stats.
- Updated `perf-events-p2_0.test.ts` for real prepared-line JSONL rows.
- Added `prepared-line-cache.test.ts` covering miss→hit byte parity, force/width/stop invalidation, commit-lane shared preparation, truncate snapshot counters, and bounded newest-window hits after >4096 unique lines.

### P3.1 session open single-load

Worktree files:

- `packages/coding-agent/src/session/session-manager.ts`
- `packages/coding-agent/test/session-open-single-load.test.ts`

Commit boundary note: `session-manager.ts` already contains pre-existing resident-cache changes in the same file, so this B summary does not claim a clean source commit for P3.1. The new test file is uncommitted because it depends on the uncommitted source change.

Behavior:

- Added `#adoptLoadedSessionFile(sessionFile, entries)` to own post-load migration/blob/resident/sanitize/index/empty-rewrite behavior.
- `SessionManager.open()` resolves the path once, loads entries once, derives cwd from that load, and adopts directly without `#initSessionFile()` / `setSessionFile()` re-load.
- `setSessionFile()` preserves pre-load teardown/error reset, loads once, and adopts.
- Preserves `#needsFullRewriteOnNextPersist = migrateToCurrentVersion(this.#fileEntries)` in the adoption path.
- Added `session-open-single-load.test.ts` covering resident-heavy single read, missing-file empty rewrite single read, and migrated-session cold full rewrite after one read.

## Verification

PASS:

- `bun test packages/tui/test/prepared-line-cache.test.ts packages/tui/test/perf-events-p2_0.test.ts`
  - 9 pass / 59 expect() calls.
- `bun test packages/tui/test/prepared-line-cache.test.ts packages/tui/test/perf-events-p2_0.test.ts packages/tui/test/input-render-latency.test.ts packages/tui/test/input-render-redteam.test.ts packages/tui/test/commit-lane.test.ts packages/tui/test/viewport-fill.test.ts packages/tui/test/above-viewport-repaint.test.ts`
  - 41 pass / 189 expect() calls.
- `bun --cwd=packages/tui run check`
  - Biome and `tsgo -p tsconfig.json --noEmit` pass.
- `bun test packages/coding-agent/test/session-open-single-load.test.ts packages/coding-agent/test/resident-materialization.test.ts packages/coding-agent/test/session-resident-cache.test.ts packages/coding-agent/test/session-resident-lifecycle.test.ts packages/coding-agent/test/session-resident-ownership.test.ts`
  - 19 pass / 75 expect() calls.
- B verifier retry `73-P22P31BVerifierR2`: DONE.

Residual non-plan gate failures:

- `bun test ... render-goldens.test.ts` currently fails six render-golden fixtures at terminal lifecycle byte-log prefix (`?2004h` / `?25l`), not prepared-line content bytes per verifier classification.
- `bun --cwd=packages/coding-agent run check:types` currently fails in `packages/coding-agent/test/jobs-overlay-model.test.ts` on `JobsSnapshot.backgroundRows` optional mismatch, outside plan 72 touched files.

## Verdict

DONE for B-stage plan acceptance. Proceed to C with the residual failures explicitly routed as environment/unrelated unless C-stage adversarial review finds they are caused by this bundle.
