# D DONE — P2.2/P3.1 adjacent cycle

## P/A/B/C summary

- P: Planned the adjacent measured optimization cycle for P2.2 TUI prepared-line caching and P3.1 session open single-load in `72_p2_2_p3_1_adjacent_plan.md`.
- A: Planner and Architect audits reached PASS for revision `p2p3-r3`, recorded in `72.8_a_p2_2_p3_1_planner_r3_pass.md` and `72.9_a_p2_2_p3_1_architect_r3_pass.md`.
- B: Implemented the prepared-line cache/metrics path and the session open single-load path, then recorded build verification and formatting retry evidence in `72.10_b_p2_2_p3_1_build_summary.md` and `72.12_b_p2_2_p3_1_format_fix.md`.
- C: Full repository check and affected tests passed, recorded in `72.13_c_p2_2_p3_1_check_pass.md`.

## Files changed in the cycle

Committed P2.2 implementation and tests:

- `packages/tui/src/tui.ts`
- `packages/tui/test/prepared-line-cache.test.ts`
- `packages/tui/test/perf-events-p2_0.test.ts`

P3.1 implementation/test files verified in the worktree:

- `packages/coding-agent/src/session/session-manager.ts`
- `packages/coding-agent/test/session-open-single-load.test.ts`
- Resident-session regression suite files exercised by focused tests.

C-stage hygiene:

- `.github/workflows/ci.yml` — commit `91861597` pins the release-capable package job to Node 24 so the Node baseline guard passes.

## Acceptance criteria met

- Prepared-line processing is centralized through the TUI render/commit paths and records real `tui.preparedLine` perf metrics.
- Prepared-line cache invalidates on forced render/width changes and remains bounded under large unique-line churn.
- Prepared-line behavior is covered for ASCII fast paths, ANSI/OSC8 content, wide/combining text, tabs, truncation, stop/new-TUI lifecycle, and commit-lane parity.
- Session open/set-session-file paths use a single load pass for explicit existing files and missing explicit files, while preserving resident materialization and migration rewrite behavior.
- Focused P2.2/P3.1 regressions passed: 60 tests, 264 assertions, 12 files.
- Full `bun run check` passed after C hygiene.
- Remaining performance queue after this cycle: P2.1 assistant streaming child reuse; P3.2 path-only `buildSessionContext()` + P3.3 startup flag dedupe together; P2.3 ctrl+t lazy tail only if measured; P3.4 visible transcript analog; P4 tool registry/schema and token-counting work later.

## WONDER — still missing

- Render-golden fixtures were not updated in this cycle; earlier golden diffs appeared to start at terminal lifecycle bytes rather than prepared-line content, but that remains a separate investigation rather than a silent golden refresh.
- The P3.1 source file contains adjacent resident-cache work from earlier cycles, so commits for that lane should continue to avoid broad path-level staging.
- The next user-reported scroll/viewport anchoring bug while the agent is working and the user is not at bottom was not part of the P2.2/P3.1 acceptance criteria. It has since been routed through a separate scroll PABCD cycle: the major unknown-viewport duplicate/pushed-row failure is fixed; the remaining minor next-input self-healing repaint timing edge is deferred and tracked in `73_scroll_repaint_timing_followup.md`.

## REFLECT — spec improvements

- C acceptance should explicitly say whether render-golden lifecycle byte drift is in-scope or a separately routed terminal harness issue.
- Plans touching files with pre-existing worktree deltas should require an explicit partial-staging/commit strategy up front.
- Scroll-anchor invariants should be added to the performance plan ontology: when the user is scrolled away from bottom, background agent output must not force bottom-follow or duplicate render chunks.
- The later scroll follow-up confirmed that unknown real-terminal viewport state must remain conservative (`isViewportAtBottom() === true` only for append-growth). Repaint glitches that self-heal on next input should be diagnosed as missing immediate repaint/compact triggers before considering tick scheduling rollback.

## Closing verdict

P2.2/P3.1 adjacent PABCD cycle: DONE.

Post-cycle routing result: the reported scroll bug was handled in `../260615_scroll_anchor_duplication/20*.md`; performance devlog follow-up state is recorded in `73_scroll_repaint_timing_followup.md`.
