# 71.8 — P2.0a A-stage audit synthesis round 2

Planner delta verdict: FAIL (`71.6_a_p2_0a_planner_delta_r2.md`)
Architect delta verdict: PASS (`71.7_a_p2_0a_architect_delta_r2.md`)

## Root cause

The remaining Planner failures are wording/acceptance mismatches introduced by the architectural fix: the body correctly gates stop auto-flush on explicit JSONL intent, but the done checklist still sounded unconditional; and the event-shape tests did not reflect the one-row-per-call rule.

## Decisions

- Accept PLANNER-A11: done checklist will say `TUI.stop()` sync flush occurs only when metrics are enabled and `JWC_PERF=1` or `JWC_PERF_LOG` is set. A negative case will assert programmatic `renderMetrics.enable()` without those env vars appends zero bytes.
- Accept PLANNER-A12: document `PI_TUI_METRICS=1` as in-memory compatibility collection only for P2.0a JSONL lifecycle; JSONL auto-flush requires `JWC_PERF=1` or `JWC_PERF_LOG`.
- Accept PLANNER-A13: JSONL integration assertions require two distinct `tui.frame` rows for `frame.count` and `frame.lines`, plus the three placeholder event rows, for five rows per anchored render.
- Accept PLANNER-A14: split enabled buffering from on-disk JSONL emission prerequisites.

## Plan sections changed

- Metrics enable/flush contract.
- TUI integration test assertions.
- Done checklist.

## Residual risk

None beyond normal implementation risk; Architect already passed the integration surface.
