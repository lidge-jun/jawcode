# 71.1 — P2.0a critic round 1

Verdict: ITERATE

Source: `agent://51-P20aPlanCritic`

## Blocking required edits

1. `packages/tui/src/metrics.ts` After block must explicitly state that `RenderMetrics` constructor default changes from `$flag("PI_TUI_METRICS")` to `isPerfMetricsEnabled()`.
2. `packages/tui/src/tui.ts` After block must pin exact call shapes: `recordCounter(source, name, value)` and `recordEvent(source, counters, labels)`. Placeholder rows must be zero-valued.
3. `packages/tui/test/perf-events-p2_0.test.ts` and done checklist must assert JSONL rows by `{ source, counters[shortKey], labels }`, not source-qualified snapshot keys.

## Main-session synthesis

The critic is correct. The plan narrowed scope correctly but still used shorthand names that could be misread at implementation time. The plan will be revised to pin constructor default, call shapes, zero-valued placeholder payloads, and JSONL parsing expectations.
