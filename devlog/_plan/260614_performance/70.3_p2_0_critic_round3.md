# 70.3 — P2.0 critic round 3

Verdict: ITERATE

Source: `agent://45-P20PlanCriticR3`

## Blocking edits

1. TUI JSONL integration fixture must use the process-wide `renderMetrics` sink and drive a real render path, not only an isolated `RenderMetrics` with manual counters.
2. Coding-agent JSONL integration fixture must assert `session.context.entries.materialized` in flushed JSONL, not only `session.open.*`.

## Main-session synthesis

The critic is correct. The plan needs the TUI test to prove the actual `tui.ts` hook path and the coding-agent test to cover both load and materialization domains required by the canonical registry and parent plan 69. The plan will be patched accordingly before rerunning critic.
