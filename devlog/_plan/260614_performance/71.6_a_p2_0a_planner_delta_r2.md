FAIL

[MEDIUM] PLANNER-A11 `71_p2_0a_metrics_core_tui_plan.md:168 vs 70,90` — done checklist says `TUI.stop()` performs sync flush unconditionally; body gates auto-flush on `JWC_PERF=1` or `JWC_PERF_LOG` and excludes programmatic enable() alone — rewrite checklist item to match body and add negative case for enable()+stop without env intent.

[MEDIUM] PLANNER-A12 `71_p2_0a_metrics_core_tui_plan.md:37-38,70,90,100` — `PI_TUI_METRICS=1` enables collection but is omitted from stop-flush intent predicate — either add it to flush intent or explicitly document in-memory-only behavior.

[MEDIUM] PLANNER-A13 `71_p2_0a_metrics_core_tui_plan.md:50-51 vs 133` — one JSONL row per `recordCounter` call conflicts with assertions allowing one `tui.frame` row carrying both `frame.count` and `frame.lines` — require two distinct flushed `tui.frame` lines and total row count includes all five calls.

[LOW] PLANNER-A14 `71_p2_0a_metrics_core_tui_plan.md:167` — done item conflates in-memory buffering with on-disk JSONL emission — split enabled buffering from JSONL flush prerequisites.
