FAIL

[med] ARCH-A13 devlog/_plan/260614_performance/70_p2_0_measurement_plan.md:68 — Persistence contract names flushPerfEventsSync() for lifecycle owners but metrics MODIFY specifies only async flushPerfEvents() while TUI.stop() is synchronous (packages/tui/src/tui.ts:645) — add flushPerfEventsSync to metrics API MODIFY and mandate TUI.stop() call the sync helper; do not leave stop() on fire-and-forget async flush.

[low] ARCH-A14 devlog/_plan/260614_performance/70_p2_0_measurement_plan.md:242 — ARCH-A8 acceptance constraint (env before @gajae-code/tui/metrics import or explicit renderMetrics.enable()) is in synthesis 70.7 but not in perf-events-p2_0.test.ts spec — document Bun preload / dynamic-import order or require replay-harness enable() pattern in the NEW test section.

Most likely to break first: TUI.stop() synchronous JSONL flush — plan names flushPerfEventsSync in persistence but omits it from the metrics API block, so B-stage may async-flush from a void stop() and lose events or block shutdown.
