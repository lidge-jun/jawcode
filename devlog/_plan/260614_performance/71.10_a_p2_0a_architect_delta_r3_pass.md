PASS

Prior Architect PASS remains valid. Planner delta wording fixes introduced no new integration blockers.

Resolved/valid:

- Hook remains pinned after `#truncateLinesToWidth` and before early returns.
- Auto-flush is gated on `JWC_PERF=1` or `JWC_PERF_LOG`; replay harness programmatic enable remains file-safe.
- `reset()` clears new event/counter fields.
- `resolvePerfLogPath()` uses `getLogsDir()`.
- Flush drains on success, retains on failure, and repeated stop is idempotent.
- JSONL placeholders are not compared to `helperStats`.
- Disabled snapshot includes counters/events fields.
- Singleton enable/reset plus isolated `JWC_PERF_LOG` is required; `runReplay` is forbidden for the stop-flush fixture.

New integration blockers: none.
