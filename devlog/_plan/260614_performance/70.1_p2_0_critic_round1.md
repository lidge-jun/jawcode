# 70.1 — P2.0 critic round 1

Verdict: ITERATE

Source: `agent://43-P20PlanCritic`

## Findings

1. **CRITICAL — JSONL persistence is a helper, not a wired deliverable.** Add default path, env override, flush triggers/lifecycle owner, append/mkdir/error policy, and test that `JWC_PERF=1` produces valid one-line JSON rows.
2. **CRITICAL — Verification does not match parent-plan acceptance.** Add named integration test exercising TUI frame, prepared-line placeholders, Markdown rebuild, ctrl+t, session-open/materialization counters in emitted JSONL.
3. **CRITICAL — No canonical counter registry.** Add table mapping source, counter key, hook site, disabled behavior, and placeholder policy.
4. **HIGH — Ambiguous RenderMetrics vs event API.** JSONL events must be the primary P2.0 contract; snapshots can expose summary only.
5. **HIGH — Cross-package metrics boundary unspecified.** State that coding-agent hooks use `@gajae-code/tui/metrics` as the process-wide sink with enabled early-return.
6. **HIGH — Counter scope conflicts with parent plans.** Either defer wrap/prepared-line counters or add zero-valued placeholders with placeholder registry labels.
7. **HIGH — Missing slice-level done-when acceptance.** Add explicit completion checklist.
8. **MEDIUM — Coding-agent verification gate too vague.** Name test files and commands.
9. **MEDIUM — Scope-creep language in session/sdk hooks.** Pin exact functions to instrument and counters-only rule.
10. **MEDIUM — Snapshot extension left as either/or.** Choose one shape.
11. **MEDIUM — Numeric caps unspecified.** Set event/label caps.
12. **MEDIUM — Replay harness / PI_TUI_METRICS migration not addressed.** Document compatibility path and default disabled test.
13. **MEDIUM — Relationship to `jwc.perf-corpus/1` unclear.** Clarify runtime JSONL vs bench schema.
14. **LOW — timestamp format unspecified.** Require ISO-8601 UTC.
15. **LOW — JSONL I/O contract incomplete.** Require UTF-8, one JSON per line, trailing newline, no pretty print, no console fallback.
16. **LOW — ctrl+t source qualifier optional.** Require `labels.renderPhase` or defer.

## Main-session synthesis

The critic is correct. The first draft described the metric API surface but did not make the runtime evidence path concrete enough to prove P2.0 is done. The plan must be revised before A-stage.

Accepted edits:

- Make JSONL events the primary P2.0 deliverable.
- Add process-wide sink and flush policy.
- Add canonical counter registry with exact keys.
- Add explicit test file names and commands.
- Pin hook functions and no-control-flow-change rule.
- Clarify `jwc.perf-events/1` is runtime evidence and separate from `jwc.perf-corpus/1` bench reports.
