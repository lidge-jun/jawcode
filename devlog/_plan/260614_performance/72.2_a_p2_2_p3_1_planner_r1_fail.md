FAIL

[MEDIUM] PLANNER-A1 72_p2_2_p3_1_adjacent_plan.md:9-14,51-115 vs 68_p2_p3_external_audit_synthesis.md:26 / 69_p2_p3_measured_execution_plan.md:74-75 — synthesis and execution plan require printable-ASCII fast path (and utils.ts helper if missing); slice B has no work item, file target, or acceptance check — add explicit fast-path rule in #prepareLineForTerminal and name packages/tui/src/utils.ts only if a new isPrintableAscii (or equivalent) helper is required.
[MEDIUM] PLANNER-A2 72_p2_2_p3_1_adjacent_plan.md:176-195 vs packages/coding-agent/src/session/session-manager.ts:2269 — #adoptLoadedSessionFile() says run migrateToCurrentVersion() but omits assigning #needsFullRewriteOnNextPersist from its return value — adoption must mirror setSessionFile exactly: this.#needsFullRewriteOnNextPersist = migrateToCurrentVersion(entries).
[MEDIUM] PLANNER-A3 72_p2_2_p3_1_adjacent_plan.md:197-201 — open() says resets resident stores as part of adoption but #adoptLoadedSessionFile() body never lists #resetResidentStores() / persist-writer teardown; setSessionFile() pre-load setup is only partially delegated — pin open path: fresh manager + #adoptLoadedSessionFile only, or require the same pre-adoption sequence as setSessionFile() (#closePersistWriter, #resetResidentStores, persist-error clear) before adoption.
[MEDIUM] PLANNER-A4 72_p2_2_p3_1_adjacent_plan.md:207-216 — countingStorage.readTextCountFor(sessionFile) is required but wrapper contract is undefined — specify wrapper API and that exactly one storage.readText(path) occurs per loadEntriesFromFile for the opened path.
[MEDIUM] PLANNER-A5 72_p2_2_p3_1_adjacent_plan.md:113-114,125-129,142 — prepared-line metrics emission shape is unspecified: one aggregated recordEvent per #doRender vs per-line events, and cache tests say tui.preparedLine metrics without naming snapshot().counters keys vs flushed JSONL — pin one event per anchored render with summed hit/miss counters and require snapshot-key assertions in prepared-line-cache.test.ts.
[MEDIUM] PLANNER-A6 72_p2_2_p3_1_adjacent_plan.md:138-141 vs 69_p2_p3_measured_execution_plan.md:96 — byte-oracle fixture omits normalization-sensitive scripts (Thai/Lao AM/NIKHAHIT cases exercised by normalizeTerminalOutput) — add at least one fixture line that changes under normalization and assert miss→hit parity across two renders.
[LOW] PLANNER-A7 72_p2_2_p3_1_adjacent_plan.md:108-111 — cache invalidation on requestRender(true, …) is required at the force entry, but only widthChanged names a #doRender branch; forced same-width redraws must still clear — state cache clear runs inside requestRender when force === true, not only in the widthChanged branch.
[LOW] PLANNER-A8 72_p2_2_p3_1_adjacent_plan.md:151-154 — stop invalidation case says tui.stop() followed by restart/re-render without defining restart — name the fixture: new VirtualTerminal + new TUI, same width, same lines, expect normalize/truncate misses again.
[LOW] PLANNER-A9 72_p2_2_p3_1_adjacent_plan.md:211-217 — resident reopen assertions reference expected by existing resident tests without pinning constants/fixtures — cite one named resident fixture or inline the minimum materialized-context / bounded-captureState() predicates.

The implementer will most likely call migrateToCurrentVersion() inside #adoptLoadedSessionFile() without assigning its boolean return to #needsFullRewriteOnNextPersist, silently breaking the cold full-rewrite persist path after migrated opens.
