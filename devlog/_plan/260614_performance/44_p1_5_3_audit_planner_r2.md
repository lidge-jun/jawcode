PASS

[resolved — accepted] PLANNER-A1 — Mandatory Add File / Move to / failed per-file grouping is now explicit source work, not conditional on failing tests.

[resolved — accepted] PLANNER-A2 — Digest/redteam ownership is separated from staleness-envelope ownership.

[resolved — accepted] PLANNER-A3 — Encoding preservation now requires either a runtime fixture or a named B-summary waiver cited in C-stage.

[resolved — accepted] PLANNER-A4 — `openai.ts` / absent `compaction-estimate-cache.test.ts` are out of scope and parent docs must be aligned.

[resolved — accepted] PLANNER-A5 — B summary must enumerate selector stripping, Add File, Move to, failed per-file grouping, and search defaults as existing vs newly added with test-file evidence.

[resolved — accepted] PLANNER-A6 — Package check semantics are explicit.

[resolved — accepted] PLANNER-A7 — Add File staleness is an explicit acceptance criterion.

The single statement an implementer would most likely misread: the Encoding fixture fallback; B/C artifacts must explicitly name a waiver if no runtime-safe Encoding fixture exists.
