# 71.12 — P2.0a B-stage verifier synthesis round 2

Verifier verdict: NEEDS_FIX

Source: `agent://60-P20aBVerifierR2`

## Issues

1. Missing test for default `resolvePerfLogPath()` using `path.join(getLogsDir(), "perf.jsonl")`.
2. Missing repeated stop/flush idempotence test proving no duplicate rows after successful sync flush.
3. Optional note: direct `JWC_PERF=1` stop branch is unit-tested through `isPerfJsonlFlushEnabled`; actual file-writing integration uses `JWC_PERF_LOG` for safety. This remains acceptable because both hit the same OR-gated `TUI.stop()` branch, while avoiding writes to the real logs directory.

## Decisions

- Add `getLogsDir()` import to `metrics.test.ts` and assert `resolvePerfLogPath({}) === path.join(getLogsDir(), "perf.jsonl")`.
- Update `perf-events-p2_0.test.ts` real TUI fixture to call `tui.stop()` twice and assert exact single rows for each expected source/key group, proving the second stop does not duplicate flushed rows.

## Verification plan

Re-run focused P2.0a gates and TUI package check.
