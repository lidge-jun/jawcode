FAIL

[high] ARCH-A1 packages/tui/src/utils.ts:141 — visibleWidth() already records real helper traffic via recordTextHelper("text.visibleWidth", …) → recordHelper, while the plan also requires zero-valued JSONL placeholders tui.text.visibleWidth.calls emitted from packages/tui/src/tui.ts until P2.1/P2.2 — constrain P2.0 to one owner: either keep JSONL placeholders only in tui.ts and leave utils.ts on recordHelper, or migrate utils.ts to recordCounter("tui.text", …) and drop the "placeholder until exact hook" status for visibleWidth.

[high] ARCH-A2 packages/coding-agent/src/sdk.ts:922 — registry counter sdk.resume.startupFlags.scan ("current branch/startup flag phase") has no matching function, flag scan, or named phase in createAgentSession(); nearest work is split across getSessionBranch (926), buildWorkspaceTree (851–853), and discoverTtsrRules (1037) — name the exact span(s) to instrument (or narrow/rename the counter) before B-stage; do not invent a new startup-flags module.

[high] ARCH-A3 packages/coding-agent/src/modes/components/full-transcript-overlay.ts:59 — eager counters transcript.overlay.entries.rendered / lines.materialized with renderPhase: navigation are unreachable on scroll: #transcriptLines returns cached lines when width is unchanged (60), and handleInput (73–88) only changes #scroll then re-renders a slice — constrain hooks to #renderItems / cache-miss path for initial, and a separate handleInput (or post-scroll render) site for navigation, or relax the JSONL test to allow only initial|unknown.

[med] ARCH-A4 packages/coding-agent/src/session/session-manager.ts:3832 — SessionManager.open() loads the file twice (loadEntriesFromFile(filePath) at 3832, then setSessionFile → loadEntriesFromFile(this.#sessionFile) at 2262); plan lists a single session.open.file.load hook at "open/load path" — pin each counter to one load site (header-probe vs resident hydrate) or accept/document double counting in fixtures.

[med] ARCH-A5 packages/coding-agent/src/sdk.ts:923 — sdk.resume.context.build is scoped to resume in the registry but loadSessionContext / buildSessionContext() runs for every createAgentSession() call, including fresh SessionManager.create() sessions — gate the counter on hasExistingSession / injected resumed manager, or rename to a non-resume-specific source key.

[med] ARCH-A6 packages/coding-agent/src/session/session-manager.ts:3559 — session.context.entries.materialized has no single "context materialization" owner: getEntries() materializes on every call and is reached from buildSessionContext() (3518), getBranch() (3508), and persistence paths — instrument exactly buildSessionContext() (or getEntries() with a one-shot guard per open) so the "non-zero under fixture" assertion is stable and not multiplied by branch/persist calls.

[med] ARCH-A7 packages/tui/src/tui.ts:645 — plan requires flushPerfEvents() on TUI.stop(), but stop() today only clears terminal state (645–680) and replay-harness.ts snapshots metrics after tui.stop() without any flush (230–241) — add async/sync flush at end of stop() when enabled, and have perf-events-p2_0.test.ts set JWC_PERF_LOG to a temp path so harness runs do not depend on a manual flush call only in tests.

[med] ARCH-A8 packages/tui/src/metrics.ts:355 — export const renderMetrics = new RenderMetrics() resolves enablement at module import; JWC_PERF=1 set after @gajae-code/tui/metrics is imported will not re-enable the singleton — constrain tests to set env before import (Bun preload) or follow existing renderMetrics.enable() / reset() pattern from replay-harness.ts:159–161.

[med] ARCH-A9 packages/tui/src/tui.ts:1505 — tui.frame.frame.emittedLines is underspecified: #doRender tracks logical newLines / #previousLines, while terminal bytes are emitted in the diff loop (1505+) with no line counter — define emittedLines as diff-path write count or drop the counter until a single write-path owner exists; do not alias it to frame.lines.

[med] ARCH-A10 devlog/_plan/260614_performance/70_p2_0_measurement_plan.md:271 — verification runs bun --cwd=packages/tui run check but not packages/coding-agent check despite new hooks in assistant-message.ts, full-transcript-overlay.ts, session-manager.ts, and sdk.ts — add bun --cwd=packages/coding-agent run check (or equivalent) to B/C gates so cross-package @gajae-code/tui/metrics imports typecheck.

[low] ARCH-A11 packages/coding-agent — no current renderMetrics / @gajae-code/tui/metrics usage; only getTerminalId from @gajae-code/tui in session-manager.ts:14 — import path is valid via packages/tui/package.json "./*" export to ./src/metrics.ts; keep all coding-agent hooks on the process-wide renderMetrics singleton, not per-instance RenderMetrics.

[low] ARCH-A12 packages/tui/src — no preparedLine / normalize.hit / truncate.hit symbols exist yet (intentional P2.2 placeholders) — emit placeholders only from the planned tui.ts frame hook using recordEvent with labels.placeholder === "true"; do not add a second prepared-line module.

Most likely to break first: coding-agent JSONL coverage for sdk.resume.startupFlags.scan — there is no corresponding startup-flag scan phase in createAgentSession() to hang the counter on, so the new packages/coding-agent/test/perf-events-p2_0.test.ts resume fixture will fail or force an ad hoc hook rename mid-implementation.
