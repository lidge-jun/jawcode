# 72.7 — P2.2/P3.1 A-stage synthesis R2

Date: 2026-06-15

## Inputs

- Planner delta R2: `72.5_a_p2_2_p3_1_planner_r2_fail.md`.
- Architect delta R2: `72.6_a_p2_2_p3_1_architect_r2_pass.md`.
- Revised plan: `72_p2_2_p3_1_adjacent_plan.md` (updated in place for revision `p2p3-r3`).

## Disposition

- PLANNER-A10 — Accepted. Added required case 3 to `session-open-single-load.test.ts`: pre-current-version JSONL fixture, single-read open assertion, first persist-triggering write, and full rewrite/current-version assertion proving `#needsFullRewriteOnNextPersist` survived adoption.
- PLANNER-A11 — Accepted. Inlined `TRUNCATION_NOTICE = "[Session persistence truncated large content]"` in the valid resident-heavy test contract.
- PLANNER-A12 — Accepted. Added done criterion that viewport repaint and differential emit paths must trust prepared lines or use the same shared truncation helper; no second copied truncation+terminator policy may remain after `#prepareLinesForTerminal()`.

## Architect R2

Architect passed r2, but dual-mode runtime requires both Planner and Architect PASS on the same revision. Because the plan changed for r3, both lenses must re-audit `p2p3-r3`.

## Residual risk

No findings are waived. Proceed to r3 delta audit.
