PASS

[LOW] PLANNER-A1 `70_p2_0_measurement_plan.md:22-23` — `## Scope` is empty while boundaries are scattered across MODIFY/non-goals — add a single scoped bullet list (packages/files in/out, placeholder policy, no-optimization rule) that mirrors the MODIFY headers.

[MEDIUM] PLANNER-A2 `70_p2_0_measurement_plan.md:110-112` vs `136-140` vs `153-155` — hook work items mix `recordCounter`, `recordEvent`, and undifferentiated "measurement-only counters" without mapping each registry row to one API — add an API column to the registry (`recordCounter` | `recordEvent`) and repeat it in each MODIFY After block.

[MEDIUM] PLANNER-A3 `70_p2_0_measurement_plan.md:70-76` vs `221` vs `278-279` — wrap/visible-width placeholder rows and `perf-events-p2_0.test.ts` require flushed `tui.text.wrap.calls` / `tui.text.visibleWidth.calls` with `labels.placeholder === "true"`, but the done checklist only names prepared-line placeholders — add an explicit done item for wrap/visible-width placeholder JSONL rows.

[MEDIUM] PLANNER-A4 `70_p2_0_measurement_plan.md:79-80` vs `170` vs `234` — `labels.renderPhase` must be `initial|navigation|unknown`, yet no fixture or assertion requires `initial` or `navigation` (only `unknown` need pass) — require at least one flushed row per exercised phase class or name the single-phase fixture and forbid `unknown`-only closure.

[MEDIUM] PLANNER-A5 `70_p2_0_measurement_plan.md:70` vs `140` — prepared-line registry rows omit `placeholder: "true"` while the table header implies label policy and `tui.ts` mandates placeholder labels — add the label column value for all six placeholder rows to match wrap/visible-width rows.

[MEDIUM] PLANNER-A6 `70_p2_0_measurement_plan.md:51-52` vs `246` vs `64-85` — persistence names bounded in-memory `perf.write.error`, redteam requires a write-error counter, but the canonical registry and done checklist never pin the key — add a registry row and a flushed/snapshot assertion for the exact write-error counter name.

[LOW] PLANNER-A7 `70_p2_0_measurement_plan.md:54` — flush owner says `TUI.stop() / equivalent shutdown path` without listing equivalents — enumerate the shutdown hook(s) in scope (e.g., `TUI.stop()` only) or add a test that names the bound path.

[LOW] PLANNER-A8 `70_p2_0_measurement_plan.md:70` vs `139` — `frame.emittedLines` is optional ("where readily available") with no deferral/placeholder policy, yet it is a non-placeholder registry counter subject to the non-zero done rule — either require a flushed non-zero row in the TUI fixture or reclassify as placeholder/deferred with explicit acceptance.

[LOW] PLANNER-A9 `70_p2_0_measurement_plan.md:225` — Disabled-mode acceptance "creates no file or writes no rows" is ambiguous when a log path already exists — require zero append bytes / zero new lines on an isolated temp path (or truncate-before-test) as the sole criterion.

[LOW] PLANNER-A10 `70_p2_0_measurement_plan.md:236` — SDK resume acceptance allows mid-implementation scope narrowing without a pre-approved fallback counter set — define the allowed narrow alternative (named helper/site + counters) in-plan before B-stage so narrowing is a plan revision, not implementer discretion.

The implementer will most likely treat "placeholder until P2.2/P2.1" as permission to skip enabled-mode JSONL emission entirely, instead of recording zero-valued rows with `labels.placeholder === "true"` on every real TUI render path.
