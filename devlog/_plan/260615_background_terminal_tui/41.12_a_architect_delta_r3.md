PASS

[closed] ARCH-A7 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:80-82 — `resolveBackgroundIds` no longer calls `manager.getJob(id, ownerFilter)`; steps 2-4 use `manager.getJob(id)` plus manual guard `!ownerFilter?.ownerId || job.ownerId === ownerFilter.ownerId` before reading metadata or adding job ids — matches `AsyncJobManager.getJob(id: string)` at `packages/coding-agent/src/async/job-manager.ts:733` and `41.11_a_synthesis_r2.md` resolution.

Most likely first break if implemented as written: steps 3-4 rely on "same owner check" by reference; implementers may add `currentJobId`/`historicalJobIds` without applying the step-2 boolean, leaking cross-owner job ids into follow/cancel.
