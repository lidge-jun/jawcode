PASS

[info] PLANNER-A1..A9 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md — prior Planner findings closed in a-r2 — no further planner-lens change required.
[info] CROSS-LENS devlog/_plan/260615_background_terminal_tui/41.8_a_synthesis_r1.md — shared Architect findings ARCH-A1..A6 are accepted and reflected in the plan — no planner blocker remains.

Most likely misread: Implementers will pass row.id to cancelSubagent() for every sub row, but running task-backed sub rows use AsyncJob ids as row.id and require resolveBackgroundIds() to obtain subagentId before cancelSubagent(subagentId, ownerFilter) and jobIds before readOutputSince().
