FAIL

[high] PLANNER-A1 41_agent_background_contract_plan.md:46-49,58-59,192-193 — `follow` unsupported rows require explanatory text, but details schema lacks machine-checkable unsupported shape — add `follow?: { status: "ok" | "unsupported"; message?: string; ... }` or ToolError rule plus test case 7 assertion.
[high] PLANNER-A2 41_agent_background_contract_plan.md:70-71,92-94 vs jobs-observer.ts row ids — cancel/follow matrix references `subagentId`, but `BackgroundRowView` exposes only `id`; running task-backed `sub` rows use job id while queued subs use subagent id — document id-resolution algorithm using job metadata/subagent records before cancel/follow.
[medium] PLANNER-A3 spec AC4 vs plan detail — detail must convey attention required; `buildBackgroundDetailItems()` does not expose explicit `terminalLatched`/attention — add explicit attention flag/detail item and test assertion.
[medium] PLANNER-A4 spec prompt scope vs plan prompt files — system prompt still describes `job` as generic background tool — add `system-prompt.md` conditional cross-reference or narrow AC7.
[medium] PLANNER-A5 spec launch guidance vs prompt scope — `monitor.md` still instructs cancel/list via `job`, but monitor rows appear in `background list` — add monitor prompt cross-reference and prompt test.
[medium] PLANNER-A6 plan output refs — `agent://` refs required when provable but existing verification path not named — add helper contract using subagent verified output/artifact metadata and tests for present/omitted refs.
[low] PLANNER-A7 plan ordering helper — canonical ordering helper placement conditional risks cycle — mandate NEW `modes/background-row-model.ts` as canonical source.
[low] PLANNER-A8 short-lived observer disposal — disposal is not test-bound — require try/finally observer.dispose and leak-guard test.
[low] PLANNER-A9 registry coverage target ambiguous — pin exact `packages/coding-agent/test/tools.test.ts` cases.

Most likely misread: Implementers will pass `row.id` to `cancelSubagent()` for every `sub` row, but running task-backed sub rows use job ids as `row.id` and require `SubagentRecord` lookup before `cancelSubagent(subagentId, ownerFilter)`.
