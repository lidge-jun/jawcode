ITERATE

# P Critic Round 2 — background footer/manage UI

Source plan: `devlog/_plan/260615_background_terminal_tui/10_p_plan.md`
Synthesis reviewed: `devlog/_plan/260615_background_terminal_tui/10.2_p_synthesis_round1.md`

## R1 closure

F1-F8: closed.

## Blocking findings

- **F9 — acknowledgement API handoff.** The plan mixes `acknowledgeFailures()` and `acknowledgeTerminal()` across observer/controller before/after snippets, leaving unclear which symbol is implemented and which callsites change.
- **F10 — queued job visibility.** Active membership is running/paused only while cancel UI references queued. Queued background work may be omitted despite the locked all-background-work scope.

## Non-blocking notes

- **N1.** Status-line migration rules are still thin.
- **N2.** Add test for `metadata.subagent`-only classification.
- **N3.** Consider testing the `ctrl+x` transient string, not just no app backgrounding.
- **N4.** Consider integration tests for footer update and overlay-open latch clearing.
