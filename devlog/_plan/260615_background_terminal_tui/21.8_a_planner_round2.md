FAIL

[medium] PLANNER-A9 21_cycle1_footer_below_panel.md:318-324 — prior A9 required an explicit rule for the existing status-line generic async `N jobs running` right-slot; a-r2 says do not add new generic counts but does not state whether the legacy right-slot count must be suppressed when composer footer shows `bg …sh` — add normative status-line rule suppressing generic async jobs-running right-slot whenever compact footer backgroundText is set, and add jobs-segment/status-line test asserting no duplicate `N jobs running` alongside `bg 1sh …`.

[low] PLANNER-A14 21_cycle1_footer_below_panel.md:126 vs 21_cycle1_footer_below_panel.md:444-450 — detail dismissal is specified in component behavior but not acceptance/tests — add acceptance and test that opening detail then Esc/back closes overlay and restores editor focus without clearing terminal latches.

[low] PLANNER-A16 21_cycle1_footer_below_panel.md:60-67 vs 21_cycle1_footer_below_panel.md:165-166 — JobsSnapshot exposes both backgroundRows and backgroundCounts but model rules only make backgroundRows canonical — state backgroundCounts is derived from backgroundRows or remove it; build compact tokens from backgroundRows only.

Most likely misread: Failed/cancelled rows clear on the next user message — implementers will still hook acknowledgeTerminalAfterUserMessage from early InputController submit handling instead of only from a centralized real agent prompt helper.
