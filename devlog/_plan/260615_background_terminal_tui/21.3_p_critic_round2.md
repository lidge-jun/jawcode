OKAY

# P Critic Round 2 — footer-below revised plan

Reviewed:
- `20_p_plan_revised.md`
- `21_cycle1_footer_below_panel.md`
- `21.2_p_synthesis_round1.md`

## Closure

R1 blockers closed:

- F1 panel keyboard routing: closed.
- F2 detail surface precision: closed.
- F3 after-visibility latch policy: closed.
- F4 slash/local clear boundary: closed.
- F5 latched-only compact footer visibility: closed.

## Non-blocking A-stage notes

- Treat `backgroundRows` as the canonical footer/panel iterator; align any remaining `backgroundJobs` wording during implementation.
- Wire expanded key routing guard in `InputController` even though the behavior is described under `interactive-mode.ts`.
- Mark `visibleSinceUserMessageSeq` during footer/panel refresh before render.
- Add the field to the concrete `BackgroundJobView` type during implementation.
- Detail UI can be inline selector or small component, but must expose the minimum fields in the plan.
- Align input-controller test wording to clearing-eligible agent prompts.
- Add model coverage for latched-only compact suffix such as `bg 1sh! · alt+x`.

## Summary

Revised roadmap and cycle-1 plan close all plan-quality blockers. Remaining items are A-stage implementation polish, not P-stage blockers.
