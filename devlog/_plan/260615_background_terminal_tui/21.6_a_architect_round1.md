FAIL

[P0] ARCH-A1 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:127 — Proposed JobsSnapshot removes monitors, activeMonitorCount, activeCronCount, worstState, and failedUnacknowledged, but cycle-1 file list omits jobs-overlay-model.ts / status-line/segments.ts even though they still read those fields (packages/coding-agent/src/modes/components/jobs-overlay-model.ts:54, packages/coding-agent/src/modes/components/status-line/segments.ts:358) — Keep legacy monitor/cron summary fields (or derived getters) on JobsSnapshot through cycle 1 and add explicit MODIFY entries for overlay model + jobs segment before landing the reshape.

[P0] ARCH-A2 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:258 — Expanded-panel routing calls backgroundFooterPanel.handleInput(data) while editor keeps focus; TUI only dispatches handleInput to the focused component (packages/tui/src/tui.ts:827), and CustomEditor already consumes ↑/↓ for history (packages/coding-agent/src/modes/components/custom-editor.ts:303) — Specify and implement a concrete non-focus path (editor.setCustomKeyHandler / ui.addInputListener / onInterruptPriority, mirroring tool-focus or BTW), not unfocused Component.handleInput.

[P0] ARCH-A3 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:80 — BackgroundFooterPanel.handleInput(): boolean conflicts with Component.handleInput?: void (packages/tui/src/tui.ts:50) — Drop the boolean return from the component contract; consume keys in InputController (or input listener) and keep panel state methods only.

[P1] ARCH-A4 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:260 — Terminal-row clearing requires a monotonic real user-agent message sequence, but codebase only has signature-based recordLocalSubmission / locallySubmittedUserSignatures (packages/coding-agent/src/modes/interactive-mode.ts:796) with no seq counter — Add #userAgentMessageSeq incremented only inside withLocalSubmission for non-slash, non-local agent prompts and pass that seq into JobsObserver.acknowledgeTerminalAfterUserMessage(seq).

[P1] ARCH-A5 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:222 — Single acknowledgeBackgroundTerminalRowsAfterUserMessage() hook before one session.prompt(...) is insufficient; clearing-eligible submits also flow through streaming queue (packages/coding-agent/src/modes/controllers/input-controller.ts:413), follow-up (:690), idle prompt (:700), and promptCustomMessage skill path (:642) — Centralize the acknowledge call inside withLocalSubmission (or one shared pre-prompt helper) for every agent-turn submission path, excluding slash/bash/python/extension/./c.

[P1] ARCH-A6 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:150 — visibleSinceUserMessageSeq is required in prose but absent from the BackgroundJobView interface (:117-125) and marking is split across UI render vs JobsObserver without a named API — Add visibleSinceUserMessageSeq?: number to the view type plus markTerminalRowsVisible(seq) on JobsObserver, called when compact footer or expanded panel first renders latched rows.

[P1] ARCH-A7 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:8 — Footer compact copy will duplicate existing status-line job surfaces: jobs segment (packages/coding-agent/src/modes/components/status-line/segments.ts:354) plus separate non-monitor async bash count (packages/coding-agent/src/modes/components/status-line.ts:759) — Define cycle-1 ownership: either migrate counts into footer-only and mute/suppress legacy segment fields, or document intentional dual display with non-overlapping kinds.

[P1] ARCH-A8 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:334 — CI gate list omits packages/coding-agent/test/jobs-overlay-model.test.ts even though its snapshot() helper hard-codes removed fields (packages/coding-agent/test/jobs-overlay-model.test.ts:12) — Add overlay-model test and any other JobsSnapshot consumers to §4 verification commands before merge.

[P2] ARCH-A9 devlog/_plan/260615_background_terminal_tui/20_p_plan_revised.md:6 — Source MOC still prescribes BackgroundJobsStrip, ctrl+b, and mount above statusContainer while cycle 1 uses BackgroundFooterPanel, alt+x, and mount after composerFooter — Mark MOC slice 110 as superseded by 20_p/21_cycle1 or add an explicit authoritative doc banner to prevent mixed implementation.

[P2] ARCH-A10 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:49 — buildBackgroundDetailItems + openBackgroundFooterDetail duplicate jobs-overlay-model.ts detail/list builders — Reuse or extend existing overlay model/detail helpers keyed by rowId instead of parallel SelectItem formatters.

[P2] ARCH-A11 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:259 — Read-only detail surface has no mount/focus contract; existing jobs detail uses editorContainer swap via SelectorController.showJobsOverlay — Specify detail host (reuse selector-controller overlay pattern) and Esc/Enter dismissal semantics in the plan.

[P2] ARCH-A12 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:127 — backgroundJobs vs backgroundRows dual arrays are undefined (active-only vs latched-inclusive?) — Name one canonical row source for model/panel (backgroundRows) and make the other a derived view or drop it.

[P2] ARCH-A13 devlog/_plan/260615_background_terminal_tui/22_cycle2_foreground_backgrounding.md:21 — Cycle 2 correctly warns against handleBackgroundCommand(), but foreground bash/subagent handles are still unspecified — Keep cycle 2 blocked until cycle 1 produces an explicit handle inventory in a fresh I/P doc.

[P3] ARCH-A14 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:261 — Line anchor interactive-mode.ts:573-579 is valid — Only comment/invariant text needs updating per plan.

[P3] ARCH-A15 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:29 — Planned imports/signatures match repo conventions — No change needed beyond wiring.

Most likely to break first: implementing the JobsSnapshot reshape in jobs-observer.ts as written will immediately break compilation and focused tests in jobs-overlay-model.ts, status-line/segments.ts, and jobs-overlay-model.test.ts because monitors/activeMonitorCount/worstState are still hard dependencies and are not listed for migration in the cycle-1 file plan.
