# 31.10_a_synthesis_round2

Revision audited: `a-r4-fixed`
Reports:
- Planner `31.8_a_planner_round2.md` — PASS
- Architect `31.9_a_architect_round2.md` — FAIL

All Architect findings accepted; no waivers.

## Resolutions

- ARCH-A24: Add `packages/coding-agent/src/main.ts` to MODIFY scope. Normal idle composer submit through `submitInteractiveInput -> session.prompt` must invoke the same real-user-agent prompt helper, either by routing through an `InteractiveMode` method/callback or by calling `markPendingSubmissionStarted`/helper before `session.prompt`. Add idle composer Enter test.
- ARCH-A25: Add explicit detail-open state API (`isBackgroundFooterDetailOpen()` or private state plus context method). Esc interrupt wrapper must check detail-open first and chain/no-op so detail overlay owns Esc before panel collapse. Test this.
- ARCH-A26: Add mutual exclusion with tool-focus: if tool focus is active, `alt+x` panel expand is blocked with a transient/status notice, or panel expansion first exits/restores tool-focus handlers. Choose the smaller/safest implementation: block panel expand while tool focus is active.
