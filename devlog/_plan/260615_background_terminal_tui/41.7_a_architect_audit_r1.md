FAIL

[P1] ARCH-A1 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:148-157 — exporting `sortBackgroundRows()` from `background-footer-panel-model.ts` and importing it into `jobs-observer.ts` creates a fragile module cycle because model currently imports jobs-observer types — make NEW `packages/coding-agent/src/modes/background-row-model.ts` sole canonical sort source and delete private sort copies.
[P1] ARCH-A2 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:70-71 — cancel/follow matrix assumes subagentId but `BackgroundRowView.id` can be job id or subagent id — specify exact id resolution: getSubagentRecord(row.id), else getJob(row.id)?.metadata?.subagent?.id, else scan records current/historical job ids; never pass job id to cancelSubagent.
[P1] ARCH-A3 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:100-107 — settings op requires current user keybinding but ToolSession has no KeybindingsManager — constrain v1 to `KEYBINDINGS` defaults with source `keybinding-default`, or add optional accessor; test must match chosen contract.
[P2] ARCH-A4 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:108-109 — detail `agent://` refs need verified metadata but BackgroundRowView lacks outputRef/subagentId — derive only when session artifact/output APIs verify metadata, otherwise omit; add helper contract.
[P2] ARCH-A5 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:140 — createTools allow-rule branch is redundant vs JobTool pattern — mirror job with `BackgroundTool.createIf` in BUILTIN_TOOLS and drop separate isToolAllowed branch.
[P3] ARCH-A6 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:85 — prompt import outline omits `with { type: "text" }` — specify text import.

Most likely first break: background cancel/follow on a running sub row will call `cancelSubagent(row.id)` or `readOutputSince(row.id)` while row.id is the AsyncJob id, not the subagentId expected by cancelSubagent.
