# 10.4_p_synthesis_round2 — Critic ITERATE response

Review: `devlog/_plan/260615_background_terminal_tui/10.3_p_critic_round2.md`
Plan: `devlog/_plan/260615_background_terminal_tui/10_p_plan.md`

## Decision summary

Both R2 blocking findings are accepted. The plan is revised rather than waived.

## Finding responses

- **F9 accepted.** `JobsObserver.acknowledgeTerminal()` is the canonical new method. `JobsOverlayController` also exposes `acknowledgeTerminal()`. Existing `acknowledgeFailures()` may remain only as a compatibility wrapper if needed by tests/callsites during the same patch, but new code and plan snippets use `acknowledgeTerminal()` consistently.
- **F10 accepted.** Queued background work is visible and active for footer/overlay purposes. Active async statuses are `running`, `paused`, and `queued` where queued records are observable through current manager/subagent record APIs. If queued subagents are not represented as `AsyncJob`s, the observer must synthesize queued `BackgroundAsyncJobView` rows from subagent records rather than dropping them.

## Plan sections changed

- Phase 1 snapshot behavior and active status definition.
- Phase 3 overlay controller before/after snippets.
- Phase 7 test coverage for queued jobs and metadata-only subagent classification.
- Acceptance criteria for queued visibility.
