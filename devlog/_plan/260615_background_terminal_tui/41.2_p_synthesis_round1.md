# P synthesis round 1 — agent background contract

Critic verdict: ITERATE (`41.1_p_critic_round1.md`)

## Evidence checked

- `packages/coding-agent/src/tools/renderers.ts` has explicit `toolRenderers` map and currently registers `jobToolRenderer` at lines 24-66.
- `packages/coding-agent/src/config/settings-schema.ts` uses `bash.autoBackground.enabled` and `bash.autoBackground.thresholdMs`, not `bash.autoBackground`.
- `packages/coding-agent/src/async/job-manager.ts` exposes `cancelSubagent(subagentId, filter)` for running/paused/queued subagents.
- `packages/coding-agent/src/tools/subagent.ts` already surfaces `agent://<subagentId>` output refs for verified subagent outputs.

## Finding decisions

- F1 `job.md` omitted — accepted. Plan now modifies `packages/coding-agent/src/prompts/tools/job.md` to explain normative split: `job` remains legacy async job polling/cancel; `background` is canonical row/list/detail/follow/settings management.
- F2 renderer registration — accepted. Plan now mandates modifying `packages/coding-agent/src/tools/renderers.ts`.
- F3 settings discovery — accepted. Plan now uses exact keys and calls `session.keybindings` is not available; instead implementation should expose keybindings through a small `ToolSession` addition or a helper that can resolve app keybindings from `KeybindingsManager`. If adding `keybindings` to `ToolSession` is too invasive, `background settings` may report the default `app.background.expand` binding from `KEYBINDINGS` with source `keybinding-default`.
- F4 detail artifact/output refs — accepted. Plan now pins detail to reuse/export `buildBackgroundDetailItems()` semantics and include `agent://` output refs for completed subagents when visible through existing output manager/record metadata.
- F5 cancel matrix — accepted. Plan now includes normative per-kind/status cancel table.
- F6 row ordering — accepted. Plan now names `sortBackgroundRows()` as the single canonical source exported from `background-footer-panel-model.ts`, and `JobsObserver` should reuse it or move current duplicate sort into a shared helper.
- F7 enablement gate — accepted. Plan now sets `background` as discoverable, not essential, and gated by `isBackgroundJobSupportEnabled(session.settings)` like `job`.
- F8 test gaps — accepted. Plan now adds explicit list limit/max, task-backed sub follow, and prompt tests.

## Changed plan sections

- Proposed API details.
- File-level plan for `background.ts`, `renderers.ts`, `background-footer-panel-model.ts`, `jobs-observer.ts`, `job.md`, settings/keybinding discovery.
- Cancel matrix.
- Test plan and acceptance mapping.
