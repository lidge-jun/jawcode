# 21.2_p_synthesis_round1 — Critic ITERATE response

Review: `21.1_p_critic_round1.md`
Plan docs: `20_p_plan_revised.md`, `21_cycle1_footer_below_panel.md`

## Decision summary

All blocking findings are accepted. The plan is revised rather than waived.

## Finding responses

- **F1 accepted.** Define key routing: while the panel is expanded, `InputController` custom handlers route up/down/Enter/Esc to `BackgroundFooterPanel.handleInput()` before editor navigation. `Esc` collapses the panel. Focus remains on editor; the panel is a footer-mode controller, not TUI focus replacement.
- **F2 accepted.** Define detail surface: cycle 1 adds a read-only detail selector/panel with minimum fields (kind, label, status, age/schedule, last output/result/error preview). `Enter` must open it. Existing jobs overlay reuse is no longer conditional in the plan.
- **F3 accepted.** Add explicit latch state: failed/cancelled rows get `visibleSinceMessageSeq` when rendered in compact footer or expanded panel. They clear only on the next real user-agent message with a greater message sequence.
- **F4 accepted.** Define “user message” as a non-empty prompt that proceeds to agent/session submission, not slash commands, local commands, empty continuation, or extension-handled input. Move the clear hook to immediately before actual `session.prompt()` / bash? For this feature, clear only before regular agent prompt submission, not slash/help/local commands.
- **F5 accepted.** Compact footer remains visible for latched-only failed/cancelled rows using attention copy, e.g. `bg 1sh! · alt+x`; `totalVisible` includes active rows plus terminal latched rows.
- **F6 accepted opportunistically.** Cron row source is canonicalized through `backgroundRows`; raw `crons` may remain only for legacy overlay/status compatibility.
- **F7 accepted opportunistically.** Add `status-line.ts` as an explicit modify target to adapt compatibility fields or stop depending on removed fields.

## Plan sections changed

- `20_p_plan_revised.md`: first-cycle file surface adds status-line target and revised lifecycle language.
- `21_cycle1_footer_below_panel.md`: product behavior, model shape, panel input behavior, detail surface, observer lifecycle state, input-controller hook placement, interactive-mode key routing, tests, and acceptance criteria.
