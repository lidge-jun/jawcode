PASS

[closed] ARCH-A24 — idle submit path through main.ts is now in MODIFY scope and must invoke shared real-user-agent prompt helper.
[closed] ARCH-A25 — detail-open Esc precedence is specified; residual P3 asks to add the context API to the types outline.
[closed] ARCH-A26 — tool-focus mutual exclusion specified.
[closed] prior findings ARCH-A16..A23 and PLANNER-A1..A7.

Residual non-blocking polish:
- ARCH-A27: header still says a-r4-fixed; rename banner.
- ARCH-A28: add isBackgroundFooterDetailOpen() to InteractiveModeContext block.

Most likely first breakage: forgetting to hook #prepareRealUserAgentPromptSubmission() into idle onInputCallback → submitInteractiveInput path.
