FAIL

[closed] ARCH-A1 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:102-108 — neutral `modes/background-row-model.ts` mandated — no further change.
[closed] ARCH-A2 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:65-83,92-97 — subagent id vs job id separation specified — no further change.
[closed] ARCH-A3 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:63,137,220 — keybinding default source specified — no further change.
[closed] ARCH-A4 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:138,209-210 — verified output ref helper/omit behavior specified — no further change.
[closed] ARCH-A5 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:148-149 — BackgroundTool.createIf registration mirrors JobTool — no further change.
[closed] ARCH-A6 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:126-127 — prompt import uses `with { type: "text" }` — no further change.
[P1] ARCH-A7 devlog/_plan/260615_background_terminal_tui/41_agent_background_contract_plan.md:80-82 — `resolveBackgroundIds` pseudocode calls `manager.getJob(row.id, ownerFilter)` but `AsyncJobManager.getJob` accepts only `(id: string)`; owner scoping must be manual — rewrite steps 2–3 to `getJob(row.id)` then reject if `ownerFilter?.ownerId && job.ownerId !== ownerFilter.ownerId` before using job metadata or adding job ids.

Most likely first break: `manager.getJob(row.id, ownerFilter)` in `resolveBackgroundIds` will not compile as written or could leak cross-owner ids if the ignored arg is removed without an owner guard.
