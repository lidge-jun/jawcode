ITERATE

# P Critic Round 1 — footer-below revised plan

Reviewed:
- `20_p_plan_revised.md`
- `21_cycle1_footer_below_panel.md`
- `22_cycle2_foreground_backgrounding.md`
- `23_cycle3_detached_terminal.md`

## Blocking findings

- **F1 — Panel keyboard routing unspecified.** Row selection/Enter detail exists, but focus/key-priority through input-controller/interactive-mode is undefined.
- **F2 — Detail surface handoff ambiguous.** `openBackgroundFooterDetail()` can reuse overlay or selector but lacks minimum fields and targets/tests.
- **F3 — “After visibility” latch policy not closed.** Need explicit state machine for failed/cancelled visible → next user message clears.
- **F4 — Unsafe latch clear on slash/command submit.** Current hook position could clear on `/help` or other non-chat commands.
- **F5 — Compact footer vs latched-only terminal rows.** Footer can disappear if active counts are zero but failed/cancelled rows remain latched.

## Non-blocking findings addressed opportunistically

- **F6 — Cron representation dual-path.** Clarify canonical panel row source.
- **F7 — status-line migration thin.** Add `status-line.ts` target/compatibility rule.

## Summary

Roadmap direction is sound but cycle 1 needs precise input routing, detail UI, latch lifecycle, and footer visibility semantics before A-stage.
