# 21.10_a_synthesis_round2_cap — A-stage round-cap escalation

Revision audited: `a-r2`
Reports:
- Planner: `21.8_a_planner_round2.md` — FAIL
- Architect: `21.9_a_architect_round2.md` — FAIL
Runtime: `jwc orchestrate verdict --audit-lens architect --revision-id a-r2 ...` returned `verdict=fail a_round=3 — round cap reached (≤3): escalate to the user (D050-20)`.

## Status

A-stage audit did not pass. Per PABCD A-stage rule, implementation must not begin and `jwc orchestrate b` must not be run until the user resolves the escalation path.

## Remaining blocking findings to resolve in a future plan revision

### Input routing / panel lifecycle

- Esc cannot rely on custom key handlers because CustomEditor handles app interrupt/onEscape before custom handlers. Plan must route expanded-panel Esc through `editor.onInterruptPriority` or an `onEscape` guard.
- Up/down/Enter handlers must be active only while the panel is expanded, then removed/disabled on collapse; static handlers would break collapsed editor navigation.

### Prompt acknowledgement lifecycle

- Terminal latch acknowledgement must be centralized in a shared real-agent-prompt helper, not only `withLocalSubmission`, because compaction/queued direct prompt paths can bypass that wrapper.
- The helper must increment `#userAgentMessageSeq`, call `JobsObserver.acknowledgeTerminalAfterUserMessage(seq)`, and refresh footer/panel only for real user-agent submissions.

### Status-line ownership

- Cycle 1 must explicitly suppress/remove the existing non-monitor async `N jobs running` status-line block so footer/panel owns generic async/subagent counts.
- `jobs-segment.test.ts` must be updated to assert no duplicate legacy generic async count when footer background text is active.

### Retained terminal rows

- JobsObserver needs an id-keyed retained latched-row map populated on failed/cancelled terminal transition and pruned only by `acknowledgeTerminalAfterUserMessage`.
- Retained rows must merge into `backgroundRows` even after AsyncJobManager retention evicts the original job.

### Planner polish

- `backgroundCounts` must be explicitly derived from `backgroundRows`, or removed from the public snapshot.
- Detail dismissal must have acceptance/test coverage: Esc/back closes detail and restores editor focus without clearing terminal latches.

## Recommended escalation decision

Because the round cap is reached, do not continue ad-hoc re-audits in this A-stage. Recommended next step is to return to P-stage with these findings as mandatory plan constraints, produce an `a-r3-fixed`/new P revision, and re-enter A only after the plan is simplified enough to pass within the gate.
