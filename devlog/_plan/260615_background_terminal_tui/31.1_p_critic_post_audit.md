ITERATE

# P Critic — post A-stage round-cap revision

Reviewed spec: `.jwc/specs/jaw-interview-footer-below-background-ui-cycle1.md`
Reviewed plan: `21_cycle1_footer_below_panel.md`

## Blocking findings

- **F1 Esc routing.** Plan must route expanded-panel Esc via `editor.onInterruptPriority` or `onEscape` guard, not custom key handler.
- **F2 Expanded-only handler lifecycle.** Up/down/Enter handlers must install/activate only on expand and remove/disable on collapse.
- **F3 Retained terminal rows.** JobsObserver needs id-keyed retained latched-row map merged into `backgroundRows` after AsyncJobManager eviction.
- **F4 backgroundCounts derivation.** `backgroundCounts` must derive from `backgroundRows` or be removed.
- **F5 Status-line suppression.** Existing non-monitor async `N jobs running` status-line block must be suppressed/removed when footer background text is active.
- **F6 Prompt acknowledgement helper.** Shared real-user-agent-prompt pre-submit helper needs a named API and callsite list beyond `withLocalSubmission`.

## Non-blocking

- **F7.** Detail dismissal needs dedicated test/AC.
- **F8.** Plan should cite `21.10_a_synthesis_round2_cap.md` constraints as incorporated.
