FAIL

[P0] ARCH-A2 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:264 — Expanded-panel Esc is still specified via custom key handlers, but CustomEditor dispatches escape through app.interrupt/onEscape before customKeyHandlers are consulted (packages/coding-agent/src/modes/components/custom-editor.ts:430-441,479) — route expanded-panel Esc through editor.onInterruptPriority or an onEscape guard that calls handleBackgroundFooterPanelKey('escape') when panel.isExpanded(); do not rely on setCustomKeyHandler('escape').

[P1] ARCH-A2 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:262 — Plan places up/down/Enter/Esc routing in setupKeyHandlers but never requires install-on-expand/remove-on-collapse; static custom handlers cannot delegate to normal editor history when collapsed — mirror tool-focus: register up/down/enter handlers only while backgroundFooterPanel.isExpanded() and remove them in collapse/toggleBackgroundFooterPanel; never leave handlers active when collapsed.

[P1] ARCH-A5 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:274 — Centralized seq/acknowledge is pinned to withLocalSubmission, but compaction-resume first prompt uses recordLocalSubmission + session.prompt directly, bypassing the wrapper (packages/coding-agent/src/modes/utils/ui-helpers.ts:791-795) — extract one shared pre-prompt helper and call it from withLocalSubmission and every compaction/queued direct prompt path that counts as a real user-agent submission.

[P1] ARCH-A7 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:323 — Ownership rule says status line must not show generic async/subagent counts, but status-line.ts still renders non-monitor getAsyncJobSnapshot running jobs and jobs-segment.test expects job running for non-monitor bash — explicitly remove/suppress the status-line.ts runningBackgroundJobs block in cycle 1 and update jobs-segment.test to assert footer-owned counts instead of legacy non-monitor job running text.

[P1] ARCH-A16 devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md:192 — Latched failed/cancelled rows are recomputed only from live AsyncJobManager jobs; after retention eviction the row vanishes unless observer keeps a tombstone store — add JobsObserver-side retained latched-row map populated on terminal transition and pruned only by acknowledgeTerminalAfterUserMessage; merge retained rows into backgroundRows on every recompute.

Most likely to break first: Static up/down/enter custom-key registration in setupKeyHandlers without expand/collapse lifecycle will break collapsed editor navigation immediately; Esc will also fail to collapse the panel because escape is handled before custom handlers.
