# C-stage check verdict

Date: 2026-06-15
Stage: PABCD C — CHECK
Plan ref: `devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md`

## Mechanical gates

- Workspace gate: `bun run check` from repo root — PASS.
  - Includes root Biome, Node 20 baseline, schema check, JWC UI/rebrand gates, workspace package checks, and Rust scope/check gate.
- Focused affected tests: `bun test test/background-footer-panel-model.test.ts test/jobs-observer.test.ts test/jobs-overlay-model.test.ts test/jobs-segment.test.ts test/main-interactive-input.test.ts` from `packages/coding-agent` — PASS (35 pass, 0 fail, 120 assertions).
- Package typecheck: `bun run check:types` from `packages/coding-agent` — PASS.

## Adversarial review against acceptance criteria

- Compact footer copy is generated from `backgroundRows` rather than stale count fields, uses `sub sh mon cron q` count order, and appends `!` for failed/cancelled/terminal-latched rows.
- Expanded panel is mounted below `ComposerFooter`, renders at most three rows, and is driven by `alt+x`.
- Expanded lifetime handlers are installed and removed by `InteractiveMode.toggleBackgroundFooterPanel()` through `InputController`; Esc is routed through `editor.onInterruptPriority`, while detail-open state prevents panel collapse.
- Tool-focus mode blocks panel expansion with a user-visible status instead of competing for up/down/enter handlers.
- Detail view is read-only and covers kind, label, status, description, started/next schedule, output/result/error previews, and Back.
- Terminal failed/cancelled rows are retained independently of `AsyncJobManager` eviction and only clear after being visible before a later real user-agent prompt.
- Successful completed jobs disappear from background rows.
- Legacy `/jobs` monitor/cron overlay remains backed by the existing monitor/cron snapshot fields.
- The old generic non-monitor status-line job count is suppressed only when footer rows include shell/subagent rows; monitor/cron status segments remain visible through `SegmentContext.jobs`.

## Residual risks

- No interactive TUI screenshot/e2e was run in this C pass; coverage is by unit tests, typecheck, workspace gates, and read-only source/verifier review.

## Verdict

PASS — all C-stage gates are green for cycle 1.
