# P synthesis round 2 — agent background contract

Critic verdict: ITERATE (`41.3_p_critic_round2.md`)

## Finding decisions

- F9 blocking — accepted. The spec explicitly requires prompt guidance for when the agent should launch long-running work in background and how user-facing TUI footer rows relate to tool-visible background rows. Plan r3 now adds mandatory prompt bullets to `background.md`, `task.md`, and `bash.md` and maps them to AC7.
- F10 non-blocking — accepted as residual implementation choice. Plan r3 keeps fallback to `KEYBINDINGS` default but recommends adding `keybindings?: KeybindingsManager` to `ToolSession` only if an existing context can supply it cleanly.
- F11 non-blocking — accepted. Plan r3 names concrete test file `packages/coding-agent/test/background-tool-prompts.test.ts`.

## Changed plan sections

- `background.md` outline now includes launch timing and TUI/footer parity requirements.
- `task.md` and `bash.md` modifications now explicitly say long-running work should be background-launched when appropriate and then managed through `background`.
- Tests now include `background-tool-prompts.test.ts` with specific assertions for launch timing, footer parity, and job/background split.
- Acceptance mapping AC7 now cites the concrete prompt test file and required prompt bullets.
