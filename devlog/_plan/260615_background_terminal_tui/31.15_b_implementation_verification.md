# B-stage implementation + verifier summary

Date: 2026-06-15
Stage: PABCD B — BUILD
Plan ref: `devlog/_plan/260615_background_terminal_tui/21_cycle1_footer_below_panel.md`

## Implemented patch surface

- Added `JobsObserver` background row snapshots for `sub`, `sh`, `mon`, `cron`, and queued subagent rows while preserving legacy monitor/cron snapshot fields for `/jobs` and status segments.
- Added terminal failed/cancelled row retention with `visibleSinceUserMessageSeq` and later real-prompt acknowledgement clearing.
- Added compact footer model copy (`bg ... · alt+x`), canonical row ordering, detail items, footer-below max-3-row panel, and read-only detail panel.
- Wired `alt+x` to expand/collapse the footer-below panel, with expanded-lifetime up/down/enter handlers and Esc routed through `editor.onInterruptPriority`.
- Wired real user-agent prompt acknowledgement through `InteractiveMode.prepareRealUserAgentPromptSubmission()` for idle submissions, initial prompt flows, `withLocalSubmission`, and compaction direct prompt delivery.
- Added status-line suppression for the old generic non-monitor async job count when the new footer owns visible shell/subagent rows.

## Verification

- Focused tests passed: `bun test test/background-footer-panel-model.test.ts test/jobs-observer.test.ts test/jobs-overlay-model.test.ts test/jobs-segment.test.ts test/main-interactive-input.test.ts` → 35 pass, 0 fail, 120 assertions.
- Package typecheck passed: `bun run check:types`.
- Package-wide `bun run check` was attempted and is blocked by unrelated pre-existing Biome issues outside this patch surface (`src/discovery/cli-jaw.ts`, `src/extensibility/skills.ts`, `src/session/session-manager.ts`, and unrelated tests). Changed files were formatted with focused Biome checks.

## Read-only verifier results

- `21-VerifyJobsModel`: NEEDS_FIX for model ordering, retained-row eviction edge, and focused coverage gaps.
- `22-VerifyTuiWiring`: DONE.
- `23-VerifyRenderStatus`: DONE.
- Fixes applied for `21-VerifyJobsModel` concerns: canonical model sort, acknowledged retained-row skip/delete, and expanded model/observer tests.
- `24-VerifyJobsModelR2`: NEEDS_FIX only for missing detail-item assertions.
- Fix applied: detail test now asserts Kind, Label, Status, Description, Started/Next, Output/Result/Error, and Back.
- `25-VerifyJobsModelR3`: DONE.

## B-stage verdict

DONE — implementation and read-only verifier pass are complete for cycle 1.
