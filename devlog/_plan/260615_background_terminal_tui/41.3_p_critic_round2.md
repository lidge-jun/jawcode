# P Critic Round 2 — agent background contract

Verdict: ITERATE

Summary:

Round-1 findings F1–F8 are closed in r2. One blocking prompt-scope gap remains vs spec; two minor non-blocking ambiguities.

Findings:

- R1-F1 closed — plan now modifies `packages/coding-agent/src/prompts/tools/job.md` with normative `job` vs `background` split.
- R1-F2 closed — plan now mandates `packages/coding-agent/src/tools/renderers.ts` update with `backgroundToolRenderer`.
- R1-F3 closed — exact settings keys and keybinding source handling are specified.
- R1-F4 closed — detail pins `buildBackgroundDetailItems()` reuse and verified `agent://` refs when provable.
- R1-F5 closed — normative cancel matrix covers sh/mon/sub/cron/q/cross-owner/terminal cases.
- R1-F6 closed — single `sortBackgroundRows()` canonical source with neutral module escape hatch if import cycle.
- R1-F7 closed — `background` discoverable, not essential, gated by `isBackgroundJobSupportEnabled` like `job`.
- R1-F8 closed — list limit/max, task-backed sub follow, named prompt tests.
- F9 blocking — prompt scope must explicitly explain when to launch long-running background work and how TUI footer rows relate to tool-visible rows (`alt+x` parity). Current `background.md` outline covers operations but not those spec bullets.
- F10 non-blocking — keybinding resolution dual-path remains acceptable with fallback.
- F11 non-blocking — prompt test file name not fixed; can resolve by naming a concrete test file.
