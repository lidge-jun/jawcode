# P Critic Round 1 — agent background contract

Verdict: ITERATE

Findings:

- F1 blocking — `job.md` omitted. Spec AC7 requires prompt guidance; plan updates `task.md` and `bash.md` only. `job` already exposes list/cancel/poll and needs cross-reference or normative split vs `background`.
- F2 blocking — renderer registration. `renderers.ts` uses explicit `toolRenderers` map; `backgroundToolRenderer` should be mandatory MODIFY, not conditional.
- F3 blocking — settings discovery. Test lists `bash.autoBackground`, but actual keys are `bash.autoBackground.enabled` and `.thresholdMs`; `readBackgroundSettings()` lacks keybinding resolution outline.
- F4 blocking — detail scope. Spec AC4 requires artifact/output references; plan does not pin reuse of `buildBackgroundDetailItems()` or subagent `agent://` artifact surfacing.
- F5 blocking — cancel matrix. Per-kind/status cancel rules ambiguous despite `AsyncJobManager.cancelSubagent` existing; cron/latched/paused/cross-owner cases need normative table.
- F6 blocking — row ordering. `sortRows` duplicated in jobs-observer and background-footer-panel-model; plan must state single canonical source for list ordering.
- F7 blocking — enablement gate. Discoverable vs essential registration and `isBackgroundJobSupportEnabled` rule left open for A-stage.
- F8 blocking — test gaps. AC3 list limit/max, task-backed sub follow, and named prompt tests not fully specified.

Strengths:

- Clear background op API (`list/detail/follow/cancel/settings`).
- Reuses canonical `BackgroundRowView`/`JobsObserver`.
- File paths, verification commands, and AC mapping present.
- Non-goals and no settings mutation clear.
- P-stage only — no source mutation in plan.
