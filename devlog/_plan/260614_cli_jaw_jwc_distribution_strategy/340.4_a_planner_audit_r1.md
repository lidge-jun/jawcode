FAIL

[HIGH] PLANNER-A4 `pending-approval.md:76-84` / `330_jawcode_cli_jaw_bootstrap_plan.md:484-507` — PARTIALLY ACCEPTED: blocking vs report-only native probe semantics are recorded, but no per-probe command/test-file ownership, no registry-faithful invocation flag, and `330.6` After `validate:jwc-release` steps add only `cli-jaw home bootstrap` while omitting registry-faithful install, pack allowlist/count, postinstall mode matrix, and native probe steps promised in pending work item 8. — Enumerate one named gate per probe (script/test path + env/CLI flag + skip matrix) and mirror the full step list in `330.6` After block or a single canonical `scripts/jwc-release-validation.ts` excerpt.

[MEDIUM] PLANNER-A6 `pending-approval.md:98-111` — PARTIALLY ACCEPTED: `130` contract items are mostly covered via `180` rows and work items 6–8, but acceptance criteria still lack explicit checkable rows for standalone runtime resolver smoke (`verify-runtime.cjs`) and no `workspace:`/`file:` runtime dependency proof. — Add two AC bullets naming `node packages/jwc/scripts/verify-runtime.cjs` (or postinstall chain proof) and manifest/pack contract command.

[MEDIUM] PLANNER-A2 `330_jawcode_cli_jaw_bootstrap_plan.md:517` — PARTIALLY ACCEPTED: seed JSON and test case 6 now require `cli = "codex"`, but `330` acceptance criteria still say settings are seeded with "current cli-jaw defaults" without the codex override. — Change acceptance bullet to require `cli = "codex"` explicitly.

[MEDIUM] PLANNER-A1 `340.3_a_synthesis_r0.md:88-105` — ACCEPTED with new ambiguity: synthesis still lists plan sections "to change" in future tense while `pending-approval.md`, `180`, and `240` already contain those revisions; revision `a-r1` audit target is unclear. — Mark synthesis packet as applied-to-r1 or replace "Exact plan sections to change" with "Applied in r1" pointers.

[LOW] PLANNER-A10 `pending-approval.md:35-37` vs `330_jawcode_cli_jaw_bootstrap_plan.md:165` — ACCEPTED with new ordering ambiguity: home-resolver/discovery alignment (work item 1) is required before claiming `CLI_JAW_HOME` support, but pending B-stage list has no execution-order dependency between items 1 and 3. — State item 1 must land before bootstrap/CLI_JAW_HOME AC can be considered satisfied.

Prior findings accepted as resolved: PLANNER-A1 (work items + AC in pending), PLANNER-A3 (postinstall settings timing in pending:24-26), PLANNER-A5 (PATH scrub/`jwc --help`/postinstall matrix in pending:66-74 and `330.4`), PLANNER-A7 (`180` bootstrap/native rows), PLANNER-A8 (`240` 10-file allowlist), PLANNER-A9 (`240` Slice B names `ci.yml`/`release.yml`), PLANNER-A11 (reproducible baseline AC in pending:101-102), PLANNER-A12 (skip/safe JSON semantics in pending:48-49 and `330.2:157-158`), PLANNER-A13 (package-local Bun AC/work item 6), PLANNER-A14 (`.d.ts` deferred in pending:31).

Most likely misread: **`330.6` After block is the full `validate:jwc-release` contract** — it only adds the bootstrap unit test step; registry-faithful install, native probes, and pack allowlist gates exist only in pending prose and `180` matrix rows, not in the folded `330` release-validation excerpt.
