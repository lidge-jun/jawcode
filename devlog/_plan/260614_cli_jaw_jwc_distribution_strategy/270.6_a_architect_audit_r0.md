FAIL

# 270.6 — A-stage architect audit r0

## Findings

- [P0] ARCH-A1 `../cli-jaw/tests/unit/cli-registry.test.ts:214-217` — Plan updates 13→14 CLI_KEYS but never touches `src/cli/readiness.ts`; `jwc` is in `CLI_KEYS` while `DEFAULT_ORDER` omits it; existing test requires every `CLI_KEYS` entry in `DEFAULT_ORDER`. — Add explicit plan step to insert `jwc` into `DEFAULT_ORDER` or narrow/waive that test.
- [P1] ARCH-A2 `../cli-jaw/public/manager/src/settings/pages/components/agent/RuntimeHeader.tsx:50-56` — Plan claims `PRIMARY_CLIS` ordering drives RuntimeHeader, but code filters `cliOptions` in `Object.keys(perCli)`/registry order (`Agent.tsx:169`); `jwc` would render after `gemini/grok`, not between `claude-e` and `agy`. — Add RuntimeHeader sort-by-PRIMARY_CLIS step or relax acceptance to primary band only.
- [P1] ARCH-A3 plan section 5 — `registerSettingsRoutes` pulls heavy settings/quota/MCP deps; no existing HTTP harness; `buildLiveCliRegistry` always awaits `fetchKiroModelInventory`. — Pin `ok()` path as `body.data.jwc.*`; mock Kiro inventory in route test or mandate curl smoke with port discovery.
- [P2] ARCH-A4 plan section 2 — `CLI_META.jwc` duplicates `CLI_REGISTRY.jwc` models/efforts with no sync helper. — Keep arrays byte-identical and add `metaFor` assertions; document drift risk.
- [P2] ARCH-A5 plan evidence table — Most anchors valid (`registry.ts:146-157`, `registry-live.ts:4-16`, `settings.ts:310-311`, `agent-meta.ts:28`, `RuntimeHeader.tsx:50-56`, `cli-registry.test.ts:19-21`, `manager-settings-model.test.ts:113-131`, `packages/jwc/package.json`, `cli-jaw/package.json:143`).
- [P3] ARCH-A6 plan verification — Focused test command omits mandatory API-proof file when route test is added. — B-stage must run both command blocks or combine them.

Most likely break: Existing readiness default order covers every canonical CLI test (`cli-registry.test.ts:214-217`) — `jwc` is already a canonical registry key but absent from `readiness.ts:154 DEFAULT_ORDER`, and the plan does not fix that mismatch.
