# 270.16 — D-stage summary

## Cycle summary

- P: Planned 270 as a cross-repo slice for cli-jaw Manager selector and `/api/cli-registry` JWC exposure, with jawcode package evidence paired to cli-jaw registry/API/frontend paths.
- A: Audited the plan through Planner and Architect lenses; the final handoff resolved live-registry `experimental`, frontend/backend metadata parity, readiness-boundary, and Kiro mock-path concerns.
- B: Built cli-jaw commit `a024fae4 feat: expose JWC in manager cli registry` and recorded read-only verifier DONE in `270.14_b_verifier_done.md`.
- C: Ran mechanical gates and adversarial review; all checks were green in `270.15_c_check_report.md`.

## Files changed

cli-jaw commit `a024fae4`:

- `src/cli/registry.ts` — normalized `CLI_REGISTRY.jwc.label` to `JWC`.
- `public/manager/src/settings/pages/components/agent/agent-meta.ts` — added `jwc` to primary ordering, added `CLI_META.jwc`, and introduced `orderRuntimeCliOptions()`.
- `public/manager/src/settings/pages/components/agent/RuntimeHeader.tsx` — renders ordered CLI options and collapses after the ordered primary count.
- `tests/unit/cli-registry.test.ts` — covers 14 keys, JWC registry metadata, live registry metadata including `experimental`, and the 270 readiness boundary excluding `jwc` from `DEFAULT_ORDER`.
- `tests/unit/manager-settings-model.test.ts` — covers JWC frontend metadata parity, primary order, ordering helper, and RuntimeHeader source coupling.
- `tests/unit/cli-registry-route.test.ts` — covers `GET /api/cli-registry` response envelope at `body.data.jwc`.

jawcode devlog:

- `200_execution_order.md` and `270_jwc_ui_cli_selector.md` mark 270 as done with commit/check evidence.
- `270.1`–`270.16` contain the P/A/B/C/D receipts for this slice.

## Acceptance criteria met

- `/api/cli-registry` exposes `body.data.jwc` with `label: "JWC"`, `binary: "jwc"`, `experimental: true`, and default model/effort metadata.
- `buildLiveCliRegistry()` preserves JWC label, binary, experimental flag, default model, and default effort.
- `PRIMARY_CLIS` includes `jwc` after `claude-e` and before `agy`.
- `RuntimeHeader` uses `orderRuntimeCliOptions(cliOptions)` and `collapsedAfter={orderedPrimaryCliCount}`.
- `metaFor('jwc')` returns JWC label/model/effort metadata instead of lowercase fallback.
- Registry tests include `jwc` in canonical keys and assert its metadata.
- Readiness default-order test explicitly keeps `jwc` out of `DEFAULT_ORDER` for slice 270.
- B-stage evidence pairs cli-jaw `CLI_REGISTRY.jwc.binary === "jwc"` with jawcode package `jawcode`, bin `jwc`, and export `./sdk`.
- Default runtime switch, Code mode UI, and Jaw mode native attach remain out of scope.

## Gates

- `bun run check` from jawcode — passed.
- `tsx --import ./tests/setup/test-home.ts --experimental-test-module-mocks --test tests/unit/cli-registry.test.ts tests/unit/manager-settings-model.test.ts tests/unit/cli-registry-route.test.ts` from cli-jaw — 37 passed, 0 failed.
- `tsx --experimental-test-module-mocks --test tests/integration/route-registration.test.ts` from cli-jaw — 5 passed, 0 failed.
- `npm run typecheck` from cli-jaw — passed.
- `npm run build:frontend` from cli-jaw — passed.
- `npm run build` from cli-jaw — passed with `[atomic-build] dist/ swapped successfully`.

## WONDER — what is still missing?

- No browser-rendered proof was captured for the dropdown; unit/source coverage proves the acceptance criterion, while browser inspection remains supplemental.
- `/api/cli-registry` is still unauthenticated; this was pre-existing and unchanged by 270, but future hardening may want a product decision.
- `CLI_META.jwc` duplicates backend models/efforts; tests guard drift, but a dynamic registry-fed frontend would reduce duplication later.
- Secondary settings surfaces may still render CLI lists in their own order; 270 only required the Agent runtime selector.

## REFLECT — how to improve the spec next time

- State whether rendered-browser proof is mandatory or supplemental before B-stage starts.
- Separate route envelope proof from live registry proof explicitly; both were needed and are now tested.
- Include a standard mock strategy for live registry augmentation boundaries such as Kiro discovery.
- Name the exact default-runtime boundary: adding JWC visibility must not alter `DEFAULT_CLI` or `DEFAULT_ORDER` until slice 280.
