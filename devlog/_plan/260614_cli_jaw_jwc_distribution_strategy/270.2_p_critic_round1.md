ITERATE

# 270.2 — P critic round 1

Reviewed plan: `270.1_p_jwc_selector_exposure_plan.md`

## Summary

The plan is scoped and file-specific, but verification is under-specified for the two headline deliverables: live `/api/cli-registry` exposure and visible Manager selector behavior. HTTP and UI checks are optional or ambiguous.

## Findings

- **P270-1 major** — AC #1 requires `/api/cli-registry`, but mandatory verification only covers registry unit tests; route/live API proof is optional.
- **P270-2 major** — `/api/cli-registry` response envelope is ambiguous (`data.jwc` vs top-level `.jwc`), and no canonical assertion path is pinned.
- **P270-3 major** — Visible primary Manager selector behavior lacks required RuntimeHeader/component/browser verification.
- **P270-4 major** — `buildLiveCliRegistry()` preservation of `jwc.label === "JWC"` is not mandatory.
- **P270-5 minor** — The plan does not require confirming `RuntimeHeader.tsx` collapse boundary/count after adding `jwc`.
- **P270-6 minor** — Partial `CLI_META.jwc` metadata omission is not explicitly justified.
- **P270-7 minor** — Primary ordering near `claude-e` is conditional instead of a firm acceptance gate.
- **P270-8 minor** — B-stage verification does not explicitly pair `binary: 'jwc'` with current jawcode package bin/export evidence.
- **P270-9 info** — `route-registration.test.ts` is weak linkage for registry payload verification.
