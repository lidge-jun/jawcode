FAIL

[HIGH] ARCH-A1 .jwc/plans/planphase/2026-06-14-1443-a05a/pending-approval.md:35 — Prior finding routed to B-stage, but the revision still splits home resolution across `packages/jwc/scripts/bootstrap-cli-jaw-home.cjs` (planned `CLI_JAW_HOME || ~/.cli-jaw` join) and `packages/coding-agent` discovery patches with no named shared resolver module or `resolveHomePath`/tilde-normalization contract; current code still hardcodes `packages/coding-agent/src/discovery/cli-jaw.ts:25` and `packages/coding-agent/src/extensibility/skills.ts:130`. — Add one canonical owner path (e.g. `packages/coding-agent/src/jwc-runtime/cli-jaw-home.ts` exported for tests) and require bootstrap to import or byte-match that contract before acceptance criteria claim `CLI_JAW_HOME` consistency.

[MEDIUM] ARCH-A2 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md:517 — `cli = "codex"` is fixed in seed (`:52`) and test case 6 (`:478`), but acceptance criteria still say settings are "seeded with current cli-jaw defaults" without the explicit `codex` override; stale wording can reintroduce upstream-default behavior during B-stage. — Align `330` acceptance criteria and user-requirements text with pending-approval final decision: cli-jaw defaults plus explicit `cli = "codex"` override.

[MEDIUM] ARCH-A4 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md:167 — Revision adds a "minimal + drift-check" constraint (`:166`) but still prescribes ~130 lines of ported merge/activation pseudocode and lists no dedicated drift contract test against `../cli-jaw/lib/mcp/skills-distribution.ts` / `skills-utils.ts`; test cases 1-7 cover bootstrap side effects only. — Replace inline pseudocode with a minimal function list plus a named drift fixture/test (base auto-activate set, orchestration category rule, ignored dirs, cooldown constants) before B-stage.

[MEDIUM] ARCH-A5 scripts/jwc-release-validation.ts:10 — Plan names canonical gate unification (pending-approval.md:86-90, `180_validation_matrix.md:32`, `240_publish_readiness_plan.md:97-108`), but the script still omits bootstrap test, pack allowlist/count contract, postinstall mode matrix, registry-faithful install, and native probes; `.github/workflows/ci.yml:47-48` and `.github/workflows/release.yml:47-48` still run `smoke-node-sdk.mjs` only. — Accepted/routed to B-stage; keep explicit same-named gate list in all four surfaces when implementing.

[MEDIUM] ARCH-A6 packages/jwc/scripts/smoke-packed-sdk.mjs:63 — Packed-smoke hardening is specified in pending-approval.md:66-74 and `330` smoke section, but current smoke still co-installs local `@gajae-code/natives`, runs only `jwc --version`, and does not scrub PATH or assert `package:bun`/bootstrap outputs. — Accepted/routed to B-stage per revised plan; do not treat current smoke or `validate:jwc-release` as authoritative until hardened.

[LOW] ARCH-A7 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md:45 — Settings SSOT is now explicit (bundled `defaults/cli-jaw/settings.json` plus tracked keys in test case 6); static-copy drift remains an accepted maintenance risk, not a release blocker. — Waived for A-stage with evidence: pending-approval.md:39-43 and `180_validation_matrix.md:16` document non-secret baseline checks.

[LOW] ARCH-A8 .jwc/plans/planphase/2026-06-14-1443-a05a/pending-approval.md:91 — Upstream `bun` package postinstall failure before jawcode postinstall is documented with remediation; jawcode cannot make it non-fatal. — Deferred with evidence in pending-approval and `240_publish_readiness_plan.md:133`; not a plan blocker.

Most likely to break first if the plan is implemented as written: implementing `packages/jwc` bootstrap home resolution and `packages/coding-agent` skill discovery as separate patches without a shared `CLI_JAW_HOME` resolver/normalization owner, causing custom-home bootstrap and `jwc` skill loading to diverge.
