FAIL

# 270.11 — A-stage planner delta r2

## Findings

- [medium] PLANNER-A7 `270.1_p_jwc_selector_exposure_plan.md:127-133` vs `:285` — Acceptance #2 requires `buildLiveCliRegistry()` to preserve `experimental`, but the mandatory live-registry test omits `registry.jwc.experimental`; add `assert.equal(registry.jwc.experimental, true)` to the live-registry test block.
- [low] PLANNER-A8 `270.1_p_jwc_selector_exposure_plan.md:299` — Risks table claims byte-identical `CLI_META.jwc` arrays are asserted via tests, but §5 only spot-checks includes; add `deepEqual` against `CLI_REGISTRY.jwc.models`/`efforts` or narrow risk wording.

Resolved prior Planner findings: PLANNER-A1..A6 and r1 low notes are resolved by mandatory RuntimeHeader ordering/collapse, route test, package evidence, supplemental browser wording, `experimental` in route/registry tests, and source-coupling imports/path/assertions.

Most likely misread: Patching `PRIMARY_CLIS`, `CLI_META`, registry label, and static registry unit test completes 270 without landing RuntimeHeader ordering/collapse and mandatory `/api/cli-registry` route test.
