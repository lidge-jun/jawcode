# 270.10 — A-stage synthesis r1

> Planner delta: `270.8_a_planner_delta_r1.md` — PASS with low notes
> Architect delta: `270.9_a_architect_delta_r1.md` — FAIL
> Revision target: `a-r2`
> Plan under revision: `270.1_p_jwc_selector_exposure_plan.md`

## Shared root cause

The a-r1 plan over-generalized the 270/280 boundary from `jwc` to all `experimental` runtimes. Existing code already places several experimental runtimes in `DEFAULT_ORDER`, so the proposed blanket test would fail. The route test also needs explicit Kiro inventory isolation because `buildLiveCliRegistry()` awaits `fetchKiroModelInventory()`.

## Local evidence

- `../cli-jaw/src/cli/readiness.ts:154` includes `pi`, `claude-e`, `agy`, and `cursor` in `DEFAULT_ORDER` even though some registry entries are experimental.
- `../cli-jaw/src/cli/registry.ts:146-157` marks `jwc` experimental and package-backed; 270's actual boundary is only that JWC should not become default runtime in this slice.
- `../cli-jaw/src/cli/registry-live.ts:6` awaits `fetchKiroModelInventory()` before returning the registry.
- `../cli-jaw/src/http/response.ts:12-14` pins the route payload path to `body.data.jwc`.

## Finding decisions

| Finding | Decision | Plan change |
|---|---|---|
| ARCH-A1 | Accept | Replace blanket `CLI_REGISTRY[key].experimental` exclusion with a `jwc`-specific assertion: non-JWC keys remain governed by existing coverage; `jwc` is explicitly absent from `DEFAULT_ORDER` in 270. |
| ARCH-A3 | Accept | Route test remains mandatory and now explicitly mocks/stubs the Kiro inventory boundary to return no live augmentation. |
| PLANNER-A1 | Accept | Pin `readFileSync` + `join` imports and `join(__dirname, '../../public/.../RuntimeHeader.tsx')` path in the source-coupling test. |
| PLANNER-A2 | Accept | Add assertion that RuntimeHeader maps `orderedCliOptions`, not only declares the helper/collapsed count. |

## Exact plan sections changed

- Planned changes §4 readiness test block.
- Planned changes §5 manager-settings-model source-coupling block.
- Planned changes §6 route test block.
- Acceptance criteria and verification notes remain semantically the same, but now refer to `jwc`-specific default-order exclusion and Kiro boundary isolation.
