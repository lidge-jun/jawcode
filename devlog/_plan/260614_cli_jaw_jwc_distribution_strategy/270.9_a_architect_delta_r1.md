FAIL

# 270.9 — A-stage architect delta r1

## Findings

- [P0] ARCH-A1 `270.1_p_jwc_selector_exposure_plan.md:139-149` — revised readiness test uses blanket `CLI_REGISTRY[key].experimental` exclusion, but `readiness.ts:154` already lists experimental `pi`, `claude-e`, `agy`, and `cursor`; proposed loop fails immediately. — Replace blanket experimental rule with a `jwc`-specific assertion: keep every existing DEFAULT_ORDER key expectation and assert only `jwc` is absent.
- [P1] ARCH-A3 `270.1_p_jwc_selector_exposure_plan.md:186-223` — route test pins `body.data.jwc`, but mounts full settings route with no `fetchKiroModelInventory` mock while synthesis says to isolate heavy Kiro boundary. — Keep route test mandatory; add explicit `node:test` mock for `../agent/kiro-models.js` returning `null`, or extract a thin `/api/cli-registry` handler.
- [P2] ARCH-A4 accepted — byte-identical `CLI_META.jwc` copy and assertions are adequate for 270.
- [P3] ARCH-A6 accepted — mandatory verification command includes the route test.
- [P3] ARCH-A2 accepted — `orderRuntimeCliOptions` + RuntimeHeader collapsedAfter/source assertions address registry-order hazard.
- [P3] ARCH-A5 accepted — cited anchors exist.

Most likely break: The revised readiness test's blanket experimental exclusion contradicts current `readiness.ts:154` and will fail before JWC UI work lands.
