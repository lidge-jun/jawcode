# 100 — pre-120 gap

## What 110 roughly completed

The fork line has enough material to proceed toward embedding:

- JWC branding and default workflow surface are documented.
- `packages/jwc` exists as public entrypoint.
- `jawcode/sdk` is now the package import surface.
- resident cli-jaw runtime architecture is documented.
- parity/default promotion planning exists.

## What changed before 120

The old 120 plan assumed fork, deploy, and package work could progress separately. They cannot:

- cli-jaw embedding depends on a stable package export.
- standalone deploy depends on package `jawcode`, not occupied package name `jwc`.
- standalone user experience depends on managed Bun.
- public release depends on visible legacy identity cleanup.
- GitHub deploy depends on CI job/artifact names that match the intended product.

## Required pre-120 output

Before code begins:

1. freeze `jawcode/sdk`;
2. document local dev via `file:`/link or packed tarball;
3. list cli-jaw repo changes needed for no-global-`jwc` smoke;
4. list visible legacy identity contracts that must be compatibility-aliased or renamed;
5. update root docs to point at this consolidated plan.

## Output status

| Output | Status | Evidence |
|---|---|---|
| `jawcode/sdk` frozen as target surface | done | `040_packaging_matrix.md`, `070_decision_register.md`, `120_embedding_slice.md` |
| local dev via `file:`/link or packed tarball | done | `030_product_surfaces.md`, `040_packaging_matrix.md`, `130_packaging_slice.md` |
| cli-jaw no-global manifest | done | `115_cli_jaw_integration_manifest.md` |
| visible legacy identity contracts | done | `050_legacy_name_flip_timing.md`, `150_legacy_name_flip_slice.md` |
| root docs point at consolidated plan | done | `README.md`, `README.jwc.md`, `AGENTS.md` |
