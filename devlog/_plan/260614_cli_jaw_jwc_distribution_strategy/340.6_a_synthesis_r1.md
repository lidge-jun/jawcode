# 340.6 — A-stage synthesis r1 / override packet

Status: r1 delta audit still returned FAIL and runtime reached A-stage round cap (`a_round=3`, revision `a-r1`). This packet records the remaining findings, the exact follow-up plan edits applied, and the basis for a user-approved override if the user chooses to proceed.

## Remaining r1 findings accepted

### Planner

- PLANNER-A4: accepted. Added named gates for pack manifest contract, registry-faithful install, native probes, postinstall matrix, and mirrored the canonical `scripts/jwc-release-validation.ts` step list in `330.6`.
- PLANNER-A6: accepted. Added acceptance criteria for `node packages/jwc/scripts/verify-runtime.cjs` and manifest/pack contract proof with no `workspace:`/`file:` runtime specs.
- PLANNER-A2: accepted. Changed `330` acceptance criteria from “current cli-jaw defaults” to “cli-jaw defaults plus explicit jawcode override `cli = "codex"`.”
- PLANNER-A1: accepted. Marked `340.3` as applied revisions instead of future-tense “sections to change.”
- PLANNER-A10: accepted. Added execution-order dependency: canonical home resolver item must land before bootstrap and `CLI_JAW_HOME` AC can pass.

### Architect

- ARCH-A1: accepted. Added canonical owner module path `packages/coding-agent/src/jwc-runtime/cli-jaw-home.ts` with `resolveCliJawHome(env?, homeDir?)` contract, and required bootstrap/discovery tests to share or byte-match it.
- ARCH-A2: accepted via same `330` codex acceptance wording change.
- ARCH-A4: accepted. Replaced the large inline ported helper pseudocode in `330` with a minimal function contract and a named drift contract test list.
- ARCH-A5: accepted/routed. Pending approval now names the canonical `scripts/jwc-release-validation.ts` step list and workflow surfaces.
- ARCH-A6: accepted/routed. Packed smoke hardening remains an explicit B-stage work item and is not considered currently satisfied.
- ARCH-A7: accepted as non-blocking static-template drift risk with tracked keys and non-secret baseline gates.
- ARCH-A8: accepted as upstream `bun` installer risk; documented with remediation and not something jawcode-local postinstall can make non-fatal.

## Applied r1 follow-up edits

- `.jwc/plans/planphase/2026-06-14-1443-a05a/pending-approval.md`
  - Added canonical home resolver owner path and execution ordering.
  - Added named native/registry/manifest gates.
  - Added canonical `validate:jwc-release` step list.
  - Added AC rows for runtime resolver smoke and manifest/pack dependency proof.
- `devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md`
  - Replaced ported pseudocode with minimal function contract plus drift test.
  - Updated `330.6` release validation excerpt with all canonical gate names.
  - Updated acceptance wording to include `cli = "codex"` override.
  - Expanded verification commands with manifest contract, postinstall matrix, registry-faithful install, native probes, and `verify-runtime.cjs`.
- `devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/340.3_a_synthesis_r0.md`
  - Marked the r0 synthesis as applied revisions.

## Override rationale

The remaining r1 findings were plan-specific omissions, not contradictory architecture. They have now been resolved in the plan artifacts, but the native A-stage audit round cap prevents a further r2/r3 automated delta audit in this session. Proceeding requires an explicit `--user-approved` override or a reset/replan cycle.

If overridden, B-stage must still implement the listed work items and C-stage must verify all release gates before the goal can be considered complete. This override does not approve npm publish; publish remains out of scope until a separate explicit approval.
