# 302.2 — P synthesis round 1

## Summary

Critic round 1 was correct. The P plan was directionally aligned with the user goal, but still too additive and Code-mode-heavy. It must become an API-level documentation pass over all 270–310 phase docs and must explicitly remove/replace contradictions in existing docs.

## Decisions by finding

| Finding | Decision | Plan change |
|---|---|---|
| C-01 | Accept | Add dedicated planned NEW doc `305_jaw_mode_jwc_runtime_attach.md` plus updates to 000/200/310/320. It documents existing Jaw mode `JWC` runtime selector/native attach API and frontend acceptance. |
| C-02 | Accept | B-stage starts with cli-jaw checkout evidence collection. If cli-jaw checkout is unavailable, B must stop or mark exact API paths blocked/TBD rather than inventing paths. |
| C-03 | Accept | 280 gets a scheduled exact-path resolution task for message POST, SSE/event stream, DB persistence evidence, and default-runtime override; placeholders must be resolved or marked as blocker. |
| C-04 | Accept | 300 update is replace-oriented: remove/replace left-session-list, permission-card, and multi-session-first assumptions that conflict with 301. Add negative acceptance criteria. |
| C-05 | Accept | 310 rows will be merged under existing sections A–E or a new section F with explicit cross-links to avoid duplicated/conflicting guidance. |
| C-06 | Accept | Add concrete auth inventory API/bridge resolution task; normal picker hidden-only behavior must cite the resolved route/bridge or mark blocker. |
| C-07 | Accept | Rename planned receipts to include `docs_pass` labels, e.g. `302.3_a_docs_pass_planner_round1.md`. |
| C-08 | Note | Preserve reference validation; 000 update remains required. |

## Root cause

The original P draft treated 301 as the main delta and assumed additive doc sections would be safe. The current docs show stale claims that conflict with 301, so the plan must specify replacements and negative acceptance criteria.

## Evidence

- `300_code_mode_ui.md` currently says left panel session list, permission request cards, and multi-session support are part of the UI plan.
- `301_manager_ui_code_jaw_design_interview.md` says Code mode is cwd/JWC-session scoped, composer-first, and permission approval mechanics are deferred.
- `320_post270_current_state_triage.md` states cli-jaw paths must be checked in cli-jaw alongside jawcode evidence.
