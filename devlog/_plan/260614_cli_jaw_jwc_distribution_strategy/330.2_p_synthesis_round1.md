# 330.2 — P critic synthesis round 1

## Verdict handled

Critic round 1 returned ITERATE. All blocking/major findings are accepted. No waiver is used.

## Evidence basis

- `packages/jwc/package.json` currently runs only `node scripts/verify-runtime.cjs --postinstall`; no cli-jaw home bootstrap exists.
- `packages/jwc/scripts/verify-runtime.cjs` only reports Bun runtime status and exits 0 in postinstall mode.
- `../cli-jaw/src/core/config.ts` defines default home as `CLI_JAW_HOME` or `~/.cli-jaw`, and `createDefaultSettings()` owns current settings shape.
- `../cli-jaw/src/cli/claude-models.ts:getDefaultClaudeModel()` returns `claude-opus-4-8`.
- `../cli-jaw/lib/mcp/skills-distribution.ts` owns current GitHub skills clone and auto-activation behavior.
- `../cli-jaw/lib/mcp/skills-utils.ts` defines registry helpers, ignored dirs for hashing, clone metadata shape, 10m cooldown, 80s timeout, and the current copy function gap.

## Finding decisions

| Finding | Decision | Plan patch |
|---|---|---|
| P-CRIT-1 | Accept | Replace placeholder Claude model values with `claude-opus-4-8` and add source note. Already patched before synthesis. |
| P-CRIT-2 | Accept | Remove ambiguous remote-settings promise. Settings seed is bundled deterministic fallback; optional future remote override is out of MVP. |
| P-CRIT-3 | Accept | Add explicit bootstrap write path from installed `defaults/cli-jaw/settings.json` to `<CLI_JAW_HOME>/settings.json`, with token substitution and no overwrite. |
| P-CRIT-4 | Accept | Add port-level pseudocode for registry load, semver comparison, tree fingerprint, ignored entries, temp clone lifecycle, and merge/update behavior. |
| P-CRIT-5 | Accept | Define JSON output schema/status values for `--json`. |
| P-CRIT-6 | Accept | Add focused tests for clone failure non-fatal behavior, settings seed parity/drift, and optional live clone gate. |
| P-CRIT-7 | Accept | Remove `jwc bootstrap cli-jaw` remediation from MVP; use package script/node script. |
| P-CRIT-8 | Accept | Mirror cli-jaw clone metadata schema `{ lastAttempt, success }`, 10 minute cooldown, 80 second timeout, and support both `JWC_FORCE_CLI_JAW_SKILLS` and existing `JAW_FORCE_CLONE`. |
| P-CRIT-9 | Accept | State Codex live skills import is out of scope for jawcode-only bootstrap. |
| P-CRIT-10 | Accept | Seed minimal `mcp.json`, `prompts/`, and `uploads/` compatibility directories/files only where safe; do not configure external MCP servers beyond empty/default-safe file. |

## Exact plan sections to change

- User requirements/non-goals: add Codex-live out-of-scope and clarify `~/.cli-jaw` compatibility means minimal data home, not full cli-jaw npm install.
- 330.1: delete remote-settings language; define bundled seed as source of truth for MVP.
- 330.2: add explicit settings read/write path, JSON output schema, clone metadata constants, merge pseudocode, safe `mcp.json`/`prompts/` behavior, and corrected remediation.
- 330.5: add tests for clone failure, settings parity, and optional live clone.
- Acceptance/verification: require `mcp.json` minimal seed and assert postinstall non-fatal failure path.

## Route decision

No requirement needs a return to I-stage. The user clarified the core ambiguity: target is a user without cli-jaw installing jawcode. The remaining issues are plan precision gaps, not requirement ambiguity.
