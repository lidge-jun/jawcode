FAIL

[HIGH] ARCH-A1 packages/coding-agent/src/discovery/cli-jaw.ts:25 — Plan requires `CLI_JAW_HOME`-aware bootstrap and acceptance tests for that env, but runtime skill discovery resolves `path.join(ctx.home, ".cli-jaw", "skills")` and `packages/coding-agent/src/extensibility/skills.ts:130` hardcodes `os.homedir()`; there is zero `CLI_JAW_HOME` handling in `packages/coding-agent`. Custom homes (e.g. `~/.cli-jaw-3458`) will bootstrap in one directory while `jwc` reads another. — Add an explicit B-stage slice to introduce one jawcode home resolver (mirroring `../cli-jaw/src/core/config.ts:18-20`) and wire discovery/skills existence checks through it before claiming `CLI_JAW_HOME` support.

[HIGH] ARCH-A2 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md:52 — Approved product decision is fresh default `cli = "codex"` (`.jwc/plans/planphase/2026-06-14-1443-a05a/pending-approval.md:33`, `340_jawcode_distribution_broadened_interview.md:232`), but slice 330 still seeds `cli: "claude"` and test case 6 at `330_jawcode_cli_jaw_bootstrap_plan.md:456` asserts `cli = "claude"`. Upstream cli-jaw default remains `claude` (`../cli-jaw/src/cli/registry.ts:227`). — Revise pending plan/test contract to seed and assert `codex`, or drop the codex override and document upstream parity.

[HIGH] ARCH-A3 packages/jwc/package.json:23-35 — Postinstall is planned to chain `bootstrap-cli-jaw-home.cjs`, but the published `files` allowlist still ships only 8 artifacts and excludes `scripts/bootstrap-cli-jaw-home.cjs` and `defaults/cli-jaw/settings.json`; `npm pack --dry-run` from the current tree cannot include bootstrap assets. — Update the plan’s ordered B-stage steps so `files` + pack-count/size guards are updated in the same patch as postinstall wiring, before any publish/smoke claims.

[HIGH] ARCH-A4 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/330_jawcode_cli_jaw_bootstrap_plan.md:113-297 — Plan ports a large cli-jaw skill clone/merge/activation routine into new CJS instead of reusing an owner module; canonical behavior lives in `../cli-jaw/lib/mcp/skills-distribution.ts` and `../cli-jaw/lib/mcp/skills-utils.ts`, while jawcode only consumes `~/.cli-jaw/skills` (`packages/coding-agent/src/discovery/cli-jaw.ts`). — Constrain implementation to a minimal Node bootstrap surface and document a drift-check against cli-jaw sources, or extract a shared contract test/fixture rather than duplicating merge logic inline.

[HIGH] ARCH-A5 scripts/jwc-release-validation.ts:10-29 — Slice E makes `bun run validate:jwc-release` canonical, but the script currently runs bundle/build/smoke-packed-sdk/MCP guards only; it omits the planned bootstrap test, pack allowlist/size/count contract, postinstall mode matrix, PATH-scrubbed Bun proof, and native probes. `.github/workflows/ci.yml:47-48` runs `smoke-node-sdk.mjs`, not `smoke-packed-sdk.mjs`, so CI and release validation already diverge. — Extend the plan with explicit, named edits to `scripts/jwc-release-validation.ts`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, and `scripts/ci-release-publish.ts` so all release surfaces run the same gate set.

[MEDIUM] ARCH-A6 packages/jwc/scripts/smoke-packed-sdk.mjs:54-65 — Packed smoke co-installs a freshly packed local `@gajae-code/natives` tarball and runs `jwc --version` without PATH scrubbing; `packages/jwc/scripts/resolve-bun-runtime.cjs:68-75` still accepts `system:PATH`, so the smoke can pass on a developer machine with system Bun and does not prove package-local Bun or bootstrap behavior the plan requires. — Harden smoke before treating release gates as authoritative: scrub PATH, assert `JWC_BUN_RUNTIME_SOURCE=package:bun`, add `jwc --help`, and assert postinstall bootstrap outputs under a temp `CLI_JAW_HOME`.

[MEDIUM] ARCH-A7 devlog/_plan/260614_cli_jaw_jwc_distribution_strategy/340_jawcode_distribution_broadened_interview.md:75 — Settings seed is a static JSON copy with drift risk versus cli-jaw `createDefaultSettings()` (`../cli-jaw/src/core/config.ts:173`); jawcode has no equivalent generator/import. — Plan must name the generation source of truth (checked-in template vs generated from cli-jaw snapshot) and the exact test keys that must track upstream changes.

[MEDIUM] ARCH-A8 packages/jwc/package.json:47 — Runtime dep `bun@1.3.14` has its own npm postinstall; `packages/jwc/scripts/verify-runtime.cjs:19-22` is non-fatal, but a failing `bun` postinstall can still abort the whole `npm install -g jawcode` before jawcode bootstrap runs (`340_jawcode_distribution_broadened_interview.md:81`). — Add an explicit release-risk row and install-failure remediation to the plan, not only jawcode-local non-fatal guards.

Most likely to break first if implemented as written: wiring `postinstall` to `scripts/bootstrap-cli-jaw-home.cjs` before expanding `packages/jwc/package.json` `files` to ship that script and `defaults/cli-jaw/settings.json`, yielding a published tarball whose install invokes a missing bootstrap entrypoint.
