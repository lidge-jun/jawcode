PASS

# 270.8 — A-stage planner delta r1

## Findings

- [low] PLANNER-A1 `270.1_p_jwc_selector_exposure_plan.md:178-184` — planned source-coupling assertion omits exact `node:fs` import and robust `join(__dirname, '../../...')` path; pin these to avoid test cwd confusion.
- [low] PLANNER-A2 `270.1_p_jwc_selector_exposure_plan.md:181-184` — source coupling checks helper and collapsedAfter strings but not that render maps `orderedCliOptions`; add assertion for `orderedCliOptions.map` or a focused RuntimeHeader render test.

Most likely misread: Adding `jwc` to `PRIMARY_CLIS` / `CLI_META` and registry label/tests completes 270 without also patching `RuntimeHeader.tsx` ordering/collapse and narrowing readiness default-order behavior for `jwc`.
