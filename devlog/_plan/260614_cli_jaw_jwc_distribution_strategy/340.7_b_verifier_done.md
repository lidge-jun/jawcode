DONE
- Shared CLI_JAW_HOME resolver verified: `packages/coding-agent/src/jwc-runtime/cli-jaw-home.ts` exports `resolveCliJawHome(env?, homeDir?)`, trims `CLI_JAW_HOME`, defaults to `<home>/.cli-jaw`, expands `~`/`~/`, and returns an absolute resolved path. `packages/coding-agent/src/discovery/cli-jaw.ts` imports and uses that resolver for the user skills directory under jaw brands.
- Postinstall/bootstrap contract verified: `packages/jwc/scripts/bootstrap-cli-jaw-home.cjs` has byte-equivalent resolver logic, seeds `settings.json` from `defaults/cli-jaw/settings.json`, creates non-secret `heartbeat.json` and `mcp.json`, supports `JWC_CLI_JAW_SKILLS_SOURCE_DIR` local source, defaults GitHub source to `https://github.com/lidge-jun/cli-jaw-skills.git`, copies filtered skills into `skills_ref`, auto-activates selected skills, and makes postinstall bootstrap failures nonfatal.
- Package manifest shipping contract verified: `packages/jwc/package.json` publishes `defaults/cli-jaw/settings.json`, `scripts/bootstrap-cli-jaw-home.cjs`, runtime resolver/verification scripts, bins, bundles, and SDK sources; `postinstall` runs runtime verification then bootstrap. No publish action is part of this implementation.
- Packed smoke coverage verified by inspection: `packages/jwc/scripts/smoke-packed-sdk.mjs` packs/install-tests `jawcode`, checks package-local Bun source via `verify-runtime.cjs --json`, validates `jwc --version`/help, imports `jawcode/sdk`, asserts bootstrap outputs including empty API key and filtered skill `node_modules`, exercises postinstall safe/CI/skip modes, supports registry-faithful install, and native probes.
- Canonical release validation verified: `scripts/jwc-release-validation.ts` runs bundle, node SDK build, bootstrap test, manifest contract test, packed smoke, postinstall matrix, registry-faithful install, native probes, release publish contract, mac MCP/CUA defaults, and public/legacy guards, ending with `[validate:jwc-release] OK` on success. It contains validation only; no npm publish action.
- Already-run command evidence accepted: focused tests passed 13/0; packed smoke variants, runtime verification, dry-run pack manifest, and `bun run validate:jwc-release` passed.
- Residual non-blocking risks: bootstrap script duplicates resolver logic rather than importing TS, so drift depends on tests; GitHub skills clone remains network-dependent outside local-fixture/safe/skip modes; actual npm publish remains separately approved and unperformed.
