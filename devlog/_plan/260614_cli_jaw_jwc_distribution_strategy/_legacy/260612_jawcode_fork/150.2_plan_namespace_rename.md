# 150.2 — plan: 네임스페이스/브랜드 흔적 제거 (@gajae-code → @jawcode, gjc → jwc)

> 상태: ⬜ 설계 (260613) — 063.1 **전략-A 연기분의 실행 플랜**. 승격(150) 차단 아님 — 제품 표면은 `jwc` bin 단일화 완료, 본 작업은 **내부 흔적 정리**.
> 설계 정본 승계: [phase1/063.1 패키지 스코프 마이그레이션](./phase1/063.1_plan_package_scope_migration.md) (전략 A/B/C/D 비교) · [phase1/065.1 내부 식별자](./phase1/065.1_plan_internal_identifiers.md) · 델타 경계: [structure/fork-delta.md](../../../structure/fork-delta.md):25-26.
> 핵심 긴장: **흔적 제거 ↔ 리베이스 비용**. 063.1이 전략 B(보존)를 택한 이유 = `@gajae-code/*` import를 바꾸면 업스트림이 그 파일을 건드릴 때마다 충돌. 본 플랜은 **upstream sync 빈도가 충분히 낮아진 뒤**(063.1 Q3) 실행하는 것을 전제로, 충돌 면적을 단계로 분리한다.

## 0. 실행 트리거 (선결)

- [ ] 063.1 Q3 충족: upstream `dev` sync가 **주 1회 미만**으로 내려간 시점 (chase 10.001 사이클 로그로 판정). 그 전에는 전략 B 유지.
- [ ] M1 done + 150 P-2 승격(기본 `cli=jwc`) 완료 — rename이 사용자 표면을 흔들지 않음을 보장한 뒤.
- [ ] `rebrand-inventory.ts`가 rename 후 기대값으로 갱신 가능한 상태 (M-G 단계).

## 1. 흔적 분류 + 명시 위치

### 1-A. 패키지 스코프 `@gajae-code/*` (10개) — [대, 충돌 최대]

package.json `name` 필드 (각 1줄):

| 디렉터리 | 현재 name | → 목표 |
|---|---|---|
| `packages/utils/package.json` | `@gajae-code/utils` | `@jawcode/utils` |
| `packages/natives/package.json` | `@gajae-code/natives` | `@jawcode/natives` |
| `packages/ai/package.json` | `@gajae-code/ai` | `@jawcode/ai` |
| `packages/agent/package.json` | `@gajae-code/agent-core` | `@jawcode/agent-core` |
| `packages/tui/package.json` | `@gajae-code/tui` | `@jawcode/tui` |
| `packages/stats/package.json` | `@gajae-code/stats` | `@jawcode/stats` |
| `packages/coding-agent/package.json` | `@gajae-code/coding-agent` | `@jawcode/coding-agent` |
| `packages/bridge-client/package.json` | `@gajae-code/bridge-client` | `@jawcode/bridge-client` |
| benchmarks 2종 (`orchestration-token-benchmark`·`typescript-edit-benchmark`) | `@gajae-code/*` (private) | `@jawcode/*` |

- **루트 catalog 블록**: `package.json:23-30` (8개 `@gajae-code/* : "0.4.4"` 키) — catalog 키 = import specifier 분모.
- **import specifier 전수**: `packages/*/src` 약 **634파일 / 3245 occurrence** (063.1 §1-B: coding-agent 525/972 최대). tsconfig `paths` 블록 없음 → **텍스트 전수 치환** (`@gajae-code/` → `@jawcode/`).
- **규모/난이도**: 대 · 충돌 최대. coding-agent 525파일이 매 업스트림 변경마다 충돌 지대 (063.1 전략 A 근거).

### 1-B. `ENGINE_NAME = "gjc"` — [중, 단일 상수 + 마이그레이션]

- 선언: `packages/utils/src/dirs.ts:20` `export const ENGINE_NAME: string = "gjc"`.
- 소비처 **11개 소스 지점** (조사 §2 확정):
  - `dirs.ts:146`(XDG `.gjc` 조인)·`:245`(`gjc.<date>.log`)·`:450`(`gjc-debug.log`)
  - `coding-agent/src/cli.ts:7,76`(import + `brand !== ENGINE_NAME` 분기)·`cli/update-cli.ts:10`·`config/settings-schema.ts:2,406,417`(기본 테마)·`discovery/helpers.ts:8,36`·`modes/components/welcome.ts:2,395`·`modes/theme/theme.ts:13,1801,1802`·`utils/tools-manager.ts:4`
  - (`dist-node/sdk.js` 번들 복사본은 재빌드로 자동 반영 — gitignored)
- 상수 1줄을 `"jwc"`로 바꾸면 위 소비처가 **자동 전환**(로그파일·XDG 세그먼트·테마 분기). 단 **이미 `.jwc/`로 전환된 경로와의 정합**은 fork-delta:25 이중 브랜드 분모 — `migrate-config-dir.ts`(이미 존재)가 `~/.gjc`→`.jwc` 마이그레이션을 담당하므로, ENGINE_NAME 플립은 그 마이그레이션 경로와 한 묶음(M-D→M-E).
- **⚠️ ENGINE_NAME 미경유 하드코딩** (§1-G) 3건은 상수 플립으로 **자동 해결 안 됨** — 별도 패치 필수.
- **규모/난이도**: 중 · `migrate-config-dir.ts` 기존재로 마이그레이션 골격 확보.

### 1-C. `GJC_*` 환경변수 — [대, ~130개 / JWC_ 미전환 ~75개]

조사 §3 확정: 고유 `GJC_*` 변수명 **약 130개**, 그중 **JWC_ alias 보유 ~55개 / 미보유 ~75개**.

- **alias 보유**(역순 폴백 `JWC_X ?? GJC_X` 이미 동작): `GJC_BRAND_NAME`·`GJC_SESSION_ID`·`GJC_AUTH_BROKER_URL`·`GJC_BRIDGE_*`·`GJC_COORDINATOR_*`(주요)·`GJC_TEAM_WORKER_*`·`GJC_TMUX_SESSION`·`GJC_SMOL_MODEL`·`GJC_PLAN_MODEL` 등.
- **alias 미보유**(JWC_ 신규 추가 필요): 대량 클러스터 —
  - `GJC_TEAM_*` (~25개: `GJC_TEAM_NAME`·`GJC_TEAM_MAX_WORKERS`·`GJC_TEAM_*_EVIDENCE_STATUSES`·`GJC_TEAM_LEADER_CWD`…)
  - `GJC_TMUX_*` (~12개: `GJC_TMUX_LAUNCHED_ENV`(10.007)·`GJC_TMUX_PROFILE*`·`GJC_TMUX_BRANCH_*`…)
  - `GJC_HARNESS_*` (`GJC_HARNESS_ROOT_REGISTRY_DIR`·`GJC_HARNESS_RPC_COMMAND`·`GJC_HARNESS_SOCKET_DIR`·`GJC_HARNESS_STATE_ROOT`)
  - `GJC_PYTHON_*`·`GJC_CRASH_DIAGNOSTICS*`·`GJC_PLUGIN_*`·`GJC_HARMONY_DEBUG`(agent/harmony-leak.ts:301)·`GJC_RECEIPT_SPOOL_DIR`(receipt-spool.ts:7, 10.011)·`GJC_STATE_DIR`·`GJC_NO_PTY`/`GJC_NO_TMUX`/`GJC_MOUSE` 등
  - E2E/테스트 전용(`GJC_E2E_*`·`GJC_*_TEST*`)은 사용자 비노출 → 우선순위 최하.
- **전략**: 일괄 제거 금지 — **JWC_ 우선 + GJC_ 폴백** 헬퍼(`readBrandEnv("X")` = `JWC_X ?? GJC_X`) 도입 후 미보유 75개를 점진 편입. GJC_ 폴백 **읽기**는 업스트림이 같은 env를 쓰는 한 충돌 0(보존 분모). 폴백 제거(M-H)는 포크 독립 후.
- **규모/난이도**: 대(수량) · 충돌 낮음(alias). 헬퍼 1개로 반자동화 권장.

### 1-D. bin 이름 — [소]

- `packages/coding-agent/package.json:31` `"gjc": "src/cli.ts"` (150.1 P-3과 동일 항목) → deprecation 또는 제거.
- `packages/stats/package.json` `gjc-stats` → `jwc-stats`.
- `packages/ai/package.json` `pi-ai` → 정책 결정(제거 vs `jwc-ai`).

### 1-E. 가드/스크립트 하드코딩 — [중, 상수화 선행]

| 파일 | 위치 | 현재 |
|---|---|---|
| `scripts/verify-g002-gates.ts` | :17,:23,:193,:228 | catalog key·ALLOWED_UNSCOPED·`rootName==="gajae-code"`·scope assert |
| `scripts/rebrand-inventory.ts` | :34,:37 | `expectedPackageScope = "@gajae-code/"` |
| `scripts/release.ts` | :243 | catalog regex |
| `scripts/sync-versions.ts` | :24 | lockstep 예외 |

- **선행 M3(063.1)**: `const WORKSPACE_SCOPE = "@gajae-code"` 단일 상수 추출 → rename 시 1파일만 변경, 업스트림 충돌 국한.

### 1-F. user-facing 문자열 — [중, 저충돌·고가치 = 1차 우선] ⭐

**사용자가 실제로 보는데 아직 `gjc`인 것** — 흔적 제거의 최우선(텍스트라 업스트림 충돌 낮음, 가시성 높음). 조사 §7 확정:

| 부류 | 위치 | 현재 → 목표 |
|---|---|---|
| **로그인 안내 에러** (8 provider) | `web/kagi.ts:139`·`web/parallel.ts:294,327`·`web/search/providers/{parallel.ts:122,kimi.ts:114,gemini.ts:393,zai.ts:283,synthetic.ts:71,codex.ts:467}` | `"gjc /login X"` → `"jwc /login X"` |
| **CLI help 예시** | `commit/cli.ts:73`·`commands/session.ts:65-69`·`cli/setup-cli.ts:495-496` | `gjc commit/session/…` → `jwc …` |
| **화면 타이틀** | `modes/components/welcome.ts:315` `APP_NAME==="gjc"?"GJC forge":"Jawcode"` | 브랜치 정리 |
| **status-line 세그먼트 id** | `modes/components/status-line/segments.ts:72` `id:"gajae"`·`presets.ts:36,60` | ⚠️ 사용자 config 키 — 변경 시 마이그레이션 |
| **User-Agent 헤더**(외부 서비스 노출) | `hindsight/client.ts:13`·`mem0/client.ts:7` `"oh-my-gajae-code"`·`web/scrapers/*.ts` `"gjc-web-fetch/1.0"`(github/docs-rs/crates-io/musicbrainz)·`web/scrapers/discogs.ts:280`·`web/search/providers/gemini.ts:178` | jaw 브랜드 UA |

### 1-G. `.gjc`/`gjc-*` ENGINE_NAME 미경유 하드코딩 — [소, 자동해결 안 됨] ⚠️

ENGINE_NAME 플립(§1-B)으로 **해결 안 되는** 직접 하드코딩 — 별도 패치 필수 (조사 §8):

| 파일 | 줄 | 리터럴 |
|---|---|---|
| `packages/utils/src/dirs.ts` | :277 | `"gjc-plugins.lock.json"` |
| `packages/utils/src/dirs.ts` | :445 | `"gjc-crash.log"` |
| `packages/utils/src/logger.ts` | :50 | `"gjc.%DATE%.log"` |
| `packages/coding-agent/src/migrate-config-dir.ts` | :16 | `LEGACY_DIR_NAME = ".gjc"` (**보존** — 마이그레이션 소스) |

### 1-H. 주석·문서 — [소, breaking 없음]

`packages/**/*.md` 약 168줄(CHANGELOG 70 최다 — **보존 권장**, 이력), 코드 주석 `dirs.ts`/`logger.ts` 등. `scripts/generate-json-schemas.ts:42,60` `$id: "https://gajae.ai/schemas/…"` **도메인 흔적** 포함.

### 1-I. 프로토콜/MCP 이름 — [대, 외부 호환 파괴] ⚠️

`coordinator/contract.ts:2` `COORDINATOR_MCP_SERVER_NAME="gjc-coordinator-mcp"` + `:5-17` `gjc_coordinator_*` MCP 도구 13개 · `harness-control-plane/seams.ts:8`·`types.ts:14` `"gajae-code"` harness · `acp-agent.ts:415` `name:"gajae-code"`(ACP 프로토콜). **외부 Hermes coordinator 설정·ACP 클라이언트와 호환 파괴** → 150.1 P-3 `migrateHermesSetupOnce()`와 묶음, 최후.

## 2. 보존 권장 (rebase 분모 — 의도적 연기, 제거 금지)

063.1 전략-B + 조사 보존 목록 대조 — 업스트림 `can1357/gajae-code` 커밋과 직접 diff 나는 지점:

| 항목 | 위치 | 이유 |
|---|---|---|
| `@gajae-code/*` import 본문 | `coding-agent/src/**` 120+ 파일 | 상류 모든 커밋이 이 specifier 사용 → cherry-pick마다 3-way 충돌 |
| `ENGINE_NAME="gjc"` 선언 | `dirs.ts:20` | 상류 dirs 커밋 핵심 |
| `GJC_*` env 폴백 **읽기** | 전 패키지 | 상류 테스트/feature가 이 변수명 사용 |
| `gjc_coordinator_*` MCP·`gjc-coordinator-mcp` | `contract.ts:2-17` | 상류 coordinator 커밋 직접 충돌 + 외부 호환 |
| `migrate-config-dir.ts` `.gjc` 리터럴 | `:16` | 마이그레이션 소스(보존이 정상) |
| `CHANGELOG.md` | `coding-agent/CHANGELOG.md` | 이력 — 변경 무의미 + 충돌 빈발 |

→ 이들은 "흔적이지만 분모". M-A/M-I 단계에서 포크 독립 선언 후에만 손댄다.

## 3. 세부 실행 문서 (세분화 — `150.2.NN` 1모듈 1문서)

각 모듈의 정확한 파일:라인·절차·게이트는 아래 sub-plan이 정본. 본 표는 순서·충돌·트리거 요약.

### 1차 — 즉시 가능 (저충돌·고가치, upstream sync 유지 중에도 OK)

| # | 문서 | 범위 | 충돌 |
|---|---|---|---|
| 01 | [150.2.01 user-facing 문자열](./150.2.01_plan_user_facing_strings.md) (M-str) | `gjc /login` 8곳·help·UA 헤더·welcome (§1-F) | 소 |
| 02 | [150.2.02 ENGINE_NAME 미경유 리터럴](./150.2.02_plan_engine_name_literals.md) (M-lit) | `gjc-plugins.lock.json`·`gjc-crash.log`·`gjc.%DATE%.log` 상수 경유화 (§1-G) | 소 |
| 03 | [150.2.03 가드 상수화](./150.2.03_plan_guard_constant_extraction.md) (M-G3) | `WORKSPACE_SCOPE` 추출 + `gajae.ai` 도메인 (§1-E/H) | 소 |
| 04 | [150.2.04 brand env 헬퍼](./150.2.04_plan_brand_env_helper.md) (M-env-helper) | `readBrandEnv()` + GJC_ 미보유 ~75개 (§1-C) | 소~중 |

> **권장**: 지금은 1차만 — 사용자 눈의 `gjc` 흔적 제거 + 리베이스 분모 보존. 02는 05의 선행.

### 2차 — 포크 독립 선언 후 (063.1 Q3 트리거: upstream sync 주 1회 미만)

| # | 문서 | 범위 | 충돌 |
|---|---|---|---|
| 05 | [150.2.05 ENGINE_NAME 플립](./150.2.05_plan_engine_name_flip.md) (M-D→E) | `~/.gjc`→`.jwc` 마이그레이션 + `ENGINE_NAME="jwc"` (§1-B) | 중 |
| 06 | [150.2.06 bin 정리](./150.2.06_plan_bin_rename.md) (M-bin) | `gjc`/`gjc-stats` + deprecation (§1-D) | 소(PATH) |
| 07 | [150.2.07 스코프 전수치환](./150.2.07_plan_package_scope_rename.md) (M-A) | `@gajae-code/*`→`@jawcode/*` 500+/120파일 (§1-A) | **최대** |
| 08 | [150.2.08 프로토콜 이름](./150.2.08_plan_protocol_names.md) (M-I) | MCP/harness/ACP + Hermes 마이그레이션 (§1-I) | 대(외부) |
| 09 | [150.2.09 GJC_ 폴백 제거](./150.2.09_plan_env_fallback_removal.md) (M-H) | `readBrandEnv` 폴백 제거 → JWC_ 단독 | 중 |

**실행 순서**: 01→02→03→04 (1차, 병렬 가능 단 02→05) · 트리거 충족 후 05→06→07→(08 Hermes 선결)→09.

## 4. 검증 게이트 (063.1 §5 승계)

`verify-g002-gates.ts` PASS · `rebrand-inventory.ts --strict` 예외 0(기대값 갱신 후) · `bun install --frozen-lockfile` · `jwc --version` · publish --dry-run.

## 5. 미해결

- 스코프 목표: `@jawcode/*` vs `@bitkyc08/*` (npm org 등록 주체) — 063.1 Q1/Q3.
- M-A 실행 시점: upstream 독립 선언 트리거 (포크가 더는 리베이스 안 받는 시점) 정의.
- `pi-ai` bin·`pi-*` 잔재(상류 pi 유래) 처리 범위 — 별도 인벤토리.
- 조사(ac8b6) 반영 ✅ 완료: §1-A~1-I + §2 보존 목록 전수 갱신 (260613). 잔여 정밀화는 M-env-helper 착수 시 GJC_* 75개 파일:라인 전수 한 번 더.
