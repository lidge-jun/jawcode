# 99.00.03 — audit: 99 밴드 UX 완성도 감사 (cli-jaw 대조, 직원 3렌즈 병렬)

> 상태: 📋 감사 완료 (260613 01:00) — 구현 판단 자료. 감사: Backend(런타임 UX) ∥ Frontend(TUI 표시 UX) ∥ Docs(cli-jaw 표면 전수 대조), 각 read-only.
> 후속 구현 (260613 01:20): P0-1(ee60fb32)·P0-2(c00338f3)·P1-1/P1-2/P1-4(60e0b6af) 완료. 2차 후속 (260613 새벽 — 99.00.04): ✅ P1-3(99.07.01 세션 슬래시 + S4 별칭) ✅ P1-5(invalidatePabcd 즉시 갱신) ✅ P1-6(Final Summary 영수증 — worklog 동형). 백로그: 60col protected-drop 행동 테스트(검증 비차단 관찰).
> 종합 판정: **코어는 완성, 표면은 미완** — 모델 컨텍스트 레이어(M2 헤더·전이 게이트·탈출 UX·HUD 데이터)는 cli-jaw와 구조 동형 이상. 사용자 가시 표면(슬래시 발견성·status 정보 밀도·게이지 실표시)에서 갭 잔존.

## P0 (버그/핵심 기능 미달 — 즉시 수리 대상)

| # | 항목 | 내용 | 출처 |
|---|------|------|------|
| P0-1 | **interview seed 세션 env 비대칭 (BUG)** | `jwc interview <idea>`(seed)는 `--session-id`만 받고 `JWC_SESSION_ID` env를 무시 — orchestrate(8331c03b)·interview cancel은 env 적용. 실측: env 설정 상태에서 seed가 shared 경로에 기록, cancel은 스코프 경로에서 못 찾음. 수정: `resolveJawInterviewArgs`(jaw-interview-runtime.ts:329-331)에 동일 env 폴백 | Backend §5 |
| P0-2 | **인터뷰 4차원 게이지 미렌더 (GAP)** | 99.04.03의 dims가 `hud.details`에 생성되지만 `renderSkillHudBar`(skill-hud/render.ts:63-72)가 details 레일을 **미참조** — 데이터만 있고 사용자 미노출. 테스트도 빌더만 검증, 렌더 테스트 부재. 수정: details 병합 렌더(또는 dims를 chips 승격)+렌더 테스트 | Frontend §3 |

## P1 (실사용 마찰 큼)

| # | 항목 | 내용 |
|---|------|------|
| P1-1 | `jwc orchestrate status` 정보 밀도 — cli-jaw 8필드(State/Scope/Audit/Verification/User approved/Plan/Worklog/Plan hash) 대비 `stage=i` 한 줄. statusText에 게이트·spec/plan ref·scope·다음 액션 추가 |
| P1-2 | `jwc goal status` 가독성 — ultragoal 엔진 boilerplate objective가 사용자 objective를 가림(goal-runtime.ts:181). cli-jaw형 Goal/Status/Mode/ID 출력 필요. goal `clear`/`reset`/`help` 동사 부재, `resume` 무조건 성공 |
| P1-3 | 슬래시 발견성 A필수 갭(Docs 전수): `/interview`·`/goalplan`·`/gd`·세션 계열(`/sessions`·`/switch`·`/fork`)·`/goal` full subcommands(plan/refine/done/history가 자동완성에 없음) — 엔진/CLI는 전부 존재, 표면만 부재. `/orchestrate` 자동완성에 reset 누락 |
| P1-4 | 60col 폭에서 pabcd 띠 조기 drop — elastic이 pabcd(21vw)를 path(35vw)보다 먼저 제거. pabcd/mode를 protected 세그먼트로 |
| P1-5 | pabcd 띠 1초 TTL 지연 — 전이 직후 상태줄 반영까지 ~1s (cli-jaw는 WS 즉시). `sendPabcdStageContext` 전이 연결 또는 TTL 무시 refresh |
| P1-6 | D 완료 영수증 표면 부재 — cli-jaw worklog `## Final Summary` 동형 없음: 완료 요약이 어디 남는지 불명. "Final Summary receipt" 위치 정의 필요 |

## P2 (개선 권장)

- memory search 출력 비동형 (relpath:line+Source/Kind/Header 블록 미충족) · memory backend 기본 off의 첫 사용 마찰
- 99.08-A 병기가 세션 헤더에만 — `goal status`/`orchestrate status` CLI에 상호 표시 없음
- goal conflict recovery — cli-jaw는 suggested commands 팔레트, jwc는 에러 텍스트만
- a→b 게이트 메시지에 `--user-approved`만 안내 (cli-jaw는 --force 우회까지)
- `/team`·`/plan`·`/deliberate`·`/planaudit`·`/review` 슬래시 어댑터 (B권장)
- compact/minimal preset의 pabcd 부재 · complete 시 완료감 피드백 · M2 헤더 사용자 토글 (P3)

## jwc 우위 확인 (감사가 인정한 것)

- 전이 거부 UX: idle 진입 안내·reset 탈출구·재진입 안내가 cli-jaw보다 구체적 (cli-jaw idle 거부는 시작 명령 안내 없음)
- interview 구조화 UI(Ask meta·라운드 헤더)는 cli-jaw elicitation fence보다 정교 — 단 `/interview` 진입 어휘 부재가 상쇄
- interview cancel 후 HUD 동기화 완전 (active_skills:[] 실측)
- 세션 스코프 파일 격리는 cli-jaw 서버 전역 상태보다 격리성 우수

## 권장 착수 순서

1. P0-1(env 비대칭 1줄급) + P0-2(dims 렌더) — 즉시
2. P1-1/P1-2 status 리치 출력 — 한 슬라이스
3. P1-3 슬래시 패리티 — 99.07 본체 (이미 스코프 문서화됨)
4. P1-4/P1-5 띠 polish — 99.04 후속
5. P1-6 Final Summary receipt — 설계 1개 필요 (worklog 동형 vs devlog vs ledger)
