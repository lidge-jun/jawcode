# 99.02.00 — plan: 병렬 랜딩 정리 (parallel surface dirty = 0)

> 상태: ✅ 완료 (260612 밤) — B(a7412632)·C(dfaa73ff)·D(ceeba38e) 랜딩, dirty 0 도달.
> v2.1 정정 (감사 권고): ① B의 99.02.00_plan_workflow_surface_revision.md는 M 스텁→git rm, 99.03.00_...은 ??→add; 구번호 스텁 git rm 전수는 grep('이동: [99.'|'git rm 대상') 실측 목록 기준(8건, 99.04.00_plan_parallel_landing.md 포함) ② B 목록 밖 devlog dirty 20+건은 B에 흡수(devlog 전부 = B) ③ §6-C 재정정: β goal 3f6989ac는 history상 complete — 게이트 서사 폐기, dirty 0 자체가 목적(060 M4 언블록 등) ④ C의 인용 갱신은 struct_har/ 내부 40+건 포함 전수 sed ⑤ D 인벤토리는 'git status 전체 − B/C 경로' 정의로 확장(6번째 memory 테스트·structure 신규 7파일·CHANGELOG 등 포함)
> v2 변경: §1 인벤토리 기준선 stale 명시(81bcea96→7b93de35, dirty 115→207), 슬라이스 D 신설(코드 일괄 정식화+직원 bgtask), 스크립트 7종 재배치 결정, §6-A 베이스라인 재실측 방침, 구번호 스텁 git rm·5-A 픽스를 슬라이스 B에 흡수
> 소속: [99.00.00 MOC](./99.00.00_moc_stabilization.md) 패키지 99.02.
> 목적: 병렬 세션들이 동시에 작업하면서 쌓인 dirty working tree를 정리해 β goal
> `3f6989ac`의 게이트 조건("병렬 표면 dirty = 0")을 충족한다.

작업 당시 jawcode `agent` 브랜치에는 3개의 병렬 세션이 동시에 작업 중이었다:
**(A)** 083.5 thinking collapse, **(B)** 082.3 docked input, **(C)** 081.8/081.9 composer.
wip 일괄 커밋(`cad33047`)으로 코드가 한 곳에 섞였지만, 각 워크스트림의 소유권은 코드
변경 이력과 devlog 근거로 식별 가능하다.

---

## 1. 파일 인벤토리 (git status --short 기준, HEAD = 81bcea96) — 실행 전 기록

> v2 실측 (HEAD = 7b93de35, 99.01 메모리 5커밋 이후): dirty 207건 = M 57 / D 95 / ?? 55.
> §1-A~C의 docs/struct 인벤토리는 유효하나, **코드 dirty가 추가됨** → 슬라이스 D가 담당.
> 코드 dirty: `mem0/`(신규 백엔드), memory 테스트 5종(search-modes 1건 fail 중), `interactive-mode.ts`·`packages/tui/`·`viewport-fill.ts`(99.09 선행분), `segments.ts`·`builtin-registry.ts`·`bash.ts`·`composer-discipline.ts`·`memories/index.ts`·`storage.ts`·`resolve.ts`·`settings-schema.ts` 수정, `CONTRIBUTING.jwc.md`·`README.jwc.md`, structure/ 전면 수정+신규 3파일.

### 1-A. `M ` — 수정됨 (스테이지 외 변경)

| 파일 | 변경 요약 | 워크스트림 |
|------|-----------|------------|
| `devlog/_plan/260612_jawcode_fork/080_moc_tui.md` | §B 참조 경로를 `085_moc_workflow_hud.md` → `99.04.00_moc_workflow_hud.md` 로 갱신 | **99 밴드 docs** — 04.00 이관 작업 |
| `devlog/_plan/260612_jawcode_fork/085.1_plan_pabcd_strip.md` | 본문 전체 → redirect stub ("이관됨") | **99 밴드 docs** — 99.04.02로 이관 |
| `devlog/_plan/260612_jawcode_fork/085.2_plan_interview_gauge.md` | 본문 전체 → redirect stub ("이관됨") | **99 밴드 docs** — 99.04.03으로 이관 |
| `devlog/_plan/260612_jawcode_fork/085.3_plan_goal_segment.md` | 본문 전체 → redirect stub ("이관됨") | **99 밴드 docs** — 99.04.04로 이관 |
| `devlog/_plan/260612_jawcode_fork/085.4_plan_hud_infra.md` | 본문 전체 → redirect stub ("이관됨") | **99 밴드 docs** — 99.04.01로 이관 |
| `devlog/_plan/260612_jawcode_fork/085.5_plan_prompt_rebrand.md` | `상위:` 링크를 `085_moc_workflow_hud` → `99.04.00_moc_workflow_hud` 로 수정 | **99 밴드 docs** — 이관 시 링크 정정 |
| `devlog/_plan/260612_jawcode_fork/085.6_plan_identity_leak_zero.md` | `상위:` 링크 동일 정정 | **99 밴드 docs** — 이관 시 링크 정정 |
| `devlog/_plan/260612_jawcode_fork/085_moc_workflow_hud.md` | 본문 전체 → redirect stub ("이관됨") | **99 밴드 docs** — 99.04.00으로 이관 |
| `devlog/_plan/260612_jawcode_fork/99.03.00_plan_workflow_surface_revision.md` | 상태 헤더 갱신(🟡→🟢) + 사용자 결정 2건(M2 채택·re-facing) 확정 기록 | **99 밴드 docs** — 이번 세션 |

### 1-B. `D ` — 삭제됨 (har_struct → struct_har 이동)

| 경로 | 설명 | 워크스트림 |
|------|------|------------|
| `har_struct/INDEX.md` | 디렉토리 전체가 `struct_har/`로 재명명됨 | **99 밴드 docs** — har_struct 재생성 (069.2 P8) |
| `har_struct/README.md` | 동일 | **99 밴드 docs** |
| `har_struct/gjc_origin/**` (32 files) | gjc_origin 스냅샷 전체 | **99 밴드 docs** |
| `har_struct/jwc_patched/**` (34 files) | jwc_patched 스냅샷 전체 (099_stabilization 포함) | **99 밴드 docs** |

> 삭제 합계: 104 파일 변경, 99줄 삽입 + 15,347줄 삭제. 디렉토리 이름만 바뀐 것(`har_struct/` → `struct_har/`)이므로 `git mv` 아님 — 별도 add/rm 필요.

### 1-C. `??` — 미추적 (untracked)

| 경로 | 설명 | 워크스트림 |
|------|------|------------|
| `devlog/_plan/260612_jawcode_fork/99.04.00_moc_workflow_hud.md` | 085 MOC 이관본 | **99 밴드 docs** |
| `devlog/_plan/260612_jawcode_fork/99.04.01_plan_hud_infra.md` | 085.4 이관본 | **99 밴드 docs** |
| `devlog/_plan/260612_jawcode_fork/99.04.02_plan_pabcd_strip.md` | 085.1 이관본 | **99 밴드 docs** |
| `devlog/_plan/260612_jawcode_fork/99.04.03_plan_interview_gauge.md` | 085.2 이관본 | **99 밴드 docs** |
| `devlog/_plan/260612_jawcode_fork/99.04.04_plan_goal_segment.md` | 085.3 이관본 | **99 밴드 docs** |
| `struct_har/` (전체 디렉토리) | har_struct 재명명 후 재생성 트리 | **99 밴드 docs** |

> ⚠️ `struct_har/README.md` 확인 결과: "STALE (260612) — Phase β 완료 후 1회 재생성" 배너 포함.
> 069.2 P8 순서 조정에 따라 재생성은 이번 세션 작업에 해당. 스냅샷 내용 자체는 현재 상태 기준.

---

## 2. 파일 → 워크스트림 매핑 요약

| 워크스트림 | 파일 수 | 비고 |
|------------|---------|------|
| **99 밴드 docs** (이번 세션) | 115 (수정 9 + 삭제 66 + 신규 6 + struct_har 재생성) | 전부 devlog/har_struct 계열 |
| **083.5 thinking collapse** (이번 세션 — 코드) | 0건 dirty 현재 없음 | cad33047에 이미 커밋됨 |
| **082.3 docked input** (별도 세션) | 0건 dirty 현재 없음 | cad33047에 이미 커밋됨 |
| **081.8/081.9 composer** (별도 세션) | 0건 dirty 현재 없음 | cad33047에 이미 커밋됨 |
| **미분류/외부** | 0건 | — |

> 현재 dirty는 **전부 "99 밴드 docs" 1개 워크스트림**이다.
> 코드 변경(wip 세션 3종)은 `cad33047` 에 일괄 선커밋되어 있음.

---

## 3. 커밋 슬라이스 정의

커밋은 사용자(메인 세션)가 직접 수행한다. 아래는 슬라이스 순서와 메시지 초안.

### 슬라이스 A — 083.5 코드+테스트 (**이미 커밋됨, 별도 액션 불필요**)

`cad33047` 에 포함됨. 이번 세션에서 커밋을 생성할 필요 없음.

단, `thinking-collapse.test.ts` 는 `cad33047` 에 untracked → tracked 로 포함됐고,
`event-controller-abort-render.test.ts` setStreaming 스텁도 동일 커밋에 존재.
`brand-visual-identity.test.ts` +22줄 compact-banner 테스트는 `184a77da` (P2 커밋)에 포함됨.

결론: **슬라이스 A는 이미 완료 상태.**

### 슬라이스 B — 99 밴드 docs 이관 (dirty 전부 청산)

**대상 파일:**
- `devlog/_plan/260612_jawcode_fork/080_moc_tui.md` (M)
- `devlog/_plan/260612_jawcode_fork/085_moc_workflow_hud.md` (M → stub)
- `devlog/_plan/260612_jawcode_fork/085.1_plan_pabcd_strip.md` (M → stub)
- `devlog/_plan/260612_jawcode_fork/085.2_plan_interview_gauge.md` (M → stub)
- `devlog/_plan/260612_jawcode_fork/085.3_plan_goal_segment.md` (M → stub)
- `devlog/_plan/260612_jawcode_fork/085.4_plan_hud_infra.md` (M → stub)
- `devlog/_plan/260612_jawcode_fork/085.5_plan_prompt_rebrand.md` (M — 링크 정정)
- `devlog/_plan/260612_jawcode_fork/085.6_plan_identity_leak_zero.md` (M — 링크 정정)
- `devlog/_plan/260612_jawcode_fork/99.03.00_plan_workflow_surface_revision.md` (M — 결정 확정)
- `devlog/_plan/260612_jawcode_fork/99.04.00_moc_workflow_hud.md` (??)
- `devlog/_plan/260612_jawcode_fork/99.04.01_plan_hud_infra.md` (??)
- `devlog/_plan/260612_jawcode_fork/99.04.02_plan_pabcd_strip.md` (??)
- `devlog/_plan/260612_jawcode_fork/99.04.03_plan_interview_gauge.md` (??)
- `devlog/_plan/260612_jawcode_fork/99.04.04_plan_goal_segment.md` (??))

**커밋 메시지 초안:**
```
[agent] docs(devlog): 085 밴드 → 99.04 이관 + 99.03 결정 확정

085.1~085.4·085 MOC를 99.04.01~99.04.04·99.04.00으로 이관;
기존 위치에 redirect stub 배치 (병렬 참조 경로 보호).
080 §B 링크, 085.5·085.6 상위 링크 99.04으로 갱신.
99.03.00 상태 헤더: 🟡→🟢, M2 채택·re-facing 확정 기록.
```

### 슬라이스 C — har_struct → struct_har 재명명 커밋

**대상 파일:**
- `har_struct/**` (66 D 파일 — `git rm -r har_struct/`)
- `struct_har/` (?? 전체 — `git add struct_har/`)

**커밋 메시지 초안:**
```
[agent] chore(structure): har_struct → struct_har 재명명 + β 후 스냅샷 재생성

069.2 P8: Phase β(.gjc→.jwc) 완료 후 1회 재생성 일정에 따라
디렉토리를 struct_har/로 재명명하고 jwc_patched 스냅샷을 현행 포크 기준으로 재생성.
gjc_origin 기준선 보존. 099_stabilization 섹션 신설.
```

> **주의**: `git rm -r har_struct/` + `git add struct_har/` 순서로 스테이지 구성.
> `git mv` 는 디렉토리 재귀 지원이 제한적이므로 separate rm/add 권장.

**슬라이스 B 흡수분 (v2):**
- 번호 재정렬 구스텁 `git rm`: `99.02.01_impl_workflow_surface.md`, `99.03.00_plan_workflow_surface_revision.md`(구), 기타 "구번호 스텁 — git rm 대상" 표기 파일 전수
- 5-A `state-read-markdown.test.ts:28` describe 레이블 1줄 픽스 (gjc→jwc)
- `patch-local-query-search.ts`·`patch-memory-search-wiring.ts`(적용 완료된 일회용 패처, 참조 0) → `devlog/_plan/260612_jawcode_fork/_scripts/`로 이동

### 슬라이스 D — 코드 일괄 정식화 (v2 신설, 사용자 확정 260612 22:24)

**방침**: 새 코드 dirty는 cad33047 방식으로 **일괄 커밋**하고, 랜딩된 내용 기준으로 99 밴드 분석 문서(99.00.02 실사용 분석 등) 갱신을 **직원 bgtask로 위임**한다.

**대상**: §1 v2 실측의 코드 dirty 전부 (mem0/, memory 테스트 5종, tui/viewport-fill, interactive-mode, segments, builtin-registry, bash.ts, composer-discipline, structure/ 수정+신규 3파일, CONTRIBUTING/README.jwc.md)

**스크립트 재배치 (사용자 확정 — "plan 아래 or struct_har")**:
- `struct-har-regenerate-*.ts` 5종 → `struct_har/_scripts/` 이동 + 인용 문서 8곳 경로 갱신 (structure/gitstructure·conventions·doc_map·upstream_lineage, struct_har/INDEX·README·083 code_facts ×2) — rebrand --strict 스캔 루트 밖이라 allowlist 불필요, *-omp.ts 41건 위반 해소
- `docs-brand-sweep.ts`는 scripts/ 유지 (rebrand 금지 토큰 무위반 확인)

**후속 bgtask (커밋 직후 등록)**:
`cli-jaw dispatch --agent "Docs"` 또는 bgtask로 — "랜딩 커밋 기준 99 밴드 분석 문서(99.00.02 등) 정합 갱신" 위임.

### 커밋 순서

```
B (docs 이관+스텁 rm+패처 이동) → C (har_struct 재명명+regenerate 스크립트 이동) → D (코드 일괄 정식화)
```

B → C 순서가 안전하다. C는 대용량 파일 변경(~15k줄)이라 독립 커밋이 리뷰에 유리.

---

## 4. 커밋하면 안 되는 파일 (외부 세션 소유)

현재 dirty에는 **다른 세션이 소유한 파일이 없다** (코드 WIP 전부 cad33047에 선커밋).
아래는 cad33047에 함께 들어있는 파일 중 이번 정리 커밋에서 **건드리지 말아야 할** 영역:

| 파일 | 이유 |
|------|------|
| `packages/coding-agent/src/tools/ask.ts` | 082.3 docked input (별도 세션 WIP — tsc 실패 포함) |
| `packages/coding-agent/src/modes/components/hook-selector.ts` | 082.3 소유 |
| `packages/coding-agent/test/hook-selector-*.test.ts` | 082.3 소유 |
| `packages/coding-agent/test/tools/ask.test.ts` | 082.3 소유 (tsc 오류 포함) |
| `packages/ai/src/providers/cursor.ts` | 081.8/081.9 composer 소유 |
| `packages/ai/src/providers/openai-completions.ts` | 081.8/081.9 소유 |
| `packages/ai/src/stream.ts` | 081.8/081.9 소유 |
| `packages/ai/src/providers/composer-discipline.ts` | 081.8/081.9 소유 |

이들은 cad33047에 이미 커밋됐으므로 **현재 working tree에는 dirty가 없다**. 재개 시 후속 픽스업 커밋이 각 세션 소유로 작성되어야 한다.

---

## 5. 테스트 그린 태스크

### 5-A. state-read-markdown "unknown jwc state flag" 테스트 — 1줄 픽스 스펙

**파일**: `packages/coding-agent/test/gjc-runtime/state-read-markdown.test.ts`
**라인**: L28 `describe` 블록 제목 + **L77** 테스트 본문

**현황 확인** (git grep):
```
L28: describe("gjc state read markdown", ...
L77: it("rejects unknown jwc state flags", ...
L81: expect(result.stderr).toContain("unknown jwc state flag: --bogus")
```

**배경**: `ff11c848`(C8/085.5-M3) 에서 `state-runtime.ts` 의 에러 문구가
`"unknown gjc state flag"` → `"unknown jwc state flag"` 로 변경됐다.
`L81` 의 expect는 이미 "jwc" 로 되어 있으므로 **L81은 이미 정합**.
그러나 `describe` 제목 L28 이 여전히 `"gjc state read markdown"` 이다.

**1줄 픽스 스펙** (적용은 하지 않음 — 참고용):
```diff
- describe("gjc state read markdown", () => {
+ describe("jwc state read markdown", () => {
```

> 실제 테스트 실패 여부는 describe 레이블과 무관(실행에 영향 없음). 하지만 
> 브랜드 일관성 스윕(99.06) 대상이므로 슬라이스 B 또는 C와 함께 1줄 픽스 추가 가능.

### 5-B. ask.test.ts tsc 실패 — 외부 의존성, 이번 슬라이스 제외

`cad33047` 커밋 메시지 명시: "tsc 1건(ask.test) — 후속 정리 전제".
소유: **082.3 docked input 세션**. 수정 범위는 `ask.ts` + `ask.test.ts` (함께 개정 필요).
이번 99.02 슬라이스에서는 **건드리지 않는다**. 082.3 세션 픽스업 커밋에서 해결.

### 5-C. 060 M4 — `interactive-mode.ts` 착수 시 언블록되는 구현 단계

**출처**: `061_design_goal_merge.md` §7 M4 (구현 상태: M1-M3·M5-M7 완료, M4 ⬜)

`interactive-mode.ts` 가 083.5 thinking collapse 작업으로 수정된 상태이므로,
M4 착수 전 해당 변경이 확정 커밋 상태여야 한다.

**M4 구현 대상** (`interactive-mode.ts` 가 랜딩 후 언블록):

1. **`GoalSubcommand` 별칭 추가**:
   `interactive-mode.ts:204-215` — `status`/`cancel` 를 `show`/`drop` 별칭으로 정규화하는
   `parseGoalSubcommand` 확장 (현재 `show`→`status`, `drop`→`cancel` 미매핑).

2. **신규 동사 M1 경유 배선**:
   `interactive-mode.ts:1681-1698` — `set`, `plan`, `refine`, `update`, `done`, `pause`, `resume`,
   `history` 동사를 `gjc-runtime/goal-runtime.ts` (M1, 이미 구현됨) 로 위임.
   현재 TUI `/goal` 슬래시는 자체 처리 중 — M1 경유로 전환.

3. **agent 2-pass pause 게이트 연결**:
   `interactive-mode.ts:1742-1748` — `#pauseGoalAction` 에 M3 상태 필드
   (`agentPauseCount`, `pauseAudit`) 연결. human pause는 비대상.

4. **`builtin-registry.ts:287-307`** — `/goal` 슬래시 커맨드 등록에 새 별칭 동사 추가.

**의존 선결**:
- M1 (`goal-runtime.ts`) ✅ 구현됨
- M3 (`goals/state.ts` 상태 필드) ✅ 구현됨
- `interactive-mode.ts` thinking-collapse 변경이 cad33047 → ceeba38e 정식 커밋으로 확정

---

## 6. 인수 기준 (acceptance)

### 6-A. 테스트 스위트 베이스라인

[v2.1 재실측 — 260612 22:45, 클린 worktree @ f44bd8fa] **베이스라인 12건** (5463 pass / 12 fail / 356 skip):
skills loadSkills ×4 · ultragoal help ·1 · stage-skill-pointers ×1 · composer editor ×1 (이상 기존 7) +
랜딩 WIP 5: BashTool ACP timeout ×1 · SettingsSelector memory tab ×2 · local-query search-modes fts ×1 · status rail chrome ×1 — 전부 소유 세션 후속 픽스업 대상. 이 12건 외 추가 실패 = 회귀.

v1 기록 (참고용 — `bun test` 실행 시 사전 알려진 실패 4건 허용):

| 테스트 파일 | 실패 원인 | 상태 |
|------------|-----------|------|
| `test/interactive-mode-editor-component.test.ts` | 에디터 컴포넌트 미구현 의존 | 기존 베이스라인 |
| `test/coordinator-mcp.test.ts` (또는 `coordinator-mcp-policy.test.ts`) | MCP coordinator 미구현 | 기존 베이스라인 |
| `test/tools/image-gen.test.ts` | 이미지 생성 미구현 | 기존 베이스라인 |
| `test/tools/image-gen-capability.test.ts` | 동일 | 기존 베이스라인 |

이 4건 외의 추가 실패는 **회귀로 간주** → 슬라이스 커밋 전 확인 필요.

> `083.5_plan_thinking_collapse.md §구현 기록` 에서도 동일 표현 확인:
> "전체 스위트 5382 pass — 실패 6건은 전부 기존 베이스라인(4) + 동시 세션 WIP(2)"

### 6-B. `check:ts` 게이트

```bash
cd /Users/jun/Developer/new/700_projects/jawcode
bun run check:ts
```

최상위 `package.json` 에서 `check:ts` =
`bun run check:tools && bun run check:node20-baseline && bun run check:schemas && bun run check:gjc-ui && bun run --workspaces --if-present check`.

각 패키지의 `check` = `biome check . && bun run check:types` (tsgo `--noEmit`).

**예상 잔여 실패**: `packages/coding-agent/test/tools/ask.test.ts` tsc 오류 1건 (082.3 소유 WIP).
이 건은 99.02 슬라이스 완료 후에도 남아 있음 — **082.3 픽스업 커밋 대기**.

### 6-C. β goal 게이트 재확인 명령

```bash
# 1. dirty 0 확인
cd /Users/jun/Developer/new/700_projects/jawcode && git status --short

# 2. β goal 현재 상태 확인
cat ~/.cli-jaw/goal/active.json | python3 -c "import sys,json; d=json.load(sys.stdin); print('status:', d['status'])"

# 3. β goal resume 가능 여부 (dirty = 0이어야 재개)
# jwc goal resume  (또는 cli-jaw goal resume 3f6989ac — 게이트 통과 후)
```

[v2 정정] β goal `3f6989ac`는 P9~P11(Phase β 본 적용, 0b603b05+d34097b8)까지 체크포인트 완료 상태로 paused.
잔여는 P8(struct_har 재생성)뿐이며 이번 슬라이스 C로 실질 완료 — dirty 청산 후 resume 시 P8 체크포인트 제출로 종결 수순.

---

## 7. 미분류 항목 플래그

**없음** — 현재 dirty 전체가 "99 밴드 docs" 워크스트림으로 명확하게 식별됨.

단, 하나 주의할 점: `struct_har/README.md` 의 STALE 배너는 이번 슬라이스 C에서
재생성된 내용을 반영하고 있으나, β P8의 "1회 재생성" 이후 추가로 한 번 더 갱신이
필요할 수 있다(069.2 P8 트리거 조건). β goal resume 시 확인.

---

## 8. 착수 체크리스트

- [x] 슬라이스 B 커밋 (99.04 이관 docs + 99.03 결정 확정 + 구스텁 rm + 5-A 픽스 + 패처 이동) — a7412632
- [x] 슬라이스 C 커밋 (har_struct → struct_har + regenerate 스크립트 5종 struct_har/_scripts/ 이동 + 인용 8곳 갱신) — dfaa73ff
- [x] 슬라이스 D 커밋 (코드 일괄 정식화) + 직원 bgtask 등록 (99 밴드 분석 문서 갱신) — ceeba38e
- [x] 베이스라인 재실측 → §6-A 표 갱신
- [x] `git status --short` → 출력 없음 확인 (dirty = 0)
- [x] `bun test` → 4건 기존 베이스라인 외 추가 실패 0 확인 — v2.1: 재실측 12건 기준
- [ ] `bun run check:ts` → ask.test.ts tsc 1건 외 0 확인
- [ ] β goal `3f6989ac` resume 가능 조건 확인
- [x] state-read-markdown L28 브랜드 레이블 1줄 픽스 (슬라이스 B 또는 99.06 스윕에 포함) — a7412632
- [x] 060 M4 TUI 별칭·게이트 착수 (interactive-mode.ts 정식 확정 후) — ceeba38e
