# 99.00.01 감사 — jwc 데일리 드라이버 준비 상태 (260612 기준선)

> 상태: 감사 v1 (260612) — 99 밴드 착수 전 기준선
> 소속: [99.00.00 MOC](./99.00.00_moc_stabilization.md). READ-ONLY 감사 — 소스 코드 미수정.
> 질문: "지금 99 밴드를 전부 끝내면 데일리 드라이버로 바로 쓸 수 있는가? 지금 당장은 어디까지 쓸 수 있는가?"

---

## 1. 설치/실행 경로

### 심링크 및 브랜드 설정

- `~/.bun/bin/jwc` → `/Users/jun/Developer/new/700_projects/jawcode/packages/jwc/bin/jwc.js` (라이브 레포 직결)
- `jwc.js` 상단 4줄:
  ```js
  process.env.JWC_BRAND_NAME = "jwc";
  process.env.GJC_BRAND_NAME = "jwc"; // legacy alias
  try { await import("../dist/jwc.bundle.js"); }
  catch { await import("@gajae-code/coding-agent/cli"); }
  ```
  브랜드 환경변수 **설정 코드가 jwc.js에 하드코딩됨** — 별도 env 주입 불필요.
- 번들(`packages/jwc/dist/jwc.bundle.js`, Jun 12 19:43 빌드, gitignore됨) 내부는 `import "@gajae-code/coding-agent/cli"` 한 줄 — 실제 동작은 workspace 심링크로 **라이브 소스**(`packages/coding-agent`)를 참조. wip 커밋 후 번들 재빌드 없어도 코드 반영됨.

### 기동 스모크 (읽기 전용, `/tmp` 기준)

| 커맨드 | 결과 |
|--------|------|
| `jwc --version` | `jwc/0.4.4` ✅ |
| `jwc --help` | 전체 플래그·서브커맨드 출력 ✅ |
| `jwc orchestrate status` | `pabcd: idle (no active orchestration). Start with: orchestrate i` ✅ |
| `jwc goal status` | `goal: none (run 'jwc goal set <objective>')` ✅ |
| `jwc interview --help` | 플래그 목록 정상 출력 ✅ |
| `jwc ralplan --help` | 플래그 목록 정상 출력 ✅ |
| `jwc memory search` | **jwc launch help 출력** (memory 서브커맨드 미구현) ❌ |

크래시 없음. 브랜드 env 없이도 기동 가능 (jwc.js가 직접 주입).

---

## 2. 상태 디렉토리 일관성 (CRITICAL)

### packages/utils/src/dirs.ts — 헤드 기준

| 상수/함수 | 값 |
|-----------|-----|
| `ENGINE_NAME` | `"gjc"` (고정, 경로/로그 파일 식별자) |
| `APP_NAME` | `process.env.JWC_BRAND_NAME \|\| GJC_BRAND_NAME \|\| "jwc"` (브랜드 표시명) |
| `CONFIG_DIR_NAME` | `".jwc"` (하드코딩) |
| `getConfigDirName()` | `JWC_CONFIG_DIR ?? GJC_CONFIG_DIR ?? PI_CONFIG_DIR ?? ".jwc"` |
| `configRoot` | `~/.jwc` (env 없을 때) |
| `getMemoriesDir()` | `~/.jwc/agent/memories/state` |

**결론: utils/dirs.ts는 완전히 `.jwc` 기반으로 전환 완료.** `.gjc` 리터럴은 `migrate-config-dir.ts` 한 파일에만 잔류 (마이그레이터가 레거시 경로를 참조하므로 의도적 예외).

### gjc-runtime 파일 — 하드코딩 경로 실측

| 파일 | 라인 | 내용 |
|------|------|------|
| `orchestrate-state.ts:229-230` | `.jwc/state/sessions/…` / `.jwc/state` | `.jwc` 리터럴 ✅ |
| `ultragoal-runtime.ts:141` | `.jwc/ultragoal` | `.jwc` 리터럴 ✅ |
| `jaw-interview-runtime.ts:17` | `.jwc/state/jaw-interview-state.json` | `.jwc` 리터럴 ✅ |
| `jaw-interview-runtime.ts:91-92` | `.jwc/state/sessions/…` / `.jwc/state` | `.jwc` 리터럴 ✅ |
| `jaw-interview-runtime.ts:206` | `~/.jwc/agent/config.yml` | `.jwc` 리터럴 ✅ |

**Phase β (.gjc→.jwc 경로 스윕, 커밋 `0b603b05`) 완료 확인됨.** 런타임 코드에서 `.gjc` 경로 하드코딩 0건.

### 온디스크 상태 맵

| 경로 | 내용 | 소유자 |
|------|------|--------|
| `~/.jwc/` | `agent/{agent.db, config.yml, …}`, `logs/`, `plugins/` | jwc 바이너리 (자체 DB) |
| `~/.gjc/` | `agent/{agent.db, config.yml, …}`, `logs/`, `plugins/` | **기존 gjc 업스트림 바이너리** (마이그레이션 sentinel 없음) |
| repo-root `.jwc/` | `state/{active, jaw-interview-state.json, …}`, `specs/` | jawcode 레포 내 세션 |
| repo-root `.gjc/` | `.jwc-migrated` sentinel만 (2026-06-12T10:55:53) | 마이그레이터가 남긴 흔적 |

**주의: `~/.gjc/`는 마이그레이션이 실행되지 않은 상태.** 해당 디렉토리에 sentinel이 없다는 것은 jwc 바이너리가 `~/.gjc/`에서 기동된 적이 없거나 이미 `~/.jwc/`가 선점해 "skipped" 처리된 것. 기능상 문제 없음 — jwc는 `~/.jwc/`만 읽고 `~/.gjc/`는 무시됨.

**split-brain 유무**: 없음. jwc 런타임 전체가 `.jwc`를 일관 사용. `~/.gjc/`는 다른 바이너리(gjc upstream) 전용 격리 공간.

**Phase β sweep은 REQUIRED였고 이미 완료됨.** 99 밴드의 "Phase β sweep" 잔여 항목은 이 경로 전환이 아니라 시스템 프롬프트·문서의 gjc 어휘 표면 정리(85.5/85.6)임.

---

## 3. dirty tree 영향

### 커밋 상태

wip 커밋 `cad33047` (Jun 12 20:19) — "병렬 TUI/composer 워크스트림 스냅샷 일괄 커밋":
- 커밋 포함: `thinking-collapse`, `hook-selector`, `ask.ts`, `composer-chrome`, TUI 컨트롤러군, `packages/ai` 진행분 (약 30 소스 파일)
- 커밋 주석: "WIP 상태 그대로 — biome format 6파일·tsc 1건(ask.test)·known 테스트 실패 포함 (후속 정리 전제)"

### 현재 실제 미커밋 변경

```
 M devlog/_plan/260612_jawcode_fork/085.x / 99.02.00 등 (9개 devlog 문서)
 M packages/coding-agent/src/modes/components/status-line/segments.ts  ← 소스 1건
?? devlog/_plan/260612_jawcode_fork/99.03.xx (5개 신규 devlog)
?? struct_har/
```

소스 변경 1건: `segments.ts` — `sessionNameSegment` 렌더 비활성화 (081.10 세션 타이틀 할루시네이션 픽스). 런타임에 이미 동작 중(워크스페이스 직접 참조이므로).

### 테스트/lint 상태

| 체크 | 결과 | 런타임 영향 |
|------|------|------------|
| `bun test ask.test.ts` | 34 pass ✅ | 없음 |
| `bun test state-read-markdown.test.ts` | 3 pass ✅ | 없음 |
| `bun test thinking-collapse.test.ts` | 5 pass ✅ | 없음 |
| `bun test hook-selector-list-slot.test.ts` | 3 pass ✅ | 없음 |
| `check:node20-baseline` | OK ✅ | 없음 |
| `check:gjc-ui` | OK ✅ | 없음 |
| `check:schemas` | **FAIL** — `schemas/config.schema.json` stale | CI 차단, 런타임 무관 |
| `biome check` (lint) | 7 errors, 전부 FIXABLE (unused vars, format) | **런타임 영향 없음** |

**결론: 알려진 실패는 전부 CI-only (스키마 stale + lint 스타일).** 런타임 usability를 막는 테스트 실패 없음. MOC 언급의 "ask.test, state-read-markdown 실패"는 wip 커밋 전 상황이었으며 현재 해소됨.

---

## 4. 워크플로 표면 동작

### 등록된 CLI 커맨드

```
goal, orchestrate, interview, ralplan, ultragoal, harness,
session, skills, state, team, config, setup, update, …
```

메모리 관련: `/memory` 슬래시 커맨드는 있음 (view/clear/enqueue/mm 계열). `jwc memory` CLI 커맨드 없음 (`jwc memory search` → launch 도움말).

### 슬래시 커맨드 현황

| 슬래시 | 상태 |
|--------|------|
| `/goal` | ✅ 존재 (set/show/pause/resume/drop) |
| `/orchestrate` | ✅ 존재 (i/p/a/b/c/d/status), jaw brand only |
| `/memory` | ✅ 존재 (view/clear/mm 계열) |
| `/interview` | ❌ 없음 (99.07 갭) |
| `/plan` (ralplan) | ❌ 없음 (99.07 갭) |
| `/team` | ❌ 없음 (99.07 갭) |
| `/sessions`, `/switch`, `/fork` | ❌ 없음 (99.07 갭) |
| `orchestrate reset` 동사 | ❌ 없음 (99.07 갭) |

### M4 TUI /goal 별칭 (99.02 잔여)

`interactive-mode.ts:204-215` 패치 미완료 — `GoalSubcommand`에 `status`/`cancel` 별칭(show/drop 정규화)과 agent 2-pass pause 게이트 미연결. `/goal` 슬래시 자체는 동작하나 별칭 verb 처리 누락. **런타임 사용은 가능하나 완전하지 않음.**

### pabcd discovery 상태 (CRITICAL GAP)

시스템 프롬프트(`system-prompt.md:21`) — "jwc exposes **exactly four default workflow skills**. Do not add, advertise, or route to other default workflow definitions."

pabcd/IPABCD/orchestrate 언급: **0건.** 모델은 `/orchestrate` 커맨드의 존재를 시스템 프롬프트에서 인지할 방법이 없음. dev-pabcd 스킬도 jaw 브랜드에서 차단됨(`skills.ts:164`). 99.03가 이 계층을 복원.

---

## 5. 기능 갭 맵

| 사용 시나리오 | 지금 상태 | 블로커 | 해소 패키지 |
|---------------|-----------|--------|------------|
| **일상 코딩 (AI 어시스턴트)** | ✅ 완전 동작 — jwc 기동, 모델 선택, 파일 조작, 세션 재개, `/compact`, `/export` | 없음 | — |
| **interview → ralplan 요구 명확화** | ✅ 동작 — `jwc interview --standard`, `jwc ralplan`, `/skill:jaw-interview` | `/interview` 슬래시 없음 (편의성 감소) | 99.07 |
| **pabcd 워크플로 자율 실행** | ⚠️ 부분 — `/orchestrate i→d` 슬래시 존재, CLI `jwc orchestrate` 동작; 그러나 모델이 존재를 인지 못함 → "pabcd 진행해" 지시 시 ralplan으로 라우팅 | **시스템 프롬프트에 pabcd 0언급 (M1 discovery 없음), 매 턴 phase 헤더 없음 (M2), 자가 전이 불가 (M3)** | **99.03 (CRITICAL)** |
| **메모리 저장/검색** | ❌ CLI 없음 — `/memory view`·`enqueue`는 동작; `jwc memory search/save` 미구현 | CLI 커맨드 신규 구현 필요 | **99.01** |
| **HUD 가시성 (단계 띠/인터뷰 게이지)** | ❌ 미구현 — jaw-interview HUD chip은 `skill-active-state.json` 경유로 일부 동작; pabcd 단계 띠·4차원 게이지·goal 세그먼트 없음 | 상태줄 신규 세그먼트 미작성 | **99.04** |
| **goal 자율 실행 (ultragoal)** | ✅ 동작 — `jwc goal set/update/done/pause/history`; `jwc goal status` | M4 TUI 별칭 미완성 (편의성), `/goal` slash는 동작 | 99.02 (M4 마감) |
| **quota/모델 가시성** | ✅ 완전 — `/quota anthropic/openai-codex/xai/kiro`, `/model`, `/effort` | — | — |
| **세션 관리** | ⚠️ 부분 — `jwc session`, `-c`/`-r` 플래그 동작; `/sessions`·`/switch`·`/fork` 슬래시 없음 | 슬래시 미구현 | 99.07 |

---

## 6. 평결 (3단계)

### (a) 지금 당장 쓸 수 있는 범위

**일반 AI 코딩 어시스턴트로서 완전 사용 가능.** 기동, 모델 선택, 파일 조작, 세션 지속, interview→ralplan 수동 워크플로 전부 동작. goal 설정/추적도 동작. 슬래시 `/orchestrate`로 pabcd 단계 수동 전이 가능 — 단, 모델에게 "pabcd 진행해"라고 말해도 모델이 `/orchestrate`를 스스로 사용하지 않음.

**MLB 20-80 스케일: 현재 50 (평균)** — 일반 코딩 보조 60, pabcd 자율화 20, 메모리 30.

### (b) 99.02 + 99.03 완료 시

- 99.02: check:schemas 해소, M4 TUI /goal 별칭 완성, biome 정리 → CI green
- 99.03: 시스템 프롬프트 M1(discovery) + M2(매 턴 phase 헤더) + M3(자가 전이) → pabcd 워크플로 자율 실행 활성화

**MLB 스케일: 62 (plus)** — 코딩 보조 60, pabcd 반자율화 65, 메모리 30.

### (c) 99 전체 + Phase β 완료 시 (99.01~99.07)

- 99.01: `jwc memory search/save/context` CLI + 세션 간 기억 e2e
- 99.04: pabcd 단계 띠 + 인터뷰 게이지 + goal 세그먼트 (HUD 가시성)
- 99.05: auth 게이트
- 99.06: 문서 stale 일괄 정리
- 99.07: `/interview`·`/plan`·`/team`·`/sessions` 슬래시 패리티 보강

**MLB 스케일: 68 (plus-plus)** — 코딩 보조 70, pabcd 자율화 70, 메모리 65, HUD 70.

Phase β goal(3f6989ac — struct_har/Node 포팅)과 auth 릴리즈 게이트(099.05)가 추가로 닫혀야 "출하 가능" 상태.

---

## 최종 판정 요약

> **99 밴드 전부를 끝내도 데일리 드라이버로 쓸 수 있다 — 단, "다 끝내야 쓸 수 있다"는 아니다.**
> 지금도 일반 AI 코딩 보조(코드 작성, 세션, goal, ralplan)는 완전 동작. pabcd 자율화(M1-M3)와 메모리 CLI가 99.03+99.01 완료 시 활성화되며, 그게 "데일리 드라이버"의 실질 임계점이다.

---

## 3대 CRITICAL 블로커

### 1. pabcd discovery 제로 (시스템 프롬프트 M1/M2/M3 부재)

시스템 프롬프트에 orchestrate/IPABCD 언급 0건. 모델이 pabcd 단계 진행 방법을 모른다. "pabcd 진행해" 입력 시 ralplan으로 리다이렉트. 99.03 M1(system-prompt `<jwc-runtime>` 개정) + M2(매 턴 phase 헤더 레일) + M3(자가 전이 문구)가 해소.
**해소 패키지: 99.03 (설계 확정됨, 구현 착수 가능)**

### 2. jwc memory CLI 커맨드 미구현

`jwc memory search/read/save/context` 서브커맨드 없음. `/memory view/enqueue`로 내부 상태 조작은 되지만 세션 횡단 기억 저장·검색·수동 save가 불가. 메모리 엔진(SQLite, `~/.jwc/agent/memories/state`)은 준비됐으나 CLI 표면이 없음.
**해소 패키지: 99.01 (설계 확정됨, 구현 착수 가능)**

### 3. check:schemas FAIL + biome 7 errors (CI 차단)

`schemas/config.schema.json` stale로 `bun run check:ts` 전체 실패. biome 7건(unused private member 3건, unused var 1건, format 3건)도 남아 있음. 런타임 동작은 무관하나 PR gate 및 새 커밋 검증을 막아 개발 루프가 지연됨. 99.02 마감이 이 청산 작업을 포함.
**해소 패키지: 99.02 (코드 완료 / 마감 미완료)**

---

*감사 기준: 260612 20:30 KST. 커밋 헤드 `81bcea96`. 소스 코드 무수정.*
