# 99.07.00 — issue: orchestrate reset 부재 + goal 동사 슬래시 패리티 (조사 완료 / 구현 ⬜ 미완)

> 상태: ✅ U1/U2 구현 완료 (260613 00:50 — 2cf37f35·c0ca9a53) + ✅ **별칭 패리티 완료** (260613 — 99.00.04 S4: /gd·/goalplan·/interview + reset 자동완성 + hint invariant 테스트. /plan·/team은 jwc 런타임 표면 부재로 이월, /goal 서브커맨드 확장은 TUI 라우팅 재설계 필요로 이월).
> 트리거(라이브 실측 260613 00:34): ① `jwc orchestrate reset` → `unknown stage 'reset' (expected i|p|a|b|c|d|complete, status, verdict, audit-prompt)` ② 모델이 jaw-interview를 닫으려고 `cancelled` phase 시도 → `unknown jaw-interview phase "cancelled"; use --force to bypass` → `--force` 우회로 complete 처리.
> 조사: Backend 직원(jwc 측) ∥ Docs 직원(cli-jaw 정본 측) 병렬 + cli-jaw dashboard chat search (260613 00:36).

## 1. 미완 항목 (구현 대상 — 본 문서 작성 시점 미패치)

| # | 항목 | 현황 | 비고 |
|---|------|------|------|
| **U1** | `jwc orchestrate reset` 동사 | **부재** — orchestrate-runtime이 reject. 실질 우회는 상태 파일 수동 삭제뿐 (`complete`/`i` 복귀는 ctx 보존이라 reset이 아님) | cli-jaw-vocab.ts:20이 `cli-jaw orchestrate reset` → "`jwc orchestrate complete` (full reset unsupported)"로 **거짓 등가 치환** 중 — 시맨틱 불일치 |
| **U2** | jaw-interview 취소 동사 | **부재** — manifest에 `cancelled` phase 없음, 모델이 `--force`로 우회하는 패턴 발생 | `clear`는 soft-close이지 reset 아님 (문서화도 부재) |

## 2. cli-jaw 정본 시맨틱 (Docs 직원 + dashboard chat 실측)

### orchestrate reset
- **어느 상태에서든 가능** ("Return to IDLE from any state" — bin/commands/orchestrate.ts:23). 재진입은 `IDLE→I` 또는 `IDLE→P` ("Interview optional: IDLE→P also valid" :26 — **I는 P의 필수 선행이 아님**, 2d3a14a8에서 jwc 프롬프트 정합 완료).
- "context cleared" = SQLite `orc_state` row를 `state='IDLE', ctx=NULL`로 (db.ts:394) — 채팅/메시지 DB는 비대상.
- 서버 full reset(pipeline.ts:616)은 추가로: active PABCD worker kill/cancel·worker registry clear·employee sessions clear·friction reset·worklog status `reset`+Final Summary append.
- 슬래시 표면: `/orchestrate [I|P|A|B|C|D|status|reset] [--force]`, alias `/pabcd` (commands.ts:267).

### goal 동사 표면 (commands.ts:246-248)
| 슬래시 | 시맨틱 | 비고 |
|--------|--------|------|
| `/goal <objective>\|plan [hint]\|refine\|status\|run\|done\|cancel\|pause\|resume\|clear\|reset\|history` | goal 상태·제한 실행 워크플로 | reset 동사가 goal에도 존재 |
| `/goalplan [hint]` | `/goal plan` 별칭 — **AI가 goal을 결정**(buildGoalPlanSteerPrompt: "YOU must decide", hint는 directional guidance). planHint는 **독립 상태 필드**로 저장, refine 전 checkpoint 금지 (handlers-workflows.ts:274, goal.ts:72) | jwc는 brief에 sentinel+hint 합산 방식 — 의미 동형이나 "hint는 objective가 아니다" invariant 테스트 부재 |
| `/gd [note]` | **`/goal done --force` 강제 완료 단축키** — 핸들러가 --force 자동 삽입 (260611 19:40 CLI_JAW_이슈2 세션에서 확정: `/goal done`은 evidence gate 유지, `/gd`만 분리) | jwc에 top-level 별칭 부재 |
| `/goaldone` | **cli-jaw에도 없음** | jwc도 불요 — 정본은 `/gd`·`/goal done` |
| `goal done` gate | `goalHasCompletionEvidence` false면 거부, `--force`만 우회 (handlers-workflows.ts:283, goal.ts:93; evidence = latest checkpoint의 non-blank evidencePaths ≥1, store.ts:214). AI 출력 내 `/goal done`도 감지·동일 게이트 (lifecycle-handler.ts:80/834). 시스템 프롬프트가 "사용자 명시 요청 없이 goal done 금지" (a1-system.md:332) | jwc `goal done`은 quality-gate-json 계약으로 상응 — 동형 유지 |

## 3. jwc 측 현황 (Backend 직원)

- 수용 동사: `i|p|a|b|c|d|complete, status, verdict, audit-prompt` — reset 없음 (orchestrate-runtime.ts:317-321 reject).
- `complete` vs reset: complete는 ctx 보존·파일 유지·재시작 시 ctx 승계 — cli-jaw reset(컨텍스트 클리어)과 불일치.
- 8331c03b(env 자동 세션 스코프) 이후 reset 정본 시맨틱: **스코프 파일 삭제 우선**, shared fallback 정리 옵션 필요.
- M2 헤더·HUD 띠는 파일 삭제 시 다음 턴부터 자동 invisible (매 턴 재읽기 — 캐시 무효화 불요).

### Backend 설계 스케치 (구현 시 그대로 사용)
- `jwc orchestrate reset`: 스코프(env/플래그) 상태 파일 삭제 → status idle. active 스테이지 중이면 현재 stage·spec_ref 요약 출력 후 진행(TTY 확인 프롬프트, `--yes` 스킵). goal ledger 미삭제(cli-jaw도 orc ctx만 클리어). cli-jaw-vocab.ts:20 치환 규칙을 실제 reset으로 갱신.
- 테스트 9건: idle no-op / mid-cycle 삭제 / complete 후 잔재 0 / 세션 스코프 격리(sess-B 무영향) / env vs 플래그 우선 / shared 정리 옵션 / unknown-stage 회귀 / M2·HUD null / active 가드.
- U2: `jwc interview cancel`(또는 `state jaw-interview reset`) — manifest `cancelled` 추가보다 **파일 삭제 방식이 clear와 구분 명확**. 최소 조치로 `clear`를 "soft close, not reset"으로 문서화.

## 4. 보스 검토 (260613 00:40 — 패치 보류 판단 포함)

1. **U1 우선순위 상**: reset 부재 자체보다 **vocab 맵의 거짓 치환**(reset→complete)이 더 위험 — 모델이 "reset했다"고 믿지만 ctx가 살아있는 상태 발생. 구현 시 vocab 갱신을 같은 커밋에 묶을 것.
2. 설계 동의 (1건 보완): Backend 스케치의 "TTY 확인 프롬프트"는 **비대화 셸(모델 실행) 경로에서 행 위험** — 99.01 좀비 사건과 동일 계열. **확인 프롬프트 없이 요약 출력+진행**을 기본으로 하고 `--dry-run`을 옵션으로 두는 편이 안전.
3. `/gd`·`/goalplan` top-level 별칭은 99.07 본체(슬래시 패리티) 스코프로 — reset과 분리 구현 가능. `/goaldone`은 만들지 않음(정본에도 없음).
4. goal-runtime에 이미 `reset` 표면이 cli-jaw `/goal` 인자열에 존재(`...clear|reset|history`) — jwc `goal` 동사와의 교차 점검을 구현 플랜에 포함할 것.
5. U2는 U1과 같은 커밋 불요 — 파일 삭제 시맨틱 공유하므로 후속 슬라이스로.

## 5. 다음 단계 (착수 시)

- [x] U1: `orchestrate reset` 구현 + vocab 갱신 + 탈출 UX(거부 메시지·system-prompt·라우팅) — 2cf37f35
- [x] U2: `jwc interview cancel` — c0ca9a53
- [ ] `/gd`·`/goalplan` 별칭 패리티 — 99.07 본체 플랜에 흡수
- [ ] "hint는 objective가 아니다" invariant 테스트 (goal plan sentinel 경로)
