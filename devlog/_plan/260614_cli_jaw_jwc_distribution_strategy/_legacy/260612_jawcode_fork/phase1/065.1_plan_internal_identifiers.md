# 065.1 — 내부 식별자(gjc-runtime 등) 처분 플랜

> 소속: [060.1 풀 jwc 포팅 MOC](./060.1_moc_full_jwc_port.md) · goal 62439906-d79 · 드래프트: sonnet 서브에이전트 (260612 16:1x)

## 1. 인벤토리

| # | 패밀리 | 대상 | src/test 참조 | 사용자 노출 |
|---|--------|------|---------------|------------|
| A | **gjc-runtime/** | `src/gjc-runtime/` (28 .ts) + `test/gjc-runtime/` (30 .ts) | 37 src + 34 test import | 없음 |
| B | **gjc-plugins/** | `src/extensibility/gjc-plugins/` (10 .ts) + fixtures | 14 + 8 | 없음 |
| C | **gjc-defaults.ts + defaults/gjc/** | `src/defaults/gjc-defaults.ts`, `src/defaults/gjc/skills/` | 5 + 3 | `embedded:gjc/…` 경로가 에이전트 컨텍스트 노출 (gjc-defaults.ts:128) |
| D | **gjc-protocol / `gjc://`** | `src/internal-urls/gjc-protocol.ts:20` `scheme = "gjc"` | 7 + 2 | **모델 노출** — read 툴이 `gjc://` URL 해석·생성 |
| E | **gjc-command / `--gjc-command`** | `src/task/gjc-command.ts`, `src/commands/setup.ts:39` | 4 + 0 | **사용자 노출** — CLI 플래그 |
| F | **심볼 패밀리** | `CANONICAL_GJC_WORKFLOW_SKILLS`·`GJC_SKILL_KEYWORD_DEFINITIONS`·`GjcTeam*`·`GjcPlugin*`·`GjcProtocolHandler` 등 | 18/11/3 파일 | 없음 (컴파일 타임) |
| H | **ENGINE_NAME** | `packages/utils/src/dirs.ts:20` `ENGINE_NAME = "gjc"` — XDG 경로(`dirs.ts:146`)·브랜드 게이트 비교·릴리스 에셋명에 사용 | 광범위 | XDG 사용자 경로에 노출 |
| G | **owner 문자열** | `"gjc-runtime"`·`"gjc-state-cli"`·`"gjc-hook"` (state-schema.ts Zod enum) | 다수 + 6 테스트 | **퍼시스트** — `.gjc/state/*.json` receipt.owner |

## 2. 가치/비용 판정

| # | rename 가치 | rename 비용 | 결론 |
|---|-------------|------------|------|
| A | 낮음 (비가시) | **최고** — 디렉터리 rename은 업스트림 체리픽 path mismatch로 파괴 + 71 import | **보존 경계 명문화** |
| B | 낮음 | 높음 | **보존 경계 명문화** |
| C | 중간 (`embedded:gjc/` 노출) | 중간 — 경로 문자열만이면 낮음 | 문자열만 **지연 rename** (디렉터리 보존) |
| D | **높음** (모델 노출) | 중간 — scheme 1줄 + legacy alias | **rename 추천 (즉시 가능)** |
| E | **높음** (CLI 플래그) | 낮음 — 4파일, 테스트 0 | **rename 추천 (즉시 가능)** |
| F | 낮음 | 높음 (import churn + 체리픽 심볼 충돌) | **보존 경계 명문화** |
| H | 낮음 (엔진 계보 식별자 — isJawBrand 게이트 구조 유지 확정 #4와 정합) | 높음 — XDG 데이터 마이그레이션(061.1 Q2 연동)·게이트 전반 | **보존** (XDG 경로 마이그레이션 여부만 061.1 Q2로 위임) |
| G | 낮음 | **매우 높음** — Zod enum + 퍼시스트 데이터 호환 + 마이그레이션 | **절대 보존** |

## 3. [확정 제안] 패밀리별 처분

### 보존 경계 명문화 — A·B·F·G (rename 금지)

structure/fork-delta.md(067.1) 또는 AGENTS.md에 명문화:
```
[BOUNDARY: gjc-internal-identifiers]
- src/gjc-runtime/, src/extensibility/gjc-plugins/ → 경로 rename 금지 (업스트림 체리픽 경계)
- CANONICAL_GJC_WORKFLOW_SKILLS, GjcTeam* 등 심볼 → 변경 금지
- receipt.owner "gjc-runtime"/"gjc-state-cli"/"gjc-hook" → 절대 변경 금지 (퍼시스트 계약)
```

### 지연 rename (업스트림 분기 확정 시) — C

- `src/defaults/gjc/` 디렉터리: 동결 (업스트림 skill 콘텐츠 변경과 충돌 위험)
- `embedded:gjc/` 문자열(gjc-defaults.ts:128): 분기 확정 시 `embedded:jwc/`로 (파일 경로 아닌 문자열)
- `gjc-defaults.ts` 파일명: 낮은 우선순위

### 즉시 rename — D: `gjc://` → `jwc://`

1. `src/internal-urls/gjc-protocol.ts:20` `scheme = "gjc"` → `"jwc"`
2. `src/internal-urls/router.ts` — legacy `"gjc"` scheme을 redirect alias로 등록 (이행 기간)
3. `test/read-tool-group.test.ts:100` `gjc://docs/...` → `jwc://` + legacy 경로 테스트 추가
4. `scripts/rebrand-inventory.ts` allowlist 정리

### 즉시 rename — E: `--gjc-command` → `--jwc-command`

1. `src/commands/setup.ts:39` `"gjc-command"` → `"jwc-command"` + deprecated alias `gjc-command` 유지
2. `src/cli/setup-cli.ts:130` 분기 갱신
3. `src/task/gjc-command.ts` `DEFAULT_CMD = "gjc"` → `"jwc"` (gjc bin 제거 확정과 정합; `PI_SUBPROCESS_CMD` fallback 유지). 파일 rename은 선택 (`git mv` 시 독립 커밋).

## 4. git mv 전략 (D·E 파일 rename 시)

- `git mv` 필수 (`git log --follow` history 보존), rename 커밋은 기능 변경과 분리 (체리픽 시 skip 가능)
- A·B 디렉터리는 `git mv` **절대 금지**

## 5. 검증 가드

```bash
bun test packages/coding-agent/test/read-tool-group.test.ts   # D
bun test packages/coding-agent/test/cli-command-surface.test.ts  # E
bun scripts/rebrand-inventory.ts --strict
```

## 6. [열린 질문]

1. `gjc://` legacy alias 유지 기간 — 3개월 vs major 릴리스까지
2. `embedded:gjc/` 경로를 에이전트가 실제 읽어 처리하는 케이스 존재 여부 (현재 filePath 필드만 확인)
3. "업스트림 분기 확정 시점" 기준 정의 (C 처분 트리거)
4. `GJC_NATIVE_VARIANT`·`GJC_COMPILED` (packages/natives 빌드 env) — 범위 외, 062.1과 후속 검토
5. `python/gjc-rpc/`·`python/robogjc/` — TS 외 범위, 별도 플랜 필요 여부
