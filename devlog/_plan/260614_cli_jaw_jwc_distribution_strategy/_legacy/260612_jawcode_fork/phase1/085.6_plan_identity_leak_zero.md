# 085.6 — 아이덴티티 누수 제로: jwc 세션 "나는 GJC" 응답 차단

> 상위: [99.04.00_moc_workflow_hud.md](./99.04.00_moc_workflow_hud.md). 본체: [085.5](./085.5_plan_prompt_rebrand.md) **하드 수정 개정판** (M2가 본 플랜의 시스템 프롬프트 수정을 흡수). identity 표면: [021](./021_plan_identity_prompting.md)(구현 완료). 스킬 주입 연계: [057](./057_plan_skill_compat_patch.md) §6 P10.
> 트리거: 260612 01:16PM 실측 — jwc TUI 세션 "너는 누구야" → **"나는 GJC, Gajae Code 코딩 에이전트야"** + thinking이 "gajae-code-system-prompt" 자기참조 + 응답 헤더 라벨 "gajae".
> ⚠️ **원칙 개정 (인터뷰 260612 02:04)**: placeholder/런타임 레이어 → **소스 하드 수정**. 정체성 문장·래퍼 태그를 소스에 직접 기입. gjc 산문 보존 종료 (085.5 §2 개정 참조).

## 0. 누수 경로 전수 (실측 — 260612 01:2x 코드 확인)

| # | 누수 | 근원 (file:line) | 처리 (개정 후) |
|---|------|------------------|----------------|
| N1 | 정체성 문장 "You are GJC, the Gajae Code coding agent" | `packages/coding-agent/src/prompts/system/system-prompt.md:3` | **085.5 M2** — 하드 수정: `You are Jaw, the coding agent running on the jwc runtime (Jawcode).` |
| N2 | 래퍼 태그 `<gajae-code-system-prompt>` — thinking 자기참조 근원 | `system-prompt.md:1,275` | **085.5 M2** — `<jawcode-system-prompt>` 하드 |
| N3 | identity 블록(021)이 L3 "You are GJC"와 공존 — 모델이 첫 문장 채택 | `system-prompt.ts:283→:588` | 소스 기본 문장이 Jaw로 바뀌어 기본 충돌 소멸. `identity.name` 설정 시 이름 우선 명시 1줄을 `renderIdentityBlock()`에 추가 (본 플랜 M1) |
| N4 | TUI 응답 헤더 "gajae" 하드코드 | `modes/components/assistant-message.ts:19` | 본 플랜 **M3** |
| N5 | 상태라인 segment "gajae" — id는 [기능], 렌더 텍스트만 대상 | `status-line/segments.ts:74`, `presets.ts:36,60`, `settings-schema.ts:72` | 본 플랜 **M3** |
| N6 | 기타 산문 GJC (L21·42·225, tools/bash.md:20, agents md, 번들 스킬) | 085.5 인벤토리 | 085.5 M2–M4 소관 |

## 1. 설계 — 정체성 단일 정본 (하드 수정판)

- **소스 기본 정체성**: 하드 문장 `You are Jaw, the coding agent running on the jwc runtime (Jawcode).` — 런타임 분기·placeholder 없음.
- **사용자 재정의**: `identity.name`(021) 설정 시 `renderIdentityBlock()`이 "This Identity section overrides the default agent name above." 1줄을 포함 — 기본 Jaw를 덮는다.
- **표시 정본 헬퍼** `gjc-runtime/agent-identity.ts` (신규, ≤30줄): `resolveAgentDisplayName(): string` = `identity.name`(settings, throwing-Proxy try/catch 가드 — 021 §감사3 패턴) → `"Jaw"` 하드 폴백. TUI 라벨·057 P10 주입 텍스트가 공용 — 표시 계층의 단일 정본. (구설계의 브랜드 분기·resolveProductDisplayName은 불요)

## 2. 구현 모듈

| M | 내용 | 파일 | 의존 |
|---|------|------|------|
| M1 | `renderIdentityBlock()` 이름-우선 1줄 + `agent-identity.ts` 헬퍼 | `system-prompt.ts:283`, 신규 `gjc-runtime/agent-identity.ts` | 없음 |
| ~~M2~~ | ~~시스템 프롬프트 placeholder~~ → **085.5 M2에 흡수** (하드 수정) | — | — |
| M3 | TUI 표시 전환 — 헤더 `"gajae"` → `resolveAgentDisplayName()` 소문자 표기, 상태라인 segment **렌더 텍스트**만 (id·스키마 키 `"gajae"` 무변경 — preset 호환) | `modes/components/assistant-message.ts:19`, `status-line/segments.ts:74` | M1 |
| M4 | 테스트 | 신규 `test/agent-identity-leak.test.ts` | M1·M3 + 085.5 M2 |

### M4 테스트 표

| 케이스 | 기대 |
|--------|------|
| 기본(identity 미설정) 시스템 프롬프트 | `You are Jaw, the coding agent running on the jwc runtime (Jawcode).` 포함, `GJC`·`Gajae` 산문 비노출 (보존 경계 식별자 제외) |
| `identity.name="별이"` | identity 블록에 이름-우선 1줄 + 모델 정체성 질의 시 별이 우선 (스냅샷) |
| settings 미초기화(SDK/임베딩 경로) | throw 없이 `"Jaw"` 폴백 |
| TUI 헤더/세그먼트 | 라벨 "jaw"(또는 identity 이름), segment id·스키마 키 `"gajae"` 유지 (preset 무회귀) |
| e2e 수동 | jwc TUI "너는 누구야" → Jaw + jwc 런타임(Jawcode) 인지 답변, GJC 비언급 |

게이트: `bun run check:ts` + 085.5 M5 개정 가드 green.

## 3. [확정] (인터뷰 260612 01:36·02:04)

1. **표시명 [확정 — 사용자]**: 에이전트 정체성 **`Jaw`**, 자기 인지 **"jwc 런타임(Jawcode) 위에서 돈다"**. 명령 구문 APP_NAME = `jwc`.
2. **구현 방식 [확정 — 사용자 02:04]**: 소스 하드 수정 — placeholder·브랜드 분기·gjc byte-동일 보장 폐기.
3. role-agent 스폰 프롬프트 정체성 1줄 주입: 057 §6 P10(M5-2)과 동일 `resolveAgentDisplayName()` 경유.

## 4. 구현 순서·연계

**085.5 M2+M5(하드 수정+가드 반전)가 선행 본체** → 본 플랜 M1 → M3 → M4.
057 P10 주입 블록 텍스트는 jwc 네이티브 어휘로 직접 생성 (하드 수정 후 누수원 없음) — 이름 표기만 M1 헬퍼 공용.
