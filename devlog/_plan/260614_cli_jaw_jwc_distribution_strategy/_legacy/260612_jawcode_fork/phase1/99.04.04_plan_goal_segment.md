# 99.04.04 — goal 상태 세그먼트 (mode 세그먼트 확장) (구 085.3)

> 이관: 085.3_plan_goal_segment.md → 99.04.04 (260612, 99 밴드 통합 — 상위: [99.00.00_moc_stabilization.md](./99.00.00_moc_stabilization.md))
> ⚠️ 경로 표기 정정 (이관 시): 구 문서의 `.gjc/` 상태 경로는 실코드 `.jwc/`로 일괄 정정함 (260612 실측).

> 상위: [99.04.00_moc_workflow_hud.md](./99.04.00_moc_workflow_hud.md). 의존: 060은 **불요** — 현 엔진(goal mode + ultragoal) 기준으로 선행 가능.
> 060 어댑터가 들어와도 표시 데이터 소스(session goal state + ultragoal HUD)는 동일.

## 1. 현황 (이미 있는 것)

- `status-line/segments.ts:125-154` `renderGoalMode()` — mode 세그먼트가 goal 상태·완료 수·pause 색을 **이미 표시** (`ctx.session.getGoalModeState()?.goal`)
- `workflow-hud.ts:128-152` `buildUltragoalHudSummary()` — goal n/m, 현재 goal, blocker를 스킬 HUD 바에 **이미 표시** (ultragoal 활성 시)

## 2. 보강 사양 (갭만)

| 갭 | 현재 | 보강 |
|----|------|------|
| 체크포인트 진행 | goal 제목/상태만 | `⛳ <obj-축약> ✓3` — 마지막 checkpoint 누계 (ultragoal ledger `goal_checkpointed` 카운트) |
| evidence 없는 진행 경고 | 없음 | 직전 checkpoint evidence 공백이면 `!ev` chip(warning) — 060의 evidence 의무 규칙 가시화 (061 §3.1 연계) |
| pause 사유 | pause 색만 | agent-pause 감사 대기 상태(061 §3.2 2-pass 게이트 도입 후) `⏸audit` 표기 — **060 구현 후 활성화** |

## 3. 구현 diff

### M1. `renderGoalMode()` 확장 (`segments.ts:125-154`)

```typescript
// 기존 goal 표기 뒤에:
const ledgerStats = ctx.ultragoal; // 99.04.01 §2에서 SegmentContext에 추가 (checkpointCount, lastEvidenceBlank)
if (ledgerStats?.checkpointCount) parts.push(theme.fg("success", `✓${ledgerStats.checkpointCount}`));
if (ledgerStats?.lastEvidenceBlank) parts.push(theme.fg("warning", "!ev"));
```

### M2. ledger 요약 읽기 — 99.04.01 인프라에 `readUltragoalLedgerStats(cwd)` 추가

- `.jwc/ultragoal/ledger.jsonl`을 tail-scan(마지막 200행 한도)해 `goal_checkpointed` 카운트 + 마지막 evidence 공백 여부만 추출
- 폴링 레일: 스킬 HUD와 동일 1초 TTL 캐시에 합류 (`status-line.ts:438-458` 패턴) — 파일 크기 미변경 시 재파싱 생략(mtime+size 캐시)

### M3. 060 연계 지점 (도입 시 1줄)

- 061 §3.2의 `pauseAudit` 세션 상태가 생기면 `renderGoalMode()`에 `⏸audit` 분기 추가 — 본 문서 범위 밖, 포인터만

## 4. 테스트

- `test/modes/status-line-goal-segment.test.ts`(신규 또는 기존 mode 세그먼트 테스트에 케이스 추가):
  checkpoint 3건 → `✓3` / 마지막 evidence 공백 → `!ev` warning / ultragoal 디렉터리 없음 → 기존 표시 그대로(회귀 0)
- ledger 200행 초과 파일 tail-scan 정확성

## 5. 비고

- mode 세그먼트 ID 재사용 — `StatusLineSegmentId` union 변경 없음, verify-gjc-ui-redesign 무접촉
- `!ev` 경고는 표시일 뿐 차단 아님 — 차단은 060 어댑터(061 §3.1) 소관
