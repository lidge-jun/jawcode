# 99.11.02 — plan: 업스트림 수용 — harness RPC submit readiness gate (선별 병합)

> 상태: ✅ **구현 완료 (260613)** — sm/types/owner hunk 적용 + `commands/harness.ts` `#submit` 선별 포팅 (startup-blocker 기능 의존부 제외). `bun test test/harness-control-plane/` **175 pass 0 fail** · `check:types` 신규 에러 0. owner.ts "diverged"는 jwc 패치가 아니라 미수용 이전 upstream 기능으로 판명 — hunk 무충돌. chase 카드 `_legacy/10/` 이동 완료.
> 소속: 99.11 업스트림 동기화 패키지 ([99.00.00 MOC](./99.00.00_moc_stabilization.md) §작업 패키지).
> 입력: 사용자 260613 — 99.11.01과 동회차 지시.
> 출처(chase 정본): [struct_har/chase/10.010](../../../../struct_har/chase/_legacy/10/10.010_gjc_chase_harness_submit_readiness.md) — **구현가치 65 (MLB) · 즉시~선별**.
> upstream: GJC `f814413` (`fix(coding-agent): gate harness RPC submit readiness (#549)`, Fixes upstream #544).
> 관련: [10.008 RPC lifecycle](../../../../struct_har/chase/_fin/10/10.008_gjc_chase_rpc_lifecycle.md) — 같은 headless 신뢰성 레인, 묶음 검증 권장.

harness control plane의 `submit`이 lifecycle/owner만 보고 허용되어, **owner는 live지만 RPC가
아직 prompt를 받을 수 없는 상태**에서 submit이 통과해 유실/오류가 났다(upstream #544).
jwc의 orchestrate/workflow-gate는 harness control plane 위에 얹혀 있으므로 headless 자율
구동(PABCD) 신뢰성에 직결 — 99.03 디스커버리 이후 자율 사용량이 늘면 노출 확률도 커진다.

## 1. 적용 근거 (260613 실측)

| 사실 | 증거 | jwc 충돌 |
|---|---|---|
| `SUBMIT_READY_LIFECYCLES = {"started","observing"}` + `submitUnavailableReason(lifecycle, ownerLive, gateReason)` 사유 체인 (terminal/blocked/not-idle/owner-not-live/gate) | `devlog/_upstream_gjc/packages/coding-agent/src/harness-control-plane/state-machine.ts:11,46-57` | **없음** — jwc `state-machine.ts` pre-fix와 identical (diff 0 실측) |
| `Observation.readyForSubmit?` + `submitUnavailableReason?` 노출 | `harness-control-plane/types.ts:213-216` | **없음** — identical |
| owner가 RPC readiness 관찰 → 게이트 사유 공급 (+90줄) | `owner.ts` (`f814413` diff) | **있음** — jwc `owner.ts` diverged → hunk 기계 적용 금지, 선별 병합 |
| `harness` 명령 표면이 readiness 노출 | `commands/harness.ts` (`f814413` diff) | 소 — jwc 명령 표면 확인 필요 |
| upstream 회귀 테스트 4파일 | cli-owner-routing · owner-frames · owner · state-machine `.test.ts` | 동등물 번안 |

## 2. 구현 절차

1. **무충돌 선반영**: `state-machine.ts` + `types.ts` hunk 그대로 적용.
2. **owner.ts 선별 병합**: jwc diverged 부분(포크 패치)을 보존하면서 RPC readiness 관찰
   로직만 이식 — 적용 전 `diff -u devlog/_upstream_gjc/.../owner.ts packages/.../owner.ts`로
   jwc 패치 경계 확정.
3. **jwc own-gate 합성**: jwc workflow-gate가 submit 경로에 자체 게이트를 두는지 확인 후,
   있다면 upstream `gateReason` 체인에 jwc 사유를 합류 (사유 문자열은 jwc 어휘 유지).
4. upstream 테스트 4파일 동등물 적용/번안 → green.
5. (선택, 권장) [10.008](../../../../struct_har/chase/_fin/10/10.008_gjc_chase_rpc_lifecycle.md) rpc-mode
   수명주기 패치와 같은 회차로 검증 — headless 신뢰성 레인 1회 검증으로 묶음.

## 3. 비범위

- `get_state` payload include-gate · malformed JSONL 복구 — [10.008](../../../../struct_har/chase/_fin/10/10.008_gjc_chase_rpc_lifecycle.md) 소관 (본 plan은 submit gate 한정).
- harness 명령 표면의 jwc re-facing(문구/어휘) — 99.03 M4 소관, 여기선 기능만.

## 4. 완료 기준

- [x] `state-machine.ts`·`types.ts` 반영 + jwc own-gate 사유 합성 방식 결정 기록.
- [x] `owner.ts` 선별 병합 (jwc diverged 패치 보존 diff 첨부).
- [x] harness-control-plane 테스트 동등물 4종 green + `check:types` 0 에러.
- [x] chase 카드 [10.010](../../../../struct_har/chase/_legacy/10/10.010_gjc_chase_harness_submit_readiness.md) 완료 체크 + `_legacy/10/` 이동, [02_gap_inventory](../../../../struct_har/chase/02_gap_inventory.md) 행 갱신.
