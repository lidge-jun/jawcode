# 082.2 — issue: 첫 글자 입력 시 캐럿이 우측으로 튀었다 복귀 (IME preedit)

> 상태: ✅ **수정 완료 (B안, 260612 10시)**. 조사: Opus 서브에이전트.
> 수정: `editor.ts` placeholder 블록(`784-789`)을 `showPlaceholder && !this.#useTerminalCursor`로 가드 —
> 하드웨어 커서 모드에선 placeholder가 마커를 끄지 않고, 기존 ghost-text 경로(`835-843`)가 marker+placeholder를
> 렌더해 커서가 입력 시작 컬럼에 동기화됨. 회귀 테스트 2건 추가(`editor.test.ts`), tui 896 pass.
> ⚠️ 실행 중 jwc는 재시작해야 반영(소스 symlink 직결, 재빌드 불필요).
> 소속: 082 밴드 (TUI 입력/IME, [082_moc_tui_input.md](./082_moc_tui_input.md)). **업스트림 gjc 코드 (0 diff)**.
> 입력: 사용자 "첫글자 입력이 오른쪽으로 처음에 가버린다 — 타이핑하면 거의 바로 돌아오지만 UX 문제". (IME 관련 추정 — 확인됨)

빈 입력창에 **첫 글자**를 치면 캐럿이 잠깐 오른쪽(잘못된 컬럼)으로 튀었다가, 이어 타이핑하면 거의 바로
제자리로 복귀한다. 한 프레임짜리 전이 글리치. **컬럼 산수 버그가 아니라 커서 표시 타이밍 + IME preedit 문제.**

## 원인 메커니즘 (신뢰도 75%)

**플레이스홀더 프레임이 하드웨어 커서를 "숨김+위치 미동기화"로 두고, 첫 글자 프레임에서 커서를 재표시하는
순간 IME 조합(preedit) 글리프가 직전 잔류 위치에서 정착 위치로 스냅**되는 것:

1. **빈 입력 = 플레이스홀더 프레임**: 에디터가 비면 `showPlaceholder = true` (`packages/tui/src/components/editor.ts:767`).
   이 분기가 "Type your message..."를 dim으로 그리며 **`hasCursor = false`로 강제 → `CURSOR_MARKER` 미방출**
   (`editor.ts:784-789`).
2. **마커 없음 → 커서 숨김만, 컬럼 미이동**: `#cursorControlSequence`가 `\x1b[?25l`(숨김)만 내보내고 물리
   컬럼은 직전 위치에 잔류 (`packages/tui/src/tui.ts:1525` `if (!cursorPos…) return {seq:"\x1b[?25l"…}`,
   `#hideCursor` `tui.ts:446-448`).
3. **첫 글자 프레임**: `showPlaceholder = false` → 마커 재방출 → `\x1b[?25h`로 커서 재표시. 최종 컬럼은
   절대 위치(`\x1b[${col+1}G`, `tui.ts:1539-1540`)라 **목표 컬럼은 정확**. 그러나 IME 활성 한글 입력은
   조합 중 음절을 **커서 재표시 시점 위치**에 그리는데, 커서가 직전 잔류 위치에서 정착 위치로 한 프레임 스냅 →
   "우측 점프 후 복귀"로 보임.

핵심: **플레이스홀더 분기가 의도적으로 마커를 꺼(`editor.ts:788`) 하드웨어 커서를 미동기화 상태로 둔다.**
컬럼 계산(`visibleWidth` 기반, width-2 정확 처리)에는 버그 없음.

## IME 연관성 (사용자 추정 확인)

**IME 특이적** (ASCII에선 거의 안 보임):
- `showHardwareCursor` 기본값 true, 존재 이유가 "IME support" (`packages/coding-agent/src/config/settings-schema.ts:689-693`).
  앱 코드에 preedit/compose 처리 0건 → 조합 글리프는 **터미널/OS IME**가 그리고, 위치는 하드웨어 커서에 종속.
- ASCII는 조합 단계 없이 즉시 width-1 확정(`editor.ts:1543-1552`) → preedit 잔상 없음.
- 한글은 첫 자모가 preedit로 잠깐 뜨고, 그 프레임이 "플레이스홀더→첫글자(커서 숨김→재표시)" 전이와 겹침.
  한글 음절 width-2라 어긋남이 1칸 더 도드라져 점프가 또렷.

## 수정 방향

| 안 | 내용 | 위치 | 평가 |
|----|------|------|------|
| **A (권장·근본)** | 플레이스홀더 프레임에서도 하드웨어 커서 모드일 때 마커를 입력 시작 컬럼(프리픽스 `> ` 뒤)에 유지 → 첫 글자 전이에 커서가 이미 올바른 컬럼에 있어 preedit 점프 없음 | `editor.ts:784-789` | 중 난이도/낮은 위험 (HW 커서 모드만) |
| **B (권장·작은 변경)** | 빈 입력 placeholder를 별도 `showPlaceholder` 대신 기존 **ghost-hint 경로**(`editor.ts:835-848`, 이미 마커를 ghost 앞에 둠)로 통합 → 빈 상태에서도 마커 항상 방출, 커서 동기화 끊김 없음 | `editor.ts:767, 839` | 중/낮음 (기존 경로 재사용) |
| C (비권장) | 전이 프레임에서 커서를 2프레임 강제 숨김 후 재표시 — 증상 은폐, 깜빡임 부작용 | tui.ts | 임시방편 |

권장: **A 또는 B (근본 해결)**. 둘 다 마커를 빈 상태에서도 유지해 IME preedit가 정착 위치에 바로 뜨게 함.

## upstream vs fork

**업스트림 gjc 코드 그대로** — `editor.ts`·`tui.ts` 모두 `_upstream_gjc`와 **0 diff**. fork 회귀 위험 낮음.
082.1과 함께 업스트림 보고 가치 있음.

## 확정 실험 (TUI 미실행으로 75%)

`PI_TUI_DEBUG=1` 실행(로그 `/tmp/tui/`, `tui.ts:1466-1494`) 후 **빈 상태→첫 한글 자모** 연속 2프레임에서:
1. 플레이스홀더 프레임 `cursorPos === null` (마커 미방출 확인)
2. 첫 글자 프레임 `cursorPos.col` 값 + 직전 물리 컬럼
ASCII 첫 글자로 반복해 점프 미재현이면 IME 특이성 확정. `editor.ts:782` 근처 `showPlaceholder/hasCursor/marker`
방출 여부를 첫 2프레임 로깅하면 마커 on/off 전이가 점프와 동일 프레임인지 직접 확인.
