# 99.00.04 — plan: 99 밴드 잔여 일괄 마감 배치 (전부 권장안)

> 상태: ✅ **구현 완료** (260613 새벽 — S1~S10 커밋 10건 랜딩 + S11 본 스윕. 99.00.00·99.20.00 MOC 표 갱신은 병렬 세션 dirty로 이월) · 클래스: C3 · goal: b3676953-b21
> 입력: 사용자 260613 02:52 — "전부, 권장안대로 가자" (인터뷰 1.5라운드 — 멀티초이스 전체 선택 + 권장안 일괄 채택)
> 조사: 소넷 서브에이전트 3기 병렬 (99.20.02/03 ∥ 99.20.07 ∥ 99.30.01·99.07·이월·99.06), 전 앵커 현행 실측

## 0. 확정 결정 (인터뷰)

| # | 결정 | 값 |
|---|---|---|
| D1 | 배치 범위 | **전부**: 99.20 수리(02 P1+P2 · 07 P1~P4 · 03 표면4) + 99.30.01 M2 + 99.07 별칭 + 이월(P1-5/P1-6) + 99.06 스윕 |
| D2 | 99.20.02 ② | **래핑 다수파 통일** (hook-selector·session-selector 2곳만 클램프 → 래핑; pageUp/Down 클램프는 의도적 — 유지) |
| D3 | 99.20.02 ⑤ | **hook 계열 규격**: `"  소문자동사 명사"` 공백 2개 구분 (5곳 통일) |
| D4 | 99.20.07 P5 | 제외 (감사 자체가 일괄 전환 비권장) |
| D5 | P1-6 영수증 | **worklog 동형** — 세션 스코프 `.jwc/state/sessions/<id>/worklog.md`에 `## Final Summary` |
| D6 | 99.30.01 M1 선택항 | 보류 (99.03 표면과 충돌 소지 — 문서 자체 권고) |
| D7 | 직원·서브에이전트 | 소넷 고정 |

## 1. ⚠ 병렬 세션 격리 전략 (오늘 99.07.01 휩쓸림 사고 재발 방지 — 필수 준수)

병렬 세션이 현재 미커밋 작업 중: composer-footer(99.20.06)·help 셀렉터(99.20.08)·quota 도킹(99.20.07 §6) —
`selector-controller.ts`·`interactive-mode.ts`·`modes/types.ts`·`command-controller.ts`·`builtin-registry.ts` dirty + untracked 4파일(quota-panel.ts 등).

1. **전 구현은 HEAD 기반 사이드 워크트리에서** 수행·커밋 (슬라이스별 원자 커밋) → 메인 브랜치는 `git reset --mixed <최종>`로 전진. 메인 워크트리 파일 무접촉 — 병렬 WIP는 dirty로 보존.
2. **P2는 미커밋 quota-panel.ts에 의존 금지** — 신규 `components/scrollable-panel.ts`를 독립 작성(quota-panel과 같은 3상태·스크롤 API). quota-panel→ScrollablePanel 통합은 병렬 랜딩 정리(후속 99.02류)로 이월 명기.
3. 커밋 전 `git show <커밋> --stat`으로 외부 hunk 휩쓸림 0 확인, 최종 사이드 워크트리 단독 tsc 0 실증.

## 2. 슬라이스 (커밋 단위 — 의존성 순)

| S | 패키지 | 내용 | 핵심 앵커 (조사 실측) |
|---|---|---|---|
| S1 | 99.20.02 ②③⑤ | 래핑 통일 2곳(hook-selector:750/763, session-selector:196/201) · 커서 글리프 2곳(tree-selector:472, user-message-selector:55 → `${theme.nav.cursor} `) · 힌트 규격 5곳(settings-list:155, tree-selector:825/879, session-selector:179, login-dialog:124) | ④는 4b/4c 의도적(문서화로 수용), 4a(session-selector esc/ctrl+c 비대칭)만 실측 후 최소 정리 |
| S2 | 99.20.03 표면4 | `input-controller` setupKeyHandlers에 `editor.onAutocompleteCancel = () => { if (!isStreaming) ui.compactViewportFill() }` — editor.ts:397 콜백 기존재·미배선 | 기존 4트리거 패턴: input-controller:328/1082/1134 |
| S3 | 99.30.01 M2 | `allTasksTerminal()` 헬퍼 + `#renderTodoList`(interactive-mode:980) 터미널 단락 — `▸ Todos (n/n) · complete` 1행 + setTodos/reloadTodos에서 터미널 시 `todoExpanded=false` 강제. auto_clear 불변 | 수동 토글 재펼침 가능 유지 |
| S4 | 99.07 별칭 | `/gd [note]` → `runNativeGoalCommand(["done","--force",…])` · `/goalplan [hint]` → `(["plan",hint])` · `/interview <request>` → `runNativeJawInterviewCommand` · `/orchestrate` subcommands에 `reset` 추가(builtin-registry:325) · `/goal` subcommands에 plan/done/refine/status 노출 · **hint invariant 테스트**(brief가 `GOAL_PLAN_PENDING_BRIEF`로 시작, hint가 objective 아님) | `/plan`·`/team`은 jwc 런타임 표면 부재 — **제외·이월 명기** (99.07.00 §5 잔여 목록에도 없음) |
| S5 | 이월 P1-5 | Path A: `/orchestrate` 핸들러(builtin-registry:328~)에서 전이 성공 시 `session.sendPabcdStageContext()`(agent-session:4564, 정의만 있고 0 호출) + `statusLine` 즉시 갱신(`#pabcdLastFetch=0` 노출 or invalidate). status/verdict/reset 서브커맨드는 제외 | TTL 폴링(status-line:450, 1000ms)은 폴백으로 유지 |
| S6 | 이월 P1-6 | orchestrate-runtime `complete` 분기(:397)에서 세션 스코프 `worklog.md`에 `## Final Summary` 결정적 영수증(전이 이력·타임스탬프·goal id·체크포인트 수) append + `--note "<text>"` 옵션으로 모델 요약 동승 + stdout에 영수증 경로 출력. **[A-F1] 슬래시 핸들러가 `--session-id runtime.session.sessionId`를 argv에 동승** — TUI 메인 프로세스엔 JWC_SESSION_ID env가 없어 세션 스코프 경로가 안 잡힘 (state-writer `appendText(:440)` 재사용) | jwc에 worklog 메커니즘 부재 확인 — 신규 소형 writer (state-writer 패턴 재사용) |
| S7 | 99.20.07 P3 | showStatus 교체: command-controller 917(new session)·1026(move)·1267(handoff)·1299(contrib) + extension-ui-controller 187/426 + selector-controller 1281(logout) + debug/index 180/241(+210/280/424 동형) | **제외 2건**: fork 안내 블록(:958~978 — 오늘 의도적 영구 표면) · OAuth login 성공(P1에 흡수) |
| S8 | 99.20.07 P4 | MCP 연결 spinner: runtime-mcp-command-controller:759~776 수동 setInterval → `statusContainer`+`Loader` 기성 패턴(command-controller:1179 선례) | Loader가 애니메이션 자체 관리 — setInterval/clearInterval 제거 |
| S9 | 99.20.07 P1 | OAuth 로그인 도킹: selector-controller `#handleOAuthLogin`(:1207~1275, 감사 앵커 :1146는 stale)·`#handleOAuthLogout` + runtime-mcp-command-controller :544~612 → `LoginDialogComponent`(login-dialog.ts, **구현 완료·인스턴스화 0건**)를 `showSelector` 프레임에 배선. 성공/실패 최종만 showStatus/showError | onComplete는 생성자 파라미터 — resolve/reject 클로저 공급 |
| S10 | 99.20.07 P2 | 신규 `components/scrollable-panel.ts`(3상태 loading/ready/message·↑↓/PgUp/PgDn/g/G·esc/q) + `showReadOncePanel(title, load)` 배선 → read-once 7종 도킹 전환: /changelog(cc:556)·/hotkeys·/tools(cc:50~58 showMarkdownPanel 경유)·/context(cc:582)·/session(cc:453)·/jobs(cc:472/494)·/memory view(cc:603) | debug 계열 2종은 P2 범위 밖(§5) — 등재만 |
| S11 | 99.06 스윕 | stale 헤더 갱신: 99.08.00(방향확정→✅ 99.04.05 C5+C6 랜딩)·99.04.00 MOC(구현⬜→✅)·99.01.03(ledger 실측 후 판정)·99.20.02/03/07(본 배치 반영)·99.30.01·99.07.00·99.00.00 MOC 패키지 표·99.00.03 이월 표 | 구현 전부 랜딩 후 **마지막** 수행 |

## 3. 검증 게이트 (C3)

- 슬라이스별: 영향 스위트 + 신규 테스트(S3 todo collapse 단위·S4 hint invariant·S6 worklog writer) — 사이드 워크트리는 natives 미빌드라 **tsc·대상 스위트는 메인 워크트리에서 reset 후 재실행**으로 보강
- 전체: 사이드 워크트리 단독 `tsc --noEmit` 0 + 커밋별 `--stat` 외부 hunk 0 + 메인 reset 후 관련 스위트 green
- 라이브: `/gd`·`/goalplan` 디스패치, todo 전부 완료 접힘, `/changelog` 도킹 패널 — 가능 범위 스모크

## 3.5 A 감사 반영 (PASS — F1 + N1~N3, 260613 03:23)

- **F1 → S6 반영**: `/orchestrate` 슬래시 핸들러에 `--session-id` 동승 (상단 S6 갱신).
- N1: S9 앵커는 워크트리 기준 :1207 / HEAD 기준 :1132 — 사이드 워크트리는 HEAD 기반이므로 **이름으로 탐색**.
- N2: S9가 `#handleOAuthLogin`을 재작성하면 병렬 세션 dirty hunk(:1129, opts 파라미터 추가분)와 **랜딩 시 충돌 확정** — reset --mixed 자체는 무손상, 병렬 랜딩 정리 단계에서 해소 (추적 명기).
- N3: S5는 `StatusLineComponent`에 공개 `invalidatePabcd()` 추가 필요 (`#pabcdLastFetch` private, 기존 `invalidate()`는 git 캐시 전용).

## 4. 리스크·이월 명기

- ScrollablePanel ↔ quota-panel 일시 중복: 병렬 랜딩 정리 시 quota-panel을 ScrollablePanel 기반으로 통합 (후속)
- P1 OAuth 도킹은 onPrompt/수동입력 플로우 회귀 위험 — LoginDialog 미사용分 showManualInput 경로 포함 테스트
- `/plan`·`/team` 슬래시: jwc 표면 설계 부재로 이월 (99.07.00 §5에 추기)
- 99.20.02 ④ 4b/4c: 의도적 분기로 수용 문서화 (수리 아님)
- **S2 ↔ 병렬 세션 충돌 (랜딩 시 병합 필요)**: 병렬 dirty interactive-mode.ts가 `editor.onAutocompleteCancel`을 자체 배선(:409/:2029, requestRender용) — S2의 input-controller 배선(압축용)과 마지막 할당 승자 문제. 랜딩 정리 시 두 동작(compactViewportFill + requestRender)을 단일 콜백으로 병합할 것 (지연 재생된 PlanAuditor 감사 §3 발견, 260613 04:33)
