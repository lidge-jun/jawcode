# 100.06 — 텍스트 임베드와 프롬프트 번들 검증

> 상태: ✅ **완료 (260613)** — 텍스트 임베드 5종 확장자 전부 문자열 인라인 확인(번들 내
> 프롬프트 문자열 실재·`embedded:jwc/` 3키 유지·`embedded:gjc/` 회귀 0),
> default-jwc-definitions 20 tests green, check:jwc-ui green. esbuild 0.21의 attribute
> 미지원은 100.01에서 플러그인으로 선해결.
> 선행: [100.05_plan_data_core_adapters.md](./100.05_plan_data_core_adapters.md)
> 상위 문서: [100_moc_node_porting.md](./100_moc_node_porting.md)

## 목표

100.1 인벤토리 G항 `import … with { type: "text" }` 158곳이 Node 번들에서 실제 문자열로 인라인되는지 검증한다.

## 범위

### 포함

- esbuild loader `.md`/`.html` 확인
- `src/defaults/jwc-defaults.ts`의 `embedded:jwc/` 리소스 포함 확인
- system prompt / compaction prompt / async-result prompt 로드 확인
- 번들 결과에서 `undefined` 프롬프트 방지

### 제외

- 프롬프트 내용 리라이트
- workflow skill semantics 변경
- jwc/gjc rebrand 추가 작업

## 검증 포인트

| 파일 | 검증 |
|------|------|
| `packages/coding-agent/src/system-prompt.ts` | system/project prompt 문자열 로드 |
| `packages/agent/src/compaction/compaction.ts` | compaction prompt 6종 로드 |
| `packages/coding-agent/src/sdk.ts` | `async-result.md` 로드 |
| `packages/coding-agent/src/defaults/jwc-defaults.ts` | embedded jwc 리소스 키 유지 |

## 구현/검증 순서

1. `build-node.ts` loader 재확인
2. Node import smoke에서 exported API 확인
3. 가능하면 작은 script로 prompt 길이/embedded key 개수 확인
4. `check:jwc-ui`와 `rebrand-inventory --strict` green 유지

## 실패 대응

| 실패 | 대응 |
|------|------|
| esbuild가 import attribute 파싱 실패 | esbuild 버전 확인, 필요 시 plugin 처리 |
| 문자열이 object/module wrapper로 들어감 | loader 설정 또는 import transform 보정 |
| embedded key가 `gjc`로 회귀 | rebrand inventory와 defaults test로 차단 |

## 완료 기준

- Node 번들에서 주요 prompt import가 문자열로 존재
- `embedded:jwc/` prefix 유지
- `check:jwc-ui` green
- default definition 관련 테스트가 green

## 다음 단계

- serve/archive/gc 등 주변 Bun API: [100.07](./100.07_plan_peripheral_runtime_apis.md)
