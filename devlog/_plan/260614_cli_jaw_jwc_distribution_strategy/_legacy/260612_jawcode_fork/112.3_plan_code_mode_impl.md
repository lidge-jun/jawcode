# 112.3 — plan: Code 모드 구현 (상주 ACP × N세션, S1~S4)

> 상태: ⬜ 설계 확정 (260613, 소넷 병렬 실사 종합) — **S1 즉시 착수 가능** (100 밴드 비의존).
> 상위: [112_moc_gui.md](./112_moc_gui.md) (D112-1 2-트랙 · D112-3 거버넌스: **설계 정본 = 본 문서**, cli-jaw `260613_jwc_code_mode/`는 구현 기록 전용).
> 선행 문서: [112.1](./phase1/112.1_plan_code_mode_acp_prototype.md) (프로토타입 v1 + 상주 child 개정) · [112.2](./phase1/112.2_audit_acp_resident_blockers.md) (블로커 감사 🟢 구조적 블로커 없음).
> 트랙: [260613_post_m1_patch_plan §3 트랙 B](../260613_post_m1_patch_plan/00_moc_post_m1_patch_plan.md) — B1→B2→C1/C2 순서 본 문서가 흡수.
> 결정 전제: 결정 7 (Code 모드 선행 — ACP → in-process 승격), 결정 11 (Claude Desktop .mcpb 드랍), D112-4 (fork UX 와이어).

## 0. 실측 요약 (260613)

| 항목 | 실측 | 앵커 |
|---|---|---|
| ACP 진입점 | `jwc acp` (`commands/acp.ts` default export) **또는** `jwc launch --mode acp` — 등록 누락 아님 (112.2 Q1 해소) | `packages/coding-agent/src/commands/acp.ts:12-24`, `commands/launch.ts:62`, `main.ts:929` |
| 프로토콜 | `@agentclientprotocol/sdk` **0.21.0**, `PROTOCOL_VERSION: 1` | `acp-agent.ts:413` |
| N세션 격리 | `#sessions = new Map<string, ManagedSessionRecord>()` — 단일 child 내 N세션 **기지원** | `acp-agent.ts:377`, 팩토리 격리 `main.ts:271-272` (`settings.cloneForCwd(cwd)`) |
| 세션 수명주기 | `newSession:452` `loadSession:465` `resumeSession:495` `closeSession:520` `listSessions:478` `unstable_forkSession:507` | `modes/acp/acp-agent.ts` |
| 커맨드 광고 | `#scheduleBootstrapUpdates` 50ms 후 `available_commands_update`+`session_info_update` | `acp-agent.ts:1550,1587` |
| 히스토리 리플레이 | tool_call `status:"completed"` 직렬 재전송 — 컴팩트 셀 즉시 렌더 전제 충족 | `acp-agent.ts:1847-1851` |
| TTY 가정 | 없음 — `hasUI: false`, stdio NDJSON | `main.ts:282`, `acp-mode.ts:16` |

**블로커 현황** (112.2 대조 260613 재실측): B1 잔존(1줄) · B2 잔존(감사 미실행 → 110.2) · B3 `enableMCP:false` 고정(`main.ts:283`, P1 수용) · B4 in-flight 손실(P1 수용, cli-jaw 자동 loadSession 재연결로 UX 복구).

## 1. 슬라이스

### S1 — ACP child 관리자 + 단일 세션 e2e (선행: B1·C1·C2)

| 작업 | 파일 | 내용 |
|---|---|---|
| **B1** (jwc, 1줄) | `packages/coding-agent/src/slash-commands/builtin-registry.ts:456` (실제 랜딩 위치; 설계 시 :331 추정) | `runNativeOrchestrateCommand(argv, process.cwd())` → `…(argv, runtime.session.sessionManager.getCwd())` |
| **C1** (cli-jaw, 1줄) | `cli-jaw/src/core/event-bus.ts:11` | `EventTopic` 유니온에 `"jwc"` 추가 |
| **C2** (cli-jaw, 신규) | `cli-jaw/src/code-mode/acp-host.ts` | `AcpHost`: spawn 1회 · 헬스체크 · 크래시 재spawn · idle 30s SIGTERM · 동시 세션 상한 4 (`code.maxConcurrentSessions`, Q10) |
| transport | `cli-jaw/src/services/code-session-transport.ts` + `acp-stdio-transport.ts` | `CodeSessionTransport` 인터페이스 — **컴포넌트는 인터페이스만 참조** (in-process 승격 시 UI 변경 0이 D112-1 완료 기준) |
| 라우트 | `cli-jaw/src/routes/code.ts` | `GET /api/code/git-info?cwd=` (`git worktree list --porcelain`) · `POST /api/code/sessions` · `POST /api/code/sessions/:id/prompt` · `DELETE /api/code/sessions/:id` · SSE `code:*` |

**검증**: Code 탭 → 폴더 선택 → 세션 생성 → `code:agent_message_chunk` SSE 스트리밍 수신. child kill → `code:session_error {reason:"child_exit"}` 발송 + 재spawn.
**주의 (트랙 B 이식 교훈)**: 장시간 호출 블로킹 금지 — undici 5분 headersTimeout (cli-jaw 260613 dispatch always-poll 패턴), 끊긴 연결 재전달은 `[worker-replay …]` 마커.

### S2 — 도구 셀·권한 다이얼로그·작업장 피커

`ToolCell`(pending→in_progress→completed/failed, 클릭 토글) · `ThoughtCell`(thinking 누적 접기) · `PermissionDialog`(SSE `code:permission_request` → `POST …/permission`) · `WorkspacePicker`(Electron `dialog.showOpenDialog` / 웹 경로 입력 폴백 — Q4).
**검증**: completed 셀 즉시 컴팩트 + 재확장 · 권한 3선택지 → 실행 재개 · 리플레이 시 과거 도구 셀 컴팩트 렌더 (`acp-agent.ts:1851` 보장).

### S3 — 멀티 세션 + 사이드바 + fork (선행: **B2 감사 = [110.2_audit_global_cwd.md](./110.2_audit_global_cwd.md) 신설 실행**)

세션 Map 탭 관리 · 사이드바(`_gjc/chats/byCwd` 확장 → `SessionInfo[]`) · `POST /api/jwc/sessions/:id/fork` + SSE `session_created {forkedFrom}` (D112-4) · resume 히스토리 복원 · idle-reap.
**B2 스코프**: `getProjectDir()` 91곳 / `process.cwd()` 79곳 — 세션 핸들·도구 경로만 수리, 프로세스 부트스트랩은 무해 분류 (112.2).
**검증**: 탭 2개 독립 스트리밍 비간섭 · fork 시 원본 유지+새 convo 포커스.

### S4 — 모델/thinking 팝업 + 사용량 + Transport 스왑 준비

ACP `configOptions` 모델 목록 + `unstable_setSessionModel` (P1 ACP 위임) · thinking `setSessionConfigOption` · `usage_update` 배지 · 슬래시 팔레트(`available_commands_update`) · 113.1 REST 스텁(`503 engine_not_attached`).
**검증**: `grep AcpStdioTransport` 컴포넌트 파일 0히트 · 모델 변경 202 pending 배지.

## 2. in-process 승격 (P3, 100/110 게이트)

상주 ACP child는 **승격 전 임시 형태** — 100 완료(`dist-node/sdk.js`) + 110 JawRuntime 후 `InProcessTransport`로 스왑. D112-1 완료 기준 = 이 스왑에서 UI 변경 0. B3(MCP)·B4(in-flight) 재평가 시점도 여기.

## 3. 비범위

- Claude Desktop `.mcpb`/MCP Apps — 결정 11 드랍.
- `/api/jwc/*` 실기능 — [113.1](./phase1/113.1_contract_rest_sse.md) + [113.2](./113.2_contract_stream_idempotency.md) 소관 (110/111 게이트).
- jaw(Chat) 모드 UI 변경 — D112-1: 엔진 교체만.

## 4. 완료 기준

- [ ] S1: B1·C1·C2 랜딩 + 단일 세션 스트리밍 e2e + child 크래시 복구.
- [ ] S2: 도구/권한/피커 e2e.
- [ ] S3: 110.2 cwd 감사 완료 + 멀티 세션 비간섭 + fork UX.
- [ ] S4: transport 격리 grep 0 + 모델/사용량 표면.
- [ ] 112 MOC 코드모드 P1 done 반영 + cli-jaw `260613_jwc_code_mode/`에 구현 기록 (정본 본 문서 포인터).

## 5. 미해결 (착수 시 결정)

Q3 동시 권한 요청 UX (세션별 표시 권장) · Q4 웹 피커 경로 취득 · Q8 디자인 토큰 공유(CSS 변수) · Q9 크로스 모드 딥링크 (S3 이후) · Q10 세션 상한 설정 노출.
