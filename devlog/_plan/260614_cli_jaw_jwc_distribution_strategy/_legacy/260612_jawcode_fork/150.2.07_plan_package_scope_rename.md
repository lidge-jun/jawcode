# 150.2.07 — M-A: `@gajae-code/*` → `@jawcode/*` 전수 치환 (2차·최대 충돌)

> 상태: ⬜ **포크 독립 선언 후에만** — 충돌 최대. 063.1 전략-A의 실제 실행.
> 상위: [150.2 §1-A](./150.2_plan_namespace_rename.md) · 설계 비교: [phase1/063.1](./phase1/063.1_plan_package_scope_migration.md).
> 트리거: 063.1 Q3 — upstream sync 주 1회 미만 + 포크가 더는 리베이스 안 받는 시점.

## 규모

- import specifier **약 500+/120+ 파일** (coding-agent/src 압도적). tsconfig `paths` 없음 → **텍스트 전수 치환**.
- package.json `name` 10개(150.2 §1-A 표) + 루트 catalog `package.json:23-30` 8키.

## 절차 (063.1 §4 M-A 승계)

1. **선행 확정**: 스코프 목표 `@jawcode/*` vs `@bitkyc08/*` (npm org 등록 주체, 063.1 Q1) + jwc 퍼블리시 의존 분리(063.1 M1 — coding-agent 번들 vs peer).
2. 150.2.03 상수(`WORKSPACE_SCOPE`) 값 변경 → 가드 한 점.
3. 전수 치환: `@gajae-code/` → `@jawcode/` (sed, package.json name + catalog + 전 src import).
4. 벤치마크 2종(private) 동시.
5. `bun install` 락 재생성 + g002 + 전 테스트.

## 리스크

- **체리픽 3-way 충돌**: 이 전환 후 업스트림 커밋을 받으면 매 파일 충돌 → **포크 독립 전제 필수**. 그 전엔 절대 실행 금지(150.2 §2 보존 분모).
- npm org 미등록 시 퍼블리시 불가 (063.1 M1 선결).

## 완료 기준

- [ ] 스코프 목표·org·jwc 의존분리 결정(063.1 Q1/M1).
- [ ] 전수 치환 + `bun install --frozen-lockfile` exit 0.
- [ ] `verify-g002-gates.ts` PASS(기대값 갱신) + 전 테스트 + publish --dry-run.
