# 140.1 — plan: chat federation 어댑터 (jwc history → cli-jaw dashboard search)

> 상태: ⬜ 설계 확정 (260613, 소넷 병렬 실사 종합) — 착수는 **승격(150) 전**, 130 done 이후 권장.
> 상위: [140_moc_federation_adapter.md](./140_moc_federation_adapter.md) (D9 — 후순위, 150 승격 조건에 [기본값] 포함).
> 실행 계획: [phase1/098 §6](./phase1/098_plan_m2_post100_execution.md).

## 0. 실측 요약 (260613)

| 항목 | 실측 | 앵커 |
|---|---|---|
| 검색 인프라 2계층 | `history.db`(FTS5, 빠름) ↔ 세션 JSONL(전문, 인덱스 없음) | `packages/coding-agent/src/session/history-storage.ts` · `session-manager.ts:3699` (`listAll` glob) |
| history 스키마 | `history(id INTEGER PK, prompt TEXT, created_at INTEGER, cwd TEXT)` + `history_fts` FTS5 (`content='history'`) | `history-storage.ts:85-103` |
| FTS 감지 구문 | `SELECT 1 FROM sqlite_master WHERE type='table' AND name='history_fts'` | `history-storage.ts:83` |
| 검색 로직 | FTS5 prefix match 우선 → substring AND LIKE 폴백, merge dedup | `history-storage.ts:174-225` |
| DB 경로 | `~/.jwc/agent/history.db` (`getHistoryDbPath()`) | `packages/utils/src/dirs.ts:389-391` |
| cli-jaw 측 플러그 지점 | `chat-federation.ts` `probeSchema()` — 외부 chat DB 등록/검색 | `cli-jaw/src/manager/memory/chat-federation.ts` |

## 1. 필드 매핑 (HistoryEntry → ChatSearchHit)

| jwc | cli-jaw | 변환 |
|---|---|---|
| `prompt` | `content` | 직접 |
| `created_at` (unix int) | `timestamp` | `new Date(created_at * 1000)` |
| `cwd` | `projectPath?` | 직접 |
| `id` | `hitId` | `"jwc-hist-" + id` |
| — | `instanceId` | `"jwc:" + basename(agentDir)` (D9 의사 instanceId) |
| — | `source` | `"jwc-history"` 고정 (출처 표시 AC) |

- 1차 수집 범위 = 홈 레벨 단일 `~/.jwc/agent/history.db` [기본값] — 워크트리별 `.jwc/` 복수 수집은 D-140-1 결정 후.
- 세션 JSONL 전문 검색(`allMessagesText`)은 **1차 스코프 외** (FTS 없는 전수 스캔이라 p95 목표 불충족).
- `ChatSearchHit` 정확한 타입은 cli-jaw 체크아웃 기준 FA-2 착수 시 재실측 (jawcode 범위 외).

## 2. 슬라이스

### FA-1 — probeSchema 확장 + 경로 주입
`probeSchema(jwcHistoryDbPath)`가 `history`+`history_fts` 감지 시 `'jwc-history'` 타입 반환 (현재는 `schema_mismatch`). jwc 쪽 변경 0 — 스키마 계약(`history-storage.ts:85-103`)을 읽기 전용 참조.
**완료**: probeSchema 단위 테스트. **의존**: jwc npm sidecar 설치 경로 접근 (결정 12).

### FA-2 — FTS5 쿼리 + Hit 매핑
`searchChatFederated()`에 `'jwc-history'` 분기: readonly open → `history_fts MATCH ?` (`history-storage.ts:120-122` 구문 재사용) → §1 매핑.
**완료**: `cli-jaw dashboard chat search "<q>"`가 jwc 히트를 출처 포함 반환 (140 AC ①). **의존**: FA-1.

### FA-3 — 옵트인 등록 + p95 측정
dashboard instances 레지스트리에 옵트인 등록 (`jwc:<agentDir>`), `LIMIT`/`days` 필터를 FTS 쿼리에 위임. 10만+ 합성 데이터 p95 측정·기록 (140 AC ②), 미등록 무회귀 (AC ③).
**완료**: p95 수치를 140 MOC AC에 기재. **의존**: FA-2, D-140-1 결정.

## 3. 비범위

- jaw.db `messages` LIKE→FTS5 전환 — D9 jawcode 범위 밖.
- 양방향 memory federation (070 포맷) — D-140-2 후속.

## 4. 완료 기준

- [ ] FA-1~3 + 140 MOC 완료 기준 3항목 증거 기재.
- [ ] [150.1 패리티 매트릭스](./150.1_parity_gap_matrix.md)의 승격 게이트 행("140 완료")에 반영.

## 5. 미해결

D-140-1 수집 범위 (FA-3 전) · D-140-2 양방향 (비차단) · D-FA-5 `ChatSearchHit` 타입 재실측 (FA-2 전).
