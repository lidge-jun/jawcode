# 150.2.06 — M-bin: `gjc`/`gjc-stats` bin 정리 (2차)

> 상태: ⬜ 연기 — 150.1 P-3 [소] 항목과 동일/묶음. 충돌 위험: PATH(기존 설치 사용자).
> 상위: [150.2 §1-D](./150.2_plan_namespace_rename.md) · [150.1 §3 P-3](./150.1_parity_gap_matrix.md).

## 대상

| 파일:라인 | 현재 | 처리 |
|---|---|---|
| `packages/coding-agent/package.json:31` | `"gjc": "src/cli.ts"` | deprecation 래퍼(경고 후 위임) 또는 제거 |
| `packages/stats/package.json:28` | `"gjc-stats": "./src/index.ts"` | `jwc-stats` |
| `packages/ai/package.json` | `"pi-ai": "./src/cli.ts"` | 별도 결정(상류 pi 유래 — 150.2 §5 미해결) |
| `packages/jwc/package.json` | `"jwc"` | ✅ 이미 완료(정본) |

## 절차

1. `gjc` bin: 즉시 제거 시 기존 설치 사용자 PATH 깨짐 → **deprecation 래퍼**(`gjc` 실행 시 "use jwc" 경고 + `jwc`로 위임) 1릴리스 유지 후 제거 권장.
2. `gjc-stats`→`jwc-stats` + `rebrand-inventory.ts expectedCliBins` 동시 갱신(150.2.03 상수 기반).
3. CI release artifact `gjc-<os>-<arch>`→`jwc-*` (150.1 D-150-2 — 이중 artifact 선행안 권장).

## 완료 기준

- [ ] bin 정리 + `rebrand-inventory --strict` 통과.
- [ ] `gjc` deprecation 경로 동작(또는 제거 결정 기록).
- [ ] g002 `:193` bin 어설션(150.2.03 상수) 통과.
