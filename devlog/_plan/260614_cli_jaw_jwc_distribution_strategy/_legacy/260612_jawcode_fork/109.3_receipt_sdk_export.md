# 109.3 — receipt: sdk.ts PABCD state export

> 상태: ✅ 완료 (260613). 커밋 `e7dac64c`.
> 상위: [130.2_plan_injection_compose.md](./130.2_plan_injection_compose.md) Slice C 선행.
> 목적: cli-jaw가 `jwc/sdk`를 통해 PABCD 상태 파일을 쓸 수 있도록 sdk 표면 확장.

## 변경

`packages/coding-agent/src/sdk.ts`에 3개 export 추가:

```typescript
export {
	type PabcdEnvelope,
	readPabcdStateWithFallback,
	writeNativeWorkflowEnvelopeAtomic,
} from "./jwc-runtime/orchestrate-state";
```

## 함수 시그니처

### `writeNativeWorkflowEnvelopeAtomic`
- **정의**: `orchestrate-state.ts:295`
- **서명**: `(cwd, envelope: PabcdEnvelope, options: { command, sessionId?, nowIso?, fromPhase?, toPhase? }) → Promise<string>`
- **쓰기 경로**: `.jwc/state/pabcd-state.json` (unscoped) 또는 `.jwc/state/sessions/{encoded}/pabcd-state.json` (scoped)
- **보장**: `writeTextAtomic()` (G1 원자적 쓰기) + SHA256 체크섬

### `readPabcdStateWithFallback`
- **정의**: `orchestrate-state.ts:339`
- **서명**: `(cwd, sessionId?) → Promise<{ ok, value, raw } | { ok: false, error, raw } | null>`
- **읽기**: scoped 우선 → shared 폴백

### `PabcdEnvelope`
- **정의**: `orchestrate-state.ts:53`
- **용도**: boss 단계 정보 타입 (Slice C에서 cli-jaw `state-machine.ts`가 구성)

## 전파 경로

```
orchestrate-state.ts (정의)
  → sdk.ts (export 추가 ✅)
    → packages/jwc/src/sdk.ts (export * → 자동 전파)
      → cli-jaw: import { writeNativeWorkflowEnvelopeAtomic } from 'jwc/sdk'
```

## 110 이후 전제 조건

- **130 Slice C (PABCD boss 하향 주입)**: 본 export로 cli-jaw `state-machine.ts`에서 직접 호출 가능 → **즉시 착수 가능**
