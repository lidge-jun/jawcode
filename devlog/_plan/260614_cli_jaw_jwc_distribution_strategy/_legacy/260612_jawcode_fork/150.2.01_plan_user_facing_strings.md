# 150.2.01 — M-str: user-facing `gjc` 문자열 제거 (1차·즉시)

> 상태: ⬜ 실행 대기 — **즉시 가능** (저충돌, upstream sync 유지 중에도 OK).
> 상위: [150.2 §1-F](./150.2_plan_namespace_rename.md) (M-str). 충돌 위험 낮음 = 텍스트, jaw 소유.
> 게이트: 수동 확인 + `bun scripts/verify-jwc-ui-redesign.ts`.

## 목표

사용자가 **실제로 보는데 아직 `gjc`/`gajae-code`인 문자열**을 jaw로 전환. 코드 로직 무변경, 문자열만.

## 대상 (정확한 위치 — 조사 §7)

### A. 로그인 안내 에러 (8 provider) — `"gjc /login X"` → `"jwc /login X"`

| 파일:라인 |
|---|
| `packages/coding-agent/src/web/kagi.ts:139` |
| `packages/coding-agent/src/web/parallel.ts:294,327` |
| `packages/coding-agent/src/web/search/providers/parallel.ts:122` |
| `packages/coding-agent/src/web/search/providers/kimi.ts:114` |
| `packages/coding-agent/src/web/search/providers/gemini.ts:393` |
| `packages/coding-agent/src/web/search/providers/zai.ts:283` |
| `packages/coding-agent/src/web/search/providers/synthetic.ts:71` |
| `packages/coding-agent/src/web/search/providers/codex.ts:467` |

> **권장**: 하드코딩 대신 `APP_NAME` 보간 — `` `${APP_NAME} /login X` ``. 그러면 ENGINE_NAME 플립(150.2.05) 시 자동 정합 + 향후 재발 방지.

### B. CLI help 예시

| 파일:라인 | 현재 |
|---|---|
| `packages/coding-agent/src/commit/cli.ts:73` | `"  gjc commit [options]"` |
| `packages/coding-agent/src/commands/session.ts:65-69` | `gjc session list/create/status/attach/remove` |
| `packages/coding-agent/src/cli/setup-cli.ts:495-496` | `gjc --model …` |

→ `APP_NAME` 보간 권장.

### C. 화면 타이틀

- `packages/coding-agent/src/modes/components/welcome.ts:315` `APP_NAME==="gjc"?"GJC forge":"Jawcode"` — 분기 자체 정리 (jwc 표면이 정본이므로 "Jawcode" 고정 또는 `APP_NAME` 기반 재작성).

### D. User-Agent 헤더 (외부 서비스 노출)

| 파일:라인 | 현재 |
|---|---|
| `packages/coding-agent/src/hindsight/client.ts:13` | `"oh-my-gajae-code"` |
| `packages/coding-agent/src/mem0/client.ts:7` | `"oh-my-gajae-code"` |
| `packages/coding-agent/src/web/scrapers/github.ts:90` | `"gjc-web-fetch/1.0"` |
| `packages/coding-agent/src/web/scrapers/docs-rs.ts:381` | `"gjc-web-fetch/1.0"` |
| `packages/coding-agent/src/web/scrapers/crates-io.ts:29` | `"gjc-web-fetch/1.0 …"` |
| `packages/coding-agent/src/web/scrapers/musicbrainz.ts:67` | `"gjc-web-fetch/1.0 …"` |
| `packages/coding-agent/src/web/scrapers/discogs.ts:280` | `"…can1357/gajae-code"` |
| `packages/coding-agent/src/web/search/providers/gemini.ts:178` | `"gajae-code"` |

> **주의**: UA 변경은 일부 서비스의 rate-limit/allowlist에 영향 가능 — discogs/musicbrainz는 UA 정책 엄격. 변경 후 해당 provider 스모크 1회.

## 비대상 (이 슬라이스 아님)

- status-line 세그먼트 `id:"gajae"`(`segments.ts:72`·`presets.ts:36,60`) — **사용자 config 키**라 변경 시 마이그레이션 필요 → 150.2.05(상태 마이그레이션)와 묶음.

## 실행 절차

1. A/B/C는 가능한 곳 `APP_NAME` 보간으로 (하드코딩 재발 차단). D는 브랜드 상수화 1개(`USER_AGENT_BRAND`) 권장.
2. `grep -rn "gjc /login\|gjc commit\|gjc session\|oh-my-gajae-code\|gjc-web-fetch" packages/coding-agent/src` 로 잔여 0 확인.

## 완료 기준

- [ ] A~D 전환 + grep 잔여 0.
- [ ] `verify-jwc-ui-redesign.ts` PASS.
- [ ] UA 민감 provider(discogs·musicbrainz·gemini) 스모크 1회.
