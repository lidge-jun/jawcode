FAIL

# 270.5 — A-stage planner audit r0

## Findings

- [high] PLANNER-A1 `270.1_p_jwc_selector_exposure_plan.md:174-177` — `270.3_p_synthesis_round1.md:28` accepted a mandatory RuntimeHeader component/source test for primary band + `collapsedAfter`; the plan only requires `manager-settings-model.test.ts` metadata checks or an optional component assertion, so the recorded decision does not map to a concrete, non-optional work item — add a required RuntimeHeader test or explicit source assertion on `collapsedAfter` vs `PRIMARY_CLIS.indexOf('jwc')` and remove the `or`.
- [high] PLANNER-A2 `270.1_p_jwc_selector_exposure_plan.md:188` — Acceptance #6 claims `jwc` is covered by RuntimeHeader primary/collapse logic, but planned changes §4 only touch `manager-settings-model.test.ts`; that criterion is not independently checkable from listed work — add a RuntimeHeader test work item or narrow acceptance #6 to metadata ordering only.
- [medium] PLANNER-A3 `270.1_p_jwc_selector_exposure_plan.md:158-159` — Curl fallback verification uses unresolved placeholders, so the API gate is not executable from the plan alone — pin the `ok()` envelope path and port discovery command in-plan, or require route test.
- [medium] PLANNER-A4 `270.1_p_jwc_selector_exposure_plan.md:189` — Acceptance #7 requires B-stage jawcode package evidence pairing, but planned changes have no work item to produce that receipt — add explicit B-stage evidence line or move #7 out of completion criteria.
- [low] PLANNER-A5 `270.1_p_jwc_selector_exposure_plan.md:126-130` vs `270_jwc_ui_cli_selector.md:57` — parent 270 still lists browser dropdown check; plan treats UI visibility as inferable from constants/tests — add supplemental manual gate or state parent browser verification is superseded.
- [low] PLANNER-A6 `270.1_p_jwc_selector_exposure_plan.md:80-88` vs `:183-187` — planned test asserts `experimental === true`, but acceptance criteria omit it — add `experimental: true` to acceptance or remove assertion.

Most likely misread: Updating `PRIMARY_CLIS` / `CLI_META` and passing registry/metadata unit tests is enough to satisfy Manager selector + `/api/cli-registry` verified, so the implementer skips the mandatory HTTP `/api/cli-registry` payload proof.
