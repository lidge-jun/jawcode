PASS

# 270.12 — A-stage architect delta r2

## Findings

- [resolved] ARCH-A1 — r1 blanket experimental rule removed; JWC-only `doesNotMatch` + other `CLI_KEYS` coverage matches `readiness.ts:154`.
- [resolved] ARCH-A2 — `orderRuntimeCliOptions`, `orderedCliOptions.map`, and `collapsedAfter={orderedPrimaryCliCount}` fix `Object.keys(perCli)` order hazard.
- [resolved] ARCH-A3 — mandatory route test pins `body.data.jwc`; Kiro boundary isolation added, but mock specifier should use resolved `.js` path to avoid binding risk.
- [accepted] ARCH-A4 — byte-identical `CLI_META.jwc` copy + assertions; drift documented as known debt.
- [accepted] ARCH-A5 — cited anchors exist.
- [resolved] ARCH-A6 — mandatory command bundles registry, manager metadata, and route tests.
- [P2] ARCH-A3 note — route-test Kiro mock should resolve `../../src/agent/kiro-models.js`; current plan path risk should be corrected in any future revision.

Most likely break: route-test Kiro mock failing to intercept if relative `.ts` specifier is used while `registry-live.ts` imports `../agent/kiro-models.js`.
