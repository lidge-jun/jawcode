# A-stage Synthesis R2

All R1 findings (ARCH-A1 through A6) confirmed addressed.

## ARCH-A7 (paused fresh result fallthrough) — Accept
Add `freshFallback.result.paused` to isolated early-return predicate. Fresh paused = terminal, skip merge/patch.

## ARCH-A8 (thrown after step 2) — Accept
Wrap helper steps 2-4 in try/catch. On throw, mark fresh actor `failed` before rethrowing.

## ARCH-A9 (stale fields on fresh record) — Accept
Extract key-only fields from actorRouting.actor before passing to allocateActorRecord, or explicitly build the key object. Prevents `failureReason` leaking.

All items are implementation-level fixes within the established architecture. Incorporating during B-stage.
