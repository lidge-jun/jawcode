# A-stage Synthesis R1

## ARCH-A1 (stale closure rebind) — Accept, updating plan
Change `attemptFreshFallback` return type to `{ result: SingleResult; freshActor: WorkflowActorRecord; freshSessionFile: string } | undefined`. Caller rebinds `finalizeWorkflowActorResult` and `taskSessionFile` on fresh fallthrough.

## ARCH-A2 (double finalize) — Accept, updating plan
Remove `finalizeWorkflowActorResult` call on isolated fresh-failure path. Helper handles all finalization internally.

## ARCH-A3 (aborted guard) — Accept, updating plan
Add `if ((result as any).aborted) return undefined;` before failure predicate. Will check actual SingleResult type for `aborted` field.

## ARCH-A4 (plan/synthesis divergence) — Accept
Updating plan body to incorporate all R2 synthesis items.

## ARCH-A5 (raw ops justified) — Accept as-is
Raw ops in helper are for the fresh actor only (R2-1 accepted). Caller rebinds `finalizeWorkflowActorResult` to fresh actor for post-processing finalization — no duplication.

## ARCH-A6 (tests) — Accept
Will add routing test for failed-resume→fresh-spawn.
