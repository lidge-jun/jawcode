# Critic R1 — ITERATE

## Findings (high severity)
1. Isolated path not diff-specified — only "symmetrically" handwaved
2. Failure predicate ambiguous — could trigger on post-merge/patch errors, not just subprocess resume failures
3. Concrete diff uses raw registry ops instead of existing `updateWorkflowActor`/`finalizeWorkflowActorResult` closures
4. Acceptance criteria miss: exactly-once retry, isolated path, fresh-spawn failure, next-call resume behavior
5. `runSubprocessBaseArgs` extraction not diff-specified

## Recommendations
- Specify isolated-path placement and subprocess-only failure predicate
- Align registry updates with existing helpers
- Add diff-level detail for base args extraction
- Strengthen ACs and verification
