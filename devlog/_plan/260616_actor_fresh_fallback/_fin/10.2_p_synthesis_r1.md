# P Synthesis R1 — Critic Findings Response

## Finding 1 (isolated path) — Accept
Will specify the exact insertion point: immediately after the first `runSubprocess` call (line ~1720), before any merge/patch post-processing. Both paths share the same retry helper.

## Finding 2 (failure predicate) — Accept
Gate on raw `runSubprocess` result only, before any post-processing enrichment. Exclude `paused` results. Predicate: `actorRunMode === "message" && !result.paused && (result.exitCode !== 0 || result.error)`.

## Finding 3 (registry ops) — Accept
Extract a shared `retryWithFreshActor` helper that takes the failed actor, reuses `updateWorkflowActor` for registry mutation, and returns the new actor + session file for the retry call.

## Finding 4 (base args extraction) — Accept
Will specify the exact shape in revised plan.

## Finding 5 (acceptance criteria) — Accept
Add ACs for exactly-once retry, isolated path, fresh-spawn failure passthrough, next-call resume.

## Finding 6 (verification) — Accept
Add explicit test cases.

## Finding 7 (terminology) — Accept
Replace "retire" with "replace via upsert" in MOC.

## Finding 8 (minor) — Accept
Align session file naming, use effectiveArtifactsDir, use key extraction from actor record.
