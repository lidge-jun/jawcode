# WP10 — 10.012 goal steering subsystem (gjc → jwc port)

> Class **C3** (largest card; ~210 src + ~310 test LOC; **atomic**). A deep read-only sub-agent
> produced a full port plan: gjc `ultragoal-runtime.ts` steering subsystem → jwc `goal-engine.ts`
> with `Ultragoal*`→`Goal*` rename. jwc plan/goal/ledger types are 1:1; the work is the helper
> layer + dispatcher + plumbing, not the command bodies.

## Source — `packages/coding-agent/src/jwc-runtime/goal-engine.ts`

Refs: gjc `gjc-runtime/ultragoal-runtime.ts` L130-150 (types), L2455-2821 (helpers + 6 cmds),
L3503-3639 (dispatcher); golden test gjc `test/gjc-runtime/ultragoal-runtime.test.ts` L964-1275.

1. **Types/consts** (~L113): `NATIVE_STEERING_KINDS` (6), `GoalSteeringKind`, `NATIVE_STEERING_KIND_SET`,
   `ReplacementSpec`, `SteeringCommandResult`.
2. **Helpers** (near `addGoalSubgoal` ~L1285), Ultragoal→Goal rename, drop session scope
   (`getGoalPaths(cwd)`): `nextGoalId(plan,offset=1)`, `requireSteeringText`, `requireSteeringEvidence`,
   `findGoalOrThrow`, `requireGoalStatus`, `parseJsonFlag`, `parseReplacementSpecs`, `parsePendingOrder`,
   `appendSteeringRejected`, `steeringPayloadSummary`, `parseNativeSteeringKind`.
3. **6 commands**: refactor existing `addGoalSubgoal` to delegate to private `addGoalSubgoalToPlan`
   (keep export — test L547 calls it); add private `splitGoalSubgoal`, `reorderPendingGoals`,
   `revisePendingGoalWording`, `annotateGoalLedger`, `markBlockedGoalSuperseded`.
4. **Dispatcher** `executeGoalSteeringCommand(args, cwd)` — port gjc L3503-3639.
5. **Route**: replace `dispatchGoalCommand` `case "steer"` hard-reject (L1596-1618) with
   `executeGoalSteeringCommand` routing for all 6 kinds.
6. **Plumbing**: add `--replacements-json`, `--order-json` to `FLAGS_WITH_VALUES` (L1374).

## Test — extend `packages/coding-agent/test/jwc-runtime/ultragoal-runtime.test.ts`

(NOT `test/goals/*` — wrong layer, they test goal-MODE runtime.) Port gjc golden L964-1275 + the
`expectRejectedSteering` helper (strip session-dir → `getGoalPaths(root).goalsPath`, `readGoalLedger`).
Per-kind: split (`[G001-sup,G003,G004,G002]`), reorder, revise, annotate (goals.json byte-identical),
mark-superseded (happy + last-goal reject), rejection-audit sweep (status 1 + plan unchanged + 1
`steering_rejected` event each).

## Risk — persisted goal state (`.jwc/goal/{goals.json,ledger.jsonl}`)

No migration (steering writes existing optional `steering?` field; `version:1` untouched). Highest-risk
path = `mark_blocked_superseded` last-goal guard (prevents empty required set → vacuous completion).

## Verification (C)

- `bun test packages/coding-agent/test/jwc-runtime/ultragoal-runtime.test.ts` all green;
  regression `test/goals/goal-mode-integration.test.ts`, `jwc-skill-state-hooks.test.ts`; tsc 0.
- Then 10.012 → `_fin/10/` (unblocks 10.021), MOC + 007 + devlog.

## Commit scope

- `goal-engine.ts` (port) + test extension — commit 1. 10.012 `_fin` move + MOC/007 — commit 2. This plan.
