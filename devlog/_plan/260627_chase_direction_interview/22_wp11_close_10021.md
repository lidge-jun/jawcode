# WP11 — 10.021 goal red-team / review mode (gjc → jwc, Decision A=split)

> Class **C3** (goal completion-gate hardening; single-file test blast radius).
> A deep read-only sub-agent compared gjc red-team/review subsystem (#610 `5bcf585c`)
> against jwc's current `goal-engine.ts` + `goal-guard.ts`. Verdict: jwc already ports
> the entire executorQa **matrix schema**, the **completion-receipt freshness gate**, the
> **review-blocker lifecycle**, and ~15 shallow-gate rejections. The single genuine
> residual for 완료 기준 #2 is **live-surface inline-evidence acceptance**.

## Confirmed already-present (no port — documented in card)

| gjc capability | jwc evidence |
|---|---|
| executorQa matrix (artifactRefs/surfaceEvidence/adversarialCases/contractCoverage cross-link) | `goal-engine.ts:886-1006` |
| completion-receipt freshness gate (qualityGateHash/jwcGoalSnapshotHash/planGeneration) | `goal-guard.ts:115-211,281` |
| review-blocker lifecycle (`record-review-blockers`, `review_blocker` kind, `review_blocked` status) | `goal-engine.ts:1767-1801`; guard `goal-guard.ts:244` |
| 완료 기준 #1 (review/red-team path documented) | `src/defaults/jwc/skills/goal/SKILL.md:176-200` |
| 완료 기준 #3 (10.012 dispatcher compatible w/ review transitions) | `review_blocker`/`review_blocked` already steered; (b) touches gate only |

## The genuine gap (완료 기준 #2 — parity)

jwc `requireSubstantiveArtifactEvidence` (`goal-engine.ts:877`) accepts `inlineEvidence`
prose for **any** surface; gjc rejects inline-only for **live** surfaces (web/cli/native)
and requires a resolvable artifact file (`validateLiveSurfaceProofPresence`
gjc `ultragoal-runtime.ts:1806`; `isLiveSurfaceFamily` `:960`). Smoking gun: jwc default
`passingQualityGate()` declares `surface:"gui/web"` + `path:"artifacts/gui-screenshot.png"`
that is **never written**, yet passes via inline prose.

## Change (adapt slice) — `src/jwc-runtime/goal-engine.ts`

1. `isLiveSurface(surface)` — web(`gui|web|browser|ui|visual`) | cli(`cli|terminal|command`) |
   native(`computer|computer-use|desktop|native|tui|desktop-input|native-input`). Mirrors gjc
   `surfaceFamily`+`isLiveSurfaceFamily`. **Excludes** api-package / algorithm-math (inline still OK).
2. `requireLiveSurfaceProof(cwd, surface, artifactIds, artifactRefs, fieldName)` — live surface ⇒
   ≥1 linked artifact must `hasExistingNonEmptyArtifact(cwd, row.path)` (REUSE existing `:864`; **no**
   byte-decode). Else throw message containing `"... do not prove live surfaces"`.
3. `validateSurfaceEvidence` → async + `cwd` param; call `requireLiveSurfaceProof` after
   `validateSurfaceArtifactCompatibility`. Caller `validateExecutorQaRedTeamEvidence` (`:1010`)
   already async/has cwd → `await validateSurfaceEvidence(cwd, …)`.

## Test — `test/jwc-runtime/ultragoal-runtime.test.ts` (single-file blast radius, confirmed)

- Seed `<root>/artifacts/{browser-run.json,gui-screenshot.png,adversarial-report.txt}` (non-empty)
  in `tempDir()` → all 20 `passingQualityGate()` complete-checkpoints resolve (1 edit). Safe: existing
  rejection tests fail on schema, not file-absence; inline always satisfied `requireSubstantive…` before.
- NEW negative: gui/web gate with artifact `path` removed (inline-only) → `expectRejectedCompleteGate`
  → assert error `toContain("do not prove live surfaces")`. Non-vacuous via existing helper (exit 1 +
  byte-unchanged goals.json/ledger.jsonl).
- Audit the existing "accepts substantive inline evidence" test (`:1232`) — keep green by ensuring its
  row is non-live OR file-backed.
- Doc parity: append one sentence to `SKILL.md:188` (live surfaces need a resolvable artifact file).

## Split-off (Decision A) — NEW follow-up card

`10.0NN_gjc_chase_goal_live_artifact_engine.md` (out of scope here, security-sensitive):
C2 screenshot byte decode (PNG/JPEG non-uniform), C4 automation transcript, C5 PTY control-seq,
**C3 CLI replay execution** (process spawn — needs allowlist security review), C8 standalone `review`
subcommand + C9 contract-strength grading, C10 computer mandatory adversarial cases. ~800+ LOC.

## Verification (C)

- `bun test test/jwc-runtime/ultragoal-runtime.test.ts` all green (incl. new negative + 20 seeded
  passing); regression `test/goals/goal-mode-integration.test.ts` + `jwc-skill-state-hooks.test.ts`; tsc 0.
- Then 10.021 → `_fin/10/`, MOC + 007 + this devlog; create split-off card.

## Commit scope

- `goal-engine.ts` + test + SKILL.md — commit 1. 10.021 `_fin` + split-off card + MOC/007/devlog — commit 2.

## Outcome — closed 2026-06-27

| step | evidence |
|------|----------|
| Source | `goal-engine.ts`: `isLiveSurface()` (gjc `surfaceFamily`+`isLiveSurfaceFamily` parity, provably equivalent flat token set) + `requireLiveSurfaceProof()` (≥1 linked artifact must resolve to a non-empty file; reuses `hasExistingNonEmptyArtifact`, no byte engine). `validateSurfaceEvidence` → async+`cwd`; sole caller `validateExecutorQaRedTeamEvidence` already async. |
| Test | `tempDir()` seeds `artifacts/{browser-run.json,gui-screenshot.png,adversarial-report.txt}` → 20 passing checkpoints resolve (1 edit). NEW negative: gui/web surface with artifact `path` stripped (inline-only) → rejected `executorQa.surfaceEvidence[0].artifactRefs ... do not prove live surfaces`. |
| **Non-vacuous** | source stashed → negative test **0/1**; restored → **1/1**. |
| Verification gate | env-clean `ultragoal-runtime`(**85/0**, incl new) + `goal-mode-integration` + `jwc-skill-state-hooks` + `goals/goal-runtime` = **121/0**; tsc **0**. 1 pre-existing env fail `jwc-runtime/goal-runtime` (subprocess `Bun 1.3.11 < 1.3.14`), confirmed unrelated (fails identically with source reverted). |
| Doc | `SKILL.md:193` — live surfaces require a resolvable artifact file, not inline prose. |
| Commits | source+test+SKILL `cdee4f7`; `_fin` + split card + MOC/007/devlog (this commit). |
| Split (Decision A) | residual deep-verification engine → NEW [10.027](../../../struct_har/chase/10.027_gjc_chase_goal_live_artifact_engine.md): screenshot byte/non-uniform decode, automation transcript, PTY, **CLI replay execution (security-sensitive)**, standalone `review` subcommand, contract-strength grading, computer mandatory cases. jwc already had matrix schema + freshness gate + review-blocker lifecycle. |
