# WP8 — 10.023 task notification context (omp 0.5.1 pre-prompt maintenance)

> First card closed with the **restored runtime test env** (devlog 18). Class **C2** (2 small
> production fixes + 1 test-port fix). Done-gate = the 3 previously-`it.skip` tests in
> `agent-session-auto-compaction-queue.test.ts` unskipped + green. A deep read-only sub-agent
> traced the exact root causes; verified omp 0.5.1 is the reference.

## Root causes (sub-agent, evidence-cited)

- **RC-A** `packages/agent/src/compaction/compaction.ts:446` — `collectMessageFragments` has no
  `custom` case → custom messages (task-notifications) count as **0 tokens** in both the display
  heuristic and the native tokenizer. Breaks Test 2 entirely; half of Test 1.
- **RC-B** `packages/coding-agent/src/session/agent-session.ts:5016` — the single pre-prompt check
  call `#checkEstimatedContextBeforePrompt()` passes **no pending messages**; the about-to-send
  prompt `message` is never estimated. The `pendingMessages` param is already plumbed through
  (`...Once` → `#estimateContextTokensForCompaction` → `#estimateContextTokensWith`). Other half of Test 1.
- **Test 3 = test-port bug** — jwc's L424 test omits `session.setTodoPhases(...)` (omp's source test
  has it; the jwc fork split it out and dropped the line) → no `todo_reminder` ever fires → timeout.
  No production change can fix it; it is a defective test.

## Changes

1. `compaction.ts:426` — add `case "custom":` to the `case "hookMessage": case "toolResult":` group
   (CustomMessage.content is `string | (text|image)[]`, identical handling). Fixes RC-A.
2. `agent-session.ts:5016` — `#checkEstimatedContextBeforePrompt()` → `#checkEstimatedContextBeforePrompt([message])`
   (`message: AgentMessage` is the `#promptWithMessage` param). Fixes RC-B.
3. Test `agent-session-auto-compaction-queue.test.ts` ~L427 — add the missing
   `session.setTodoPhases([{ name: "Execution", tasks: [{ content: "Finish pending task", status: "in_progress" }] }]);`
   (restores the omp test's required setup). + unskip the 3 tests (already unskipped).

## Verification (C)

- `bun test packages/coding-agent/test/agent-session-auto-compaction-queue.test.ts` → all green (8/8).
- Regression (RC-A widens custom-message token counting): `bun test packages/agent/test/` (compaction-
  telemetry, remote-compaction, pruning-redteam) + the full queue test file. tsc clean.
- Then 10.023 → `_fin/10/`, MOC + 007 + devlog.

## Commit scope

- `compaction.ts` + `agent-session.ts` (prod) + test file (unskip + setTodoPhases) — commit 1.
- 10.023 `_fin` move + MOC/007 — commit 2. This plan. No push.
