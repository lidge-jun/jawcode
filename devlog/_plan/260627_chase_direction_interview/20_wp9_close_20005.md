# WP9 — 20.005 steering/queued follow-up delivery guarantee (omp 42ffc83)

> Class **C3** (concurrency: turn-boundary queue race). A deep read-only sub-agent traced jwc's
> two-layer queue model, empirically reproduced the strand, and reduced the omp 3-part fix to the
> **single part jwc genuinely needs**. Decision A = adapt; scope `full_queue_race` but jwc already
> has the idle-path, so the real gap is the settle-time drain.

## Decision per omp part (sub-agent, evidence-cited)

- **Part 1** (agent-loop yield-boundary `lateSteering` re-poll) → **SKIP**: the settle drain covers
  the same stranded steer; a loop-level re-poll bypasses the session's double-continue gate and
  risks a turn-budget edge.
- **Part 2** (`#drainStrandedQueuedMessages` at settle) → **PORT (adapted, required)**: genuinely
  missing (`rg drainStranded` = 0); the only part the strand-probe proves is needed.
- **Part 3** (steer stale-`isStreaming` after image await) → **SKIP-redundant**: jwc's followUp
  idle-path + the Part-2 drain cover it.

## Change A (required) — `packages/coding-agent/src/session/agent-session.ts`

1. New method near the in-flight helpers:
   ```ts
   #drainStrandedQueuedMessages(): void {
       if (this.#canAutoContinueForFollowUp() && this.agent.hasQueuedMessages()) {
           this.#scheduleAgentContinue({
               shouldContinue: () => this.#canAutoContinueForFollowUp() && this.agent.hasQueuedMessages(),
           });
       }
   }
   ```
2. `#endInFlight` (L1130-1134, inside `count===0`): call it after `#flushPendingAgentEnd()`.
3. `#resetInFlight` (L1137-1142): call it after `#flushPendingAgentEnd()`.

**No double-continue** (the crux): `#canAutoContinueForFollowUp()` is false while `isStreaming`, so
the drain no-ops when any turn is running; `#scheduleAgentContinue` re-checks `shouldContinue()` at
exec; `agent.continue()` throws `AgentBusyError` if streaming; one continue drains BOTH queues. The
followUp enqueue scheduler (:5327) and this drain gate on the identical predicate → cannot both fire.

## Test — NEW `packages/coding-agent/test/agent-session-queued-steer-delivery.test.ts`

Harness per jwc `agent-session-queued-prompts.test.ts`. Scenarios (each fails on `main`, passes with fix):
1. Stranded steer injected from an `agent_end` subscriber (settle window) → delivered without manual
   prompt (`mock.calls===2`, `hasQueuedSteering()===false`).
2. Stranded raw `followUp` at settle → delivered.
3. No double-continue: one steer at settle + 3-response mock → `mock.calls===2` (single continue drains).
4. Public mid-stream `steer()`+`followUp()` still in order (regression guard).

## Verification (C)

- New test green; regression: `agent-session-queued-prompts`, `…-steer-interrupt`, `session/yield-queue`,
  `async-yield-queue`, `agent/test/agent-steering-queue`, `agent/test/agent-queue-removal` (baseline 20/0). tsc 0.
- Then 20.005 → `_fin/20/`, MOC + devlog.

## Commit scope

- `agent-session.ts` (Change A) + new test — commit 1. 20.005 `_fin` move + 20_MOC — commit 2. This plan.
