# WP5 — close 10.005 task·subagent (jwc-adequate doc-judgment)

> PABCD work-phase 5. Class **C1** (doc-judgment; no code). A read-only diff-triage sub-agent
> checked all 5 undetermined adapt cards; **10.005 is the only one jwc already satisfies** —
> closeable by diff evidence with no code change. The other four (10.003, 10.023, 20.005, 20.006)
> are genuinely net-new and stay blocked (runtime tests broken).

## Why 10.005 is jwc-adequate (adapt = no-op)

Triage evidence (jwc vs gjc clone):
- OAuth `sessionId` session-sticky behavior **byte-identical** to gjc
  (`packages/ai/src/utils/anthropic-auth.ts` = 0 non-rename diff lines) → Decision F "no provider
  leak" is satisfied by the existing impl, not pending.
- `forkContext: allowed` gate intact (41 refs in `packages/coding-agent/src/task/index.ts`).
- jwc owns the **larger canonical** bundled task-agent surface (`agents.ts` 4 bundled agents vs
  gjc's 2; `task/index.ts` 713-line intentional fork divergence).
- Decision-C proof tests exist with **0 skips**: `default-jwc-definitions.test.ts`,
  `task-fork-context.test.ts`, `task-bundled-agent-surface.test.ts`.

So the card's adapt target is already held by jwc; the close is a documented evidence-based
judgment. Verification gate = **diff** (no code change → no new behavior to test). The 완료 기준
asks for exactly those existing focused tests, which are present.

## Actions

1. `10.005` `완료` — mark done with the jwc-adequate judgment (adapt = no-op; diff + existing tests
   are the proof); note Decision F resolved (byte-identical OAuth = no new leak).
2. Outbound link depth-fix: L3 `./10_gjc_chase_MOC.md` → `../../10_gjc_chase_MOC.md`; status `⬜` → `✅ _fin`.
3. `git mv` → `struct_har/chase/_fin/10/`.
4. `10_gjc_chase_MOC.md` L54 row → `./_fin/10/…` + `✅ _fin`. (`007_follow_index.md` has no 10.005 row.)

## Verification (C)

- Diff/doc gate met (jwc-adequate documented); zero chase-root links to 10.005; moved-card links
  resolve; `git diff --check` clean. No code → tsc/test N/A.

## Commit scope

- 10.005 edits + `git mv`; `10_gjc_chase_MOC.md` row; this plan doc. No push.

## After this: true blocked remainder (pause-legitimate)

10.002 (C4 auth), 10.003 (net-new timeout/cache), 10.007 (net-new tmux self-heal), 10.012 (net-new
goal steering), 10.021 (gated on 10.012), 10.023 (skipped race tests), 20.005 (net-new steering),
20.006 (net-new TUI) — all need runtime tests blocked by the native drift, or net-new code unsafe
tsc-only. No further faithful autonomous closure without the test-env/verification-standard decision.
