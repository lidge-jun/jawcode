# A Synthesis R2 — Ctrl+O current-turn scope

Revision: `ctrlo-r2`

## Verdicts

- Planner lens: FAIL (`agent://89-CtrlOPlannerDeltaR2`)
- Architect lens: FAIL (`agent://90-CtrlOArchitectDeltaR2`)

## Shared root cause

R2 resolved the optimistic-submit design but still left the test harness and active-tool minimization instructions too loose. Both lenses specifically rejected the `interactive-mode-status.test.ts` harness reference because it only drives `UiHelpers`, not real `InteractiveMode` wrapper methods.

## Finding decisions

- PLANNER-A3 / ARCH-A9: accept. Replace the harness reference with the real `InteractiveMode` construction pattern from `interactive-mode-editor-component.test.ts` and require the new test to exercise public `InteractiveMode` rebuild/submit entrypoints.
- PLANNER-A5: accept. Pin rebuild hook to `InteractiveMode.rebuildChatFromMessages()` immediately after `this.renderSessionContext(context)` and `renderInitialMessages()` after `#uiHelpers.renderInitialMessages(...)`.
- PLANNER-A7: accept. Skip minimization for both pending tools and current `liveToolContainer` children.
- PLANNER-A8: accept. Test 0 must instantiate `InputController` with the real mode context or call a public Ctrl+O delegate and assert pre-boundary components are untouched.
- PLANNER-A9 / ARCH-A10: accept. Add `ToolExecutionHandle` type import to implementation notes.
- ARCH-A11: accept. Restore explicit Test 0 and make Test 2 a clear event-controller cross-reference.

## Plan changes for `ctrlo-r3`

- Updated minimization guard to skip `pendingTools` and `liveToolContainer` children.
- Added explicit `ToolExecutionHandle` import note.
- Reinserted Test 0 with real `InteractiveMode` + `InputController` wiring.
- Replaced `interactive-mode-status.test.ts` harness citation with `interactive-mode-editor-component.test.ts` real-mode construction pattern.
- Clarified Test 2 ownership and cross-reference.

Decision: revise plan and run delta A re-audit for `ctrlo-r3`.
