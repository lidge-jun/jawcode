# D DONE — Ctrl+O current-turn scope

## P/A/B/C summary

- P: Planned Ctrl+O current-turn expansion/collapse in `10_ctrl_o_turn_scope_plan.md`, with Critic OKAY in `10.4_p_critic_round4_okay.md`.
- A: Dual audit failed twice; all findings were accepted into `ctrlo-r3`, including optimistic-submit boundary, real InteractiveMode rebuild tests, active-tool minimization guard, and explicit Ctrl+T non-goal.
- B: Implemented commit `91ff4860 fix: scope ctrl-o to current turn`; verifier `agent://91-CtrlOBVerifier` returned DONE and was recorded in `10.12_b_verifier_done.md`.
- C: `bun run check` passed and affected tests passed 34 tests / 139 assertions; evidence recorded in `10.13_c_check_pass.md`.

## Files changed

Implementation:

- `packages/coding-agent/src/modes/types.ts`
- `packages/coding-agent/src/modes/components/user-message.ts`
- `packages/coding-agent/src/modes/interactive-mode.ts`
- `packages/coding-agent/src/modes/controllers/event-controller.ts`
- `packages/coding-agent/src/modes/controllers/input-controller.ts`

Tests:

- `packages/coding-agent/test/input-controller-keybindings.test.ts`
- `packages/coding-agent/test/modes/controllers/event-controller-message-start.test.ts`
- `packages/coding-agent/test/interactive-mode-current-turn-boundary.test.ts`

Devlog:

- `devlog/_plan/260615_ctrl_o_turn_scope/*`

## Acceptance criteria met

- Ctrl+O now targets chat children from `currentTurnStartIndex`, plus live tool container children and the streaming assistant component.
- `currentTurnStartIndex` moves after optimistic real-user submissions, before the `message_start` echo.
- Real non-synthetic user `message_start` moves the boundary after the user component is present.
- Synthetic user/replay messages are ignored for boundary movement and rebuild scans.
- Full rebuild paths reset the boundary after the last non-synthetic `UserMessageComponent`.
- Collapse calls `setExpanded(false)` and fully minimizes completed current-turn tool components via `setMinimized(true)`.
- Active pending/live tools are not force-minimized by Ctrl+O collapse.
- Ctrl+T/full transcript code path was not modified and remains covered by the focused keybinding suite.

## Verification

- `bun run check` — PASS.
- `bun test packages/coding-agent/test/input-controller-keybindings.test.ts packages/coding-agent/test/modes/controllers/event-controller-message-start.test.ts packages/coding-agent/test/interactive-mode-current-turn-boundary.test.ts` — PASS, 34 tests / 139 assertions.
- `bun biome check ...touched files...` — PASS.
- `bun --cwd=packages/coding-agent run check:types` — PASS (`CODING_AGENT_TYPES_DONE`).

## WONDER — still missing

- This cycle did not change `toggleThinkingBlockVisibility()`; it remains on the prior `liveToggleEligible` contract by explicit A-stage non-goal.
- The newly reported scroll duplicate issue still reproduces in the user’s fresh build. That is a separate scroll/tick/rendering follow-up and was not part of the Ctrl+O objective.
- The PABCD runtime displayed a stale unrelated plan ref during this cycle; artifacts name the actual Ctrl+O plan explicitly to preserve traceability.

## REFLECT — spec improvements

- Ctrl+O and Ctrl+T need separate ontology names: “current-turn output expansion” vs “full transcript overlay” so future changes do not blur their scopes.
- Future keybinding specs should state whether optimistic local UI state must update before server echo events.
- Rebuild-boundary acceptance should always include at least one real-mode test rather than UiHelpers-only fixtures.

## Closing verdict

Ctrl+O current-turn expansion/collapse fix: DONE.
