# P Critic Round 1 — Ctrl+O current-turn scope

Verdict: ITERATE

Source: `agent://83-CtrlOPlanCritic`

## Findings

- PLAN-BOUNDARY-INIT (blocking): plan did not rebind `currentTurnStartIndex` after full chat rebuilds (`renderInitialMessages()` / `rebuildChatFromMessages()`).
- PLAN-BOUNDARY-SYNTHETIC (blocking): plan said real user only but the snippet updated the boundary unconditionally.
- PLAN-BOUNDARY-PLACEMENT (blocking): snippet placement did not prove the update was inside the user branch only.
- PLAN-TEST-HARNESS (blocking): test fixtures were not explicitly updated with the new context field, and the boundary movement test owner was optional.

## Main-session synthesis

Accepted all findings.

Plan updates applied in `10_ctrl_o_turn_scope_plan.md`:

- Added `InteractiveMode` rebuild-boundary helper and required calls after `renderInitialMessages()`, `rebuildChatFromMessages()`, and any direct full `renderSessionContext()` rebuild path.
- Replaced user-boundary snippet with exact placement inside `else if (event.message.role === "user")` and exact `!event.message.synthetic` guard.
- Mandated boundary movement tests in `packages/coding-agent/test/modes/controllers/event-controller-message-start.test.ts`, including a synthetic-user non-move case.
- Mandated `currentTurnStartIndex: 0` fixture updates for touched `InteractiveModeContext` mocks.

Decision: revise and rerun Critic.
