# P Critic Round 4 — Ctrl+O current-turn scope

Verdict: OKAY

Source: `agent://86-CtrlOPlanCriticR4`

## Evidence

- Rebuild boundary hooks are pinned to `InteractiveMode.renderInitialMessages()` and `InteractiveMode.rebuildChatFromMessages()` only; bare `renderSessionContext()` is explicitly excluded.
- Event-boundary wiring is exact: update only inside `role === "user"` plus `!event.message.synthetic`, after optimistic/non-optimistic add logic.
- Ctrl+O sweep contract is explicit: collect from `currentTurnStartIndex`, union live tool container and streaming component, collapse minimizable tools with `setMinimized(true)` after `setExpanded(false)`.
- Verification is pinned to existing keybinding/event-controller tests plus new `interactive-mode-current-turn-boundary.test.ts`.
- Acceptance criteria cover rebuild-boundary reset and Ctrl+T non-interference.

Decision: proceed to final P artifact.
