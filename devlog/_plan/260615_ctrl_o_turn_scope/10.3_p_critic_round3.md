# P Critic Round 3 — Ctrl+O current-turn scope

Verdict: ITERATE

Source: `agent://85-CtrlOPlanCriticR3`

## Findings

- PLAN-VERIFY-TEST4 (blocking): rebuild-boundary Test 4 had no pinned file and was not included in focused verification.
- PLAN-TEST4-SURFACE (blocking): Test 4 allowed a `UiHelpers`-only path, which could pass without wiring `InteractiveMode` hooks.
- PLAN-REBUILD-HOOK-SITE (blocking): bare `InteractiveMode.renderSessionContext()` should not reset boundary because it may be incremental.

## Main-session synthesis

Accepted all findings.

Plan updates applied in `10_ctrl_o_turn_scope_plan.md`:

- Pinned Test 4 to new file `packages/coding-agent/test/interactive-mode-current-turn-boundary.test.ts`.
- Required Test 4 to call an `InteractiveMode` public rebuild entrypoint (`renderInitialMessages()` or `rebuildChatFromMessages()`), not a `UiHelpers`-only path.
- Updated focused `bun test` and Biome acceptance commands to include the new test file.
- Restricted rebuild hook sites to `InteractiveMode.renderInitialMessages()` and `InteractiveMode.rebuildChatFromMessages()` only; explicitly excluded bare `renderSessionContext()`.

Decision: revise and rerun Critic.
