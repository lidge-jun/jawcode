# P Critic Round 2 — Ctrl+O current-turn scope

Verdict: ITERATE

Source: `agent://84-CtrlOPlanCriticR2`

## Findings

- PLAN-TEST-FIXTURE-BOUNDARY (blocking): `event-controller-message-start.test.ts` fixture must have `chatContainer`, `currentTurnStartIndex`, and an `addMessageToChat` mock that mutates children.
- PLAN-REBUILD-TEST (blocking): acceptance requires rebuild-boundary behavior but plan did not mandate a test for rebuild/resume paths.

## Main-session synthesis

Accepted both findings.

Plan updates applied in `10_ctrl_o_turn_scope_plan.md`:

- Added explicit `UserMessageComponent.isSyntheticUserMessage` marker requirement.
- Updated rebuild helper plan to use `instanceof UserMessageComponent && !child.isSyntheticUserMessage`.
- Added exact fixture wiring requirement for `event-controller-message-start.test.ts` with real `chatContainer` mutation.
- Added Test 4 requiring a public-method rebuild-boundary regression with `[older assistant, real user, current assistant]` plus synthetic-user ignore behavior.
- Updated focused Biome command to include `user-message.ts`.

Decision: revise and rerun Critic.
