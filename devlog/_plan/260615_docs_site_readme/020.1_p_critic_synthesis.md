# P Critic Round 1 — Synthesis

## Findings Disposition

| ID | Finding | Decision | Action |
|---|---|---|---|
| F1 | jawcode-logo.png missing from file list | Accept | Add copy step: `cp artifacts/logos/jawcode-logo.png docs-site/assets/` |
| F2 | deploy workflow unspecified | Accept | Add: trigger dev/main push, publish `docs-site/`, `actions/deploy-pages@v4` |
| F3 | subpage content specs inline | Partially accept | MOC specs are sufficient reference — add "see MOC §페이지별 콘텐츠 스펙" cross-ref |
| F4 | settings page missing | Waive | MOC doesn't include settings page. Configuration section is in README only. No settings.html planned. |
| F5 | tool list source | Accept | Source: `README.md` current tool tables + `BUILTIN_TOOLS` from `packages/coding-agent/src/tools/index.ts`. 5-category mapping defined in MOC. |
| F6 | head/meta integration | Accept | Stream 4 produces assets → Stream 1 references them by path. Order: parallel, paths are deterministic. |
| F7 | Stream 2 depends on i18n | Accept | Add i18n.js + locales to Stream 2 dependencies |
| F8 | promote verification to AC | Accept | AC7-AC10 added |
| F9 | tui-mockup merged into style.css | Accept | Note added |
| F10 | ko/ dir superseded | Accept | Note: JSON locales replace ko/ directory approach |

## Plan Patches Applied

### File list update
- ADD: `docs-site/assets/jawcode-logo.png` (copied from `artifacts/logos/`)

### Deploy workflow spec
```yaml
# .github/workflows/deploy-docs.yml
name: Deploy Docs
on:
  push:
    branches: [main, dev]
    paths: ['docs-site/**']
permissions:
  pages: write
  id-token: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs-site
      - id: deployment
        uses: actions/deploy-pages@v4
```

### Stream dependencies update
- Stream 2 depends on: style.css (Stream 1) + i18n.js + locales (Stream 1)
- Stream 4 assets referenced by deterministic paths — no ordering dependency

### Additional acceptance criteria
- AC7: 반응형 1024/768/375px 레이아웃 깨지지 않음
- AC8: `<head>` OG meta + favicon 링크 포함
- AC9: tools.html 도구 수 = README 도구 수 = BUILTIN_TOOLS 수
- AC10: ko.json 키 수 = en.json 키 수

### Notes
- settings.html: 범위 외 (MOC에 없음, README Configuration 섹션으로 충분)
- tui-mockup.css: style.css에 통합, 별도 파일 없음
- ko/ HTML 디렉토리 접근 → JSON locale 단일 페이지 접근으로 대체 (MOC에 확정)
- k-writing 규칙: ko.json 작성 시 `ux-writing-ko.md` 참조 필수
