# 21.5 P Synthesis Round 2 — Jaw-Interview Handoff Cleanup

Date: 2026-06-15
Critic receipt: `21.4_p_critic_round2.md`
Previous plan: `21.3_p_jaw_interview_handoff_cleanup_plan_r2.md`
Revised plan: `21.6_p_jaw_interview_handoff_cleanup_plan_r3.md`

## Decision summary

All round-2 findings are accepted. Local source inspection confirmed `resetPabcdState()` currently retires actor namespaces, then calls `fs.unlink(target.path)`, then pushes `removed` and appends a line. R3 aligns with that exact block and adds active-state assertions for reset tests.

## Finding decisions

### R33-1 — reset snippet not merge-safe with actor-registry + `fs.unlink`

Accepted. R3 uses the actual try-block order from `orchestrate-runtime.ts`: compute namespace, retire registry if needed, `await fs.unlink(target.path)`, then retire jaw-interview state, then push removed/line. It does not replace `fs.unlink` with `fs.rm`.

### R33-2 — reset tests omit skill-active-state assertion

Accepted. R3 requires reset tests to assert no active `jaw-interview` entry remains in `skill-active-state.json`, matching acceptance criteria.

### R33-3 — helper ellipsis must keep phase aligned

Accepted. R3 expands the guard helper patch fully enough to show both mode state and active-state row use the same `phase` parameter.

### R33-4 — duplicate seed helpers vs shared util

Partially accepted. For this patch, local helpers are acceptable because existing runtime tests already keep fixtures local. R3 says deduplicate only if a shared test util already exists nearby; do not create a new shared test utility solely for these tests.
