PASS
ARCH-B1: resolved — a-r2 line 69 guards the non-custom branch with `tone ? ... : undefined`, so after `tone === "custom"` fails, strict TS narrows the indexed branch to the preset union before `TONE_PRESETS[tone]`; this is valid for both `Record<string,string>` and optional `Record<TonePreset,string>` variants, and a-r2 lines 1/4 are revision-history metadata only with no integration surface (devlog/_plan/260702_tone_command/10_plan.md:1,4,62,69; tsconfig.base.json:8).

(lens: architect · revision a-r2 · model openai/gpt-5.5:high · read-only delta re-audit round 3)
