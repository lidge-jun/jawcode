FAIL

Lens: architect / integration-risk · revision a-r0 · read-only audit (no files modified, no gates run — conclusions from source reading only)

Findings:

[high] ARCH-A1 packages/coding-agent/src/config/settings-schema.ts:3089-3092 — /tone off contract `settings.set("identity.tone", undefined)` fails tsgo: SettingValue enum branch is V[number] without undefined; tool.renderMode precedent covers only get-side (?? fallback), no repo code set-unsets an enum — widen enum branch of SettingValue to V[number]|undefined when default is undefined, or add Settings.unset(path); no as-casts at call sites.

[medium] ARCH-A2 packages/coding-agent/src/config/settings.ts:816-848 — persisted unset unproven: #saveNow writes undefined into re-read YAML; possible null resurrection on reload (get() treats null as present) — acceptance for /tone off must include config.yml restart round-trip; delete key if null resurrects.

[low] ARCH-A3 packages/coding-agent/src/slash-commands/builtin-registry.ts:1922,1936-1957 — handleTui custom-noargs firing mechanism unspecified; identity-auto works in TUI only via adaptTuiSlashRuntime handle delegation — TUI custom-noargs branch must delegate to handle(command, adaptTuiSlashRuntime(runtime.ctx)).

[low] ARCH-A4 packages/coding-agent/src/slash-commands/builtin-registry.ts:934 — /effort template normalizes args with trim().toLowerCase(); copying it corrupts /tone custom text — tokenize first word only for dispatch; store remainder verbatim.

[low] ARCH-A5 packages/coding-agent/src/config/settings-schema.ts:287 — identity.vibe UI description already says 'Tone and personality lines' — settings tab shows two 'tone' owners — disambiguate vibe/tone descriptions in same settings-schema edit.

Anchors verified: all plan file:line references match (renderIdentityBlock 304-341, identity.language 290-298, /effort 917-977, /identity 581-589, identity-auto 602-622/607-618, with-type-text imports 14-16, tool.renderMode 748-751); brand gates scan prompts/agents only; schema generator handles default-undefined; root check:ts chains check:schemas.

The single point most likely to break first if the plan is implemented as written: tsgo compile error on settings.set("identity.tone", undefined) in the /tone off handler and test case 5.
