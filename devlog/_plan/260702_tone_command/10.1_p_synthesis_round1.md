# 10.1 — P Critic round 1 synthesis (/tone)

> critic review: `.jwc/plans/planphase/2026-07-02-1325-8f6b/stage-01-critic.md` (verdict ITERATE, recorded `p_round=1`)
> plan under review: [10_plan.md](./10_plan.md)

## 결정 요약

4건 전부 수용(accept). 플랜만 수정, 스펙/설계 변경 없음. waive 없음.

| ID | severity | 결정 | 근거 & 플랜 반영 |
|---|---|---|---|
| F-01 | major | accept | spec acceptance 2·3(슬래시 커맨드 표면)이 검증 절에 미매핑이었음 — 사실. `## 검증` 절에 수동 1~4단계 추가 (`/tone sarcastic`→status/identity→새 프롬프트 `## Tone` 주입, custom inline, custom 무인자 랜딩, off 보존). |
| F-02 | major | accept | custom 무인자 lane이 구현자 재량에 열려 있었음 — 사실 (`identity-auto` 선례 `builtin-registry.ts:607-618` 확인). §4에 "custom 무인자 계약" 신설: 공유 `buildToneCustomInstruction()` 헬퍼, 빈 입력 시 무저장 종료, 저장 순서 toneCustom→tone, 개행 보존, 종료 요약 고지. 인라인 lane의 빈 문자열 에러 처리도 명문화. |
| F-03 | minor | accept | 검증 절이 schema regen/CHANGELOG/fork-delta diff를 안 덮었음 — 사실. 최종 diff 체크리스트(정확히 12개 파일 밴드; 최초 표기 "11개"는 A-stage PLANNER-A4 지적으로 정정) 추가. |
| F-04 | minor | accept | `bun run check:ts`는 root 전 워크스페이스 게이트인데 "coding-agent 영향 범위"로 오기 — 사실 (`package.json:95` vs `packages/coding-agent/package.json:28-30`). 집중 게이트를 `bun --cwd packages/coding-agent run check`로 교체, root 게이트는 스코프를 정확히 표기. |

## 근본 원인 분류

검증 절이 렌더러(단위 테스트) 중심으로 작성되어 커맨드 표면 acceptance가 계획-검증 매핑에서 누락된 것 — 계획 완결성 결함(카테고리: verification mapping gap). 설계 자체의 결함 아님.

## 변경된 플랜 섹션

- §4 `/tone` 스펙: handle/handleTui 주석 구체화 + "custom 무인자 계약" 소절 신설 + 분기 계약 1줄 교체.
- `## 검증 (acceptance)`: 자동/수동/최종 diff 체크리스트 3단 구조로 재작성.
