# P Critic R1 — ITERATE

## Accepted
1. **Loop plan MOC schema** — need to define required structure (phase format, done/pending markers, how agent picks next)
2. **Idle-origin loop creation** — routing for idle→loop without going through interview
4. **Iteration awareness** — MOC numbering + cycle boundary "phase N of M" reporting
6. **research-setup skip** — loop gate skipped in research-setup mode
8. **Korean in English prompt** — use neutral phrasing

## Partially accepted
3. **HITL/HOTL detection** — existing convention (goal presence = HOTL). Reference convention, don't redefine.

## Waived
5. **Verification is presence-only** — prompt-only changes; behavioral verification requires integration testing out of scope for this plan. File read + check:ts is sufficient.
7. **Completion reporting without goal** — D-stage already says "state completion plainly" for non-goal cases. No additional instruction needed.
