# 00 — moc: M1 이후 패치 사이클 종합 계획 (post-M1)

> 상태: 🟡 계획 수립 (260613) — 트랙 A/B/C 전부 미착수, 본 문서가 진입점.
> 입력: 사용자 "jwc 기본 MVP가 끝난 상태 — devlog도 이제 어떻게 패치할지 계획 아주 상세하게".
> 전제: 99.12 M1 릴리스 게이트 done 선언(`2d1afb52`, G1~G9 증거 — 090 정본),
> 99.00.05 완료 표기(`a3befc7a`). cli-jaw 측 260613 배치(스트림 가시성·메모리·디스패치
> 하드닝, 커밋 23건, 풀스위트 4,029/0실패) 완료 — 트랙 C의 이식 원천.
> 관련: [99.00.00 MOC](../260612_jawcode_fork/phase1/99.00.00_moc_99band.md) · [99.00.04 §4 이월](../260612_jawcode_fork/phase1/99.00.04_plan_batch_closeout.md) ·
> [112 MOC GUI](../260612_jawcode_fork/112_moc_gui.md) · [113 web native surfaces](../260612_jawcode_fork/113_plan_web_native_surfaces.md) ·
> cli-jaw `devlog/_plan/260613_jwc_code_mode/` (D112-3: 설계 정본은 본 repo, cli-jaw 폴더는 사후 구현 기록 전용)

## 1. 한눈에 — 트랙 구조와 순서

| 트랙 | 내용 | 선행조건 | 문서 위치 |
|---|---|---|---|
| **A. 99밴드 잔여 마감** | 이월 4건 + 99.11① PR 추적 | 없음 — 즉시 가능 | `260612_jawcode_fork/phase1/99.*` 기존 번호 체계 이어서 |
| **B. Code Mode (112.x)** | B1 1줄 수리 → B2 cwd 감사 → cli-jaw C1/C2 | B1이 전체 게이트 | 정본 = `112.x`, cli-jaw 쪽은 구현 기록만 |
| **C. Web Native Surfaces (113.x)** | cli-jaw 260613 검증 패턴 5종 계약 반영 | 113.1 계약 문서 개정 먼저 | `113.x` 신규 하위 문서 |

권장 실행 순서: **A(이월 정리, 반나절) → B(code mode 착수, cli-jaw 연동) → C(113 계약 개정은 B와 병행 가능, 구현은 후순위)**.
근거: A는 병렬 세션 랜딩 충돌 리스크가 시간이 갈수록 커지는 정리 작업이고, B는 cli-jaw 측 acp-host(C2)가 착수 대기로 묶여 있어 jwc 쪽 B1이 풀리면 두 repo가 병렬 진행 가능.

## 2. 트랙 A — 99밴드 잔여 마감 (이월 5건)

99.00.04 §4 이월 목록 기준. 각 항목에 문서 번호·작업·검증·리스크 명기:

### A-1. 99.00.00 · 99.20.00 MOC 표 현행화
- **무엇**: 배치 세션이 랜딩한 슬라이스(99.20.02/03, 99.30.01, 99.07, P1-5/6, 99.20.07 P1~P4, 99.06)가 MOC 표에 미반영 — 병렬 세션 dirty라 스윕에서 제외됐던 것.
- **작업**: 병렬 세션 랜딩 완료 확인 후(`git status`로 두 MOC 파일 clean 확인) 표만 갱신. **내용 수정 금지, 상태 칸만**.
- **검증**: 표의 각 done 행이 커밋 SHA를 가리키는지 교차 확인 (`22858113..707f3188` 범위 + 이후).
- **리스크**: 병렬 세션이 아직 dirty면 **착수 금지** — 충돌 시 마지막 할당 승자 문제 재발.

### A-2. /plan·/team 표면 설계
- **무엇**: 커맨드는 등록돼 있으나 표면(UI) 설계 부재로 이월.
- **작업**: `99.20.xx_design_plan_team_surfaces.md` 신설 (다음 빈 NN). `_fin/260613_cmd_audit/00`의 슬래시 커맨드 논리 감사 결과를 선행 입력으로 — P1 1줄 수리는 260613 랜딩 완료(v2 재감사 절 참조).
- **검증**: 표면 mockup + 디스패처 게이트(TUI `:1382`·ACP) 양쪽 경로 명세.

### A-3. quota-panel → ScrollablePanel 통합
- **무엇**: 99.20.07에서 read-once 7종을 ScrollablePanel로 도킹했지만 quota-panel은 병렬 세션 랜딩 정리 대기로 이월.
- **작업**: 병렬 세션의 priority 핀(alias 인프라 위에 얹힌 것 확인됨)과 병합 — `99.20.07_impl_*.md`에 후속 슬라이스로 기록.
- **검증**: 기존 99.20.07 P2 패턴(도킹 후 스크롤/포커스 링 동작) 그대로 재사용.

### A-4. editor.onAutocompleteCancel 이중 배선 병합
- **무엇**: 본 세션과 병렬 세션이 같은 핸들러를 각자 배선(`:409/:2029`, requestRender용) — 랜딩 시 마지막 할당이 이기는 충돌. 99.00.04 §4에 추적 명기됨.
- **작업**: 병렬 세션 랜딩 후 단일 다중 리스너 패턴(배열 or 합성 함수)으로 통일하는 1파일 수리.
- **검증**: ESC 취소 시 requestRender + S2 쪽 동작 둘 다 발화하는 단위 테스트.

### A-5. 99.11① PR 추적
- **무엇**: 99밴드 중 유일하게 남은 패키지 절반.
- **작업**: 99.11 문서의 ① 정의 확인 후 별도 슬라이스로 — M1 게이트와 무관하므로 우선순위 최하.

## 3. 트랙 B — Code Mode (112.x, cli-jaw 연동)

정본 문서 7종 존재 확인(112 / 112.1 / 112.2 / 113 / 113.1 / 110.1 / 130.1). 차단 해소 순서:

1. **B1 (jwc, 1줄)**: `builtin-registry.ts:328` `/orchestrate`가 `process.cwd()` 사용 → 세션 cwd로 교체. 112.2 감사의 1번 차단. **이것부터** — 이후 전부가 세션 격리에 의존.
2. **B2 (jwc)**: 전역 cwd 가정 전수 감사 → `110.2_audit_global_cwd.md` 신설. B1 패턴의 형제 사례 수색 (cli-jaw 260612 감사가 한 "결함 클래스 전수 수색" 방법론 그대로).
3. **B3/B4 (jwc)**: 112.2의 잔여 차단 — 문서 재독 후 본 MOC에 상태 반영 (현재 미확인).
4. **C1 (cli-jaw)**: `src/core/event-bus.ts:11` EventTopic에 `'jwc'` 추가 — 1줄.
5. **C2 (cli-jaw)**: acp-host 모듈 (cli-jaw `260613_jwc_code_mode/10_phase1_acp_host.md`, 현재 ⬜ 착수 대기). 상주 ACP child × N세션 통합.

**거버넌스 (D112-3 재확인)**: 설계 변경·차단 추가·결정은 **본 repo 112.x에만** 기록. cli-jaw `260613_jwc_code_mode/`에는 C1/C2 구현 결과만. 두 repo 문서가 같은 내용을 중복 서술하기 시작하면 즉시 정본 쪽으로 합치고 포인터로 대체.

**cli-jaw 260613 배치에서 가져올 것**: dispatch always-poll 패턴(블로킹 HTTP는 undici 5분 headersTimeout에 죽음 — ACP host의 장시간 호출도 동일 함정), `[worker-replay …]` 마커(끊긴 연결의 지연 재전달 식별).

## 4. 트랙 C — Web Native Surfaces (113.x) 계약 이식

cli-jaw 260613 배치에서 **실증된** 패턴 5종을 113.1 REST/SSE 계약에 선반영. jwc가 웹 표면을 갖기 전에 계약 레벨에서 넣는 게 비용이 1/10:

| # | 패턴 | cli-jaw 근거 | 113.1 반영 항목 |
|---|---|---|---|
| 1 | **SSE 리플레이 멱등성**: 이벤트 id + `sseReplay` 마킹 + run 스코프 누적 커서(`textLen`/`traceSeq`) | 260612 docs 06-09 + 260613 P1-b | 이벤트 envelope에 id·replay 플래그·누적 커서 필드를 처음부터 명세 |
| 2 | **keep-alive는 데이터 이벤트로**: SSE 코멘트는 JS에 안 보임 → 클라이언트 워치독 불가 | 260613 Phase 4 (`{topic:system,event:ping}` + 2.5주기 강제 재연결) | ping 이벤트 + 클라이언트 staleness 워치독을 계약에 포함 |
| 3 | **스냅샷 하이드레이션 멱등성**: 시그니처 no-op 스킵 + 식별자(stepRef/seq) 기반 머지 | 260613 Phase 1-c/3 (ordinal 발산 가드 포함) | 스냅샷 응답에 안정 식별자 필수 명세 — "표시용 배열에 id 없는 항목 금지" |
| 4 | **장시간 호출은 202+폴링**: 블로킹 응답은 클라이언트 타임아웃 캐스케이드 | 260613 Phase 6 (undici 300s) | 워커/긴 작업 API는 202 + result 폴 + verdict 동봉(완료 전 저장) 패턴 고정 |
| 5 | **audience 게이트 설계**: internal 전용 스트림은 "클릭해야 렌더" 버그의 뿌리 | 260613 docs 00-03 (직원 스트림 비가시성) | 표면에 보여야 할 진행 신호는 처음부터 public+sanitized 레인으로 설계 |

**작업**: `113.2_contract_stream_idempotency.md` 신설 — 위 표를 계약 조항으로 전개, cli-jaw 코드 참조는 절대경로 인용(본 repo 관례).

## 5. 운영 규칙 (이번 사이클 공통)

1. **커밋**: `[agent]` 접두 + 슬라이스당 1커밋 (기존 99밴드 관례 유지). 병렬 세션 dirty 파일은 절대 스테이징하지 않기 — A-1/A-3/A-4는 병렬 랜딩 확인이 선행조건.
2. **검증 게이트**: 슬라이스마다 (a) 타입체크, (b) 영향 테스트, (c) 신규 실패 0 확인 후 랜딩. 워크트리는 `/var/tmp` 사용 (`/tmp`는 외부 정리로 삭제된 사고 전례).
3. **문서 상태 헤더**: 본 repo 관례(✅/🟢/🟡/⬜/⚠️ + 날짜 + 커밋 SHA 인라인) 유지 — 상태 헤더가 곧 체인지로그.
4. **goal 연동**: 트랙 단위로 `cli-jaw goal` 1개씩 — 트랙 A는 단일 goal로 묶고, B는 B1 랜딩 후 별도 goal 개설 권장 (C2가 cli-jaw 쪽이라 repo 경계가 갈림).

## 6. 완료 정의

- 트랙 A: 이월 5건 전부 ✅ + 99.00.00 MOC에 "이월 없음" 명기.
- 트랙 B: B1~B4 해소 + cli-jaw C1/C2 랜딩 + 112 MOC에 코드모드 P1 done.
- 트랙 C: 113.2 계약 문서 ✅ (구현은 113 본 계획의 다음 마일스톤으로 이연 가능).
- 본 문서: 각 트랙 완료 시 상태 헤더 갱신, 전체 완료 시 `_fin/` 이동 후보.
