# A-Stage Synthesis

## Findings Resolution

| Finding | Resolution |
|---|---|
| ARCH-A1 (CRITICAL): permission gates by name only, not op | Accept. Add 'background' to PERMISSION_REQUIRED_TOOLS. Add op-level branch in getPermissionIntent() — return shell-execution intent only for op==='start_monitor', undefined otherwise. |
| ARCH-A2 (MAJOR): loadMode 'essential' insufficient, need DEFAULT_ESSENTIAL_TOOL_NAMES | Accept. Add 'background' to the array. |
| ARCH-A3 (MAJOR): import/export cleanup for deleted tools | Accept. Plan must include import/export removal in index.ts. |
| ARCH-A4 (MINOR): line numbers ~2 off | Accept. Implementer reads fresh. |
| ARCH-A5 (MINOR): flat schema pollution | Accept as-is for now. Field descriptions say "only for start_monitor". Discriminated union is future work. |

All findings accepted. Plan updated with ARCH-A1 permission fix, ARCH-A2 essential array, ARCH-A3 import cleanup.

## Verdict
PASS
