FAIL

[HIGH] packages/coding-agent/src/defaults/jwc-defaults.ts:208 — The plan only replaces `existing` with `removeLegacyManagedCuaDriver(...)`, but `installDefaultMcpConfig()` returns immediately when managed defaults are already `"matching"`; after `cua-driver` is removed from `managedDefaults`, a file containing matching `context7` plus the exact legacy `cua-driver` entry will be considered matching and never written, failing legacy cleanup and leaving CLI startup free to preconnect it — Track `cleanupNeeded` from the original read, make check-mode report `different` when cleanup is needed, and write the cleaned config even when the remaining managed defaults match.

[HIGH] packages/coding-agent/src/runtime-mcp/manager.ts:457 — `MCPManager.connectServers()` is startup-oriented: it races full tool loading against the 250ms grace window and can return `tools: []` while the server is still connecting, so a lazy `computer_use` first call that uses this result/catalog can falsely return missing-backend-tool before `cua-driver` finishes connecting — Use direct `connectToServer()` + `listTools(connection,{signal})`, or explicitly `await manager.waitForConnection("cua-driver")` and then call exported `listTools`/`callTool` with a real timeout/abort path instead of trusting the immediate result from `connectServers()`.

[MEDIUM] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:172 — The proxy action table binds public actions to backend names such as `start_session`/`end_session`, but the repo contains no source-backed `cua-driver` catalog for those tools; the only plan-level safeguard is a runtime catalog miss at line 187, which converts an implementation mismatch into user-visible `ok:false` for supported-looking actions — Pin the current `cua-driver mcp` tool catalog in a fixture/doc source used by tests, and remove or gate any proxy actions absent from that catalog.

[MEDIUM] packages/coding-agent/src/main.ts:913 — CLI startup still discovers/connects all configured MCP servers before session creation; because current default-config behavior preserves existing `computer-use` entries, adding a built-in `computer_use` can leave stale `computer-use` MCP tooling preconnected and discoverable alongside the new proxy — Add an explicit migration decision/test for exact legacy `computer-use` entries (cleanup or documented preservation), and verify BM25/active-tool output when both stale MCP and built-in proxy are present.

[LOW] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:291 — The verification list omits `packages/coding-agent/test/tool-discovery/initial-tools.test.ts`, which is the existing gate asserting every `BUILTIN_TOOLS` entry has `loadMode` and every discoverable tool has a `summary`; this is directly impacted by adding `computer_use` to `BUILTIN_TOOLS` — Add that focused test file to the verification plan and extend it if `computer_use` needs special discovery metadata.

Most likely to break first: the exact legacy `cua-driver` cleanup path, because the planned one-line cleanup still hits the existing `"matching"` early return and leaves the old server entry on disk.
