FAIL
[high] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:77 / packages/coding-agent/src/defaults/jwc-defaults.ts:205-209 — legacy cleanup can be skipped when existing context7 already matches, because `installDefaultMcpConfig()` returns early on `status === "matching"`; acceptance criterion 3 would not be met for a config containing matching context7 plus exact legacy `cua-driver` — make `removeLegacyManagedCuaDriver` return `{config, changed}` and force a write when `changed && !check`, with check-mode reporting a non-matching/remediation-needed status.
[high] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:187,217 / packages/coding-agent/src/runtime-mcp/tool-bridge.ts:214-235 — backend tool lookup is underspecified: `MCPManager.getTools()` exposes normalized wrapper names like `mcp__cua-driver_<tool>` while the proxy action table names raw backend tools like `start_session`; indexing by the wrong name will make every backend tool look missing — specify that `LazyCuaDriverBackend` indexes MCP wrappers by `mcpToolName` (raw backend name) and invokes that wrapper, or use direct `listTools(connection)`/`callTool(connection, rawName, ...)` consistently.
[medium] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:196-201 — connection failure behavior is a scope hole: the plan says subsequent calls reuse the same pending/fulfilled promise but never says to clear a rejected first-connect promise, so a transient missing-binary/permission failure can poison the session and fail every later call without retry — require `#connectPromise` to be cleared on rejection and `dispose()` to clear both manager and promise.
[medium] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:259 / packages/coding-agent/src/session/agent-session.ts:345-430 — cleanup wiring leaves an implementation branch (“config field, or pass a closure”) even though this stage requires unambiguous steps — choose one concrete path, preferably add `toolCleanups?: Map<string, () => Promise<void> | void>` or `runToolCleanups?: () => Promise<void>` to `AgentSessionConfig` and pass it at the existing `new AgentSession({...})` call.
[medium] devlog/_plan/computer_use/80_lazy_cua_proxy_execution_plan.md:166,285 — error contract and cancellation are not independently checkable: “abort map to ok:false unless framework requires rethrow” does not define which behavior tests should assert, while acceptance criterion 8 covers missing binary/permissions only — define cancellation as either mandatory `ToolAbortError` rethrow or mandatory `{ok:false}` envelope, and add a focused assertion for that chosen behavior.
The single statement an implementer would most likely misread: “Add the cleanup map to `CreateAgentSessionOptions`/`AgentSession` construction via a narrow config field, or pass a closure into `AgentSession` if existing construction shape allows it.”
