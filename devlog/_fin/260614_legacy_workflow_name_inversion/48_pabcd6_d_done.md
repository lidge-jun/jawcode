# D done — PABCD-6

Dual-read: `legacy-storage.ts`, migrate `persistPath` → canonical `plan-state.json` / `goal-state.json`; goal ledger via `resolveGoalStoragePaths`; tests 7/7 + `bun run check` green.