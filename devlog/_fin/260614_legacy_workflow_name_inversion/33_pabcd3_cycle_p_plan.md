# PABCD-3 — verify planphase writer

Verify `jwc planphase --write`, `.jwc/plans/planphase/`, `planphase-state.json`, legacy `jwc ralplan` alias.

```bash
bun test packages/coding-agent/test/jwc-runtime/ralplan-runtime.test.ts
```