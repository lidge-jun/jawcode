FAIL

[HIGH] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §6.1–§6.3 — Phase 4 actor-local compaction and Codex prewarm lack named hooks on the subagent actor resume path (`task/executor.ts` / `task/index.ts` before `runMode: message`); only metadata fields exist on `WorkflowActorRecord` — Add cycle-2 integration spec: open actor `sessionFile`, threshold check, call existing `AgentSession` compaction, set `compactedAt`/`needsCompact`, then optional `prewarmCodexContent` with failure-injection test.
[HIGH] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §8.2 — Gate commands reference missing tests `orchestrate-actor-retirement.test.ts`, `task-workflow-actor-routing.test.ts`, `task-executor-self-fork.test.ts` while repo ships `orchestrate-actor-lifecycle.test.ts` and `task-fork-context.test.ts` — Rewrite §8.2 to match on-disk files or add the missing tests before CI.
[HIGH] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §4.4–§4.5 — Requires `cache_affinity: false` signaling and forced fork-failure tests; `packages/coding-agent/src/task` has no `cache_affinity` metadata (`86_d_done_summary.md` notes gap) — Define receipt field on `SingleResult`/task receipt before implementing fallback policy.
[MEDIUM] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §3.5–§3.6 — Workflow actor process-restart / `context_unavailable` acceptance not implemented or tested; `context_unavailable` is detached `job-manager` only — Add TaskTool test: persisted `actors.json` without in-process runner must not silently resume.
[MEDIUM] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §3.1 — Planned `task-workflow-actor-routing.test.ts` and `planner_subagent_id` non-use invariant lack dedicated coverage — Add black-box TaskTool test with mocked PABCD state asserting second compatible call uses same `sessionFile` and `runMode: message`.
[MEDIUM] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §5.1–§5.3 — Phase 3 B/C prompts (`orchestrate-b.md`, `orchestrate-c.md`) and `plan-writer.ts` wiring still outstanding vs cycle-1 P/A-only evidence — Add mechanical prompt parity checks before B-stage.
[LOW] devlog/_plan/260614_subagent_cache_actor_lifecycle/80_pabcd_phase_execution_plan.md §11 vs §7 — Phase 5 model UI largely done (`model-registry.ts`, `model-selector.ts`) but §7.1 still says pre-step discovery — Mark Phase 5 complete in plan topology for cycle 2 focus.

Single point most likely to break first: §8.2 copy-paste of non-existent test file paths (`task-workflow-actor-routing.test.ts`, `task-executor-self-fork.test.ts`, `orchestrate-actor-retirement.test.ts`) will fail the first `bun test` gate in cycle-2 B/C closeout.
